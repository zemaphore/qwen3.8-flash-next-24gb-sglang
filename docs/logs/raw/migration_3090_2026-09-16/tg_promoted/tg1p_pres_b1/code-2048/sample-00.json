{
 "started_utc": "2026-09-17T03:57:49.582503+00:00",
 "wall_seconds": 16.324960921992897,
 "prompt_tokens": 2048,
 "completion_tokens": 512,
 "stream_events": 512,
 "ended_utc": "2026-09-17T03:58:05.907614+00:00",
 "warmup": true,
 "sample_index": 0,
 "requested_decode_tokens": 512,
 "gpu": {
  "n": 58,
  "temperature_c": {
   "min": 71.0,
   "max": 75.0,
   "mean": 74.74137931034483
  },
  "power_w": {
   "min": 202.71,
   "max": 271.7,
   "mean": 259.21258620689656
  },
  "util_pct": {
   "min": 43.0,
   "max": 100.0,
   "mean": 86.34482758620689
  },
  "clock_sm_mhz": {
   "min": 1650.0,
   "max": 1920.0,
   "mean": 1906.0344827586207
  },
  "clock_mem_mhz": {
   "min": 9501.0,
   "max": 9501.0,
   "mean": 9501.0
  },
  "memory_free_mib": {
   "min": 2380.0
  }
 },
 "output_text_sha256": "b9de33ad026672d7e3309517ff48bde90a9c25111654e90ecd8d6e6ec975853f",
 "output_text": "++       +        b+        b_z+        b_zp+        b_zp_num+        b_zp_num =+        b_zp_num = +        b_zp_num = 1+        b_zp_num = 12+        b_zp_num = 128+        b_zp_num = 128\n+        b_zp_num = 128\n++        b_zp_num = 128\n+   +        b_zp_num = 128\n+    elif+        b_zp_num = 128\n+    elif has+        b_zp_num = 128\n+    elif has_z+        b_zp_num = 128\n+    elif has_zp+        b_zp_num = 128\n+    elif has_zp and+        b_zp_num = 128\n+    elif has_zp and use+        b_zp_num = 128\n+    elif has_zp and use_int+        b_zp_num = 128\n+    elif has_zp and use_int2+        b_zp_num = 128\n+    elif has_zp and use_int2_w+        b_zp_num = 128\n+    elif has_zp and use_int2_w2+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_z+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_sh+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16)+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) *+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+   +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_z+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_z+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_sh+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None]+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] %+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2)+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) *+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+   +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_z+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_z+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_sh+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N)+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) %+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+   +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        #+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/z+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain:+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        #+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+           +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+           +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            ++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_exp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts *+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+           +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            ++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None]+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] *+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+           +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            ++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :]+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] *+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_z+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+           +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_z+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+               +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_z+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+               +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                ++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_exp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts *+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+               +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                ++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None]+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] *+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+               +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                ++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :]+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] *+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+           +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+   +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+           +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+               +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+               +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                ++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_exp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts *+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+               +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                ++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + offs+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + offs_k+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + offs_k[:,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + offs_k[:, None+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + offs_k[:, None]+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + offs_k[:, None] *+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + offs_k[:, None] * stride+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + offs_k[:, None] * stride_b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + offs_k[:, None] * stride_bsk+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + offs_k[:, None] * stride_bsk\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + offs_k[:, None] * stride_bsk\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + offs_k[:, None] * stride_bsk\n+               +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + offs_k[:, None] * stride_bsk\n+                ++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + offs_k[:, None] * stride_bsk\n+                + offs+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + offs_k[:, None] * stride_bsk\n+                + offs_bn+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + offs_k[:, None] * stride_bsk\n+                + offs_bn[+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + offs_k[:, None] * stride_bsk\n+                + offs_bn[None+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + offs_k[:, None] * stride_bsk\n+                + offs_bn[None,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + offs_k[:, None] * stride_bsk\n+                + offs_bn[None, :]+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + offs_k[:, None] * stride_bsk\n+                + offs_bn[None, :] *+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + offs_k[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + offs_k[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + offs_k[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + offs_k[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + offs_k[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + offs_k[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n+           +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + offs_k[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n+            )+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + offs_k[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n+            )\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + offs_k[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n+            )\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + offs_k[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n+            )\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + offs_k[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n+            )\n+        elif+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + offs_k[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n+            )\n+        elif use+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + offs_k[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n+            )\n+        elif use_int+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + offs_k[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n+            )\n+        elif use_int4+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + offs_k[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n+            )\n+        elif use_int4_w+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + offs_k[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n+            )\n+        elif use_int4_w4+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + offs_k[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n+            )\n+        elif use_int4_w4a+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + offs_k[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n+            )\n+        elif use_int4_w4a1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + offs_k[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n+            )\n+        elif use_int4_w4a16+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + offs_k[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n+            )\n+        elif use_int4_w4a16:+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + offs_k[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n+            )\n+        elif use_int4_w4a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + offs_k[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n+            )\n+        elif use_int4_w4a16:\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + offs_k[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n+            )\n+        elif use_int4_w4a16:\n+           +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + offs_k[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n+            )\n+        elif use_int4_w4a16:\n+            block+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + offs_k[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n+            )\n+        elif use_int4_w4a16:\n+            block_start+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + offs_k[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n+            )\n+        elif use_int4_w4a16:\n+            block_start =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + offs_k[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n+            )\n+        elif use_int4_w4a16:\n+            block_start = tl+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + offs_k[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n+            )\n+        elif use_int4_w4a16:\n+            block_start = tl.arange+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + offs_k[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n+            )\n+        elif use_int4_w4a16:\n+            block_start = tl.arange(+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + offs_k[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n+            )\n+        elif use_int4_w4a16:\n+            block_start = tl.arange(0+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + offs_k[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n+            )\n+        elif use_int4_w4a16:\n+            block_start = tl.arange(0,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + offs_k[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n+            )\n+        elif use_int4_w4a16:\n+            block_start = tl.arange(0, BLOCK+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + offs_k[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n+            )\n+        elif use_int4_w4a16:\n+            block_start = tl.arange(0, BLOCK_SIZE+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + offs_k[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n+            )\n+        elif use_int4_w4a16:\n+            block_start = tl.arange(0, BLOCK_SIZE_K+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + offs_k[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n+            )\n+        elif use_int4_w4a16:\n+            block_start = tl.arange(0, BLOCK_SIZE_K)+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + offs_k[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n+            )\n+        elif use_int4_w4a16:\n+            block_start = tl.arange(0, BLOCK_SIZE_K) //+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + offs_k[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n+            )\n+        elif use_int4_w4a16:\n+            block_start = tl.arange(0, BLOCK_SIZE_K) // group+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + offs_k[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n+            )\n+        elif use_int4_w4a16:\n+            block_start = tl.arange(0, BLOCK_SIZE_K) // group_size+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + offs_k[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n+            )\n+        elif use_int4_w4a16:\n+            block_start = tl.arange(0, BLOCK_SIZE_K) // group_size\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + offs_k[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n+            )\n+        elif use_int4_w4a16:\n+            block_start = tl.arange(0, BLOCK_SIZE_K) // group_size\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + offs_k[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n+            )\n+        elif use_int4_w4a16:\n+            block_start = tl.arange(0, BLOCK_SIZE_K) // group_size\n+           +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + offs_k[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n+            )\n+        elif use_int4_w4a16:\n+            block_start = tl.arange(0, BLOCK_SIZE_K) // group_size\n+            scale+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + offs_k[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n+            )\n+        elif use_int4_w4a16:\n+            block_start = tl.arange(0, BLOCK_SIZE_K) // group_size\n+            scale_ptrs+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + offs_k[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n+            )\n+        elif use_int4_w4a16:\n+            block_start = tl.arange(0, BLOCK_SIZE_K) // group_size\n+            scale_ptrs =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + offs_k[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n+            )\n+        elif use_int4_w4a16:\n+            block_start = tl.arange(0, BLOCK_SIZE_K) // group_size\n+            scale_ptrs = (+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + offs_k[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n+            )\n+        elif use_int4_w4a16:\n+            block_start = tl.arange(0, BLOCK_SIZE_K) // group_size\n+            scale_ptrs = (\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + offs_k[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n+            )\n+        elif use_int4_w4a16:\n+            block_start = tl.arange(0, BLOCK_SIZE_K) // group_size\n+            scale_ptrs = (\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + offs_k[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n+            )\n+        elif use_int4_w4a16:\n+            block_start = tl.arange(0, BLOCK_SIZE_K) // group_size\n+            scale_ptrs = (\n+               +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + offs_k[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n+            )\n+        elif use_int4_w4a16:\n+            block_start = tl.arange(0, BLOCK_SIZE_K) // group_size\n+            scale_ptrs = (\n+                b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + offs_k[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n+            )\n+        elif use_int4_w4a16:\n+            block_start = tl.arange(0, BLOCK_SIZE_K) // group_size\n+            scale_ptrs = (\n+                b_scale+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + offs_k[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n+            )\n+        elif use_int4_w4a16:\n+            block_start = tl.arange(0, BLOCK_SIZE_K) // group_size\n+            scale_ptrs = (\n+                b_scale_ptr+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + offs_k[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n+            )\n+        elif use_int4_w4a16:\n+            block_start = tl.arange(0, BLOCK_SIZE_K) // group_size\n+            scale_ptrs = (\n+                b_scale_ptr\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + offs_k[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n+            )\n+        elif use_int4_w4a16:\n+            block_start = tl.arange(0, BLOCK_SIZE_K) // group_size\n+            scale_ptrs = (\n+                b_scale_ptr\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + offs_k[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n+            )\n+        elif use_int4_w4a16:\n+            block_start = tl.arange(0, BLOCK_SIZE_K) // group_size\n+            scale_ptrs = (\n+                b_scale_ptr\n+               +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + offs_k[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n+            )\n+        elif use_int4_w4a16:\n+            block_start = tl.arange(0, BLOCK_SIZE_K) // group_size\n+            scale_ptrs = (\n+                b_scale_ptr\n+                ++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + offs_k[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n+            )\n+        elif use_int4_w4a16:\n+            block_start = tl.arange(0, BLOCK_SIZE_K) // group_size\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + offs_k[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n+            )\n+        elif use_int4_w4a16:\n+            block_start = tl.arange(0, BLOCK_SIZE_K) // group_size\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_exp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + offs_k[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n+            )\n+        elif use_int4_w4a16:\n+            block_start = tl.arange(0, BLOCK_SIZE_K) // group_size\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + offs_k[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n+            )\n+        elif use_int4_w4a16:\n+            block_start = tl.arange(0, BLOCK_SIZE_K) // group_size\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts *+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + offs_k[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n+            )\n+        elif use_int4_w4a16:\n+            block_start = tl.arange(0, BLOCK_SIZE_K) // group_size\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + offs_k[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n+            )\n+        elif use_int4_w4a16:\n+            block_start = tl.arange(0, BLOCK_SIZE_K) // group_size\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + offs_k[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n+            )\n+        elif use_int4_w4a16:\n+            block_start = tl.arange(0, BLOCK_SIZE_K) // group_size\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + offs_k[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n+            )\n+        elif use_int4_w4a16:\n+            block_start = tl.arange(0, BLOCK_SIZE_K) // group_size\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + offs_k[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n+            )\n+        elif use_int4_w4a16:\n+            block_start = tl.arange(0, BLOCK_SIZE_K) // group_size\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + offs_k[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n+            )\n+        elif use_int4_w4a16:\n+            block_start = tl.arange(0, BLOCK_SIZE_K) // group_size\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+               +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + offs_k[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n+            )\n+        elif use_int4_w4a16:\n+            block_start = tl.arange(0, BLOCK_SIZE_K) // group_size\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                ++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + offs_k[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n+            )\n+        elif use_int4_w4a16:\n+            block_start = tl.arange(0, BLOCK_SIZE_K) // group_size\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + block+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + offs_k[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n+            )\n+        elif use_int4_w4a16:\n+            block_start = tl.arange(0, BLOCK_SIZE_K) // group_size\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + block_start+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + offs_k[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n+            )\n+        elif use_int4_w4a16:\n+            block_start = tl.arange(0, BLOCK_SIZE_K) // group_size\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + block_start[:,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + offs_k[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n+            )\n+        elif use_int4_w4a16:\n+            block_start = tl.arange(0, BLOCK_SIZE_K) // group_size\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + block_start[:, None+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + offs_k[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n+            )\n+        elif use_int4_w4a16:\n+            block_start = tl.arange(0, BLOCK_SIZE_K) // group_size\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + block_start[:, None]+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + offs_k[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n+            )\n+        elif use_int4_w4a16:\n+            block_start = tl.arange(0, BLOCK_SIZE_K) // group_size\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + block_start[:, None] *+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + offs_k[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n+            )\n+        elif use_int4_w4a16:\n+            block_start = tl.arange(0, BLOCK_SIZE_K) // group_size\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + block_start[:, None] * stride+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + offs_k[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n+            )\n+        elif use_int4_w4a16:\n+            block_start = tl.arange(0, BLOCK_SIZE_K) // group_size\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + block_start[:, None] * stride_b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + offs_k[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n+            )\n+        elif use_int4_w4a16:\n+            block_start = tl.arange(0, BLOCK_SIZE_K) // group_size\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + block_start[:, None] * stride_bsk+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + offs_k[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n+            )\n+        elif use_int4_w4a16:\n+            block_start = tl.arange(0, BLOCK_SIZE_K) // group_size\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + block_start[:, None] * stride_bsk\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + offs_k[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n+            )\n+        elif use_int4_w4a16:\n+            block_start = tl.arange(0, BLOCK_SIZE_K) // group_size\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + block_start[:, None] * stride_bsk\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + offs_k[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n+            )\n+        elif use_int4_w4a16:\n+            block_start = tl.arange(0, BLOCK_SIZE_K) // group_size\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + block_start[:, None] * stride_bsk\n+               +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + offs_k[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n+            )\n+        elif use_int4_w4a16:\n+            block_start = tl.arange(0, BLOCK_SIZE_K) // group_size\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + block_start[:, None] * stride_bsk\n+                ++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + offs_k[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n+            )\n+        elif use_int4_w4a16:\n+            block_start = tl.arange(0, BLOCK_SIZE_K) // group_size\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + block_start[:, None] * stride_bsk\n+                + offs+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + offs_k[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n+            )\n+        elif use_int4_w4a16:\n+            block_start = tl.arange(0, BLOCK_SIZE_K) // group_size\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + block_start[:, None] * stride_bsk\n+                + offs_bn+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + offs_k[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n+            )\n+        elif use_int4_w4a16:\n+            block_start = tl.arange(0, BLOCK_SIZE_K) // group_size\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + block_start[:, None] * stride_bsk\n+                + offs_bn[+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + offs_k[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n+            )\n+        elif use_int4_w4a16:\n+            block_start = tl.arange(0, BLOCK_SIZE_K) // group_size\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + block_start[:, None] * stride_bsk\n+                + offs_bn[None+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + offs_k[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n+            )\n+        elif use_int4_w4a16:\n+            block_start = tl.arange(0, BLOCK_SIZE_K) // group_size\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + block_start[:, None] * stride_bsk\n+                + offs_bn[None,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + offs_k[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n+            )\n+        elif use_int4_w4a16:\n+            block_start = tl.arange(0, BLOCK_SIZE_K) // group_size\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + block_start[:, None] * stride_bsk\n+                + offs_bn[None, :]+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + offs_k[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n+            )\n+        elif use_int4_w4a16:\n+            block_start = tl.arange(0, BLOCK_SIZE_K) // group_size\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + block_start[:, None] * stride_bsk\n+                + offs_bn[None, :] *+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + offs_k[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n+            )\n+        elif use_int4_w4a16:\n+            block_start = tl.arange(0, BLOCK_SIZE_K) // group_size\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + block_start[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + offs_k[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n+            )\n+        elif use_int4_w4a16:\n+            block_start = tl.arange(0, BLOCK_SIZE_K) // group_size\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + block_start[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + offs_k[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n+            )\n+        elif use_int4_w4a16:\n+            block_start = tl.arange(0, BLOCK_SIZE_K) // group_size\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + block_start[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + offs_k[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n+            )\n+        elif use_int4_w4a16:\n+            block_start = tl.arange(0, BLOCK_SIZE_K) // group_size\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + block_start[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + offs_k[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n+            )\n+        elif use_int4_w4a16:\n+            block_start = tl.arange(0, BLOCK_SIZE_K) // group_size\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + block_start[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + offs_k[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n+            )\n+        elif use_int4_w4a16:\n+            block_start = tl.arange(0, BLOCK_SIZE_K) // group_size\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + block_start[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n+           +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + offs_k[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n+            )\n+        elif use_int4_w4a16:\n+            block_start = tl.arange(0, BLOCK_SIZE_K) // group_size\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + block_start[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n+            )+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + offs_k[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n+            )\n+        elif use_int4_w4a16:\n+            block_start = tl.arange(0, BLOCK_SIZE_K) // group_size\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + block_start[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n+            )\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + offs_k[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n+            )\n+        elif use_int4_w4a16:\n+            block_start = tl.arange(0, BLOCK_SIZE_K) // group_size\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + block_start[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n+            )\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + offs_k[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n+            )\n+        elif use_int4_w4a16:\n+            block_start = tl.arange(0, BLOCK_SIZE_K) // group_size\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + block_start[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n+            )\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + offs_k[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n+            )\n+        elif use_int4_w4a16:\n+            block_start = tl.arange(0, BLOCK_SIZE_K) // group_size\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + block_start[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n+            )\n+        if+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + offs_k[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n+            )\n+        elif use_int4_w4a16:\n+            block_start = tl.arange(0, BLOCK_SIZE_K) // group_size\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + block_start[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n+            )\n+        if has+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + offs_k[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n+            )\n+        elif use_int4_w4a16:\n+            block_start = tl.arange(0, BLOCK_SIZE_K) // group_size\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + block_start[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n+            )\n+        if has_z+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + offs_k[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n+            )\n+        elif use_int4_w4a16:\n+            block_start = tl.arange(0, BLOCK_SIZE_K) // group_size\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + block_start[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n+            )\n+        if has_zp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + offs_k[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n+            )\n+        elif use_int4_w4a16:\n+            block_start = tl.arange(0, BLOCK_SIZE_K) // group_size\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + block_start[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n+            )\n+        if has_zp:+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + offs_k[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n+            )\n+        elif use_int4_w4a16:\n+            block_start = tl.arange(0, BLOCK_SIZE_K) // group_size\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + block_start[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n+            )\n+        if has_zp:\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + offs_k[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n+            )\n+        elif use_int4_w4a16:\n+            block_start = tl.arange(0, BLOCK_SIZE_K) // group_size\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + block_start[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n+            )\n+        if has_zp:\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + offs_k[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n+            )\n+        elif use_int4_w4a16:\n+            block_start = tl.arange(0, BLOCK_SIZE_K) // group_size\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + block_start[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n+            )\n+        if has_zp:\n+           +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + offs_k[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n+            )\n+        elif use_int4_w4a16:\n+            block_start = tl.arange(0, BLOCK_SIZE_K) // group_size\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + block_start[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n+            )\n+        if has_zp:\n+            b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + offs_k[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n+            )\n+        elif use_int4_w4a16:\n+            block_start = tl.arange(0, BLOCK_SIZE_K) // group_size\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + block_start[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n+            )\n+        if has_zp:\n+            b_z+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + offs_k[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n+            )\n+        elif use_int4_w4a16:\n+            block_start = tl.arange(0, BLOCK_SIZE_K) // group_size\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + block_start[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n+            )\n+        if has_zp:\n+            b_zp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + offs_k[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n+            )\n+        elif use_int4_w4a16:\n+            block_start = tl.arange(0, BLOCK_SIZE_K) // group_size\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + block_start[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n+            )\n+        if has_zp:\n+            b_zp_ptrs+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + offs_k[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n+            )\n+        elif use_int4_w4a16:\n+            block_start = tl.arange(0, BLOCK_SIZE_K) // group_size\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + block_start[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n+            )\n+        if has_zp:\n+            b_zp_ptrs =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + offs_k[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n+            )\n+        elif use_int4_w4a16:\n+            block_start = tl.arange(0, BLOCK_SIZE_K) // group_size\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + block_start[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n+            )\n+        if has_zp:\n+            b_zp_ptrs = (+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + offs_k[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n+            )\n+        elif use_int4_w4a16:\n+            block_start = tl.arange(0, BLOCK_SIZE_K) // group_size\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + block_start[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n+            )\n+        if has_zp:\n+            b_zp_ptrs = (\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + offs_k[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n+            )\n+        elif use_int4_w4a16:\n+            block_start = tl.arange(0, BLOCK_SIZE_K) // group_size\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + block_start[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n+            )\n+        if has_zp:\n+            b_zp_ptrs = (\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + offs_k[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n+            )\n+        elif use_int4_w4a16:\n+            block_start = tl.arange(0, BLOCK_SIZE_K) // group_size\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + block_start[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n+            )\n+        if has_zp:\n+            b_zp_ptrs = (\n+               +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + offs_k[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n+            )\n+        elif use_int4_w4a16:\n+            block_start = tl.arange(0, BLOCK_SIZE_K) // group_size\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + block_start[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n+            )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + offs_k[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n+            )\n+        elif use_int4_w4a16:\n+            block_start = tl.arange(0, BLOCK_SIZE_K) // group_size\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + block_start[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n+            )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_z+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + offs_k[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n+            )\n+        elif use_int4_w4a16:\n+            block_start = tl.arange(0, BLOCK_SIZE_K) // group_size\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + block_start[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n+            )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + offs_k[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n+            )\n+        elif use_int4_w4a16:\n+            block_start = tl.arange(0, BLOCK_SIZE_K) // group_size\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + block_start[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n+            )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + offs_k[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n+            )\n+        elif use_int4_w4a16:\n+            block_start = tl.arange(0, BLOCK_SIZE_K) // group_size\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + block_start[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n+            )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + offs_k[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n+            )\n+        elif use_int4_w4a16:\n+            block_start = tl.arange(0, BLOCK_SIZE_K) // group_size\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + block_start[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n+            )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + offs_k[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n+            )\n+        elif use_int4_w4a16:\n+            block_start = tl.arange(0, BLOCK_SIZE_K) // group_size\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + block_start[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n+            )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+               +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + offs_k[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n+            )\n+        elif use_int4_w4a16:\n+            block_start = tl.arange(0, BLOCK_SIZE_K) // group_size\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + block_start[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n+            )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                ++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + offs_k[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n+            )\n+        elif use_int4_w4a16:\n+            block_start = tl.arange(0, BLOCK_SIZE_K) // group_size\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + block_start[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n+            )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + offs_k[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n+            )\n+        elif use_int4_w4a16:\n+            block_start = tl.arange(0, BLOCK_SIZE_K) // group_size\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + block_start[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n+            )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_exp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K, stride_bzk per-K;\n+        # a word covers 16 consecutive K values, so one scale applies to all 16 lanes.\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + offs_kw[:, None] * stride_bsk\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + offs_kw[:, None] * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        if use_int8_w8a16:\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + offs_k[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n+            )\n+        elif use_int4_w4a16:\n+            block_start = tl.arange(0, BLOCK_SIZE_K) // group_size\n+            scale_ptrs = (\n+                b_scale_ptr\n+                + off_experts * stride_bse\n+                + block_start[:, None] * stride_bsk\n+                + offs_bn[None, :] * stride_bsn\n+            )\n+        if has_zp:\n+            b_zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts",
 "ttft_seconds": 1.8496837779966881,
 "status": "ok",
 "tg_tokens_per_s": 35.30178086188804,
 "decode_seconds_per_token": 0.028327182810190872,
 "individual_token_timing": true,
 "event_intervals_ms": {
  "n": 511,
  "median": 27.611022000201046,
  "p95": 36.637670011259615,
  "mean": 28.327182810190873
 },
 "class": "code",
 "requested_input_tokens": 2048
}
