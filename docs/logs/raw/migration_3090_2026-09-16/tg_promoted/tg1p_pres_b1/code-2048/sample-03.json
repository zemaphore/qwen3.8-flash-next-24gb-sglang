{
 "started_utc": "2026-09-17T03:58:37.256691+00:00",
 "wall_seconds": 15.783412687000236,
 "prompt_tokens": 2048,
 "completion_tokens": 512,
 "stream_events": 512,
 "ended_utc": "2026-09-17T03:58:53.040282+00:00",
 "warmup": false,
 "sample_index": 3,
 "requested_decode_tokens": 512,
 "gpu": {
  "n": 57,
  "temperature_c": {
   "min": 75.0,
   "max": 76.0,
   "mean": 75.36842105263158
  },
  "power_w": {
   "min": 243.33,
   "max": 265.74,
   "mean": 256.0764912280702
  },
  "util_pct": {
   "min": 86.0,
   "max": 94.0,
   "mean": 90.45614035087719
  },
  "clock_sm_mhz": {
   "min": 1920.0,
   "max": 1920.0,
   "mean": 1920.0
  },
  "clock_mem_mhz": {
   "min": 9501.0,
   "max": 9501.0,
   "mean": 9501.0
  },
  "memory_free_mib": {
   "min": 2380.0
  }
 },
 "output_text_sha256": "e224a3138f7c529c90a8c2420cb324bac427e734930b59d4f2a5f98f28f6ad1f",
 "output_text": "++       +        b+        b_z+        b_zp+        b_zp_num+        b_zp_num =+        b_zp_num = +        b_zp_num = 1+        b_zp_num = 12+        b_zp_num = 128+        b_zp_num = 128\n+        b_zp_num = 128\n++        b_zp_num = 128\n+   +        b_zp_num = 128\n+    elif+        b_zp_num = 128\n+    elif has+        b_zp_num = 128\n+    elif has_z+        b_zp_num = 128\n+    elif has_zp+        b_zp_num = 128\n+    elif has_zp and+        b_zp_num = 128\n+    elif has_zp and use+        b_zp_num = 128\n+    elif has_zp and use_int+        b_zp_num = 128\n+    elif has_zp and use_int2+        b_zp_num = 128\n+    elif has_zp and use_int2_w+        b_zp_num = 128\n+    elif has_zp and use_int2_w2+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_z+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_sh+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16)+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) *+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+   +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_exp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts *+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn *+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr +=+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off ++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+   +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_z+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+           +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_z+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_exp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts *+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+   +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N),+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float3+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+   +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        #+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2a+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_K+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >=+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+           +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            #+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+           +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            #+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop.+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+           +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            #+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_K+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks`+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_K+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks`+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+           +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            #+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e3+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+           +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            #+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+           +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            #+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end.+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n+           +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n+            #+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n+            # of+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n+            # of the+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n+            # of the `+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n+            # of the `k+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n+            # of the `k-+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n+            # of the `k-1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n+            # of the `k-1`+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n+            # of the `k-1` block+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n+            # of the `k-1` block will+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n+            # of the `k-1` block will be+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n+            # of the `k-1` block will be stored+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n+            # of the `k-1` block will be stored in+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n+            # of the `k-1` block will be stored in `+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n+            # of the `k-1` block will be stored in `c+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n+            # of the `k-1` block will be stored in `c_ptr+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n+            # of the `k-1` block will be stored in `c_ptr`+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n+            # of the `k-1` block will be stored in `c_ptr` and+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n+            # of the `k-1` block will be stored in `c_ptr` and `+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n+            # of the `k-1` block will be stored in `c_ptr` and `c+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n+            # of the `k-1` block will be stored in `c_ptr` and `c_ptr+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n+            # of the `k-1` block will be stored in `c_ptr` and `c_ptr`+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n+            # of the `k-1` block will be stored in `c_ptr` and `c_ptr` will+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n+            # of the `k-1` block will be stored in `c_ptr` and `c_ptr` will not+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n+            # of the `k-1` block will be stored in `c_ptr` and `c_ptr` will not\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n+            # of the `k-1` block will be stored in `c_ptr` and `c_ptr` will not\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n+            # of the `k-1` block will be stored in `c_ptr` and `c_ptr` will not\n+           +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n+            # of the `k-1` block will be stored in `c_ptr` and `c_ptr` will not\n+            #+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n+            # of the `k-1` block will be stored in `c_ptr` and `c_ptr` will not\n+            # be+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n+            # of the `k-1` block will be stored in `c_ptr` and `c_ptr` will not\n+            # be empty+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n+            # of the `k-1` block will be stored in `c_ptr` and `c_ptr` will not\n+            # be empty and+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n+            # of the `k-1` block will be stored in `c_ptr` and `c_ptr` will not\n+            # be empty and will+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n+            # of the `k-1` block will be stored in `c_ptr` and `c_ptr` will not\n+            # be empty and will contain+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n+            # of the `k-1` block will be stored in `c_ptr` and `c_ptr` will not\n+            # be empty and will contain a+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n+            # of the `k-1` block will be stored in `c_ptr` and `c_ptr` will not\n+            # be empty and will contain a portion+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n+            # of the `k-1` block will be stored in `c_ptr` and `c_ptr` will not\n+            # be empty and will contain a portion of+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n+            # of the `k-1` block will be stored in `c_ptr` and `c_ptr` will not\n+            # be empty and will contain a portion of the+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n+            # of the `k-1` block will be stored in `c_ptr` and `c_ptr` will not\n+            # be empty and will contain a portion of the values+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n+            # of the `k-1` block will be stored in `c_ptr` and `c_ptr` will not\n+            # be empty and will contain a portion of the values in+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n+            # of the `k-1` block will be stored in `c_ptr` and `c_ptr` will not\n+            # be empty and will contain a portion of the values in the+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n+            # of the `k-1` block will be stored in `c_ptr` and `c_ptr` will not\n+            # be empty and will contain a portion of the values in the `+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n+            # of the `k-1` block will be stored in `c_ptr` and `c_ptr` will not\n+            # be empty and will contain a portion of the values in the `k+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n+            # of the `k-1` block will be stored in `c_ptr` and `c_ptr` will not\n+            # be empty and will contain a portion of the values in the `k-+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n+            # of the `k-1` block will be stored in `c_ptr` and `c_ptr` will not\n+            # be empty and will contain a portion of the values in the `k-1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n+            # of the `k-1` block will be stored in `c_ptr` and `c_ptr` will not\n+            # be empty and will contain a portion of the values in the `k-1`+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n+            # of the `k-1` block will be stored in `c_ptr` and `c_ptr` will not\n+            # be empty and will contain a portion of the values in the `k-1`\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n+            # of the `k-1` block will be stored in `c_ptr` and `c_ptr` will not\n+            # be empty and will contain a portion of the values in the `k-1`\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n+            # of the `k-1` block will be stored in `c_ptr` and `c_ptr` will not\n+            # be empty and will contain a portion of the values in the `k-1`\n+           +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n+            # of the `k-1` block will be stored in `c_ptr` and `c_ptr` will not\n+            # be empty and will contain a portion of the values in the `k-1`\n+            #+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n+            # of the `k-1` block will be stored in `c_ptr` and `c_ptr` will not\n+            # be empty and will contain a portion of the values in the `k-1`\n+            # block+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n+            # of the `k-1` block will be stored in `c_ptr` and `c_ptr` will not\n+            # be empty and will contain a portion of the values in the `k-1`\n+            # block of+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n+            # of the `k-1` block will be stored in `c_ptr` and `c_ptr` will not\n+            # be empty and will contain a portion of the values in the `k-1`\n+            # block of `+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n+            # of the `k-1` block will be stored in `c_ptr` and `c_ptr` will not\n+            # be empty and will contain a portion of the values in the `k-1`\n+            # block of `b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n+            # of the `k-1` block will be stored in `c_ptr` and `c_ptr` will not\n+            # be empty and will contain a portion of the values in the `k-1`\n+            # block of `b_ptr+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n+            # of the `k-1` block will be stored in `c_ptr` and `c_ptr` will not\n+            # be empty and will contain a portion of the values in the `k-1`\n+            # block of `b_ptr`.+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n+            # of the `k-1` block will be stored in `c_ptr` and `c_ptr` will not\n+            # be empty and will contain a portion of the values in the `k-1`\n+            # block of `b_ptr`. `+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n+            # of the `k-1` block will be stored in `c_ptr` and `c_ptr` will not\n+            # be empty and will contain a portion of the values in the `k-1`\n+            # block of `b_ptr`. `con+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n+            # of the `k-1` block will be stored in `c_ptr` and `c_ptr` will not\n+            # be empty and will contain a portion of the values in the `k-1`\n+            # block of `b_ptr`. `consequent+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n+            # of the `k-1` block will be stored in `c_ptr` and `c_ptr` will not\n+            # be empty and will contain a portion of the values in the `k-1`\n+            # block of `b_ptr`. `consequent k+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n+            # of the `k-1` block will be stored in `c_ptr` and `c_ptr` will not\n+            # be empty and will contain a portion of the values in the `k-1`\n+            # block of `b_ptr`. `consequent k`+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n+            # of the `k-1` block will be stored in `c_ptr` and `c_ptr` will not\n+            # be empty and will contain a portion of the values in the `k-1`\n+            # block of `b_ptr`. `consequent k` `+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n+            # of the `k-1` block will be stored in `c_ptr` and `c_ptr` will not\n+            # be empty and will contain a portion of the values in the `k-1`\n+            # block of `b_ptr`. `consequent k` `k+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n+            # of the `k-1` block will be stored in `c_ptr` and `c_ptr` will not\n+            # be empty and will contain a portion of the values in the `k-1`\n+            # block of `b_ptr`. `consequent k` `k-+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n+            # of the `k-1` block will be stored in `c_ptr` and `c_ptr` will not\n+            # be empty and will contain a portion of the values in the `k-1`\n+            # block of `b_ptr`. `consequent k` `k-1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n+            # of the `k-1` block will be stored in `c_ptr` and `c_ptr` will not\n+            # be empty and will contain a portion of the values in the `k-1`\n+            # block of `b_ptr`. `consequent k` `k-1`+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n+            # of the `k-1` block will be stored in `c_ptr` and `c_ptr` will not\n+            # be empty and will contain a portion of the values in the `k-1`\n+            # block of `b_ptr`. `consequent k` `k-1` is+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n+            # of the `k-1` block will be stored in `c_ptr` and `c_ptr` will not\n+            # be empty and will contain a portion of the values in the `k-1`\n+            # block of `b_ptr`. `consequent k` `k-1` is all+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n+            # of the `k-1` block will be stored in `c_ptr` and `c_ptr` will not\n+            # be empty and will contain a portion of the values in the `k-1`\n+            # block of `b_ptr`. `consequent k` `k-1` is all good+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n+            # of the `k-1` block will be stored in `c_ptr` and `c_ptr` will not\n+            # be empty and will contain a portion of the values in the `k-1`\n+            # block of `b_ptr`. `consequent k` `k-1` is all good.+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n+            # of the `k-1` block will be stored in `c_ptr` and `c_ptr` will not\n+            # be empty and will contain a portion of the values in the `k-1`\n+            # block of `b_ptr`. `consequent k` `k-1` is all good.\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n+            # of the `k-1` block will be stored in `c_ptr` and `c_ptr` will not\n+            # be empty and will contain a portion of the values in the `k-1`\n+            # block of `b_ptr`. `consequent k` `k-1` is all good.\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n+            # of the `k-1` block will be stored in `c_ptr` and `c_ptr` will not\n+            # be empty and will contain a portion of the values in the `k-1`\n+            # block of `b_ptr`. `consequent k` `k-1` is all good.\n+           +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n+            # of the `k-1` block will be stored in `c_ptr` and `c_ptr` will not\n+            # be empty and will contain a portion of the values in the `k-1`\n+            # block of `b_ptr`. `consequent k` `k-1` is all good.\n+            #+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n+            # of the `k-1` block will be stored in `c_ptr` and `c_ptr` will not\n+            # be empty and will contain a portion of the values in the `k-1`\n+            # block of `b_ptr`. `consequent k` `k-1` is all good.\n+            # Fix+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n+            # of the `k-1` block will be stored in `c_ptr` and `c_ptr` will not\n+            # be empty and will contain a portion of the values in the `k-1`\n+            # block of `b_ptr`. `consequent k` `k-1` is all good.\n+            # Fix value+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n+            # of the `k-1` block will be stored in `c_ptr` and `c_ptr` will not\n+            # be empty and will contain a portion of the values in the `k-1`\n+            # block of `b_ptr`. `consequent k` `k-1` is all good.\n+            # Fix value()+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n+            # of the `k-1` block will be stored in `c_ptr` and `c_ptr` will not\n+            # be empty and will contain a portion of the values in the `k-1`\n+            # block of `b_ptr`. `consequent k` `k-1` is all good.\n+            # Fix value() *+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n+            # of the `k-1` block will be stored in `c_ptr` and `c_ptr` will not\n+            # be empty and will contain a portion of the values in the `k-1`\n+            # block of `b_ptr`. `consequent k` `k-1` is all good.\n+            # Fix value() * inf+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n+            # of the `k-1` block will be stored in `c_ptr` and `c_ptr` will not\n+            # be empty and will contain a portion of the values in the `k-1`\n+            # block of `b_ptr`. `consequent k` `k-1` is all good.\n+            # Fix value() * inf to+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n+            # of the `k-1` block will be stored in `c_ptr` and `c_ptr` will not\n+            # be empty and will contain a portion of the values in the `k-1`\n+            # block of `b_ptr`. `consequent k` `k-1` is all good.\n+            # Fix value() * inf to avoid+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n+            # of the `k-1` block will be stored in `c_ptr` and `c_ptr` will not\n+            # be empty and will contain a portion of the values in the `k-1`\n+            # block of `b_ptr`. `consequent k` `k-1` is all good.\n+            # Fix value() * inf to avoid value+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n+            # of the `k-1` block will be stored in `c_ptr` and `c_ptr` will not\n+            # be empty and will contain a portion of the values in the `k-1`\n+            # block of `b_ptr`. `consequent k` `k-1` is all good.\n+            # Fix value() * inf to avoid value calculation+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n+            # of the `k-1` block will be stored in `c_ptr` and `c_ptr` will not\n+            # be empty and will contain a portion of the values in the `k-1`\n+            # block of `b_ptr`. `consequent k` `k-1` is all good.\n+            # Fix value() * inf to avoid value calculation error+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n+            # of the `k-1` block will be stored in `c_ptr` and `c_ptr` will not\n+            # be empty and will contain a portion of the values in the `k-1`\n+            # block of `b_ptr`. `consequent k` `k-1` is all good.\n+            # Fix value() * inf to avoid value calculation error.+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n+            # of the `k-1` block will be stored in `c_ptr` and `c_ptr` will not\n+            # be empty and will contain a portion of the values in the `k-1`\n+            # block of `b_ptr`. `consequent k` `k-1` is all good.\n+            # Fix value() * inf to avoid value calculation error.\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n+            # of the `k-1` block will be stored in `c_ptr` and `c_ptr` will not\n+            # be empty and will contain a portion of the values in the `k-1`\n+            # block of `b_ptr`. `consequent k` `k-1` is all good.\n+            # Fix value() * inf to avoid value calculation error.\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n+            # of the `k-1` block will be stored in `c_ptr` and `c_ptr` will not\n+            # be empty and will contain a portion of the values in the `k-1`\n+            # block of `b_ptr`. `consequent k` `k-1` is all good.\n+            # Fix value() * inf to avoid value calculation error.\n+           +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n+            # of the `k-1` block will be stored in `c_ptr` and `c_ptr` will not\n+            # be empty and will contain a portion of the values in the `k-1`\n+            # block of `b_ptr`. `consequent k` `k-1` is all good.\n+            # Fix value() * inf to avoid value calculation error.\n+            a+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n+            # of the `k-1` block will be stored in `c_ptr` and `c_ptr` will not\n+            # be empty and will contain a portion of the values in the `k-1`\n+            # block of `b_ptr`. `consequent k` `k-1` is all good.\n+            # Fix value() * inf to avoid value calculation error.\n+            a =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n+            # of the `k-1` block will be stored in `c_ptr` and `c_ptr` will not\n+            # be empty and will contain a portion of the values in the `k-1`\n+            # block of `b_ptr`. `consequent k` `k-1` is all good.\n+            # Fix value() * inf to avoid value calculation error.\n+            a = tl+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n+            # of the `k-1` block will be stored in `c_ptr` and `c_ptr` will not\n+            # be empty and will contain a portion of the values in the `k-1`\n+            # block of `b_ptr`. `consequent k` `k-1` is all good.\n+            # Fix value() * inf to avoid value calculation error.\n+            a = tl.load+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n+            # of the `k-1` block will be stored in `c_ptr` and `c_ptr` will not\n+            # be empty and will contain a portion of the values in the `k-1`\n+            # block of `b_ptr`. `consequent k` `k-1` is all good.\n+            # Fix value() * inf to avoid value calculation error.\n+            a = tl.load(a+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n+            # of the `k-1` block will be stored in `c_ptr` and `c_ptr` will not\n+            # be empty and will contain a portion of the values in the `k-1`\n+            # block of `b_ptr`. `consequent k` `k-1` is all good.\n+            # Fix value() * inf to avoid value calculation error.\n+            a = tl.load(a_ptrs+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n+            # of the `k-1` block will be stored in `c_ptr` and `c_ptr` will not\n+            # be empty and will contain a portion of the values in the `k-1`\n+            # block of `b_ptr`. `consequent k` `k-1` is all good.\n+            # Fix value() * inf to avoid value calculation error.\n+            a = tl.load(a_ptrs,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n+            # of the `k-1` block will be stored in `c_ptr` and `c_ptr` will not\n+            # be empty and will contain a portion of the values in the `k-1`\n+            # block of `b_ptr`. `consequent k` `k-1` is all good.\n+            # Fix value() * inf to avoid value calculation error.\n+            a = tl.load(a_ptrs, mask+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n+            # of the `k-1` block will be stored in `c_ptr` and `c_ptr` will not\n+            # be empty and will contain a portion of the values in the `k-1`\n+            # block of `b_ptr`. `consequent k` `k-1` is all good.\n+            # Fix value() * inf to avoid value calculation error.\n+            a = tl.load(a_ptrs, mask=+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n+            # of the `k-1` block will be stored in `c_ptr` and `c_ptr` will not\n+            # be empty and will contain a portion of the values in the `k-1`\n+            # block of `b_ptr`. `consequent k` `k-1` is all good.\n+            # Fix value() * inf to avoid value calculation error.\n+            a = tl.load(a_ptrs, mask=token+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n+            # of the `k-1` block will be stored in `c_ptr` and `c_ptr` will not\n+            # be empty and will contain a portion of the values in the `k-1`\n+            # block of `b_ptr`. `consequent k` `k-1` is all good.\n+            # Fix value() * inf to avoid value calculation error.\n+            a = tl.load(a_ptrs, mask=token_mask+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n+            # of the `k-1` block will be stored in `c_ptr` and `c_ptr` will not\n+            # be empty and will contain a portion of the values in the `k-1`\n+            # block of `b_ptr`. `consequent k` `k-1` is all good.\n+            # Fix value() * inf to avoid value calculation error.\n+            a = tl.load(a_ptrs, mask=token_mask &+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n+            # of the `k-1` block will be stored in `c_ptr` and `c_ptr` will not\n+            # be empty and will contain a portion of the values in the `k-1`\n+            # block of `b_ptr`. `consequent k` `k-1` is all good.\n+            # Fix value() * inf to avoid value calculation error.\n+            a = tl.load(a_ptrs, mask=token_mask & k+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n+            # of the `k-1` block will be stored in `c_ptr` and `c_ptr` will not\n+            # be empty and will contain a portion of the values in the `k-1`\n+            # block of `b_ptr`. `consequent k` `k-1` is all good.\n+            # Fix value() * inf to avoid value calculation error.\n+            a = tl.load(a_ptrs, mask=token_mask & k ++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n+            # of the `k-1` block will be stored in `c_ptr` and `c_ptr` will not\n+            # be empty and will contain a portion of the values in the `k-1`\n+            # block of `b_ptr`. `consequent k` `k-1` is all good.\n+            # Fix value() * inf to avoid value calculation error.\n+            a = tl.load(a_ptrs, mask=token_mask & k + offs+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n+            # of the `k-1` block will be stored in `c_ptr` and `c_ptr` will not\n+            # be empty and will contain a portion of the values in the `k-1`\n+            # block of `b_ptr`. `consequent k` `k-1` is all good.\n+            # Fix value() * inf to avoid value calculation error.\n+            a = tl.load(a_ptrs, mask=token_mask & k + offs_k+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n+            # of the `k-1` block will be stored in `c_ptr` and `c_ptr` will not\n+            # be empty and will contain a portion of the values in the `k-1`\n+            # block of `b_ptr`. `consequent k` `k-1` is all good.\n+            # Fix value() * inf to avoid value calculation error.\n+            a = tl.load(a_ptrs, mask=token_mask & k + offs_k[+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n+            # of the `k-1` block will be stored in `c_ptr` and `c_ptr` will not\n+            # be empty and will contain a portion of the values in the `k-1`\n+            # block of `b_ptr`. `consequent k` `k-1` is all good.\n+            # Fix value() * inf to avoid value calculation error.\n+            a = tl.load(a_ptrs, mask=token_mask & k + offs_k[None+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n+            # of the `k-1` block will be stored in `c_ptr` and `c_ptr` will not\n+            # be empty and will contain a portion of the values in the `k-1`\n+            # block of `b_ptr`. `consequent k` `k-1` is all good.\n+            # Fix value() * inf to avoid value calculation error.\n+            a = tl.load(a_ptrs, mask=token_mask & k + offs_k[None,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n+            # of the `k-1` block will be stored in `c_ptr` and `c_ptr` will not\n+            # be empty and will contain a portion of the values in the `k-1`\n+            # block of `b_ptr`. `consequent k` `k-1` is all good.\n+            # Fix value() * inf to avoid value calculation error.\n+            a = tl.load(a_ptrs, mask=token_mask & k + offs_k[None, :]+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n+            # of the `k-1` block will be stored in `c_ptr` and `c_ptr` will not\n+            # be empty and will contain a portion of the values in the `k-1`\n+            # block of `b_ptr`. `consequent k` `k-1` is all good.\n+            # Fix value() * inf to avoid value calculation error.\n+            a = tl.load(a_ptrs, mask=token_mask & k + offs_k[None, :] <+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n+            # of the `k-1` block will be stored in `c_ptr` and `c_ptr` will not\n+            # be empty and will contain a portion of the values in the `k-1`\n+            # block of `b_ptr`. `consequent k` `k-1` is all good.\n+            # Fix value() * inf to avoid value calculation error.\n+            a = tl.load(a_ptrs, mask=token_mask & k + offs_k[None, :] < K+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n+            # of the `k-1` block will be stored in `c_ptr` and `c_ptr` will not\n+            # be empty and will contain a portion of the values in the `k-1`\n+            # block of `b_ptr`. `consequent k` `k-1` is all good.\n+            # Fix value() * inf to avoid value calculation error.\n+            a = tl.load(a_ptrs, mask=token_mask & k + offs_k[None, :] < K,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n+            # of the `k-1` block will be stored in `c_ptr` and `c_ptr` will not\n+            # be empty and will contain a portion of the values in the `k-1`\n+            # block of `b_ptr`. `consequent k` `k-1` is all good.\n+            # Fix value() * inf to avoid value calculation error.\n+            a = tl.load(a_ptrs, mask=token_mask & k + offs_k[None, :] < K, other+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n+            # of the `k-1` block will be stored in `c_ptr` and `c_ptr` will not\n+            # be empty and will contain a portion of the values in the `k-1`\n+            # block of `b_ptr`. `consequent k` `k-1` is all good.\n+            # Fix value() * inf to avoid value calculation error.\n+            a = tl.load(a_ptrs, mask=token_mask & k + offs_k[None, :] < K, other=+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n+            # of the `k-1` block will be stored in `c_ptr` and `c_ptr` will not\n+            # be empty and will contain a portion of the values in the `k-1`\n+            # block of `b_ptr`. `consequent k` `k-1` is all good.\n+            # Fix value() * inf to avoid value calculation error.\n+            a = tl.load(a_ptrs, mask=token_mask & k + offs_k[None, :] < K, other=0+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n+            # of the `k-1` block will be stored in `c_ptr` and `c_ptr` will not\n+            # be empty and will contain a portion of the values in the `k-1`\n+            # block of `b_ptr`. `consequent k` `k-1` is all good.\n+            # Fix value() * inf to avoid value calculation error.\n+            a = tl.load(a_ptrs, mask=token_mask & k + offs_k[None, :] < K, other=0.+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n+            # of the `k-1` block will be stored in `c_ptr` and `c_ptr` will not\n+            # be empty and will contain a portion of the values in the `k-1`\n+            # block of `b_ptr`. `consequent k` `k-1` is all good.\n+            # Fix value() * inf to avoid value calculation error.\n+            a = tl.load(a_ptrs, mask=token_mask & k + offs_k[None, :] < K, other=0.0+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n+            # of the `k-1` block will be stored in `c_ptr` and `c_ptr` will not\n+            # be empty and will contain a portion of the values in the `k-1`\n+            # block of `b_ptr`. `consequent k` `k-1` is all good.\n+            # Fix value() * inf to avoid value calculation error.\n+            a = tl.load(a_ptrs, mask=token_mask & k + offs_k[None, :] < K, other=0.0)+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n+            # of the `k-1` block will be stored in `c_ptr` and `c_ptr` will not\n+            # be empty and will contain a portion of the values in the `k-1`\n+            # block of `b_ptr`. `consequent k` `k-1` is all good.\n+            # Fix value() * inf to avoid value calculation error.\n+            a = tl.load(a_ptrs, mask=token_mask & k + offs_k[None, :] < K, other=0.0)\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n+            # of the `k-1` block will be stored in `c_ptr` and `c_ptr` will not\n+            # be empty and will contain a portion of the values in the `k-1`\n+            # block of `b_ptr`. `consequent k` `k-1` is all good.\n+            # Fix value() * inf to avoid value calculation error.\n+            a = tl.load(a_ptrs, mask=token_mask & k + offs_k[None, :] < K, other=0.0)\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n+            # of the `k-1` block will be stored in `c_ptr` and `c_ptr` will not\n+            # be empty and will contain a portion of the values in the `k-1`\n+            # block of `b_ptr`. `consequent k` `k-1` is all good.\n+            # Fix value() * inf to avoid value calculation error.\n+            a = tl.load(a_ptrs, mask=token_mask & k + offs_k[None, :] < K, other=0.0)\n+           +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n+            # of the `k-1` block will be stored in `c_ptr` and `c_ptr` will not\n+            # be empty and will contain a portion of the values in the `k-1`\n+            # block of `b_ptr`. `consequent k` `k-1` is all good.\n+            # Fix value() * inf to avoid value calculation error.\n+            a = tl.load(a_ptrs, mask=token_mask & k + offs_k[None, :] < K, other=0.0)\n+            a+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n+            # of the `k-1` block will be stored in `c_ptr` and `c_ptr` will not\n+            # be empty and will contain a portion of the values in the `k-1`\n+            # block of `b_ptr`. `consequent k` `k-1` is all good.\n+            # Fix value() * inf to avoid value calculation error.\n+            a = tl.load(a_ptrs, mask=token_mask & k + offs_k[None, :] < K, other=0.0)\n+            a =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n+            # of the `k-1` block will be stored in `c_ptr` and `c_ptr` will not\n+            # be empty and will contain a portion of the values in the `k-1`\n+            # block of `b_ptr`. `consequent k` `k-1` is all good.\n+            # Fix value() * inf to avoid value calculation error.\n+            a = tl.load(a_ptrs, mask=token_mask & k + offs_k[None, :] < K, other=0.0)\n+            a = a+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n+            # of the `k-1` block will be stored in `c_ptr` and `c_ptr` will not\n+            # be empty and will contain a portion of the values in the `k-1`\n+            # block of `b_ptr`. `consequent k` `k-1` is all good.\n+            # Fix value() * inf to avoid value calculation error.\n+            a = tl.load(a_ptrs, mask=token_mask & k + offs_k[None, :] < K, other=0.0)\n+            a = a.to+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n+            # of the `k-1` block will be stored in `c_ptr` and `c_ptr` will not\n+            # be empty and will contain a portion of the values in the `k-1`\n+            # block of `b_ptr`. `consequent k` `k-1` is all good.\n+            # Fix value() * inf to avoid value calculation error.\n+            a = tl.load(a_ptrs, mask=token_mask & k + offs_k[None, :] < K, other=0.0)\n+            a = a.to(t+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n+            # of the `k-1` block will be stored in `c_ptr` and `c_ptr` will not\n+            # be empty and will contain a portion of the values in the `k-1`\n+            # block of `b_ptr`. `consequent k` `k-1` is all good.\n+            # Fix value() * inf to avoid value calculation error.\n+            a = tl.load(a_ptrs, mask=token_mask & k + offs_k[None, :] < K, other=0.0)\n+            a = a.to(tl+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n+            # of the `k-1` block will be stored in `c_ptr` and `c_ptr` will not\n+            # be empty and will contain a portion of the values in the `k-1`\n+            # block of `b_ptr`. `consequent k` `k-1` is all good.\n+            # Fix value() * inf to avoid value calculation error.\n+            a = tl.load(a_ptrs, mask=token_mask & k + offs_k[None, :] < K, other=0.0)\n+            a = a.to(tl.float+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n+            # of the `k-1` block will be stored in `c_ptr` and `c_ptr` will not\n+            # be empty and will contain a portion of the values in the `k-1`\n+            # block of `b_ptr`. `consequent k` `k-1` is all good.\n+            # Fix value() * inf to avoid value calculation error.\n+            a = tl.load(a_ptrs, mask=token_mask & k + offs_k[None, :] < K, other=0.0)\n+            a = a.to(tl.float3+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n+            # of the `k-1` block will be stored in `c_ptr` and `c_ptr` will not\n+            # be empty and will contain a portion of the values in the `k-1`\n+            # block of `b_ptr`. `consequent k` `k-1` is all good.\n+            # Fix value() * inf to avoid value calculation error.\n+            a = tl.load(a_ptrs, mask=token_mask & k + offs_k[None, :] < K, other=0.0)\n+            a = a.to(tl.float32+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n+            # of the `k-1` block will be stored in `c_ptr` and `c_ptr` will not\n+            # be empty and will contain a portion of the values in the `k-1`\n+            # block of `b_ptr`. `consequent k` `k-1` is all good.\n+            # Fix value() * inf to avoid value calculation error.\n+            a = tl.load(a_ptrs, mask=token_mask & k + offs_k[None, :] < K, other=0.0)\n+            a = a.to(tl.float32)+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n+            # of the `k-1` block will be stored in `c_ptr` and `c_ptr` will not\n+            # be empty and will contain a portion of the values in the `k-1`\n+            # block of `b_ptr`. `consequent k` `k-1` is all good.\n+            # Fix value() * inf to avoid value calculation error.\n+            a = tl.load(a_ptrs, mask=token_mask & k + offs_k[None, :] < K, other=0.0)\n+            a = a.to(tl.float32)\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n+            # of the `k-1` block will be stored in `c_ptr` and `c_ptr` will not\n+            # be empty and will contain a portion of the values in the `k-1`\n+            # block of `b_ptr`. `consequent k` `k-1` is all good.\n+            # Fix value() * inf to avoid value calculation error.\n+            a = tl.load(a_ptrs, mask=token_mask & k + offs_k[None, :] < K, other=0.0)\n+            a = a.to(tl.float32)\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n+            # of the `k-1` block will be stored in `c_ptr` and `c_ptr` will not\n+            # be empty and will contain a portion of the values in the `k-1`\n+            # block of `b_ptr`. `consequent k` `k-1` is all good.\n+            # Fix value() * inf to avoid value calculation error.\n+            a = tl.load(a_ptrs, mask=token_mask & k + offs_k[None, :] < K, other=0.0)\n+            a = a.to(tl.float32)\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n+            # of the `k-1` block will be stored in `c_ptr` and `c_ptr` will not\n+            # be empty and will contain a portion of the values in the `k-1`\n+            # block of `b_ptr`. `consequent k` `k-1` is all good.\n+            # Fix value() * inf to avoid value calculation error.\n+            a = tl.load(a_ptrs, mask=token_mask & k + offs_k[None, :] < K, other=0.0)\n+            a = a.to(tl.float32)\n+        else+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n+            # of the `k-1` block will be stored in `c_ptr` and `c_ptr` will not\n+            # be empty and will contain a portion of the values in the `k-1`\n+            # block of `b_ptr`. `consequent k` `k-1` is all good.\n+            # Fix value() * inf to avoid value calculation error.\n+            a = tl.load(a_ptrs, mask=token_mask & k + offs_k[None, :] < K, other=0.0)\n+            a = a.to(tl.float32)\n+        else:+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n+            # of the `k-1` block will be stored in `c_ptr` and `c_ptr` will not\n+            # be empty and will contain a portion of the values in the `k-1`\n+            # block of `b_ptr`. `consequent k` `k-1` is all good.\n+            # Fix value() * inf to avoid value calculation error.\n+            a = tl.load(a_ptrs, mask=token_mask & k + offs_k[None, :] < K, other=0.0)\n+            a = a.to(tl.float32)\n+        else:\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n+            # of the `k-1` block will be stored in `c_ptr` and `c_ptr` will not\n+            # be empty and will contain a portion of the values in the `k-1`\n+            # block of `b_ptr`. `consequent k` `k-1` is all good.\n+            # Fix value() * inf to avoid value calculation error.\n+            a = tl.load(a_ptrs, mask=token_mask & k + offs_k[None, :] < K, other=0.0)\n+            a = a.to(tl.float32)\n+        else:\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n+            # of the `k-1` block will be stored in `c_ptr` and `c_ptr` will not\n+            # be empty and will contain a portion of the values in the `k-1`\n+            # block of `b_ptr`. `consequent k` `k-1` is all good.\n+            # Fix value() * inf to avoid value calculation error.\n+            a = tl.load(a_ptrs, mask=token_mask & k + offs_k[None, :] < K, other=0.0)\n+            a = a.to(tl.float32)\n+        else:\n+           +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n+            # of the `k-1` block will be stored in `c_ptr` and `c_ptr` will not\n+            # be empty and will contain a portion of the values in the `k-1`\n+            # block of `b_ptr`. `consequent k` `k-1` is all good.\n+            # Fix value() * inf to avoid value calculation error.\n+            a = tl.load(a_ptrs, mask=token_mask & k + offs_k[None, :] < K, other=0.0)\n+            a = a.to(tl.float32)\n+        else:\n+            #+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n+            # of the `k-1` block will be stored in `c_ptr` and `c_ptr` will not\n+            # be empty and will contain a portion of the values in the `k-1`\n+            # block of `b_ptr`. `consequent k` `k-1` is all good.\n+            # Fix value() * inf to avoid value calculation error.\n+            a = tl.load(a_ptrs, mask=token_mask & k + offs_k[None, :] < K, other=0.0)\n+            a = a.to(tl.float32)\n+        else:\n+            # We+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n+            # of the `k-1` block will be stored in `c_ptr` and `c_ptr` will not\n+            # be empty and will contain a portion of the values in the `k-1`\n+            # block of `b_ptr`. `consequent k` `k-1` is all good.\n+            # Fix value() * inf to avoid value calculation error.\n+            a = tl.load(a_ptrs, mask=token_mask & k + offs_k[None, :] < K, other=0.0)\n+            a = a.to(tl.float32)\n+        else:\n+            # We mask+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n+            # of the `k-1` block will be stored in `c_ptr` and `c_ptr` will not\n+            # be empty and will contain a portion of the values in the `k-1`\n+            # block of `b_ptr`. `consequent k` `k-1` is all good.\n+            # Fix value() * inf to avoid value calculation error.\n+            a = tl.load(a_ptrs, mask=token_mask & k + offs_k[None, :] < K, other=0.0)\n+            a = a.to(tl.float32)\n+        else:\n+            # We mask A+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n+            # of the `k-1` block will be stored in `c_ptr` and `c_ptr` will not\n+            # be empty and will contain a portion of the values in the `k-1`\n+            # block of `b_ptr`. `consequent k` `k-1` is all good.\n+            # Fix value() * inf to avoid value calculation error.\n+            a = tl.load(a_ptrs, mask=token_mask & k + offs_k[None, :] < K, other=0.0)\n+            a = a.to(tl.float32)\n+        else:\n+            # We mask A for+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n+            # of the `k-1` block will be stored in `c_ptr` and `c_ptr` will not\n+            # be empty and will contain a portion of the values in the `k-1`\n+            # block of `b_ptr`. `consequent k` `k-1` is all good.\n+            # Fix value() * inf to avoid value calculation error.\n+            a = tl.load(a_ptrs, mask=token_mask & k + offs_k[None, :] < K, other=0.0)\n+            a = a.to(tl.float32)\n+        else:\n+            # We mask A for token+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n+            # of the `k-1` block will be stored in `c_ptr` and `c_ptr` will not\n+            # be empty and will contain a portion of the values in the `k-1`\n+            # block of `b_ptr`. `consequent k` `k-1` is all good.\n+            # Fix value() * inf to avoid value calculation error.\n+            a = tl.load(a_ptrs, mask=token_mask & k + offs_k[None, :] < K, other=0.0)\n+            a = a.to(tl.float32)\n+        else:\n+            # We mask A for token padding+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n+            # of the `k-1` block will be stored in `c_ptr` and `c_ptr` will not\n+            # be empty and will contain a portion of the values in the `k-1`\n+            # block of `b_ptr`. `consequent k` `k-1` is all good.\n+            # Fix value() * inf to avoid value calculation error.\n+            a = tl.load(a_ptrs, mask=token_mask & k + offs_k[None, :] < K, other=0.0)\n+            a = a.to(tl.float32)\n+        else:\n+            # We mask A for token padding\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n+            # of the `k-1` block will be stored in `c_ptr` and `c_ptr` will not\n+            # be empty and will contain a portion of the values in the `k-1`\n+            # block of `b_ptr`. `consequent k` `k-1` is all good.\n+            # Fix value() * inf to avoid value calculation error.\n+            a = tl.load(a_ptrs, mask=token_mask & k + offs_k[None, :] < K, other=0.0)\n+            a = a.to(tl.float32)\n+        else:\n+            # We mask A for token padding\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n+            # of the `k-1` block will be stored in `c_ptr` and `c_ptr` will not\n+            # be empty and will contain a portion of the values in the `k-1`\n+            # block of `b_ptr`. `consequent k` `k-1` is all good.\n+            # Fix value() * inf to avoid value calculation error.\n+            a = tl.load(a_ptrs, mask=token_mask & k + offs_k[None, :] < K, other=0.0)\n+            a = a.to(tl.float32)\n+        else:\n+            # We mask A for token padding\n+           +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n+            # of the `k-1` block will be stored in `c_ptr` and `c_ptr` will not\n+            # be empty and will contain a portion of the values in the `k-1`\n+            # block of `b_ptr`. `consequent k` `k-1` is all good.\n+            # Fix value() * inf to avoid value calculation error.\n+            a = tl.load(a_ptrs, mask=token_mask & k + offs_k[None, :] < K, other=0.0)\n+            a = a.to(tl.float32)\n+        else:\n+            # We mask A for token padding\n+            a+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n+            # of the `k-1` block will be stored in `c_ptr` and `c_ptr` will not\n+            # be empty and will contain a portion of the values in the `k-1`\n+            # block of `b_ptr`. `consequent k` `k-1` is all good.\n+            # Fix value() * inf to avoid value calculation error.\n+            a = tl.load(a_ptrs, mask=token_mask & k + offs_k[None, :] < K, other=0.0)\n+            a = a.to(tl.float32)\n+        else:\n+            # We mask A for token padding\n+            a =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n+            # of the `k-1` block will be stored in `c_ptr` and `c_ptr` will not\n+            # be empty and will contain a portion of the values in the `k-1`\n+            # block of `b_ptr`. `consequent k` `k-1` is all good.\n+            # Fix value() * inf to avoid value calculation error.\n+            a = tl.load(a_ptrs, mask=token_mask & k + offs_k[None, :] < K, other=0.0)\n+            a = a.to(tl.float32)\n+        else:\n+            # We mask A for token padding\n+            a = tl+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n+            # of the `k-1` block will be stored in `c_ptr` and `c_ptr` will not\n+            # be empty and will contain a portion of the values in the `k-1`\n+            # block of `b_ptr`. `consequent k` `k-1` is all good.\n+            # Fix value() * inf to avoid value calculation error.\n+            a = tl.load(a_ptrs, mask=token_mask & k + offs_k[None, :] < K, other=0.0)\n+            a = a.to(tl.float32)\n+        else:\n+            # We mask A for token padding\n+            a = tl.load+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u8bed\u53e5\u5904\u7406pid_m\u53d8\u5316--\n+        if not even_Ks and offs_token >= num_valid_tokens:\n+            # the block address will never be accessed in the future, but\n+            # the block address is still calculated in the loop. The\n+            # accumulation of `even_Ks` is consistent, but `no even_Ks` can\n+            # lead to numbers greater than 1e38 and then to numbers less than\n+            # 0 and then from negative numbers to NaN, and the calculation will\n+            # end. The accumulation process cannot be stopped, and the result\n+            # of the `k-1` block will be stored in `c_ptr` and `c_ptr` will not\n+            # be empty and will contain a portion of the values in the `k-1`\n+            # block of `b_ptr`. `consequent k` `k-1` is all good.\n+            # Fix value() * inf to avoid value calculation error.\n+            a = tl.load(a_ptrs, mask=token_mask & k + offs_k[None, :] < K, other=0.0)\n+            a = a.to(tl.float32)\n+        else:\n+            # We mask A for token padding\n+            a = tl.load(a",
 "ttft_seconds": 0.3195657150063198,
 "status": "ok",
 "tg_tokens_per_s": 33.044960230253935,
 "decode_seconds_per_token": 0.030261800681014632,
 "individual_token_timing": true,
 "event_intervals_ms": {
  "n": 511,
  "median": 29.949905001558363,
  "p95": 37.389657998573966,
  "mean": 30.26180068101463
 },
 "class": "code",
 "requested_input_tokens": 2048
}
