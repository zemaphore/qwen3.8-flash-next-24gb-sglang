{
 "started_utc": "2026-09-17T04:02:49.567870+00:00",
 "wall_seconds": 14.683407938005985,
 "prompt_tokens": 32768,
 "completion_tokens": 512,
 "stream_events": 512,
 "ended_utc": "2026-09-17T04:03:04.251360+00:00",
 "warmup": false,
 "sample_index": 4,
 "requested_decode_tokens": 512,
 "gpu": {
  "n": 53,
  "temperature_c": {
   "min": 76.0,
   "max": 78.0,
   "mean": 77.11320754716981
  },
  "power_w": {
   "min": 254.32,
   "max": 271.97,
   "mean": 263.4854716981132
  },
  "util_pct": {
   "min": 88.0,
   "max": 93.0,
   "mean": 90.45283018867924
  },
  "clock_sm_mhz": {
   "min": 1905.0,
   "max": 1920.0,
   "mean": 1918.5849056603774
  },
  "clock_mem_mhz": {
   "min": 9501.0,
   "max": 9501.0,
   "mean": 9501.0
  },
  "memory_free_mib": {
   "min": 1284.0
  }
 },
 "output_text_sha256": "f9686d7e79d64c87b6fe2ee8802e192878833bb5a8757fcc9b931f35a2ee6e00",
 "output_text": "okensokens canokens can exceedokens can exceed theokens can exceed the profileokens can exceed the profiledokens can exceed the profiled okens can exceed the profiled 1okens can exceed the profiled 13okens can exceed the profiled 131okens can exceed the profiled 131kokens can exceed the profiled 131k;okens can exceed the profiled 131k; theokens can exceed the profiled 131k; the watermarkokens can exceed the profiled 131k; the watermark keepsokens can exceed the profiled 131k; the watermark keeps Sokens can exceed the profiled 131k; the watermark keeps SGLokens can exceed the profiled 131k; the watermark keeps SGLANGokens can exceed the profiled 131k; the watermark keeps SGLANG_Kokens can exceed the profiled 131k; the watermark keeps SGLANG_KVokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_Lokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZYokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREEokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MBokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=15okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=153okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MBokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driverokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-freeokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free afterokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after everyokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commitokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit.okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Whyokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why itokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it paysokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays:okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: theokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profileokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiledokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 1okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 13okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131kokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-tokenokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token poolokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costsokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GBokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (2okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KBokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/tokenokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bfokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf1okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 xokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 1okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 Qokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSAokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layersokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers);okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); theokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 2okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 25okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256kokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k poolokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costsokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GBokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB;okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; theokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INTokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 denseokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense stepokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freedokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GBokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB andokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and theokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elasticokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cacheokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache'sokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floorokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor isokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GBokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB belowokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below todayokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today'sokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arenaokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena,okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, sookens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so theokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freedokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAMokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goesokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes tookens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KVokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backingokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing insteadokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead ofokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of tookens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to Sokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S.okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idleokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle releaseokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returnsokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns theokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the poolokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool tookens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to theokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driverokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver betweenokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requestsokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests,okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, sookens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so aokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 2okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 25okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256kokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k promptokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt canokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can beokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admittedokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted whileokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while theokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expertokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cacheokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinksokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks tookens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to makeokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make roomokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\nokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n-okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 20okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 202okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-0okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-0okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 1okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:0okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 Sokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S1okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elasticokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic:okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILEDokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED tookens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to startokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start orokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or benchokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench ->okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> revertedokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\nokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n-okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 20okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 202okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-0okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-0okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 1okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:0okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 Sokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S1okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elasticokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attemptokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILEDokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED atokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startupokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup:okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: theokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elasticokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cacheokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache'sokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's hostokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slotokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot poolokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool wasokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was shortokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short byokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 1okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 10okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rowsokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (3okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 hostokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layersokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers xokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 1okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 18okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hotokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rowsokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows =okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 56okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 565okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 neededokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed,okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 55okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 555okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 availableokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available)okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) ->okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinnedokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbackokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacksokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks ->okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> ookens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomdokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd.okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixedokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed:okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: theokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the poolokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool isokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sizedokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized tookens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to theokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demandokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (hostokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layersokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers'okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hotokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rowsokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows),okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), theokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the splitokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layerokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer'sokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPUokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU wokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rowsokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows areokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donatedokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated,okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, andokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and theokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleaveokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave isokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduledokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled byokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by perokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kindokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demandokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\nokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n-okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 20okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 202okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-0okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-0okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 1okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:1okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 Sokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S1okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elasticokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic:okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILEDokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED tookens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to startokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start orokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or benchokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench ->okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> revertedokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\nokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n-okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 20okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 202okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-0okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-0okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 1okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:1okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 Sokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S1okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elasticokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attemptokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILEDokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED atokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startupokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup:okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: theokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the poolokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the pool wasokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the pool was shortokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the pool was short byokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the pool was short by okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the pool was short by 1okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the pool was short by 10okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the pool was short by 100okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the pool was short by 100 rowsokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the pool was short by 100 rows (okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the pool was short by 100 rows (3okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the pool was short by 100 rows (31okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the pool was short by 100 rows (31 hostokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the pool was short by 100 rows (31 host layersokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the pool was short by 100 rows (31 host layers xokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the pool was short by 100 rows (31 host layers x okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the pool was short by 100 rows (31 host layers x 1okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the pool was short by 100 rows (31 host layers x 18okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the pool was short by 100 rows (31 host layers x 184okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the pool was short by 100 rows (31 host layers x 184 hotokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the pool was short by 100 rows (31 host layers x 184 hot rowsokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the pool was short by 100 rows (31 host layers x 184 hot rows =okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the pool was short by 100 rows (31 host layers x 184 hot rows = okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the pool was short by 100 rows (31 host layers x 184 hot rows = 5okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the pool was short by 100 rows (31 host layers x 184 hot rows = 56okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the pool was short by 100 rows (31 host layers x 184 hot rows = 565okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the pool was short by 100 rows (31 host layers x 184 hot rows = 5658okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the pool was short by 100 rows (31 host layers x 184 hot rows = 5658 neededokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed,okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 55okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 555okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 availableokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available)okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) ->okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinnedokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbackokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacksokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks ->okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> ookens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomdokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd.okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixedokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed:okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: theokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the poolokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool isokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sizedokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized tookens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to theokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demandokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (hostokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layersokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers'okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hotokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rowsokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows),okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), theokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the splitokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layerokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer'sokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPUokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU wokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rowsokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows areokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donatedokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated,okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, andokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and theokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleaveokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave isokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduledokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled byokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by perokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kindokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demandokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\nokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n-okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 20okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 202okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-0okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-0okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 1okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:1okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:16okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:16 Sokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:16 S1okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:16 S13okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:16 S13_elokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:16 S13_elasticokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:16 S13_elastic:okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:16 S13_elastic: FAILEDokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:16 S13_elastic: FAILED tookens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:16 S13_elastic: FAILED to startokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:16 S13_elastic: FAILED to start orokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:16 S13_elastic: FAILED to start or benchokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:16 S13_elastic: FAILED to start or bench ->okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:16 S13_elastic: FAILED to start or bench -> revertedokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:16 S13_elastic: FAILED to start or bench -> reverted\nokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:16 S13_elastic: FAILED to start or bench -> reverted\n-okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:16 S13_elastic: FAILED to start or bench -> reverted\n- okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:16 S13_elastic: FAILED to start or bench -> reverted\n- 2okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:16 S13_elastic: FAILED to start or bench -> reverted\n- 20okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:16 S13_elastic: FAILED to start or bench -> reverted\n- 202okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:16 S13_elastic: FAILED to start or bench -> reverted\n- 2026okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:16 S13_elastic: FAILED to start or bench -> reverted\n- 2026-okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:16 S13_elastic: FAILED to start or bench -> reverted\n- 2026-0okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:16 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:16 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:16 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-0okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:16 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:16 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:16 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 1okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:16 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:16 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:16 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:1okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:16 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:18okens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:16 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:18 Sokens can exceed the profiled 131k; the watermark keeps SGLANG_KV_LAZY_FREE_MB=1536 MB driver-free after every commit. Why it pays: the profiled 131k-token pool costs 3.2 GB (24 KB/token bf16 x 12 QSA layers); the 256k pool costs 6.2 GB; the INT8 dense step freed 1.6 GB and the elastic cache's floor is 3.5 GB below today's arena, so the freed VRAM goes to KV backing instead of to S. Idle release returns the pool to the driver between requests, so a 256k prompt can be admitted while the expert cache shrinks to make room.\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic cache's host slot pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:13 S13_elastic attempt 3 FAILED at startup: the pool was short by 100 rows (31 host layers x 184 hot rows = 5658 needed, 5558 available) -> pinned fallbacks -> oomd. Fixed: the pool is sized to the demand (host layers' hot rows), the split layer's GPU w2 rows are donated, and the interleave is scheduled by per-kind demand.\n- 2026-09-02 13:16 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:18 S1",
 "ttft_seconds": 0.2937771779979812,
 "status": "ok",
 "tg_tokens_per_s": 35.51182333582636,
 "decode_seconds_per_token": 0.028159635469664632,
 "individual_token_timing": true,
 "event_intervals_ms": {
  "n": 511,
  "median": 27.642571003525518,
  "p95": 36.15900799923111,
  "mean": 28.159635469664632
 },
 "class": "prose",
 "requested_input_tokens": 32768
}
