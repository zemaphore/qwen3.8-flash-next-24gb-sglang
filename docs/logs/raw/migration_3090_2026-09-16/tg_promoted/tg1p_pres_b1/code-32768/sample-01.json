{
 "started_utc": "2026-09-17T03:59:58.241575+00:00",
 "wall_seconds": 19.04831080099393,
 "prompt_tokens": 32768,
 "completion_tokens": 512,
 "stream_events": 512,
 "ended_utc": "2026-09-17T04:00:17.289992+00:00",
 "warmup": false,
 "sample_index": 1,
 "requested_decode_tokens": 512,
 "gpu": {
  "n": 68,
  "temperature_c": {
   "min": 74.0,
   "max": 76.0,
   "mean": 75.1029411764706
  },
  "power_w": {
   "min": 231.37,
   "max": 274.15,
   "mean": 247.81970588235296
  },
  "util_pct": {
   "min": 88.0,
   "max": 99.0,
   "mean": 92.45588235294117
  },
  "clock_sm_mhz": {
   "min": 1365.0,
   "max": 1920.0,
   "mean": 1893.75
  },
  "clock_mem_mhz": {
   "min": 9501.0,
   "max": 9501.0,
   "mean": 9501.0
  },
  "memory_free_mib": {
   "min": 2084.0
  }
 },
 "output_text_sha256": "811664156683bf10e519cf667734ae6e5f58546fe2ce2ef3973311e3c4b89c62",
 "output_text": "++       +        \"\"\"+        \"\"\"Grow+        \"\"\"Grow every+        \"\"\"Grow every layer+        \"\"\"Grow every layer until+        \"\"\"Grow every layer until `+        \"\"\"Grow every layer until `reserve+        \"\"\"Grow every layer until `reserve_mb+        \"\"\"Grow every layer until `reserve_mb`+        \"\"\"Grow every layer until `reserve_mb` of+        \"\"\"Grow every layer until `reserve_mb` of VR+        \"\"\"Grow every layer until `reserve_mb` of VRAM+        \"\"\"Grow every layer until `reserve_mb` of VRAM is+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(res+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) <<+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S <+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <=+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free -+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) //+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L *+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].k+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S ++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)),+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except Arena+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+   +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Sh+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb`+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VR+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(res+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) <<+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S >+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >=+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve -+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) //+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L *+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].k+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(),+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S -+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        self.pending+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        self.pending_reg+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        self.pending_regrow+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        self.pending_regrow =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        self.pending_regrow = True+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        self.pending_regrow = True\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        self.pending_regrow = True\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return min+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return min(st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return min(st.S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return min(st.S for+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return min(st.S for st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return min(st.S for st in+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return min(st.S for st in self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return min(st.S for st in self.layers+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return min(st.S for st in self.layers.values+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return min(st.S for st in self.layers.values())+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return min(st.S for st in self.layers.values())\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return min(st.S for st in self.layers.values())\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return min(st.S for st in self.layers.values())\n+\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return min(st.S for st in self.layers.values())\n+\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return min(st.S for st in self.layers.values())\n+\n+   +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return min(st.S for st in self.layers.values())\n+\n+    def+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return min(st.S for st in self.layers.values())\n+\n+    def write+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return min(st.S for st in self.layers.values())\n+\n+    def write_status+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return min(st.S for st in self.layers.values())\n+\n+    def write_status(self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return min(st.S for st in self.layers.values())\n+\n+    def write_status(self):+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return min(st.S for st in self.layers.values())\n+\n+    def write_status(self):\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return min(st.S for st in self.layers.values())\n+\n+    def write_status(self):\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return min(st.S for st in self.layers.values())\n+\n+    def write_status(self):\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return min(st.S for st in self.layers.values())\n+\n+    def write_status(self):\n+        if+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return min(st.S for st in self.layers.values())\n+\n+    def write_status(self):\n+        if not+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return min(st.S for st in self.layers.values())\n+\n+    def write_status(self):\n+        if not self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return min(st.S for st in self.layers.values())\n+\n+    def write_status(self):\n+        if not self.+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return min(st.S for st in self.layers.values())\n+\n+    def write_status(self):\n+        if not self.ctl+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return min(st.S for st in self.layers.values())\n+\n+    def write_status(self):\n+        if not self.ctl:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return min(st.S for st in self.layers.values())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return min(st.S for st in self.layers.values())\n+\n+    def write_status(self):\n+        if not self.ctl:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return min(st.S for st in self.layers.values())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return min(st.S for st in self.layers.values())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return min(st.S for st in self.layers.values())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return min(st.S for st in self.layers.values())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return min(st.S for st in self.layers.values())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return min(st.S for st in self.layers.values())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return min(st.S for st in self.layers.values())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return min(st.S for st in self.layers.values())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return min(st.S for st in self.layers.values())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return min(st.S for st in self.layers.values())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return min(st.S for st in self.layers.values())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return min(st.S for st in self.layers.values())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return min(st.S for st in self.layers.values())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return min(st.S for st in self.layers.values())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return min(st.S for st in self.layers.values())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return min(st.S for st in self.layers.values())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return min(st.S for st in self.layers.values())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return min(st.S for st in self.layers.values())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return min(st.S for st in self.layers.values())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return min(st.S for st in self.layers.values())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return min(st.S for st in self.layers.values())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return min(st.S for st in self.layers.values())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return min(st.S for st in self.layers.values())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return min(st.S for st in self.layers.values())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return min(st.S for st in self.layers.values())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch={+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return min(st.S for st in self.layers.values())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch={int+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return min(st.S for st in self.layers.values())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch={int(time+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return min(st.S for st in self.layers.values())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch={int(time.time+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return min(st.S for st in self.layers.values())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch={int(time.time())+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return min(st.S for st in self.layers.values())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch={int(time.time())}\",+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return min(st.S for st in self.layers.values())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch={int(time.time())}\", f+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return min(st.S for st in self.layers.values())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch={int(time.time())}\", f\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return min(st.S for st in self.layers.values())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch={int(time.time())}\", f\"free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return min(st.S for st in self.layers.values())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch={int(time.time())}\", f\"free={+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return min(st.S for st in self.layers.values())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch={int(time.time())}\", f\"free={free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return min(st.S for st in self.layers.values())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch={int(time.time())}\", f\"free={free //+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return min(st.S for st in self.layers.values())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch={int(time.time())}\", f\"free={free // +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return min(st.S for st in self.layers.values())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch={int(time.time())}\", f\"free={free // 2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return min(st.S for st in self.layers.values())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch={int(time.time())}\", f\"free={free // 2**+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return min(st.S for st in self.layers.values())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch={int(time.time())}\", f\"free={free // 2**2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return min(st.S for st in self.layers.values())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch={int(time.time())}\", f\"free={free // 2**20+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return min(st.S for st in self.layers.values())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch={int(time.time())}\", f\"free={free // 2**20}+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return min(st.S for st in self.layers.values())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch={int(time.time())}\", f\"free={free // 2**20} MB+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return min(st.S for st in self.layers.values())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch={int(time.time())}\", f\"free={free // 2**20} MB\",+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return min(st.S for st in self.layers.values())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch={int(time.time())}\", f\"free={free // 2**20} MB\", f+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return min(st.S for st in self.layers.values())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch={int(time.time())}\", f\"free={free // 2**20} MB\", f\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return min(st.S for st in self.layers.values())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch={int(time.time())}\", f\"free={free // 2**20} MB\", f\"total+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return min(st.S for st in self.layers.values())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch={int(time.time())}\", f\"free={free // 2**20} MB\", f\"total={+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return min(st.S for st in self.layers.values())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch={int(time.time())}\", f\"free={free // 2**20} MB\", f\"total={total+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return min(st.S for st in self.layers.values())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch={int(time.time())}\", f\"free={free // 2**20} MB\", f\"total={total //+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return min(st.S for st in self.layers.values())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch={int(time.time())}\", f\"free={free // 2**20} MB\", f\"total={total // +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return min(st.S for st in self.layers.values())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch={int(time.time())}\", f\"free={free // 2**20} MB\", f\"total={total // 2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return min(st.S for st in self.layers.values())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch={int(time.time())}\", f\"free={free // 2**20} MB\", f\"total={total // 2**+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return min(st.S for st in self.layers.values())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch={int(time.time())}\", f\"free={free // 2**20} MB\", f\"total={total // 2**2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return min(st.S for st in self.layers.values())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch={int(time.time())}\", f\"free={free // 2**20} MB\", f\"total={total // 2**20+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return min(st.S for st in self.layers.values())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch={int(time.time())}\", f\"free={free // 2**20} MB\", f\"total={total // 2**20}+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return min(st.S for st in self.layers.values())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch={int(time.time())}\", f\"free={free // 2**20} MB\", f\"total={total // 2**20} MB+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return min(st.S for st in self.layers.values())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch={int(time.time())}\", f\"free={free // 2**20} MB\", f\"total={total // 2**20} MB\"]+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return min(st.S for st in self.layers.values())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch={int(time.time())}\", f\"free={free // 2**20} MB\", f\"total={total // 2**20} MB\"]\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return min(st.S for st in self.layers.values())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch={int(time.time())}\", f\"free={free // 2**20} MB\", f\"total={total // 2**20} MB\"]\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return min(st.S for st in self.layers.values())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch={int(time.time())}\", f\"free={free // 2**20} MB\", f\"total={total // 2**20} MB\"]\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return min(st.S for st in self.layers.values())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch={int(time.time())}\", f\"free={free // 2**20} MB\", f\"total={total // 2**20} MB\"]\n+        for+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return min(st.S for st in self.layers.values())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch={int(time.time())}\", f\"free={free // 2**20} MB\", f\"total={total // 2**20} MB\"]\n+        for lid+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return min(st.S for st in self.layers.values())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch={int(time.time())}\", f\"free={free // 2**20} MB\", f\"total={total // 2**20} MB\"]\n+        for lid in+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return min(st.S for st in self.layers.values())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch={int(time.time())}\", f\"free={free // 2**20} MB\", f\"total={total // 2**20} MB\"]\n+        for lid in sorted+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return min(st.S for st in self.layers.values())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch={int(time.time())}\", f\"free={free // 2**20} MB\", f\"total={total // 2**20} MB\"]\n+        for lid in sorted(self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return min(st.S for st in self.layers.values())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch={int(time.time())}\", f\"free={free // 2**20} MB\", f\"total={total // 2**20} MB\"]\n+        for lid in sorted(self.layers+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return min(st.S for st in self.layers.values())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch={int(time.time())}\", f\"free={free // 2**20} MB\", f\"total={total // 2**20} MB\"]\n+        for lid in sorted(self.layers):+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return min(st.S for st in self.layers.values())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch={int(time.time())}\", f\"free={free // 2**20} MB\", f\"total={total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return min(st.S for st in self.layers.values())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch={int(time.time())}\", f\"free={free // 2**20} MB\", f\"total={total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return min(st.S for st in self.layers.values())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch={int(time.time())}\", f\"free={free // 2**20} MB\", f\"total={total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return min(st.S for st in self.layers.values())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch={int(time.time())}\", f\"free={free // 2**20} MB\", f\"total={total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return min(st.S for st in self.layers.values())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch={int(time.time())}\", f\"free={free // 2**20} MB\", f\"total={total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return min(st.S for st in self.layers.values())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch={int(time.time())}\", f\"free={free // 2**20} MB\", f\"total={total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return min(st.S for st in self.layers.values())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch={int(time.time())}\", f\"free={free // 2**20} MB\", f\"total={total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return min(st.S for st in self.layers.values())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch={int(time.time())}\", f\"free={free // 2**20} MB\", f\"total={total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[l+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return min(st.S for st in self.layers.values())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch={int(time.time())}\", f\"free={free // 2**20} MB\", f\"total={total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return min(st.S for st in self.layers.values())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch={int(time.time())}\", f\"free={free // 2**20} MB\", f\"total={total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return min(st.S for st in self.layers.values())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch={int(time.time())}\", f\"free={free // 2**20} MB\", f\"total={total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return min(st.S for st in self.layers.values())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch={int(time.time())}\", f\"free={free // 2**20} MB\", f\"total={total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return min(st.S for st in self.layers.values())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch={int(time.time())}\", f\"free={free // 2**20} MB\", f\"total={total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return min(st.S for st in self.layers.values())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch={int(time.time())}\", f\"free={free // 2**20} MB\", f\"total={total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return min(st.S for st in self.layers.values())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch={int(time.time())}\", f\"free={free // 2**20} MB\", f\"total={total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return min(st.S for st in self.layers.values())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch={int(time.time())}\", f\"free={free // 2**20} MB\", f\"total={total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return min(st.S for st in self.layers.values())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch={int(time.time())}\", f\"free={free // 2**20} MB\", f\"total={total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return min(st.S for st in self.layers.values())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch={int(time.time())}\", f\"free={free // 2**20} MB\", f\"total={total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return min(st.S for st in self.layers.values())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch={int(time.time())}\", f\"free={free // 2**20} MB\", f\"total={total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return min(st.S for st in self.layers.values())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch={int(time.time())}\", f\"free={free // 2**20} MB\", f\"total={total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return min(st.S for st in self.layers.values())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch={int(time.time())}\", f\"free={free // 2**20} MB\", f\"total={total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return min(st.S for st in self.layers.values())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch={int(time.time())}\", f\"free={free // 2**20} MB\", f\"total={total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows *+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return min(st.S for st in self.layers.values())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch={int(time.time())}\", f\"free={free // 2**20} MB\", f\"total={total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return min(st.S for st in self.layers.values())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch={int(time.time())}\", f\"free={free // 2**20} MB\", f\"total={total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return min(st.S for st in self.layers.values())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch={int(time.time())}\", f\"free={free // 2**20} MB\", f\"total={total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return min(st.S for st in self.layers.values())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch={int(time.time())}\", f\"free={free // 2**20} MB\", f\"total={total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return min(st.S for st in self.layers.values())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch={int(time.time())}\", f\"free={free // 2**20} MB\", f\"total={total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return min(st.S for st in self.layers.values())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch={int(time.time())}\", f\"free={free // 2**20} MB\", f\"total={total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return min(st.S for st in self.layers.values())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch={int(time.time())}\", f\"free={free // 2**20} MB\", f\"total={total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.k+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return min(st.S for st in self.layers.values())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch={int(time.time())}\", f\"free={free // 2**20} MB\", f\"total={total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return min(st.S for st in self.layers.values())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch={int(time.time())}\", f\"free={free // 2**20} MB\", f\"total={total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return min(st.S for st in self.layers.values())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch={int(time.time())}\", f\"free={free // 2**20} MB\", f\"total={total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values())+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return min(st.S for st in self.layers.values())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch={int(time.time())}\", f\"free={free // 2**20} MB\", f\"total={total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values())\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return min(st.S for st in self.layers.values())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch={int(time.time())}\", f\"free={free // 2**20} MB\", f\"total={total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values())\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return min(st.S for st in self.layers.values())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch={int(time.time())}\", f\"free={free // 2**20} MB\", f\"total={total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values())\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return min(st.S for st in self.layers.values())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch={int(time.time())}\", f\"free={free // 2**20} MB\", f\"total={total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values())\n+            lines+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return min(st.S for st in self.layers.values())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch={int(time.time())}\", f\"free={free // 2**20} MB\", f\"total={total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values())\n+            lines.append+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return min(st.S for st in self.layers.values())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch={int(time.time())}\", f\"free={free // 2**20} MB\", f\"total={total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values())\n+            lines.append(f+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return min(st.S for st in self.layers.values())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch={int(time.time())}\", f\"free={free // 2**20} MB\", f\"total={total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values())\n+            lines.append(f\"L+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return min(st.S for st in self.layers.values())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch={int(time.time())}\", f\"free={free // 2**20} MB\", f\"total={total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values())\n+            lines.append(f\"L{+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return min(st.S for st in self.layers.values())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch={int(time.time())}\", f\"free={free // 2**20} MB\", f\"total={total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values())\n+            lines.append(f\"L{st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return min(st.S for st in self.layers.values())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch={int(time.time())}\", f\"free={free // 2**20} MB\", f\"total={total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values())\n+            lines.append(f\"L{st.l+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return min(st.S for st in self.layers.values())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch={int(time.time())}\", f\"free={free // 2**20} MB\", f\"total={total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values())\n+            lines.append(f\"L{st.lid+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return min(st.S for st in self.layers.values())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch={int(time.time())}\", f\"free={free // 2**20} MB\", f\"total={total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values())\n+            lines.append(f\"L{st.lid}:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = min(S + max(1, min(step, room)), self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        return min(st.S for st in self.layers.values())\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            need = (reserve - free) // (self.L * sum(k.rb for k in self.layers[next(iter(self.layers))].kinds.values()))\n+            S = max(self.s_floor(), S - max(1, min(step, need)))\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        return min(st.S for st in self.layers.values())\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch={int(time.time())}\", f\"free={free // 2**20} MB\", f\"total={total // 2**20} MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values())\n+            lines.append(f\"L{st.lid}: S",
 "ttft_seconds": 1.3686411649978254,
 "status": "ok",
 "tg_tokens_per_s": 28.903353120779997,
 "decode_seconds_per_token": 0.034598061886496216,
 "individual_token_timing": true,
 "event_intervals_ms": {
  "n": 511,
  "median": 34.10391199577134,
  "p95": 44.652122000115924,
  "mean": 34.59806188649622
 },
 "class": "code",
 "requested_input_tokens": 32768
}
