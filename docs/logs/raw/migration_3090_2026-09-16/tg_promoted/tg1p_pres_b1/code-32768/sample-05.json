{
 "started_utc": "2026-09-17T04:01:09.879371+00:00",
 "wall_seconds": 17.45035403899965,
 "prompt_tokens": 32768,
 "completion_tokens": 512,
 "stream_events": 512,
 "ended_utc": "2026-09-17T04:01:27.329865+00:00",
 "warmup": false,
 "sample_index": 5,
 "requested_decode_tokens": 512,
 "gpu": {
  "n": 63,
  "temperature_c": {
   "min": 74.0,
   "max": 76.0,
   "mean": 75.61904761904762
  },
  "power_w": {
   "min": 230.86,
   "max": 262.71,
   "mean": 249.38603174603173
  },
  "util_pct": {
   "min": 90.0,
   "max": 95.0,
   "mean": 92.73015873015873
  },
  "clock_sm_mhz": {
   "min": 1920.0,
   "max": 1920.0,
   "mean": 1920.0
  },
  "clock_mem_mhz": {
   "min": 9501.0,
   "max": 9501.0,
   "mean": 9501.0
  },
  "memory_free_mib": {
   "min": 2080.0
  }
 },
 "output_text_sha256": "5fa28f34d3202f997d409128501f8de574b4514419c747b77a3a674b5393b381",
 "output_text": "++       +        \"\"\"+        \"\"\"Grow+        \"\"\"Grow every+        \"\"\"Grow every layer+        \"\"\"Grow every layer until+        \"\"\"Grow every layer until `+        \"\"\"Grow every layer until `reserve+        \"\"\"Grow every layer until `reserve_mb+        \"\"\"Grow every layer until `reserve_mb`+        \"\"\"Grow every layer until `reserve_mb` of+        \"\"\"Grow every layer until `reserve_mb` of VR+        \"\"\"Grow every layer until `reserve_mb` of VRAM+        \"\"\"Grow every layer until `reserve_mb` of VRAM is+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >>+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >=+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S <+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S ++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0]+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >>+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >=+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB):+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VR+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\",+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free /+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 10+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 102+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+   +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Sh+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb`+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VR+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0]+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >>+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >=+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S >+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S -+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >>+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >=+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                self.pending+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                self.pending_reg+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                self.pending_regrow+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                self.pending_regrow =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                self.pending_regrow = True+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                self.pending_regrow = True\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                self.pending_regrow = True\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                self.pending_regrow = True\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                self.pending_regrow = True\n+                return+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                self.pending_regrow = True\n+                return\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                self.pending_regrow = True\n+                return\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                self.pending_regrow = True\n+                return\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                self.pending_regrow = True\n+                return\n+            S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                self.pending_regrow = True\n+                return\n+            S =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                self.pending_regrow = True\n+                return\n+            S = S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                self.pending_regrow = True\n+                return\n+            S = S_next+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                self.pending_regrow = True\n+                return\n+            S = S_next\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                self.pending_regrow = True\n+                return\n+            S = S_next\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                self.pending_regrow = True\n+                return\n+            S = S_next\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                self.pending_regrow = True\n+                return\n+            S = S_next\n+        logger+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                self.pending_regrow = True\n+                return\n+            S = S_next\n+        logger.warning+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                self.pending_regrow = True\n+                return\n+            S = S_next\n+        logger.warning(\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                self.pending_regrow = True\n+                return\n+            S = S_next\n+        logger.warning(\"elastic+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                self.pending_regrow = True\n+                return\n+            S = S_next\n+        logger.warning(\"elastic free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                self.pending_regrow = True\n+                return\n+            S = S_next\n+        logger.warning(\"elastic free(%+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                self.pending_regrow = True\n+                return\n+            S = S_next\n+        logger.warning(\"elastic free(%d+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                self.pending_regrow = True\n+                return\n+            S = S_next\n+        logger.warning(\"elastic free(%d MB+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                self.pending_regrow = True\n+                return\n+            S = S_next\n+        logger.warning(\"elastic free(%d MB):+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                self.pending_regrow = True\n+                return\n+            S = S_next\n+        logger.warning(\"elastic free(%d MB): reached+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                self.pending_regrow = True\n+                return\n+            S = S_next\n+        logger.warning(\"elastic free(%d MB): reached S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                self.pending_regrow = True\n+                return\n+            S = S_next\n+        logger.warning(\"elastic free(%d MB): reached S_floor+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                self.pending_regrow = True\n+                return\n+            S = S_next\n+        logger.warning(\"elastic free(%d MB): reached S_floor=%+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                self.pending_regrow = True\n+                return\n+            S = S_next\n+        logger.warning(\"elastic free(%d MB): reached S_floor=%d+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                self.pending_regrow = True\n+                return\n+            S = S_next\n+        logger.warning(\"elastic free(%d MB): reached S_floor=%d,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                self.pending_regrow = True\n+                return\n+            S = S_next\n+        logger.warning(\"elastic free(%d MB): reached S_floor=%d, VR+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                self.pending_regrow = True\n+                return\n+            S = S_next\n+        logger.warning(\"elastic free(%d MB): reached S_floor=%d, VRAM+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                self.pending_regrow = True\n+                return\n+            S = S_next\n+        logger.warning(\"elastic free(%d MB): reached S_floor=%d, VRAM free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                self.pending_regrow = True\n+                return\n+            S = S_next\n+        logger.warning(\"elastic free(%d MB): reached S_floor=%d, VRAM free %.+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                self.pending_regrow = True\n+                return\n+            S = S_next\n+        logger.warning(\"elastic free(%d MB): reached S_floor=%d, VRAM free %.2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                self.pending_regrow = True\n+                return\n+            S = S_next\n+        logger.warning(\"elastic free(%d MB): reached S_floor=%d, VRAM free %.2f+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                self.pending_regrow = True\n+                return\n+            S = S_next\n+        logger.warning(\"elastic free(%d MB): reached S_floor=%d, VRAM free %.2f GB+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                self.pending_regrow = True\n+                return\n+            S = S_next\n+        logger.warning(\"elastic free(%d MB): reached S_floor=%d, VRAM free %.2f GB\",+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                self.pending_regrow = True\n+                return\n+            S = S_next\n+        logger.warning(\"elastic free(%d MB): reached S_floor=%d, VRAM free %.2f GB\",\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                self.pending_regrow = True\n+                return\n+            S = S_next\n+        logger.warning(\"elastic free(%d MB): reached S_floor=%d, VRAM free %.2f GB\",\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                self.pending_regrow = True\n+                return\n+            S = S_next\n+        logger.warning(\"elastic free(%d MB): reached S_floor=%d, VRAM free %.2f GB\",\n+                      +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                self.pending_regrow = True\n+                return\n+            S = S_next\n+        logger.warning(\"elastic free(%d MB): reached S_floor=%d, VRAM free %.2f GB\",\n+                       reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                self.pending_regrow = True\n+                return\n+            S = S_next\n+        logger.warning(\"elastic free(%d MB): reached S_floor=%d, VRAM free %.2f GB\",\n+                       reserve_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                self.pending_regrow = True\n+                return\n+            S = S_next\n+        logger.warning(\"elastic free(%d MB): reached S_floor=%d, VRAM free %.2f GB\",\n+                       reserve_mb,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                self.pending_regrow = True\n+                return\n+            S = S_next\n+        logger.warning(\"elastic free(%d MB): reached S_floor=%d, VRAM free %.2f GB\",\n+                       reserve_mb, S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                self.pending_regrow = True\n+                return\n+            S = S_next\n+        logger.warning(\"elastic free(%d MB): reached S_floor=%d, VRAM free %.2f GB\",\n+                       reserve_mb, S,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                self.pending_regrow = True\n+                return\n+            S = S_next\n+        logger.warning(\"elastic free(%d MB): reached S_floor=%d, VRAM free %.2f GB\",\n+                       reserve_mb, S, torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                self.pending_regrow = True\n+                return\n+            S = S_next\n+        logger.warning(\"elastic free(%d MB): reached S_floor=%d, VRAM free %.2f GB\",\n+                       reserve_mb, S, torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                self.pending_regrow = True\n+                return\n+            S = S_next\n+        logger.warning(\"elastic free(%d MB): reached S_floor=%d, VRAM free %.2f GB\",\n+                       reserve_mb, S, torch.cuda.mem+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                self.pending_regrow = True\n+                return\n+            S = S_next\n+        logger.warning(\"elastic free(%d MB): reached S_floor=%d, VRAM free %.2f GB\",\n+                       reserve_mb, S, torch.cuda.mem_get+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                self.pending_regrow = True\n+                return\n+            S = S_next\n+        logger.warning(\"elastic free(%d MB): reached S_floor=%d, VRAM free %.2f GB\",\n+                       reserve_mb, S, torch.cuda.mem_get_info+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                self.pending_regrow = True\n+                return\n+            S = S_next\n+        logger.warning(\"elastic free(%d MB): reached S_floor=%d, VRAM free %.2f GB\",\n+                       reserve_mb, S, torch.cuda.mem_get_info()[+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                self.pending_regrow = True\n+                return\n+            S = S_next\n+        logger.warning(\"elastic free(%d MB): reached S_floor=%d, VRAM free %.2f GB\",\n+                       reserve_mb, S, torch.cuda.mem_get_info()[0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                self.pending_regrow = True\n+                return\n+            S = S_next\n+        logger.warning(\"elastic free(%d MB): reached S_floor=%d, VRAM free %.2f GB\",\n+                       reserve_mb, S, torch.cuda.mem_get_info()[0]+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                self.pending_regrow = True\n+                return\n+            S = S_next\n+        logger.warning(\"elastic free(%d MB): reached S_floor=%d, VRAM free %.2f GB\",\n+                       reserve_mb, S, torch.cuda.mem_get_info()[0] /+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                self.pending_regrow = True\n+                return\n+            S = S_next\n+        logger.warning(\"elastic free(%d MB): reached S_floor=%d, VRAM free %.2f GB\",\n+                       reserve_mb, S, torch.cuda.mem_get_info()[0] / +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                self.pending_regrow = True\n+                return\n+            S = S_next\n+        logger.warning(\"elastic free(%d MB): reached S_floor=%d, VRAM free %.2f GB\",\n+                       reserve_mb, S, torch.cuda.mem_get_info()[0] / 1+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                self.pending_regrow = True\n+                return\n+            S = S_next\n+        logger.warning(\"elastic free(%d MB): reached S_floor=%d, VRAM free %.2f GB\",\n+                       reserve_mb, S, torch.cuda.mem_get_info()[0] / 10+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                self.pending_regrow = True\n+                return\n+            S = S_next\n+        logger.warning(\"elastic free(%d MB): reached S_floor=%d, VRAM free %.2f GB\",\n+                       reserve_mb, S, torch.cuda.mem_get_info()[0] / 102+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                self.pending_regrow = True\n+                return\n+            S = S_next\n+        logger.warning(\"elastic free(%d MB): reached S_floor=%d, VRAM free %.2f GB\",\n+                       reserve_mb, S, torch.cuda.mem_get_info()[0] / 1024+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                self.pending_regrow = True\n+                return\n+            S = S_next\n+        logger.warning(\"elastic free(%d MB): reached S_floor=%d, VRAM free %.2f GB\",\n+                       reserve_mb, S, torch.cuda.mem_get_info()[0] / 1024)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                self.pending_regrow = True\n+                return\n+            S = S_next\n+        logger.warning(\"elastic free(%d MB): reached S_floor=%d, VRAM free %.2f GB\",\n+                       reserve_mb, S, torch.cuda.mem_get_info()[0] / 1024)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                self.pending_regrow = True\n+                return\n+            S = S_next\n+        logger.warning(\"elastic free(%d MB): reached S_floor=%d, VRAM free %.2f GB\",\n+                       reserve_mb, S, torch.cuda.mem_get_info()[0] / 1024)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                self.pending_regrow = True\n+                return\n+            S = S_next\n+        logger.warning(\"elastic free(%d MB): reached S_floor=%d, VRAM free %.2f GB\",\n+                       reserve_mb, S, torch.cuda.mem_get_info()[0] / 1024)\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                self.pending_regrow = True\n+                return\n+            S = S_next\n+        logger.warning(\"elastic free(%d MB): reached S_floor=%d, VRAM free %.2f GB\",\n+                       reserve_mb, S, torch.cuda.mem_get_info()[0] / 1024)\n+        self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                self.pending_regrow = True\n+                return\n+            S = S_next\n+        logger.warning(\"elastic free(%d MB): reached S_floor=%d, VRAM free %.2f GB\",\n+                       reserve_mb, S, torch.cuda.mem_get_info()[0] / 1024)\n+        self.pending+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                self.pending_regrow = True\n+                return\n+            S = S_next\n+        logger.warning(\"elastic free(%d MB): reached S_floor=%d, VRAM free %.2f GB\",\n+                       reserve_mb, S, torch.cuda.mem_get_info()[0] / 1024)\n+        self.pending_reg+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                self.pending_regrow = True\n+                return\n+            S = S_next\n+        logger.warning(\"elastic free(%d MB): reached S_floor=%d, VRAM free %.2f GB\",\n+                       reserve_mb, S, torch.cuda.mem_get_info()[0] / 1024)\n+        self.pending_regrow+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                self.pending_regrow = True\n+                return\n+            S = S_next\n+        logger.warning(\"elastic free(%d MB): reached S_floor=%d, VRAM free %.2f GB\",\n+                       reserve_mb, S, torch.cuda.mem_get_info()[0] / 1024)\n+        self.pending_regrow =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                self.pending_regrow = True\n+                return\n+            S = S_next\n+        logger.warning(\"elastic free(%d MB): reached S_floor=%d, VRAM free %.2f GB\",\n+                       reserve_mb, S, torch.cuda.mem_get_info()[0] / 1024)\n+        self.pending_regrow = True+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                self.pending_regrow = True\n+                return\n+            S = S_next\n+        logger.warning(\"elastic free(%d MB): reached S_floor=%d, VRAM free %.2f GB\",\n+                       reserve_mb, S, torch.cuda.mem_get_info()[0] / 1024)\n+        self.pending_regrow = True\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                self.pending_regrow = True\n+                return\n+            S = S_next\n+        logger.warning(\"elastic free(%d MB): reached S_floor=%d, VRAM free %.2f GB\",\n+                       reserve_mb, S, torch.cuda.mem_get_info()[0] / 1024)\n+        self.pending_regrow = True\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                self.pending_regrow = True\n+                return\n+            S = S_next\n+        logger.warning(\"elastic free(%d MB): reached S_floor=%d, VRAM free %.2f GB\",\n+                       reserve_mb, S, torch.cuda.mem_get_info()[0] / 1024)\n+        self.pending_regrow = True\n+\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                self.pending_regrow = True\n+                return\n+            S = S_next\n+        logger.warning(\"elastic free(%d MB): reached S_floor=%d, VRAM free %.2f GB\",\n+                       reserve_mb, S, torch.cuda.mem_get_info()[0] / 1024)\n+        self.pending_regrow = True\n+\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                self.pending_regrow = True\n+                return\n+            S = S_next\n+        logger.warning(\"elastic free(%d MB): reached S_floor=%d, VRAM free %.2f GB\",\n+                       reserve_mb, S, torch.cuda.mem_get_info()[0] / 1024)\n+        self.pending_regrow = True\n+\n+   +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                self.pending_regrow = True\n+                return\n+            S = S_next\n+        logger.warning(\"elastic free(%d MB): reached S_floor=%d, VRAM free %.2f GB\",\n+                       reserve_mb, S, torch.cuda.mem_get_info()[0] / 1024)\n+        self.pending_regrow = True\n+\n+    def+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                self.pending_regrow = True\n+                return\n+            S = S_next\n+        logger.warning(\"elastic free(%d MB): reached S_floor=%d, VRAM free %.2f GB\",\n+                       reserve_mb, S, torch.cuda.mem_get_info()[0] / 1024)\n+        self.pending_regrow = True\n+\n+    def poll+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                self.pending_regrow = True\n+                return\n+            S = S_next\n+        logger.warning(\"elastic free(%d MB): reached S_floor=%d, VRAM free %.2f GB\",\n+                       reserve_mb, S, torch.cuda.mem_get_info()[0] / 1024)\n+        self.pending_regrow = True\n+\n+    def poll(self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                self.pending_regrow = True\n+                return\n+            S = S_next\n+        logger.warning(\"elastic free(%d MB): reached S_floor=%d, VRAM free %.2f GB\",\n+                       reserve_mb, S, torch.cuda.mem_get_info()[0] / 1024)\n+        self.pending_regrow = True\n+\n+    def poll(self):+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                self.pending_regrow = True\n+                return\n+            S = S_next\n+        logger.warning(\"elastic free(%d MB): reached S_floor=%d, VRAM free %.2f GB\",\n+                       reserve_mb, S, torch.cuda.mem_get_info()[0] / 1024)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                self.pending_regrow = True\n+                return\n+            S = S_next\n+        logger.warning(\"elastic free(%d MB): reached S_floor=%d, VRAM free %.2f GB\",\n+                       reserve_mb, S, torch.cuda.mem_get_info()[0] / 1024)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                self.pending_regrow = True\n+                return\n+            S = S_next\n+        logger.warning(\"elastic free(%d MB): reached S_floor=%d, VRAM free %.2f GB\",\n+                       reserve_mb, S, torch.cuda.mem_get_info()[0] / 1024)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                self.pending_regrow = True\n+                return\n+            S = S_next\n+        logger.warning(\"elastic free(%d MB): reached S_floor=%d, VRAM free %.2f GB\",\n+                       reserve_mb, S, torch.cuda.mem_get_info()[0] / 1024)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                self.pending_regrow = True\n+                return\n+            S = S_next\n+        logger.warning(\"elastic free(%d MB): reached S_floor=%d, VRAM free %.2f GB\",\n+                       reserve_mb, S, torch.cuda.mem_get_info()[0] / 1024)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                self.pending_regrow = True\n+                return\n+            S = S_next\n+        logger.warning(\"elastic free(%d MB): reached S_floor=%d, VRAM free %.2f GB\",\n+                       reserve_mb, S, torch.cuda.mem_get_info()[0] / 1024)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                self.pending_regrow = True\n+                return\n+            S = S_next\n+        logger.warning(\"elastic free(%d MB): reached S_floor=%d, VRAM free %.2f GB\",\n+                       reserve_mb, S, torch.cuda.mem_get_info()[0] / 1024)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                self.pending_regrow = True\n+                return\n+            S = S_next\n+        logger.warning(\"elastic free(%d MB): reached S_floor=%d, VRAM free %.2f GB\",\n+                       reserve_mb, S, torch.cuda.mem_get_info()[0] / 1024)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the Mo+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                self.pending_regrow = True\n+                return\n+            S = S_next\n+        logger.warning(\"elastic free(%d MB): reached S_floor=%d, VRAM free %.2f GB\",\n+                       reserve_mb, S, torch.cuda.mem_get_info()[0] / 1024)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                self.pending_regrow = True\n+                return\n+            S = S_next\n+        logger.warning(\"elastic free(%d MB): reached S_floor=%d, VRAM free %.2f GB\",\n+                       reserve_mb, S, torch.cuda.mem_get_info()[0] / 1024)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                self.pending_regrow = True\n+                return\n+            S = S_next\n+        logger.warning(\"elastic free(%d MB): reached S_floor=%d, VRAM free %.2f GB\",\n+                       reserve_mb, S, torch.cuda.mem_get_info()[0] / 1024)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                self.pending_regrow = True\n+                return\n+            S = S_next\n+        logger.warning(\"elastic free(%d MB): reached S_floor=%d, VRAM free %.2f GB\",\n+                       reserve_mb, S, torch.cuda.mem_get_info()[0] / 1024)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                self.pending_regrow = True\n+                return\n+            S = S_next\n+        logger.warning(\"elastic free(%d MB): reached S_floor=%d, VRAM free %.2f GB\",\n+                       reserve_mb, S, torch.cuda.mem_get_info()[0] / 1024)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (pref+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                self.pending_regrow = True\n+                return\n+            S = S_next\n+        logger.warning(\"elastic free(%d MB): reached S_floor=%d, VRAM free %.2f GB\",\n+                       reserve_mb, S, torch.cuda.mem_get_info()[0] / 1024)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                self.pending_regrow = True\n+                return\n+            S = S_next\n+        logger.warning(\"elastic free(%d MB): reached S_floor=%d, VRAM free %.2f GB\",\n+                       reserve_mb, S, torch.cuda.mem_get_info()[0] / 1024)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/e+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                self.pending_regrow = True\n+                return\n+            S = S_next\n+        logger.warning(\"elastic free(%d MB): reached S_floor=%d, VRAM free %.2f GB\",\n+                       reserve_mb, S, torch.cuda.mem_get_info()[0] / 1024)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                self.pending_regrow = True\n+                return\n+            S = S_next\n+        logger.warning(\"elastic free(%d MB): reached S_floor=%d, VRAM free %.2f GB\",\n+                       reserve_mb, S, torch.cuda.mem_get_info()[0] / 1024)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                self.pending_regrow = True\n+                return\n+            S = S_next\n+        logger.warning(\"elastic free(%d MB): reached S_floor=%d, VRAM free %.2f GB\",\n+                       reserve_mb, S, torch.cuda.mem_get_info()[0] / 1024)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                self.pending_regrow = True\n+                return\n+            S = S_next\n+        logger.warning(\"elastic free(%d MB): reached S_floor=%d, VRAM free %.2f GB\",\n+                       reserve_mb, S, torch.cuda.mem_get_info()[0] / 1024)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                self.pending_regrow = True\n+                return\n+            S = S_next\n+        logger.warning(\"elastic free(%d MB): reached S_floor=%d, VRAM free %.2f GB\",\n+                       reserve_mb, S, torch.cuda.mem_get_info()[0] / 1024)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                self.pending_regrow = True\n+                return\n+            S = S_next\n+        logger.warning(\"elastic free(%d MB): reached S_floor=%d, VRAM free %.2f GB\",\n+                       reserve_mb, S, torch.cuda.mem_get_info()[0] / 1024)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                self.pending_regrow = True\n+                return\n+            S = S_next\n+        logger.warning(\"elastic free(%d MB): reached S_floor=%d, VRAM free %.2f GB\",\n+                       reserve_mb, S, torch.cuda.mem_get_info()[0] / 1024)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                self.pending_regrow = True\n+                return\n+            S = S_next\n+        logger.warning(\"elastic free(%d MB): reached S_floor=%d, VRAM free %.2f GB\",\n+                       reserve_mb, S, torch.cuda.mem_get_info()[0] / 1024)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                self.pending_regrow = True\n+                return\n+            S = S_next\n+        logger.warning(\"elastic free(%d MB): reached S_floor=%d, VRAM free %.2f GB\",\n+                       reserve_mb, S, torch.cuda.mem_get_info()[0] / 1024)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                self.pending_regrow = True\n+                return\n+            S = S_next\n+        logger.warning(\"elastic free(%d MB): reached S_floor=%d, VRAM free %.2f GB\",\n+                       reserve_mb, S, torch.cuda.mem_get_info()[0] / 1024)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                self.pending_regrow = True\n+                return\n+            S = S_next\n+        logger.warning(\"elastic free(%d MB): reached S_floor=%d, VRAM free %.2f GB\",\n+                       reserve_mb, S, torch.cuda.mem_get_info()[0] / 1024)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls +=+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                self.pending_regrow = True\n+                return\n+            S = S_next\n+        logger.warning(\"elastic free(%d MB): reached S_floor=%d, VRAM free %.2f GB\",\n+                       reserve_mb, S, torch.cuda.mem_get_info()[0] / 1024)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                self.pending_regrow = True\n+                return\n+            S = S_next\n+        logger.warning(\"elastic free(%d MB): reached S_floor=%d, VRAM free %.2f GB\",\n+                       reserve_mb, S, torch.cuda.mem_get_info()[0] / 1024)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                self.pending_regrow = True\n+                return\n+            S = S_next\n+        logger.warning(\"elastic free(%d MB): reached S_floor=%d, VRAM free %.2f GB\",\n+                       reserve_mb, S, torch.cuda.mem_get_info()[0] / 1024)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                self.pending_regrow = True\n+                return\n+            S = S_next\n+        logger.warning(\"elastic free(%d MB): reached S_floor=%d, VRAM free %.2f GB\",\n+                       reserve_mb, S, torch.cuda.mem_get_info()[0] / 1024)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                self.pending_regrow = True\n+                return\n+            S = S_next\n+        logger.warning(\"elastic free(%d MB): reached S_floor=%d, VRAM free %.2f GB\",\n+                       reserve_mb, S, torch.cuda.mem_get_info()[0] / 1024)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                self.pending_regrow = True\n+                return\n+            S = S_next\n+        logger.warning(\"elastic free(%d MB): reached S_floor=%d, VRAM free %.2f GB\",\n+                       reserve_mb, S, torch.cuda.mem_get_info()[0] / 1024)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                self.pending_regrow = True\n+                return\n+            S = S_next\n+        logger.warning(\"elastic free(%d MB): reached S_floor=%d, VRAM free %.2f GB\",\n+                       reserve_mb, S, torch.cuda.mem_get_info()[0] / 1024)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                self.pending_regrow = True\n+                return\n+            S = S_next\n+        logger.warning(\"elastic free(%d MB): reached S_floor=%d, VRAM free %.2f GB\",\n+                       reserve_mb, S, torch.cuda.mem_get_info()[0] / 1024)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                self.pending_regrow = True\n+                return\n+            S = S_next\n+        logger.warning(\"elastic free(%d MB): reached S_floor=%d, VRAM free %.2f GB\",\n+                       reserve_mb, S, torch.cuda.mem_get_info()[0] / 1024)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                self.pending_regrow = True\n+                return\n+            S = S_next\n+        logger.warning(\"elastic free(%d MB): reached S_floor=%d, VRAM free %.2f GB\",\n+                       reserve_mb, S, torch.cuda.mem_get_info()[0] / 1024)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls %+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                self.pending_regrow = True\n+                return\n+            S = S_next\n+        logger.warning(\"elastic free(%d MB): reached S_floor=%d, VRAM free %.2f GB\",\n+                       reserve_mb, S, torch.cuda.mem_get_info()[0] / 1024)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                self.pending_regrow = True\n+                return\n+            S = S_next\n+        logger.warning(\"elastic free(%d MB): reached S_floor=%d, VRAM free %.2f GB\",\n+                       reserve_mb, S, torch.cuda.mem_get_info()[0] / 1024)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 1+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                self.pending_regrow = True\n+                return\n+            S = S_next\n+        logger.warning(\"elastic free(%d MB): reached S_floor=%d, VRAM free %.2f GB\",\n+                       reserve_mb, S, torch.cuda.mem_get_info()[0] / 1024)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 10+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                self.pending_regrow = True\n+                return\n+            S = S_next\n+        logger.warning(\"elastic free(%d MB): reached S_floor=%d, VRAM free %.2f GB\",\n+                       reserve_mb, S, torch.cuda.mem_get_info()[0] / 1024)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                self.pending_regrow = True\n+                return\n+            S = S_next\n+        logger.warning(\"elastic free(%d MB): reached S_floor=%d, VRAM free %.2f GB\",\n+                       reserve_mb, S, torch.cuda.mem_get_info()[0] / 1024)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 ==+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                self.pending_regrow = True\n+                return\n+            S = S_next\n+        logger.warning(\"elastic free(%d MB): reached S_floor=%d, VRAM free %.2f GB\",\n+                       reserve_mb, S, torch.cuda.mem_get_info()[0] / 1024)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                self.pending_regrow = True\n+                return\n+            S = S_next\n+        logger.warning(\"elastic free(%d MB): reached S_floor=%d, VRAM free %.2f GB\",\n+                       reserve_mb, S, torch.cuda.mem_get_info()[0] / 1024)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                self.pending_regrow = True\n+                return\n+            S = S_next\n+        logger.warning(\"elastic free(%d MB): reached S_floor=%d, VRAM free %.2f GB\",\n+                       reserve_mb, S, torch.cuda.mem_get_info()[0] / 1024)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                self.pending_regrow = True\n+                return\n+            S = S_next\n+        logger.warning(\"elastic free(%d MB): reached S_floor=%d, VRAM free %.2f GB\",\n+                       reserve_mb, S, torch.cuda.mem_get_info()[0] / 1024)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                self.pending_regrow = True\n+                return\n+            S = S_next\n+        logger.warning(\"elastic free(%d MB): reached S_floor=%d, VRAM free %.2f GB\",\n+                       reserve_mb, S, torch.cuda.mem_get_info()[0] / 1024)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                self.pending_regrow = True\n+                return\n+            S = S_next\n+        logger.warning(\"elastic free(%d MB): reached S_floor=%d, VRAM free %.2f GB\",\n+                       reserve_mb, S, torch.cuda.mem_get_info()[0] / 1024)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                self.pending_regrow = True\n+                return\n+            S = S_next\n+        logger.warning(\"elastic free(%d MB): reached S_floor=%d, VRAM free %.2f GB\",\n+                       reserve_mb, S, torch.cuda.mem_get_info()[0] / 1024)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                self.pending_regrow = True\n+                return\n+            S = S_next\n+        logger.warning(\"elastic free(%d MB): reached S_floor=%d, VRAM free %.2f GB\",\n+                       reserve_mb, S, torch.cuda.mem_get_info()[0] / 1024)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                self.pending_regrow = True\n+                return\n+            S = S_next\n+        logger.warning(\"elastic free(%d MB): reached S_floor=%d, VRAM free %.2f GB\",\n+                       reserve_mb, S, torch.cuda.mem_get_info()[0] / 1024)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                self.pending_regrow = True\n+                return\n+            S = S_next\n+        logger.warning(\"elastic free(%d MB): reached S_floor=%d, VRAM free %.2f GB\",\n+                       reserve_mb, S, torch.cuda.mem_get_info()[0] / 1024)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                self.pending_regrow = True\n+                return\n+            S = S_next\n+        logger.warning(\"elastic free(%d MB): reached S_floor=%d, VRAM free %.2f GB\",\n+                       reserve_mb, S, torch.cuda.mem_get_info()[0] / 1024)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                self.pending_regrow = True\n+                return\n+            S = S_next\n+        logger.warning(\"elastic free(%d MB): reached S_floor=%d, VRAM free %.2f GB\",\n+                       reserve_mb, S, torch.cuda.mem_get_info()[0] / 1024)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                self.pending_regrow = True\n+                return\n+            S = S_next\n+        logger.warning(\"elastic free(%d MB): reached S_floor=%d, VRAM free %.2f GB\",\n+                       reserve_mb, S, torch.cuda.mem_get_info()[0] / 1024)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                self.pending_regrow = True\n+                return\n+            S = S_next\n+        logger.warning(\"elastic free(%d MB): reached S_floor=%d, VRAM free %.2f GB\",\n+                       reserve_mb, S, torch.cuda.mem_get_info()[0] / 1024)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                self.pending_regrow = True\n+                return\n+            S = S_next\n+        logger.warning(\"elastic free(%d MB): reached S_floor=%d, VRAM free %.2f GB\",\n+                       reserve_mb, S, torch.cuda.mem_get_info()[0] / 1024)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                self.pending_regrow = True\n+                return\n+            S = S_next\n+        logger.warning(\"elastic free(%d MB): reached S_floor=%d, VRAM free %.2f GB\",\n+                       reserve_mb, S, torch.cuda.mem_get_info()[0] / 1024)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self.+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                self.pending_regrow = True\n+                return\n+            S = S_next\n+        logger.warning(\"elastic free(%d MB): reached S_floor=%d, VRAM free %.2f GB\",\n+                       reserve_mb, S, torch.cuda.mem_get_info()[0] / 1024)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self.ctl+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                self.pending_regrow = True\n+                return\n+            S = S_next\n+        logger.warning(\"elastic free(%d MB): reached S_floor=%d, VRAM free %.2f GB\",\n+                       reserve_mb, S, torch.cuda.mem_get_info()[0] / 1024)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self.ctl is+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                self.pending_regrow = True\n+                return\n+            S = S_next\n+        logger.warning(\"elastic free(%d MB): reached S_floor=%d, VRAM free %.2f GB\",\n+                       reserve_mb, S, torch.cuda.mem_get_info()[0] / 1024)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self.ctl is None+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                self.pending_regrow = True\n+                return\n+            S = S_next\n+        logger.warning(\"elastic free(%d MB): reached S_floor=%d, VRAM free %.2f GB\",\n+                       reserve_mb, S, torch.cuda.mem_get_info()[0] / 1024)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self.ctl is None:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                self.pending_regrow = True\n+                return\n+            S = S_next\n+        logger.warning(\"elastic free(%d MB): reached S_floor=%d, VRAM free %.2f GB\",\n+                       reserve_mb, S, torch.cuda.mem_get_info()[0] / 1024)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self.ctl is None:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                self.pending_regrow = True\n+                return\n+            S = S_next\n+        logger.warning(\"elastic free(%d MB): reached S_floor=%d, VRAM free %.2f GB\",\n+                       reserve_mb, S, torch.cuda.mem_get_info()[0] / 1024)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self.ctl is None:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                self.pending_regrow = True\n+                return\n+            S = S_next\n+        logger.warning(\"elastic free(%d MB): reached S_floor=%d, VRAM free %.2f GB\",\n+                       reserve_mb, S, torch.cuda.mem_get_info()[0] / 1024)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self.ctl is None:\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                self.pending_regrow = True\n+                return\n+            S = S_next\n+        logger.warning(\"elastic free(%d MB): reached S_floor=%d, VRAM free %.2f GB\",\n+                       reserve_mb, S, torch.cuda.mem_get_info()[0] / 1024)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self.ctl is None:\n+            return+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                self.pending_regrow = True\n+                return\n+            S = S_next\n+        logger.warning(\"elastic free(%d MB): reached S_floor=%d, VRAM free %.2f GB\",\n+                       reserve_mb, S, torch.cuda.mem_get_info()[0] / 1024)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self.ctl is None:\n+            return\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                self.pending_regrow = True\n+                return\n+            S = S_next\n+        logger.warning(\"elastic free(%d MB): reached S_floor=%d, VRAM free %.2f GB\",\n+                       reserve_mb, S, torch.cuda.mem_get_info()[0] / 1024)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self.ctl is None:\n+            return\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                self.pending_regrow = True\n+                return\n+            S = S_next\n+        logger.warning(\"elastic free(%d MB): reached S_floor=%d, VRAM free %.2f GB\",\n+                       reserve_mb, S, torch.cuda.mem_get_info()[0] / 1024)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self.ctl is None:\n+            return\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                self.pending_regrow = True\n+                return\n+            S = S_next\n+        logger.warning(\"elastic free(%d MB): reached S_floor=%d, VRAM free %.2f GB\",\n+                       reserve_mb, S, torch.cuda.mem_get_info()[0] / 1024)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self.ctl is None:\n+            return\n+        try+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        reserves more than the last step took).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Exception:\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                return\n+            S = S_next\n+        logger.warning(\"elastic fill(%d MB): reached S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 1024)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >> 20 >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                self.pending_regrow = True\n+                return\n+            S = S_next\n+        logger.warning(\"elastic free(%d MB): reached S_floor=%d, VRAM free %.2f GB\",\n+                       reserve_mb, S, torch.cuda.mem_get_info()[0] / 1024)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self.ctl is None:\n+            return\n+        try:",
 "ttft_seconds": 0.42161635699449107,
 "status": "ok",
 "tg_tokens_per_s": 30.008233286157683,
 "decode_seconds_per_token": 0.0333241877475434,
 "individual_token_timing": true,
 "event_intervals_ms": {
  "n": 511,
  "median": 32.819375992403366,
  "p95": 44.167639993247576,
  "mean": 33.3241877475434
 },
 "class": "code",
 "requested_input_tokens": 32768
}
