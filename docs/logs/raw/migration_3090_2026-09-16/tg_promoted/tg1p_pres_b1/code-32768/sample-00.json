{
 "started_utc": "2026-09-17T03:59:23.388194+00:00",
 "wall_seconds": 34.85036330099683,
 "prompt_tokens": 32768,
 "completion_tokens": 512,
 "stream_events": 512,
 "ended_utc": "2026-09-17T03:59:58.238663+00:00",
 "warmup": true,
 "sample_index": 0,
 "requested_decode_tokens": 512,
 "gpu": {
  "n": 125,
  "temperature_c": {
   "min": 71.0,
   "max": 76.0,
   "mean": 74.6
  },
  "power_w": {
   "min": 150.47,
   "max": 282.01,
   "mean": 242.02064000000001
  },
  "util_pct": {
   "min": 0.0,
   "max": 100.0,
   "mean": 81.616
  },
  "clock_sm_mhz": {
   "min": 1380.0,
   "max": 1920.0,
   "mean": 1766.28
  },
  "clock_mem_mhz": {
   "min": 9501.0,
   "max": 9501.0,
   "mean": 9501.0
  },
  "memory_free_mib": {
   "min": 1324.0
  }
 },
 "output_text_sha256": "c6729d98b21169755292719ff2f6d91d4e6192df37ae595d984121417cca6da5",
 "output_text": "++       +        \"\"\"+        \"\"\"Grow+        \"\"\"Grow every+        \"\"\"Grow every layer+        \"\"\"Grow every layer until+        \"\"\"Grow every layer until `+        \"\"\"Grow every layer until `reserve+        \"\"\"Grow every layer until `reserve_mb+        \"\"\"Grow every layer until `reserve_mb`+        \"\"\"Grow every layer until `reserve_mb` of+        \"\"\"Grow every layer until `reserve_mb` of VR+        \"\"\"Grow every layer until `reserve_mb` of VRAM+        \"\"\"Grow every layer until `reserve_mb` of VRAM is+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >>+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >=+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(res+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S ++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (Runtime+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, Arena+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(),+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S -+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VR+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\",+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+   +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Sh+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb`+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VR+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >>+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >=+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(res+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(),+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S -+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_reg+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+   +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _reg+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_reg+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.reg+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_reg+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+   +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write_status+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write_status(self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write_status(self):+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())}+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0}+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free >>+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free >> +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free >> 2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free >> 20+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free >> 20}+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free >> 20} total+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free >> 20} total {+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free >> 20} total {total+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free >> 20} total {total >>+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free >> 20} total {total >> +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free >> 20} total {total >> 2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free >> 20} total {total >> 20+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free >> 20} total {total >> 20}+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free >> 20} total {total >> 20}\"]+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free >> 20} total {total >> 20}\"]\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free >> 20} total {total >> 20}\"]\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free >> 20} total {total >> 20}\"]\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free >> 20} total {total >> 20}\"]\n+        for+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free >> 20} total {total >> 20}\"]\n+        for lid+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free >> 20} total {total >> 20}\"]\n+        for lid in+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free >> 20} total {total >> 20}\"]\n+        for lid in sorted+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free >> 20} total {total >> 20}\"]\n+        for lid in sorted(self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free >> 20} total {total >> 20}\"]\n+        for lid in sorted(self.layers+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free >> 20} total {total >> 20}\"]\n+        for lid in sorted(self.layers):+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free >> 20} total {total >> 20}\"]\n+        for lid in sorted(self.layers):\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free >> 20} total {total >> 20}\"]\n+        for lid in sorted(self.layers):\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free >> 20} total {total >> 20}\"]\n+        for lid in sorted(self.layers):\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free >> 20} total {total >> 20}\"]\n+        for lid in sorted(self.layers):\n+            st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free >> 20} total {total >> 20}\"]\n+        for lid in sorted(self.layers):\n+            st =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free >> 20} total {total >> 20}\"]\n+        for lid in sorted(self.layers):\n+            st = self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free >> 20} total {total >> 20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free >> 20} total {total >> 20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[l+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free >> 20} total {total >> 20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free >> 20} total {total >> 20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free >> 20} total {total >> 20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free >> 20} total {total >> 20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free >> 20} total {total >> 20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free >> 20} total {total >> 20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free >> 20} total {total >> 20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free >> 20} total {total >> 20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free >> 20} total {total >> 20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free >> 20} total {total >> 20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free >> 20} total {total >> 20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free >> 20} total {total >> 20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free >> 20} total {total >> 20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free >> 20} total {total >> 20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows *+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free >> 20} total {total >> 20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free >> 20} total {total >> 20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free >> 20} total {total >> 20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free >> 20} total {total >> 20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free >> 20} total {total >> 20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free >> 20} total {total >> 20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free >> 20} total {total >> 20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.k+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free >> 20} total {total >> 20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free >> 20} total {total >> 20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free >> 20} total {total >> 20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values())+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free >> 20} total {total >> 20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) >>+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free >> 20} total {total >> 20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) >> +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free >> 20} total {total >> 20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) >> 2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free >> 20} total {total >> 20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) >> 20+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free >> 20} total {total >> 20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) >> 20\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free >> 20} total {total >> 20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) >> 20\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free >> 20} total {total >> 20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) >> 20\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free >> 20} total {total >> 20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) >> 20\n+            lines+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free >> 20} total {total >> 20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) >> 20\n+            lines.append+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free >> 20} total {total >> 20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) >> 20\n+            lines.append(f+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free >> 20} total {total >> 20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) >> 20\n+            lines.append(f\"L+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free >> 20} total {total >> 20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) >> 20\n+            lines.append(f\"L{+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free >> 20} total {total >> 20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) >> 20\n+            lines.append(f\"L{lid+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free >> 20} total {total >> 20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) >> 20\n+            lines.append(f\"L{lid}+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free >> 20} total {total >> 20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) >> 20\n+            lines.append(f\"L{lid} S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free >> 20} total {total >> 20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) >> 20\n+            lines.append(f\"L{lid} S {+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free >> 20} total {total >> 20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) >> 20\n+            lines.append(f\"L{lid} S {st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free >> 20} total {total >> 20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) >> 20\n+            lines.append(f\"L{lid} S {st.S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free >> 20} total {total >> 20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) >> 20\n+            lines.append(f\"L{lid} S {st.S}+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free >> 20} total {total >> 20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) >> 20\n+            lines.append(f\"L{lid} S {st.S} arena+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free >> 20} total {total >> 20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) >> 20\n+            lines.append(f\"L{lid} S {st.S} arena {+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free >> 20} total {total >> 20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) >> 20\n+            lines.append(f\"L{lid} S {st.S} arena {arena+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free >> 20} total {total >> 20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) >> 20\n+            lines.append(f\"L{lid} S {st.S} arena {arena}+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free >> 20} total {total >> 20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) >> 20\n+            lines.append(f\"L{lid} S {st.S} arena {arena} pool+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free >> 20} total {total >> 20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) >> 20\n+            lines.append(f\"L{lid} S {st.S} arena {arena} pool {+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free >> 20} total {total >> 20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) >> 20\n+            lines.append(f\"L{lid} S {st.S} arena {arena} pool {len+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free >> 20} total {total >> 20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) >> 20\n+            lines.append(f\"L{lid} S {st.S} arena {arena} pool {len(self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free >> 20} total {total >> 20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) >> 20\n+            lines.append(f\"L{lid} S {st.S} arena {arena} pool {len(self.pool+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free >> 20} total {total >> 20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) >> 20\n+            lines.append(f\"L{lid} S {st.S} arena {arena} pool {len(self.pool[N+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free >> 20} total {total >> 20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) >> 20\n+            lines.append(f\"L{lid} S {st.S} arena {arena} pool {len(self.pool[NAMES+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free >> 20} total {total >> 20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) >> 20\n+            lines.append(f\"L{lid} S {st.S} arena {arena} pool {len(self.pool[NAMES[+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free >> 20} total {total >> 20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) >> 20\n+            lines.append(f\"L{lid} S {st.S} arena {arena} pool {len(self.pool[NAMES[0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free >> 20} total {total >> 20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) >> 20\n+            lines.append(f\"L{lid} S {st.S} arena {arena} pool {len(self.pool[NAMES[0]])+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free >> 20} total {total >> 20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) >> 20\n+            lines.append(f\"L{lid} S {st.S} arena {arena} pool {len(self.pool[NAMES[0]])}\")+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free >> 20} total {total >> 20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) >> 20\n+            lines.append(f\"L{lid} S {st.S} arena {arena} pool {len(self.pool[NAMES[0]])}\")\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free >> 20} total {total >> 20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) >> 20\n+            lines.append(f\"L{lid} S {st.S} arena {arena} pool {len(self.pool[NAMES[0]])}\")\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free >> 20} total {total >> 20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) >> 20\n+            lines.append(f\"L{lid} S {st.S} arena {arena} pool {len(self.pool[NAMES[0]])}\")\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free >> 20} total {total >> 20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) >> 20\n+            lines.append(f\"L{lid} S {st.S} arena {arena} pool {len(self.pool[NAMES[0]])}\")\n+        try+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free >> 20} total {total >> 20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) >> 20\n+            lines.append(f\"L{lid} S {st.S} arena {arena} pool {len(self.pool[NAMES[0]])}\")\n+        try:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free >> 20} total {total >> 20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) >> 20\n+            lines.append(f\"L{lid} S {st.S} arena {arena} pool {len(self.pool[NAMES[0]])}\")\n+        try:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free >> 20} total {total >> 20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) >> 20\n+            lines.append(f\"L{lid} S {st.S} arena {arena} pool {len(self.pool[NAMES[0]])}\")\n+        try:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free >> 20} total {total >> 20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) >> 20\n+            lines.append(f\"L{lid} S {st.S} arena {arena} pool {len(self.pool[NAMES[0]])}\")\n+        try:\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free >> 20} total {total >> 20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) >> 20\n+            lines.append(f\"L{lid} S {st.S} arena {arena} pool {len(self.pool[NAMES[0]])}\")\n+        try:\n+            with+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free >> 20} total {total >> 20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) >> 20\n+            lines.append(f\"L{lid} S {st.S} arena {arena} pool {len(self.pool[NAMES[0]])}\")\n+        try:\n+            with open+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step rows at a time).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                logger.warning(\"elastic fill: VRAM exhausted, settled at S=%d\", S)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve the operator set).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.set_S(self.regrow_reserve)\n+        self.pending_regrow = False\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free >> 20} total {total >> 20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) >> 20\n+            lines.append(f\"L{lid} S {st.S} arena {arena} pool {len(self.pool[NAMES[0]])}\")\n+        try:\n+            with open(self",
 "ttft_seconds": 17.64329671600717,
 "status": "ok",
 "tg_tokens_per_s": 29.697265121022017,
 "decode_seconds_per_token": 0.03367313440900397,
 "individual_token_timing": true,
 "event_intervals_ms": {
  "n": 511,
  "median": 33.41357401222922,
  "p95": 42.477641996811144,
  "mean": 33.67313440900397
 },
 "class": "code",
 "requested_input_tokens": 32768
}
