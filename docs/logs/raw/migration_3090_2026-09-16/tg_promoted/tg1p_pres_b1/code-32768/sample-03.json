{
 "started_utc": "2026-09-17T04:00:35.166619+00:00",
 "wall_seconds": 17.72933421000198,
 "prompt_tokens": 32768,
 "completion_tokens": 512,
 "stream_events": 512,
 "ended_utc": "2026-09-17T04:00:52.896100+00:00",
 "warmup": false,
 "sample_index": 3,
 "requested_decode_tokens": 512,
 "gpu": {
  "n": 63,
  "temperature_c": {
   "min": 75.0,
   "max": 76.0,
   "mean": 75.19047619047619
  },
  "power_w": {
   "min": 229.9,
   "max": 258.99,
   "mean": 247.29984126984127
  },
  "util_pct": {
   "min": 89.0,
   "max": 96.0,
   "mean": 92.0
  },
  "clock_sm_mhz": {
   "min": 1920.0,
   "max": 1920.0,
   "mean": 1920.0
  },
  "clock_mem_mhz": {
   "min": 9501.0,
   "max": 9501.0,
   "mean": 9501.0
  },
  "memory_free_mib": {
   "min": 2080.0
  }
 },
 "output_text_sha256": "4bd52a3fc0e77f812d3554ddc75b35fff7a473ea531f7e31a0ad3f549aa2fb7b",
 "output_text": "++       +        \"\"\"+        \"\"\"Grow+        \"\"\"Grow every+        \"\"\"Grow every layer+        \"\"\"Grow every layer until+        \"\"\"Grow every layer until `+        \"\"\"Grow every layer until `reserve+        \"\"\"Grow every layer until `reserve_mb+        \"\"\"Grow every layer until `reserve_mb`+        \"\"\"Grow every layer until `reserve_mb` of+        \"\"\"Grow every layer until `reserve_mb` of VR+        \"\"\"Grow every layer until `reserve_mb` of VRAM+        \"\"\"Grow every layer until `reserve_mb` of VRAM is+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >>+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >=+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S ++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\",+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+   +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Sh+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb`+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VR+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >>+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >=+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(),+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S -+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\",+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_reg+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+   +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _reg+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_reg+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_reg+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S0\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >>+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >=+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= self.reg+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= self.regrow+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= self.regrow_reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= self.regrow_reserve:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= self.regrow_reserve:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= self.regrow_reserve:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= self.regrow_reserve:\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= self.regrow_reserve:\n+                break+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= self.regrow_reserve:\n+                break\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= self.regrow_reserve:\n+                break\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= self.regrow_reserve:\n+                break\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= self.regrow_reserve:\n+                break\n+            S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= self.regrow_reserve:\n+                break\n+            S =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= self.regrow_reserve:\n+                break\n+            S = min+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= self.regrow_reserve:\n+                break\n+            S = min(self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= self.regrow_reserve:\n+                break\n+            S = min(self.E+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= self.regrow_reserve:\n+                break\n+            S = min(self.E,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= self.regrow_reserve:\n+                break\n+            S = min(self.E, S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= self.regrow_reserve:\n+                break\n+            S = min(self.E, S ++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= self.regrow_reserve:\n+                break\n+            S = min(self.E, S + +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= self.regrow_reserve:\n+                break\n+            S = min(self.E, S + 8+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= self.regrow_reserve:\n+                break\n+            S = min(self.E, S + 8)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= self.regrow_reserve:\n+                break\n+            S = min(self.E, S + 8)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= self.regrow_reserve:\n+                break\n+            S = min(self.E, S + 8)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= self.regrow_reserve:\n+                break\n+            S = min(self.E, S + 8)\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= self.regrow_reserve:\n+                break\n+            S = min(self.E, S + 8)\n+            try+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= self.regrow_reserve:\n+                break\n+            S = min(self.E, S + 8)\n+            try:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= self.regrow_reserve:\n+                break\n+            S = min(self.E, S + 8)\n+            try:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= self.regrow_reserve:\n+                break\n+            S = min(self.E, S + 8)\n+            try:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= self.regrow_reserve:\n+                break\n+            S = min(self.E, S + 8)\n+            try:\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= self.regrow_reserve:\n+                break\n+            S = min(self.E, S + 8)\n+            try:\n+                self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= self.regrow_reserve:\n+                break\n+            S = min(self.E, S + 8)\n+            try:\n+                self.set+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= self.regrow_reserve:\n+                break\n+            S = min(self.E, S + 8)\n+            try:\n+                self.set_S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= self.regrow_reserve:\n+                break\n+            S = min(self.E, S + 8)\n+            try:\n+                self.set_S(S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= self.regrow_reserve:\n+                break\n+            S = min(self.E, S + 8)\n+            try:\n+                self.set_S(S)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= self.regrow_reserve:\n+                break\n+            S = min(self.E, S + 8)\n+            try:\n+                self.set_S(S)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= self.regrow_reserve:\n+                break\n+            S = min(self.E, S + 8)\n+            try:\n+                self.set_S(S)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= self.regrow_reserve:\n+                break\n+            S = min(self.E, S + 8)\n+            try:\n+                self.set_S(S)\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= self.regrow_reserve:\n+                break\n+            S = min(self.E, S + 8)\n+            try:\n+                self.set_S(S)\n+            except+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= self.regrow_reserve:\n+                break\n+            S = min(self.E, S + 8)\n+            try:\n+                self.set_S(S)\n+            except Exception+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= self.regrow_reserve:\n+                break\n+            S = min(self.E, S + 8)\n+            try:\n+                self.set_S(S)\n+            except Exception as+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= self.regrow_reserve:\n+                break\n+            S = min(self.E, S + 8)\n+            try:\n+                self.set_S(S)\n+            except Exception as e+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= self.regrow_reserve:\n+                break\n+            S = min(self.E, S + 8)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= self.regrow_reserve:\n+                break\n+            S = min(self.E, S + 8)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= self.regrow_reserve:\n+                break\n+            S = min(self.E, S + 8)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= self.regrow_reserve:\n+                break\n+            S = min(self.E, S + 8)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= self.regrow_reserve:\n+                break\n+            S = min(self.E, S + 8)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= self.regrow_reserve:\n+                break\n+            S = min(self.E, S + 8)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= self.regrow_reserve:\n+                break\n+            S = min(self.E, S + 8)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= self.regrow_reserve:\n+                break\n+            S = min(self.E, S + 8)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= self.regrow_reserve:\n+                break\n+            S = min(self.E, S + 8)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic reg+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= self.regrow_reserve:\n+                break\n+            S = min(self.E, S + 8)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic regrow+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= self.regrow_reserve:\n+                break\n+            S = min(self.E, S + 8)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic regrow:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= self.regrow_reserve:\n+                break\n+            S = min(self.E, S + 8)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic regrow: %+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= self.regrow_reserve:\n+                break\n+            S = min(self.E, S + 8)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic regrow: %s+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= self.regrow_reserve:\n+                break\n+            S = min(self.E, S + 8)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic regrow: %s\",+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= self.regrow_reserve:\n+                break\n+            S = min(self.E, S + 8)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic regrow: %s\", e+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= self.regrow_reserve:\n+                break\n+            S = min(self.E, S + 8)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic regrow: %s\", e)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= self.regrow_reserve:\n+                break\n+            S = min(self.E, S + 8)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic regrow: %s\", e)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= self.regrow_reserve:\n+                break\n+            S = min(self.E, S + 8)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic regrow: %s\", e)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= self.regrow_reserve:\n+                break\n+            S = min(self.E, S + 8)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic regrow: %s\", e)\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= self.regrow_reserve:\n+                break\n+            S = min(self.E, S + 8)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic regrow: %s\", e)\n+                break+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= self.regrow_reserve:\n+                break\n+            S = min(self.E, S + 8)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic regrow: %s\", e)\n+                break\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= self.regrow_reserve:\n+                break\n+            S = min(self.E, S + 8)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic regrow: %s\", e)\n+                break\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= self.regrow_reserve:\n+                break\n+            S = min(self.E, S + 8)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic regrow: %s\", e)\n+                break\n+\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= self.regrow_reserve:\n+                break\n+            S = min(self.E, S + 8)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic regrow: %s\", e)\n+                break\n+\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= self.regrow_reserve:\n+                break\n+            S = min(self.E, S + 8)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic regrow: %s\", e)\n+                break\n+\n+   +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= self.regrow_reserve:\n+                break\n+            S = min(self.E, S + 8)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic regrow: %s\", e)\n+                break\n+\n+    def+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= self.regrow_reserve:\n+                break\n+            S = min(self.E, S + 8)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic regrow: %s\", e)\n+                break\n+\n+    def write+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= self.regrow_reserve:\n+                break\n+            S = min(self.E, S + 8)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic regrow: %s\", e)\n+                break\n+\n+    def write_status+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= self.regrow_reserve:\n+                break\n+            S = min(self.E, S + 8)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic regrow: %s\", e)\n+                break\n+\n+    def write_status(self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= self.regrow_reserve:\n+                break\n+            S = min(self.E, S + 8)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic regrow: %s\", e)\n+                break\n+\n+    def write_status(self):+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= self.regrow_reserve:\n+                break\n+            S = min(self.E, S + 8)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic regrow: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= self.regrow_reserve:\n+                break\n+            S = min(self.E, S + 8)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic regrow: %s\", e)\n+                break\n+\n+    def write_status(self):\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= self.regrow_reserve:\n+                break\n+            S = min(self.E, S + 8)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic regrow: %s\", e)\n+                break\n+\n+    def write_status(self):\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= self.regrow_reserve:\n+                break\n+            S = min(self.E, S + 8)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic regrow: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= self.regrow_reserve:\n+                break\n+            S = min(self.E, S + 8)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic regrow: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= self.regrow_reserve:\n+                break\n+            S = min(self.E, S + 8)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic regrow: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= self.regrow_reserve:\n+                break\n+            S = min(self.E, S + 8)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic regrow: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= self.regrow_reserve:\n+                break\n+            S = min(self.E, S + 8)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic regrow: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= self.regrow_reserve:\n+                break\n+            S = min(self.E, S + 8)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic regrow: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= self.regrow_reserve:\n+                break\n+            S = min(self.E, S + 8)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic regrow: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= self.regrow_reserve:\n+                break\n+            S = min(self.E, S + 8)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic regrow: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= self.regrow_reserve:\n+                break\n+            S = min(self.E, S + 8)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic regrow: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= self.regrow_reserve:\n+                break\n+            S = min(self.E, S + 8)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic regrow: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= self.regrow_reserve:\n+                break\n+            S = min(self.E, S + 8)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic regrow: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= self.regrow_reserve:\n+                break\n+            S = min(self.E, S + 8)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic regrow: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= self.regrow_reserve:\n+                break\n+            S = min(self.E, S + 8)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic regrow: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= self.regrow_reserve:\n+                break\n+            S = min(self.E, S + 8)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic regrow: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= self.regrow_reserve:\n+                break\n+            S = min(self.E, S + 8)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic regrow: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= self.regrow_reserve:\n+                break\n+            S = min(self.E, S + 8)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic regrow: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free = torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= self.regrow_reserve:\n+                break\n+            S = min(self.E, S + 8)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic regrow: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free = torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= self.regrow_reserve:\n+                break\n+            S = min(self.E, S + 8)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic regrow: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free = torch.cuda.mem+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= self.regrow_reserve:\n+                break\n+            S = min(self.E, S + 8)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic regrow: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free = torch.cuda.mem_get+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= self.regrow_reserve:\n+                break\n+            S = min(self.E, S + 8)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic regrow: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free = torch.cuda.mem_get_info+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= self.regrow_reserve:\n+                break\n+            S = min(self.E, S + 8)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic regrow: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free = torch.cuda.mem_get_info()[+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= self.regrow_reserve:\n+                break\n+            S = min(self.E, S + 8)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic regrow: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free = torch.cuda.mem_get_info()[0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= self.regrow_reserve:\n+                break\n+            S = min(self.E, S + 8)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic regrow: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free = torch.cuda.mem_get_info()[0]+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= self.regrow_reserve:\n+                break\n+            S = min(self.E, S + 8)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic regrow: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >>+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= self.regrow_reserve:\n+                break\n+            S = min(self.E, S + 8)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic regrow: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= self.regrow_reserve:\n+                break\n+            S = min(self.E, S + 8)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic regrow: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= self.regrow_reserve:\n+                break\n+            S = min(self.E, S + 8)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic regrow: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= self.regrow_reserve:\n+                break\n+            S = min(self.E, S + 8)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic regrow: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= self.regrow_reserve:\n+                break\n+            S = min(self.E, S + 8)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic regrow: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= self.regrow_reserve:\n+                break\n+            S = min(self.E, S + 8)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic regrow: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= self.regrow_reserve:\n+                break\n+            S = min(self.E, S + 8)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic regrow: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        lines+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= self.regrow_reserve:\n+                break\n+            S = min(self.E, S + 8)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic regrow: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        lines =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= self.regrow_reserve:\n+                break\n+            S = min(self.E, S + 8)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic regrow: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        lines = [+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= self.regrow_reserve:\n+                break\n+            S = min(self.E, S + 8)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic regrow: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        lines = [f+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= self.regrow_reserve:\n+                break\n+            S = min(self.E, S + 8)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic regrow: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        lines = [f\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= self.regrow_reserve:\n+                break\n+            S = min(self.E, S + 8)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic regrow: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        lines = [f\"free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= self.regrow_reserve:\n+                break\n+            S = min(self.E, S + 8)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic regrow: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        lines = [f\"free_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= self.regrow_reserve:\n+                break\n+            S = min(self.E, S + 8)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic regrow: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        lines = [f\"free_mb={+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= self.regrow_reserve:\n+                break\n+            S = min(self.E, S + 8)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic regrow: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        lines = [f\"free_mb={free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= self.regrow_reserve:\n+                break\n+            S = min(self.E, S + 8)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic regrow: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        lines = [f\"free_mb={free}+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= self.regrow_reserve:\n+                break\n+            S = min(self.E, S + 8)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic regrow: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        lines = [f\"free_mb={free}\"]+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= self.regrow_reserve:\n+                break\n+            S = min(self.E, S + 8)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic regrow: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        lines = [f\"free_mb={free}\"]\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= self.regrow_reserve:\n+                break\n+            S = min(self.E, S + 8)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic regrow: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        lines = [f\"free_mb={free}\"]\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= self.regrow_reserve:\n+                break\n+            S = min(self.E, S + 8)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic regrow: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        lines = [f\"free_mb={free}\"]\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= self.regrow_reserve:\n+                break\n+            S = min(self.E, S + 8)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic regrow: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        lines = [f\"free_mb={free}\"]\n+        for+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= self.regrow_reserve:\n+                break\n+            S = min(self.E, S + 8)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic regrow: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        lines = [f\"free_mb={free}\"]\n+        for lid+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= self.regrow_reserve:\n+                break\n+            S = min(self.E, S + 8)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic regrow: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        lines = [f\"free_mb={free}\"]\n+        for lid in+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        self.pending_regrow = True\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (one step-sized round).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= self.regrow_reserve:\n+                break\n+            S = min(self.E, S + 8)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic regrow: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        lines = [f\"free_mb={free}\"]\n+        for lid in sorted",
 "ttft_seconds": 0.4333245599991642,
 "status": "ok",
 "tg_tokens_per_s": 29.544557504807223,
 "decode_seconds_per_token": 0.03384718149314943,
 "individual_token_timing": true,
 "event_intervals_ms": {
  "n": 511,
  "median": 33.855782996397465,
  "p95": 43.69001601298805,
  "mean": 33.84718149314943
 },
 "class": "code",
 "requested_input_tokens": 32768
}
