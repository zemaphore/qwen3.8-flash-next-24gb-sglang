{
 "started_utc": "2026-09-17T04:00:52.899310+00:00",
 "wall_seconds": 16.974750790002872,
 "prompt_tokens": 32768,
 "completion_tokens": 512,
 "stream_events": 512,
 "ended_utc": "2026-09-17T04:01:09.874394+00:00",
 "warmup": false,
 "sample_index": 4,
 "requested_decode_tokens": 512,
 "gpu": {
  "n": 61,
  "temperature_c": {
   "min": 74.0,
   "max": 76.0,
   "mean": 75.47540983606558
  },
  "power_w": {
   "min": 234.23,
   "max": 262.77,
   "mean": 250.52245901639347
  },
  "util_pct": {
   "min": 90.0,
   "max": 95.0,
   "mean": 92.78688524590164
  },
  "clock_sm_mhz": {
   "min": 1890.0,
   "max": 1920.0,
   "mean": 1919.016393442623
  },
  "clock_mem_mhz": {
   "min": 9501.0,
   "max": 9501.0,
   "mean": 9501.0
  },
  "memory_free_mib": {
   "min": 2080.0
  }
 },
 "output_text_sha256": "bba0282f86850fe9b9cc6e5ef3a172b6cdc2776b20c7a7631f6e781f525311c5",
 "output_text": "++       +        \"\"\"+        \"\"\"Grow+        \"\"\"Grow every+        \"\"\"Grow every layer+        \"\"\"Grow every layer until+        \"\"\"Grow every layer until `+        \"\"\"Grow every layer until `reserve+        \"\"\"Grow every layer until `reserve_mb+        \"\"\"Grow every layer until `reserve_mb`+        \"\"\"Grow every layer until `reserve_mb` of+        \"\"\"Grow every layer until `reserve_mb` of VR+        \"\"\"Grow every layer until `reserve_mb` of VRAM+        \"\"\"Grow every layer until `reserve_mb` of VRAM is+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-me+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S <+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] /+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <=+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(f+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free -+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values()+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.k+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room //+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S ++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (Arena+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+   +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Sh+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb`+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VR+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S >+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] /+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >=+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values()+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.k+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(res+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb -+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) //+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S -+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n+            except+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n+            except (+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n+            except (Arena+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n+            except (ArenaOOM+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n+            except (ArenaOOM,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n+            except (ArenaOOM, RuntimeError+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n+            except (ArenaOOM, RuntimeError):+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n+            except (ArenaOOM, RuntimeError):\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n+            except (ArenaOOM, RuntimeError):\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n+            except (ArenaOOM, RuntimeError):\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n+            except (ArenaOOM, RuntimeError):\n+                break+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = max+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = max(st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = max(st.S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = max(st.S for+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = max(st.S for st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = max(st.S for st in+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = max(st.S for st in self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = max(st.S for st in self.layers+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = max(st.S for st in self.layers.values+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = max(st.S for st in self.layers.values())+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = max(st.S for st in self.layers.values())\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = max(st.S for st in self.layers.values())\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+   +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    #+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    # ----------------------------------------------------------------+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    # ------------------------------------------------------------------+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    # ------------------------------------------------------------------ control+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+   +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the Mo+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (pref+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/e+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls +=+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls %+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 1+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 ==+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._aut+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autof+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free = torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free = torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free = torch.cuda.mem+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free = torch.cuda.mem_get+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free = torch.cuda.mem_get_info+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free = torch.cuda.mem_get_info()[+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free = torch.cuda.mem_get_info()[0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free = torch.cuda.mem_get_info()[0]+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free = torch.cuda.mem_get_info()[0] /+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free = torch.cuda.mem_get_info()[0] / +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free = torch.cuda.mem_get_info()[0] / 2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free = torch.cuda.mem_get_info()[0] / 2**+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free = torch.cuda.mem_get_info()[0] / 2**2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free = torch.cuda.mem_get_info()[0] / 2**20+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free > self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free > self._+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free > self._aut+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free > self._autof+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free > self._autofill+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free > self._autofill ++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free > self._autofill + +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free > self._autofill + 1+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free > self._autofill + 12+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free > self._autofill + 128+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free > self._autofill + 128:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free > self._autofill + 128:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free > self._autofill + 128:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free > self._autofill + 128:\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free > self._autofill + 128:\n+                self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free > self._autofill + 128:\n+                self.fill+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int(free - reserve_mb)\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, room // per))\n+            try:\n+                self.set_S(S + step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            per = sum(k.arena.row_bytes for st in self.layers.values() for k in st.kinds.values())\n+            step = max(1, min(step, int(reserve_mb - free) // per))\n+            try:\n+                self.set_S(S - step)\n+            except (ArenaOOM, RuntimeError):\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 16 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free > self._autofill + 128:\n+                self.fill(self",
 "ttft_seconds": 0.4256790350045776,
 "status": "ok",
 "tg_tokens_per_s": 30.878065449631922,
 "decode_seconds_per_token": 0.03238544855185934,
 "individual_token_timing": true,
 "event_intervals_ms": {
  "n": 511,
  "median": 31.64066799217835,
  "p95": 43.288994012982585,
  "mean": 32.38544855185934
 },
 "class": "code",
 "requested_input_tokens": 32768
}
