{
 "started_utc": "2026-09-17T04:00:17.293183+00:00",
 "wall_seconds": 17.868273186002625,
 "prompt_tokens": 32768,
 "completion_tokens": 512,
 "stream_events": 512,
 "ended_utc": "2026-09-17T04:00:35.161641+00:00",
 "warmup": false,
 "sample_index": 2,
 "requested_decode_tokens": 512,
 "gpu": {
  "n": 64,
  "temperature_c": {
   "min": 74.0,
   "max": 76.0,
   "mean": 75.125
  },
  "power_w": {
   "min": 228.67,
   "max": 260.63,
   "mean": 246.18734375
  },
  "util_pct": {
   "min": 85.0,
   "max": 95.0,
   "mean": 91.828125
  },
  "clock_sm_mhz": {
   "min": 1905.0,
   "max": 1920.0,
   "mean": 1919.53125
  },
  "clock_mem_mhz": {
   "min": 9501.0,
   "max": 9501.0,
   "mean": 9501.0
  },
  "memory_free_mib": {
   "min": 2080.0
  }
 },
 "output_text_sha256": "e72cc6c2d3ec11577360e0830ce5525a57fa7ca5abb4620ea2bd05ba92d76897",
 "output_text": "++       +        \"\"\"+        \"\"\"Grow+        \"\"\"Grow every+        \"\"\"Grow every layer+        \"\"\"Grow every layer until+        \"\"\"Grow every layer until `+        \"\"\"Grow every layer until `reserve+        \"\"\"Grow every layer until `reserve_mb+        \"\"\"Grow every layer until `reserve_mb`+        \"\"\"Grow every layer until `reserve_mb` of+        \"\"\"Grow every layer until `reserve_mb` of VR+        \"\"\"Grow every layer until `reserve_mb` of VRAM+        \"\"\"Grow every layer until `reserve_mb` of VRAM is+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >>+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >=+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >=+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S ++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+   +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Sh+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb`+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VR+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >>+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >=+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <=+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S -+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+   +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _reg+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autof+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >>+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free <+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.reg+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve or+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve or S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve or S >=+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve or S >= self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve or S >= self.E+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve or S >= self.E:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve or S >= self.E:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve or S >= self.E:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve or S >= self.E:\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve or S >= self.E:\n+                break+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve or S >= self.E:\n+                break\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve or S >= self.E:\n+                break\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve or S >= self.E:\n+                break\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve or S >= self.E:\n+                break\n+            S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve or S >= self.E:\n+                break\n+            S_new+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve or S >= self.E:\n+                break\n+            S_new =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve or S >= self.E:\n+                break\n+            S_new = min+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve or S >= self.E:\n+                break\n+            S_new = min(S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve or S >= self.E:\n+                break\n+            S_new = min(S ++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve or S >= self.E:\n+                break\n+            S_new = min(S + +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve or S >= self.E:\n+                break\n+            S_new = min(S + 8+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve or S >= self.E:\n+                break\n+            S_new = min(S + 8,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve or S >= self.E:\n+                break\n+            S_new = min(S + 8, self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve or S >= self.E:\n+                break\n+            S_new = min(S + 8, self.E+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve or S >= self.E:\n+                break\n+            S_new = min(S + 8, self.E)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve or S >= self.E:\n+                break\n+            S_new = min(S + 8, self.E)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve or S >= self.E:\n+                break\n+            S_new = min(S + 8, self.E)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve or S >= self.E:\n+                break\n+            S_new = min(S + 8, self.E)\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve or S >= self.E:\n+                break\n+            S_new = min(S + 8, self.E)\n+            try+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve or S >= self.E:\n+                break\n+            S_new = min(S + 8, self.E)\n+            try:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve or S >= self.E:\n+                break\n+            S_new = min(S + 8, self.E)\n+            try:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve or S >= self.E:\n+                break\n+            S_new = min(S + 8, self.E)\n+            try:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve or S >= self.E:\n+                break\n+            S_new = min(S + 8, self.E)\n+            try:\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve or S >= self.E:\n+                break\n+            S_new = min(S + 8, self.E)\n+            try:\n+                self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve or S >= self.E:\n+                break\n+            S_new = min(S + 8, self.E)\n+            try:\n+                self.set+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve or S >= self.E:\n+                break\n+            S_new = min(S + 8, self.E)\n+            try:\n+                self.set_S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve or S >= self.E:\n+                break\n+            S_new = min(S + 8, self.E)\n+            try:\n+                self.set_S(S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve or S >= self.E:\n+                break\n+            S_new = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_new+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve or S >= self.E:\n+                break\n+            S_new = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_new)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve or S >= self.E:\n+                break\n+            S_new = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_new)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve or S >= self.E:\n+                break\n+            S_new = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_new)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve or S >= self.E:\n+                break\n+            S_new = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_new)\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve or S >= self.E:\n+                break\n+            S_new = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve or S >= self.E:\n+                break\n+            S_new = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve or S >= self.E:\n+                break\n+            S_new = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve or S >= self.E:\n+                break\n+            S_new = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve or S >= self.E:\n+                break\n+            S_new = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve or S >= self.E:\n+                break\n+            S_new = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve or S >= self.E:\n+                break\n+            S_new = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve or S >= self.E:\n+                break\n+            S_new = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve or S >= self.E:\n+                break\n+            S_new = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve or S >= self.E:\n+                break\n+            S_new = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve or S >= self.E:\n+                break\n+            S_new = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve or S >= self.E:\n+                break\n+            S_new = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve or S >= self.E:\n+                break\n+            S_new = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve or S >= self.E:\n+                break\n+            S_new = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve or S >= self.E:\n+                break\n+            S_new = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve or S >= self.E:\n+                break\n+            S_new = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve or S >= self.E:\n+                break\n+            S_new = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve or S >= self.E:\n+                break\n+            S_new = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve or S >= self.E:\n+                break\n+            S_new = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        self.pending+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve or S >= self.E:\n+                break\n+            S_new = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        self.pending_reg+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve or S >= self.E:\n+                break\n+            S_new = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        self.pending_regrow+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve or S >= self.E:\n+                break\n+            S_new = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        self.pending_regrow =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve or S >= self.E:\n+                break\n+            S_new = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        self.pending_regrow = False+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve or S >= self.E:\n+                break\n+            S_new = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        self.pending_regrow = False\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve or S >= self.E:\n+                break\n+            S_new = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        self.pending_regrow = False\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve or S >= self.E:\n+                break\n+            S_new = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        self.pending_regrow = False\n+\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve or S >= self.E:\n+                break\n+            S_new = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        self.pending_regrow = False\n+\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve or S >= self.E:\n+                break\n+            S_new = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        self.pending_regrow = False\n+\n+   +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve or S >= self.E:\n+                break\n+            S_new = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        self.pending_regrow = False\n+\n+    def+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve or S >= self.E:\n+                break\n+            S_new = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        self.pending_regrow = False\n+\n+    def poll+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve or S >= self.E:\n+                break\n+            S_new = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        self.pending_regrow = False\n+\n+    def poll(self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve or S >= self.E:\n+                break\n+            S_new = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        self.pending_regrow = False\n+\n+    def poll(self):+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve or S >= self.E:\n+                break\n+            S_new = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        self.pending_regrow = False\n+\n+    def poll(self):\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve or S >= self.E:\n+                break\n+            S_new = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        self.pending_regrow = False\n+\n+    def poll(self):\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve or S >= self.E:\n+                break\n+            S_new = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        self.pending_regrow = False\n+\n+    def poll(self):\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve or S >= self.E:\n+                break\n+            S_new = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        self.pending_regrow = False\n+\n+    def poll(self):\n+        \"\"\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve or S >= self.E:\n+                break\n+            S_new = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        self.pending_regrow = False\n+\n+    def poll(self):\n+        \"\"\"Called+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve or S >= self.E:\n+                break\n+            S_new = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        self.pending_regrow = False\n+\n+    def poll(self):\n+        \"\"\"Called from+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve or S >= self.E:\n+                break\n+            S_new = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        self.pending_regrow = False\n+\n+    def poll(self):\n+        \"\"\"Called from the+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve or S >= self.E:\n+                break\n+            S_new = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        self.pending_regrow = False\n+\n+    def poll(self):\n+        \"\"\"Called from the Mo+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve or S >= self.E:\n+                break\n+            S_new = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        self.pending_regrow = False\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve or S >= self.E:\n+                break\n+            S_new = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        self.pending_regrow = False\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve or S >= self.E:\n+                break\n+            S_new = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        self.pending_regrow = False\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve or S >= self.E:\n+                break\n+            S_new = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        self.pending_regrow = False\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve or S >= self.E:\n+                break\n+            S_new = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        self.pending_regrow = False\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (pref+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve or S >= self.E:\n+                break\n+            S_new = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        self.pending_regrow = False\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve or S >= self.E:\n+                break\n+            S_new = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        self.pending_regrow = False\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/e+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve or S >= self.E:\n+                break\n+            S_new = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        self.pending_regrow = False\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve or S >= self.E:\n+                break\n+            S_new = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        self.pending_regrow = False\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve or S >= self.E:\n+                break\n+            S_new = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        self.pending_regrow = False\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve or S >= self.E:\n+                break\n+            S_new = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        self.pending_regrow = False\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve or S >= self.E:\n+                break\n+            S_new = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        self.pending_regrow = False\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve or S >= self.E:\n+                break\n+            S_new = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        self.pending_regrow = False\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve or S >= self.E:\n+                break\n+            S_new = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        self.pending_regrow = False\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve or S >= self.E:\n+                break\n+            S_new = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        self.pending_regrow = False\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve or S >= self.E:\n+                break\n+            S_new = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        self.pending_regrow = False\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve or S >= self.E:\n+                break\n+            S_new = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        self.pending_regrow = False\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve or S >= self.E:\n+                break\n+            S_new = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        self.pending_regrow = False\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls +=+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve or S >= self.E:\n+                break\n+            S_new = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        self.pending_regrow = False\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve or S >= self.E:\n+                break\n+            S_new = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        self.pending_regrow = False\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve or S >= self.E:\n+                break\n+            S_new = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        self.pending_regrow = False\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve or S >= self.E:\n+                break\n+            S_new = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        self.pending_regrow = False\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve or S >= self.E:\n+                break\n+            S_new = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        self.pending_regrow = False\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve or S >= self.E:\n+                break\n+            S_new = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        self.pending_regrow = False\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve or S >= self.E:\n+                break\n+            S_new = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        self.pending_regrow = False\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve or S >= self.E:\n+                break\n+            S_new = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        self.pending_regrow = False\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve or S >= self.E:\n+                break\n+            S_new = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        self.pending_regrow = False\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve or S >= self.E:\n+                break\n+            S_new = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        self.pending_regrow = False\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls ==+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve or S >= self.E:\n+                break\n+            S_new = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        self.pending_regrow = False\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls == +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve or S >= self.E:\n+                break\n+            S_new = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        self.pending_regrow = False\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls == 1+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve or S >= self.E:\n+                break\n+            S_new = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        self.pending_regrow = False\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls == 1:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve or S >= self.E:\n+                break\n+            S_new = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        self.pending_regrow = False\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls == 1:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve or S >= self.E:\n+                break\n+            S_new = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        self.pending_regrow = False\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls == 1:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve or S >= self.E:\n+                break\n+            S_new = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        self.pending_regrow = False\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls == 1:\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve or S >= self.E:\n+                break\n+            S_new = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        self.pending_regrow = False\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls == 1:\n+            logger+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve or S >= self.E:\n+                break\n+            S_new = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        self.pending_regrow = False\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls == 1:\n+            logger.info+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve or S >= self.E:\n+                break\n+            S_new = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        self.pending_regrow = False\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls == 1:\n+            logger.info(\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve or S >= self.E:\n+                break\n+            S_new = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        self.pending_regrow = False\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls == 1:\n+            logger.info(\"elastic+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve or S >= self.E:\n+                break\n+            S_new = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        self.pending_regrow = False\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls == 1:\n+            logger.info(\"elastic:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve or S >= self.E:\n+                break\n+            S_new = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        self.pending_regrow = False\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls == 1:\n+            logger.info(\"elastic: first+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve or S >= self.E:\n+                break\n+            S_new = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        self.pending_regrow = False\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls == 1:\n+            logger.info(\"elastic: first call+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a failed grow\n+        leaves the state intact).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S >= self.E:\n+                break\n+            S_new = min(S + step, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb or S <= self.s_floor():\n+                break\n+            S_new = max(S - step, self.s_floor())\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back after a forced shrink, up to the reserve the autofill keeps free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free < self.regrow_reserve or S >= self.E:\n+                break\n+            S_new = min(S + 8, self.E)\n+            try:\n+                self.set_S(S_new)\n+            except Exception:\n+                break\n+            S = S_new\n+        self.pending_regrow = False\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls == 1:\n+            logger.info(\"elastic: first call,",
 "ttft_seconds": 0.4591241419984726,
 "status": "ok",
 "tg_tokens_per_s": 29.35251140572161,
 "decode_seconds_per_token": 0.03406863508807197,
 "individual_token_timing": true,
 "event_intervals_ms": {
  "n": 511,
  "median": 33.89158500067424,
  "p95": 44.404448999557644,
  "mean": 34.06863508807197
 },
 "class": "code",
 "requested_input_tokens": 32768
}
