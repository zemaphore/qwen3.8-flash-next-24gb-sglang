{
 "started_utc": "2026-09-17T01:03:30.682338+00:00",
 "wall_seconds": 15.161866676004138,
 "prompt_tokens": 2048,
 "completion_tokens": 512,
 "stream_events": 512,
 "ended_utc": "2026-09-17T01:03:45.844287+00:00",
 "warmup": false,
 "sample_index": 3,
 "requested_decode_tokens": 512,
 "gpu": {
  "n": 54,
  "temperature_c": {
   "min": 74.0,
   "max": 76.0,
   "mean": 75.0
  },
  "power_w": {
   "min": 258.31,
   "max": 276.1,
   "mean": 264.98962962962963
  },
  "util_pct": {
   "min": 77.0,
   "max": 100.0,
   "mean": 89.46296296296296
  },
  "clock_sm_mhz": {
   "min": 1605.0,
   "max": 1920.0,
   "mean": 1893.0555555555557
  },
  "clock_mem_mhz": {
   "min": 9501.0,
   "max": 9501.0,
   "mean": 9501.0
  },
  "memory_free_mib": {
   "min": 2540.0
  }
 },
 "output_text_sha256": "7c75ce1eaa0cdaaa728c1857416501c33382fc8489a838ef01e262155e9c6a72",
 "output_text": ".py.py --.py --url.py --url http.py --url http://.py --url http://1.py --url http://12.py --url http://127.py --url http://127..py --url http://127.0.py --url http://127.0..py --url http://127.0.0.py --url http://127.0.0..py --url http://127.0.0.1.py --url http://127.0.0.1:.py --url http://127.0.0.1:3.py --url http://127.0.0.1:30.py --url http://127.0.0.1:300.py --url http://127.0.0.1:3000.py --url http://127.0.0.1:30000.py --url http://127.0.0.1:30000 --.py --url http://127.0.0.1:30000 --prompt.py --url http://127.0.0.1:30000 --prompt-t.py --url http://127.0.0.1:30000 --prompt-tokens.py --url http://127.0.0.1:30000 --prompt-tokens .py --url http://127.0.0.1:30000 --prompt-tokens 1.py --url http://127.0.0.1:30000 --prompt-tokens 10.py --url http://127.0.0.1:30000 --prompt-tokens 101.py --url http://127.0.0.1:30000 --prompt-tokens 101 .py --url http://127.0.0.1:30000 --prompt-tokens 101 4.py --url http://127.0.0.1:30000 --prompt-tokens 101 42.py --url http://127.0.0.1:30000 --prompt-tokens 101 421.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 17.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 170.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 68.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 682.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 100.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 1000.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-t.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 12.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers..py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches S.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGL.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`..py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`;.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 26.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,14.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAM.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPA.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:4.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:44.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448)..py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`),.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root..py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `S.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGL.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`V.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults;.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sg.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--ven.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them..py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n##.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsg.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/        .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (q.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-7.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a25.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a2552.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a25520.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch).py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Trit.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/       .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher),.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/         .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...).py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/          .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAM.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPA.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log),.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes, the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/        .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/         phase.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/         phase1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/         phase1_state.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/         phase1_state.json.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/         phase1_state.json (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/         phase1_state.json (the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/         phase1_state.json (the accepted.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/         phase1_state.json (the accepted state.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/         phase1_state.json (the accepted state),.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/         phase1_state.json (the accepted state), the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/         phase1_state.json (the accepted state), the accepted.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/         phase1_state.json (the accepted state), the accepted KV.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/         phase1_state.json (the accepted state), the accepted KV-cache.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/         phase1_state.json (the accepted state), the accepted KV-cache tier.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/         phase1_state.json (the accepted state), the accepted KV-cache tier table.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/         phase1_state.json (the accepted state), the accepted KV-cache tier table\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/         phase1_state.json (the accepted state), the accepted KV-cache tier table\nREADME.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/         phase1_state.json (the accepted state), the accepted KV-cache tier table\nREADME.md.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/         phase1_state.json (the accepted state), the accepted KV-cache tier table\nREADME.md      .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/         phase1_state.json (the accepted state), the accepted KV-cache tier table\nREADME.md       this.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/         phase1_state.json (the accepted state), the accepted KV-cache tier table\nREADME.md       this file.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/         phase1_state.json (the accepted state), the accepted KV-cache tier table\nREADME.md       this file\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/         phase1_state.json (the accepted state), the accepted KV-cache tier table\nREADME.md       this file\n```.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/         phase1_state.json (the accepted state), the accepted KV-cache tier table\nREADME.md       this file\n```\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/         phase1_state.json (the accepted state), the accepted KV-cache tier table\nREADME.md       this file\n```\n\n##.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/         phase1_state.json (the accepted state), the accepted KV-cache tier table\nREADME.md       this file\n```\n\n## What.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/         phase1_state.json (the accepted state), the accepted KV-cache tier table\nREADME.md       this file\n```\n\n## What is.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/         phase1_state.json (the accepted state), the accepted KV-cache tier table\nREADME.md       this file\n```\n\n## What is in.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/         phase1_state.json (the accepted state), the accepted KV-cache tier table\nREADME.md       this file\n```\n\n## What is in the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/         phase1_state.json (the accepted state), the accepted KV-cache tier table\nREADME.md       this file\n```\n\n## What is in the repository.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/         phase1_state.json (the accepted state), the accepted KV-cache tier table\nREADME.md       this file\n```\n\n## What is in the repository\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/         phase1_state.json (the accepted state), the accepted KV-cache tier table\nREADME.md       this file\n```\n\n## What is in the repository\n\n###.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/         phase1_state.json (the accepted state), the accepted KV-cache tier table\nREADME.md       this file\n```\n\n## What is in the repository\n\n### The.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/         phase1_state.json (the accepted state), the accepted KV-cache tier table\nREADME.md       this file\n```\n\n## What is in the repository\n\n### The serving.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/         phase1_state.json (the accepted state), the accepted KV-cache tier table\nREADME.md       this file\n```\n\n## What is in the repository\n\n### The serving patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/         phase1_state.json (the accepted state), the accepted KV-cache tier table\nREADME.md       this file\n```\n\n## What is in the repository\n\n### The serving patch and.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/         phase1_state.json (the accepted state), the accepted KV-cache tier table\nREADME.md       this file\n```\n\n## What is in the repository\n\n### The serving patch and the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/         phase1_state.json (the accepted state), the accepted KV-cache tier table\nREADME.md       this file\n```\n\n## What is in the repository\n\n### The serving patch and the patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/         phase1_state.json (the accepted state), the accepted KV-cache tier table\nREADME.md       this file\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/         phase1_state.json (the accepted state), the accepted KV-cache tier table\nREADME.md       this file\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/         phase1_state.json (the accepted state), the accepted KV-cache tier table\nREADME.md       this file\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/         phase1_state.json (the accepted state), the accepted KV-cache tier table\nREADME.md       this file\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sg.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/         phase1_state.json (the accepted state), the accepted KV-cache tier table\nREADME.md       this file\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/         phase1_state.json (the accepted state), the accepted KV-cache tier table\nREADME.md       this file\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang/q.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/         phase1_state.json (the accepted state), the accepted KV-cache tier table\nREADME.md       this file\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang/qwen.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/         phase1_state.json (the accepted state), the accepted KV-cache tier table\nREADME.md       this file\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang/qwen4.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/         phase1_state.json (the accepted state), the accepted KV-cache tier table\nREADME.md       this file\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang/qwen4exp.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/         phase1_state.json (the accepted state), the accepted KV-cache tier table\nREADME.md       this file\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang/qwen4exp-serving.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/         phase1_state.json (the accepted state), the accepted KV-cache tier table\nREADME.md       this file\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang/qwen4exp-serving-.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/         phase1_state.json (the accepted state), the accepted KV-cache tier table\nREADME.md       this file\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang/qwen4exp-serving-7.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/         phase1_state.json (the accepted state), the accepted KV-cache tier table\nREADME.md       this file\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang/qwen4exp-serving-73.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/         phase1_state.json (the accepted state), the accepted KV-cache tier table\nREADME.md       this file\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang/qwen4exp-serving-73a.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/         phase1_state.json (the accepted state), the accepted KV-cache tier table\nREADME.md       this file\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang/qwen4exp-serving-73a2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/         phase1_state.json (the accepted state), the accepted KV-cache tier table\nREADME.md       this file\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang/qwen4exp-serving-73a25.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/         phase1_state.json (the accepted state), the accepted KV-cache tier table\nREADME.md       this file\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang/qwen4exp-serving-73a255.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/         phase1_state.json (the accepted state), the accepted KV-cache tier table\nREADME.md       this file\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang/qwen4exp-serving-73a2552.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/         phase1_state.json (the accepted state), the accepted KV-cache tier table\nREADME.md       this file\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang/qwen4exp-serving-73a25520.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/         phase1_state.json (the accepted state), the accepted KV-cache tier table\nREADME.md       this file\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang/qwen4exp-serving-73a255206.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/         phase1_state.json (the accepted state), the accepted KV-cache tier table\nREADME.md       this file\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang/qwen4exp-serving-73a255206f.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/         phase1_state.json (the accepted state), the accepted KV-cache tier table\nREADME.md       this file\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang/qwen4exp-serving-73a255206f.patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/         phase1_state.json (the accepted state), the accepted KV-cache tier table\nREADME.md       this file\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang/qwen4exp-serving-73a255206f.patch`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/         phase1_state.json (the accepted state), the accepted KV-cache tier table\nREADME.md       this file\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang/qwen4exp-serving-73a255206f.patch` is.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/         phase1_state.json (the accepted state), the accepted KV-cache tier table\nREADME.md       this file\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/         phase1_state.json (the accepted state), the accepted KV-cache tier table\nREADME.md       this file\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the serving.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/         phase1_state.json (the accepted state), the accepted KV-cache tier table\nREADME.md       this file\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the serving patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/         phase1_state.json (the accepted state), the accepted KV-cache tier table\nREADME.md       this file\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the serving patch,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/         phase1_state.json (the accepted state), the accepted KV-cache tier table\nREADME.md       this file\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the serving patch, against.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/         phase1_state.json (the accepted state), the accepted KV-cache tier table\nREADME.md       this file\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the serving patch, against S.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/         phase1_state.json (the accepted state), the accepted KV-cache tier table\nREADME.md       this file\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the serving patch, against SGL.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/         phase1_state.json (the accepted state), the accepted KV-cache tier table\nREADME.md       this file\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the serving patch, against SGLang.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/         phase1_state.json (the accepted state), the accepted KV-cache tier table\nREADME.md       this file\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the serving patch, against SGLang at.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/         phase1_state.json (the accepted state), the accepted KV-cache tier table\nREADME.md       this file\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the serving patch, against SGLang at commit.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/         phase1_state.json (the accepted state), the accepted KV-cache tier table\nREADME.md       this file\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the serving patch, against SGLang at commit `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/         phase1_state.json (the accepted state), the accepted KV-cache tier table\nREADME.md       this file\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the serving patch, against SGLang at commit `7.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/         phase1_state.json (the accepted state), the accepted KV-cache tier table\nREADME.md       this file\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the serving patch, against SGLang at commit `73.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/         phase1_state.json (the accepted state), the accepted KV-cache tier table\nREADME.md       this file\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the serving patch, against SGLang at commit `73a.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/         phase1_state.json (the accepted state), the accepted KV-cache tier table\nREADME.md       this file\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the serving patch, against SGLang at commit `73a2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/         phase1_state.json (the accepted state), the accepted KV-cache tier table\nREADME.md       this file\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the serving patch, against SGLang at commit `73a25.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/         phase1_state.json (the accepted state), the accepted KV-cache tier table\nREADME.md       this file\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the serving patch, against SGLang at commit `73a255.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/         phase1_state.json (the accepted state), the accepted KV-cache tier table\nREADME.md       this file\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the serving patch, against SGLang at commit `73a2552.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/         phase1_state.json (the accepted state), the accepted KV-cache tier table\nREADME.md       this file\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the serving patch, against SGLang at commit `73a25520.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/         phase1_state.json (the accepted state), the accepted KV-cache tier table\nREADME.md       this file\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the serving patch, against SGLang at commit `73a255206.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/         phase1_state.json (the accepted state), the accepted KV-cache tier table\nREADME.md       this file\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the serving patch, against SGLang at commit `73a255206f.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/         phase1_state.json (the accepted state), the accepted KV-cache tier table\nREADME.md       this file\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the serving patch, against SGLang at commit `73a255206f`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/         phase1_state.json (the accepted state), the accepted KV-cache tier table\nREADME.md       this file\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the serving patch, against SGLang at commit `73a255206f`\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/         phase1_state.json (the accepted state), the accepted KV-cache tier table\nREADME.md       this file\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the serving patch, against SGLang at commit `73a255206f`\n(.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/         phase1_state.json (the accepted state), the accepted KV-cache tier table\nREADME.md       this file\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the serving patch, against SGLang at commit `73a255206f`\n(2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/         phase1_state.json (the accepted state), the accepted KV-cache tier table\nREADME.md       this file\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the serving patch, against SGLang at commit `73a255206f`\n(20.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/         phase1_state.json (the accepted state), the accepted KV-cache tier table\nREADME.md       this file\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the serving patch, against SGLang at commit `73a255206f`\n(202.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/         phase1_state.json (the accepted state), the accepted KV-cache tier table\nREADME.md       this file\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the serving patch, against SGLang at commit `73a255206f`\n(2026.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/         phase1_state.json (the accepted state), the accepted KV-cache tier table\nREADME.md       this file\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the serving patch, against SGLang at commit `73a255206f`\n(2026-.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/         phase1_state.json (the accepted state), the accepted KV-cache tier table\nREADME.md       this file\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the serving patch, against SGLang at commit `73a255206f`\n(2026-0.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/         phase1_state.json (the accepted state), the accepted KV-cache tier table\nREADME.md       this file\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the serving patch, against SGLang at commit `73a255206f`\n(2026-07.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/         phase1_state.json (the accepted state), the accepted KV-cache tier table\nREADME.md       this file\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the serving patch, against SGLang at commit `73a255206f`\n(2026-07-.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/         phase1_state.json (the accepted state), the accepted KV-cache tier table\nREADME.md       this file\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the serving patch, against SGLang at commit `73a255206f`\n(2026-07-2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/         phase1_state.json (the accepted state), the accepted KV-cache tier table\nREADME.md       this file\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the serving patch, against SGLang at commit `73a255206f`\n(2026-07-29.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/         phase1_state.json (the accepted state), the accepted KV-cache tier table\nREADME.md       this file\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the serving patch, against SGLang at commit `73a255206f`\n(2026-07-29,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/         phase1_state.json (the accepted state), the accepted KV-cache tier table\nREADME.md       this file\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the serving patch, against SGLang at commit `73a255206f`\n(2026-07-29, the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/         phase1_state.json (the accepted state), the accepted KV-cache tier table\nREADME.md       this file\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the serving patch, against SGLang at commit `73a255206f`\n(2026-07-29, the commit.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/         phase1_state.json (the accepted state), the accepted KV-cache tier table\nREADME.md       this file\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the serving patch, against SGLang at commit `73a255206f`\n(2026-07-29, the commit the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/         phase1_state.json (the accepted state), the accepted KV-cache tier table\nREADME.md       this file\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the serving patch, against SGLang at commit `73a255206f`\n(2026-07-29, the commit the stack.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/         phase1_state.json (the accepted state), the accepted KV-cache tier table\nREADME.md       this file\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the serving patch, against SGLang at commit `73a255206f`\n(2026-07-29, the commit the stack was.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/         phase1_state.json (the accepted state), the accepted KV-cache tier table\nREADME.md       this file\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the serving patch, against SGLang at commit `73a255206f`\n(2026-07-29, the commit the stack was measured.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/         phase1_state.json (the accepted state), the accepted KV-cache tier table\nREADME.md       this file\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the serving patch, against SGLang at commit `73a255206f`\n(2026-07-29, the commit the stack was measured on.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/         phase1_state.json (the accepted state), the accepted KV-cache tier table\nREADME.md       this file\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the serving patch, against SGLang at commit `73a255206f`\n(2026-07-29, the commit the stack was measured on)..py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/         phase1_state.json (the accepted state), the accepted KV-cache tier table\nREADME.md       this file\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the serving patch, against SGLang at commit `73a255206f`\n(2026-07-29, the commit the stack was measured on). It.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/         phase1_state.json (the accepted state), the accepted KV-cache tier table\nREADME.md       this file\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the serving patch, against SGLang at commit `73a255206f`\n(2026-07-29, the commit the stack was measured on). It is.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/         phase1_state.json (the accepted state), the accepted KV-cache tier table\nREADME.md       this file\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the serving patch, against SGLang at commit `73a255206f`\n(2026-07-29, the commit the stack was measured on). It is the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/         phase1_state.json (the accepted state), the accepted KV-cache tier table\nREADME.md       this file\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the serving patch, against SGLang at commit `73a255206f`\n(2026-07-29, the commit the stack was measured on). It is the squ.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/         phase1_state.json (the accepted state), the accepted KV-cache tier table\nREADME.md       this file\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the serving patch, against SGLang at commit `73a255206f`\n(2026-07-29, the commit the stack was measured on). It is the squashed.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/         phase1_state.json (the accepted state), the accepted KV-cache tier table\nREADME.md       this file\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the serving patch, against SGLang at commit `73a255206f`\n(2026-07-29, the commit the stack was measured on). It is the squashed result.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/         phase1_state.json (the accepted state), the accepted KV-cache tier table\nREADME.md       this file\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the serving patch, against SGLang at commit `73a255206f`\n(2026-07-29, the commit the stack was measured on). It is the squashed result of.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/         phase1_state.json (the accepted state), the accepted KV-cache tier table\nREADME.md       this file\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the serving patch, against SGLang at commit `73a255206f`\n(2026-07-29, the commit the stack was measured on). It is the squashed result of the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/         phase1_state.json (the accepted state), the accepted KV-cache tier table\nREADME.md       this file\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the serving patch, against SGLang at commit `73a255206f`\n(2026-07-29, the commit the stack was measured on). It is the squashed result of the patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/         phase1_state.json (the accepted state), the accepted KV-cache tier table\nREADME.md       this file\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the serving patch, against SGLang at commit `73a255206f`\n(2026-07-29, the commit the stack was measured on). It is the squashed result of the patch scripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/         phase1_state.json (the accepted state), the accepted KV-cache tier table\nREADME.md       this file\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the serving patch, against SGLang at commit `73a255206f`\n(2026-07-29, the commit the stack was measured on). It is the squashed result of the patch scripts in.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/         phase1_state.json (the accepted state), the accepted KV-cache tier table\nREADME.md       this file\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the serving patch, against SGLang at commit `73a255206f`\n(2026-07-29, the commit the stack was measured on). It is the squashed result of the patch scripts in\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/         phase1_state.json (the accepted state), the accepted KV-cache tier table\nREADME.md       this file\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the serving patch, against SGLang at commit `73a255206f`\n(2026-07-29, the commit the stack was measured on). It is the squashed result of the patch scripts in\n`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/         phase1_state.json (the accepted state), the accepted KV-cache tier table\nREADME.md       this file\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the serving patch, against SGLang at commit `73a255206f`\n(2026-07-29, the commit the stack was measured on). It is the squashed result of the patch scripts in\n`patches.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/         phase1_state.json (the accepted state), the accepted KV-cache tier table\nREADME.md       this file\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the serving patch, against SGLang at commit `73a255206f`\n(2026-07-29, the commit the stack was measured on). It is the squashed result of the patch scripts in\n`patches/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/         phase1_state.json (the accepted state), the accepted KV-cache tier table\nREADME.md       this file\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the serving patch, against SGLang at commit `73a255206f`\n(2026-07-29, the commit the stack was measured on). It is the squashed result of the patch scripts in\n`patches/`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/         phase1_state.json (the accepted state), the accepted KV-cache tier table\nREADME.md       this file\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the serving patch, against SGLang at commit `73a255206f`\n(2026-07-29, the commit the stack was measured on). It is the squashed result of the patch scripts in\n`patches/` (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/         phase1_state.json (the accepted state), the accepted KV-cache tier table\nREADME.md       this file\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the serving patch, against SGLang at commit `73a255206f`\n(2026-07-29, the commit the stack was measured on). It is the squashed result of the patch scripts in\n`patches/` (the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/         phase1_state.json (the accepted state), the accepted KV-cache tier table\nREADME.md       this file\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the serving patch, against SGLang at commit `73a255206f`\n(2026-07-29, the commit the stack was measured on). It is the squashed result of the patch scripts in\n`patches/` (the same.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/         phase1_state.json (the accepted state), the accepted KV-cache tier table\nREADME.md       this file\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the serving patch, against SGLang at commit `73a255206f`\n(2026-07-29, the commit the stack was measured on). It is the squashed result of the patch scripts in\n`patches/` (the same files.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/         phase1_state.json (the accepted state), the accepted KV-cache tier table\nREADME.md       this file\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the serving patch, against SGLang at commit `73a255206f`\n(2026-07-29, the commit the stack was measured on). It is the squashed result of the patch scripts in\n`patches/` (the same files,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/         phase1_state.json (the accepted state), the accepted KV-cache tier table\nREADME.md       this file\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the serving patch, against SGLang at commit `73a255206f`\n(2026-07-29, the commit the stack was measured on). It is the squashed result of the patch scripts in\n`patches/` (the same files, in.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/         phase1_state.json (the accepted state), the accepted KV-cache tier table\nREADME.md       this file\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the serving patch, against SGLang at commit `73a255206f`\n(2026-07-29, the commit the stack was measured on). It is the squashed result of the patch scripts in\n`patches/` (the same files, in the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/         phase1_state.json (the accepted state), the accepted KV-cache tier table\nREADME.md       this file\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the serving patch, against SGLang at commit `73a255206f`\n(2026-07-29, the commit the stack was measured on). It is the squashed result of the patch scripts in\n`patches/` (the same files, in the order.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/         phase1_state.json (the accepted state), the accepted KV-cache tier table\nREADME.md       this file\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the serving patch, against SGLang at commit `73a255206f`\n(2026-07-29, the commit the stack was measured on). It is the squashed result of the patch scripts in\n`patches/` (the same files, in the order `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/         phase1_state.json (the accepted state), the accepted KV-cache tier table\nREADME.md       this file\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the serving patch, against SGLang at commit `73a255206f`\n(2026-07-29, the commit the stack was measured on). It is the squashed result of the patch scripts in\n`patches/` (the same files, in the order `scripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/         phase1_state.json (the accepted state), the accepted KV-cache tier table\nREADME.md       this file\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the serving patch, against SGLang at commit `73a255206f`\n(2026-07-29, the commit the stack was measured on). It is the squashed result of the patch scripts in\n`patches/` (the same files, in the order `scripts/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/         phase1_state.json (the accepted state), the accepted KV-cache tier table\nREADME.md       this file\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the serving patch, against SGLang at commit `73a255206f`\n(2026-07-29, the commit the stack was measured on). It is the squashed result of the patch scripts in\n`patches/` (the same files, in the order `scripts/serve.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/         phase1_state.json (the accepted state), the accepted KV-cache tier table\nREADME.md       this file\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the serving patch, against SGLang at commit `73a255206f`\n(2026-07-29, the commit the stack was measured on). It is the squashed result of the patch scripts in\n`patches/` (the same files, in the order `scripts/serve.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/         phase1_state.json (the accepted state), the accepted KV-cache tier table\nREADME.md       this file\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the serving patch, against SGLang at commit `73a255206f`\n(2026-07-29, the commit the stack was measured on). It is the squashed result of the patch scripts in\n`patches/` (the same files, in the order `scripts/serve.sh`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/         phase1_state.json (the accepted state), the accepted KV-cache tier table\nREADME.md       this file\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the serving patch, against SGLang at commit `73a255206f`\n(2026-07-29, the commit the stack was measured on). It is the squashed result of the patch scripts in\n`patches/` (the same files, in the order `scripts/serve.sh` applies.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/         phase1_state.json (the accepted state), the accepted KV-cache tier table\nREADME.md       this file\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the serving patch, against SGLang at commit `73a255206f`\n(2026-07-29, the commit the stack was measured on). It is the squashed result of the patch scripts in\n`patches/` (the same files, in the order `scripts/serve.sh` applies them.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/         phase1_state.json (the accepted state), the accepted KV-cache tier table\nREADME.md       this file\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the serving patch, against SGLang at commit `73a255206f`\n(2026-07-29, the commit the stack was measured on). It is the squashed result of the patch scripts in\n`patches/` (the same files, in the order `scripts/serve.sh` applies them),.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/         phase1_state.json (the accepted state), the accepted KV-cache tier table\nREADME.md       this file\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the serving patch, against SGLang at commit `73a255206f`\n(2026-07-29, the commit the stack was measured on). It is the squashed result of the patch scripts in\n`patches/` (the same files, in the order `scripts/serve.sh` applies them), plus.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/         phase1_state.json (the accepted state), the accepted KV-cache tier table\nREADME.md       this file\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the serving patch, against SGLang at commit `73a255206f`\n(2026-07-29, the commit the stack was measured on). It is the squashed result of the patch scripts in\n`patches/` (the same files, in the order `scripts/serve.sh` applies them), plus the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/         phase1_state.json (the accepted state), the accepted KV-cache tier table\nREADME.md       this file\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the serving patch, against SGLang at commit `73a255206f`\n(2026-07-29, the commit the stack was measured on). It is the squashed result of the patch scripts in\n`patches/` (the same files, in the order `scripts/serve.sh` applies them), plus the two.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/         phase1_state.json (the accepted state), the accepted KV-cache tier table\nREADME.md       this file\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the serving patch, against SGLang at commit `73a255206f`\n(2026-07-29, the commit the stack was measured on). It is the squashed result of the patch scripts in\n`patches/` (the same files, in the order `scripts/serve.sh` applies them), plus the two files.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/         phase1_state.json (the accepted state), the accepted KV-cache tier table\nREADME.md       this file\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the serving patch, against SGLang at commit `73a255206f`\n(2026-07-29, the commit the stack was measured on). It is the squashed result of the patch scripts in\n`patches/` (the same files, in the order `scripts/serve.sh` applies them), plus the two files that.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/         phase1_state.json (the accepted state), the accepted KV-cache tier table\nREADME.md       this file\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the serving patch, against SGLang at commit `73a255206f`\n(2026-07-29, the commit the stack was measured on). It is the squashed result of the patch scripts in\n`patches/` (the same files, in the order `scripts/serve.sh` applies them), plus the two files that are.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/         phase1_state.json (the accepted state), the accepted KV-cache tier table\nREADME.md       this file\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the serving patch, against SGLang at commit `73a255206f`\n(2026-07-29, the commit the stack was measured on). It is the squashed result of the patch scripts in\n`patches/` (the same files, in the order `scripts/serve.sh` applies them), plus the two files that are not.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/         phase1_state.json (the accepted state), the accepted KV-cache tier table\nREADME.md       this file\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the serving patch, against SGLang at commit `73a255206f`\n(2026-07-29, the commit the stack was measured on). It is the squashed result of the patch scripts in\n`patches/` (the same files, in the order `scripts/serve.sh` applies them), plus the two files that are not\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools (bench.py, nll.py, ...)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes, the measurement logs\nassets/         phase1_state.json (the accepted state), the accepted KV-cache tier table\nREADME.md       this file\n```\n\n## What is in the repository\n\n### The serving patch and the patch scripts\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the serving patch, against SGLang at commit `73a255206f`\n(2026-07-29, the commit the stack was measured on). It is the squashed result of the patch scripts in\n`patches/` (the same files, in the order `scripts/serve.sh` applies them), plus the two files that are not\nin",
 "ttft_seconds": 1.1595152520021657,
 "status": "ok",
 "tg_tokens_per_s": 36.494054240104354,
 "decode_seconds_per_token": 0.027401723947159357,
 "individual_token_timing": true,
 "event_intervals_ms": {
  "n": 511,
  "median": 27.084310000645928,
  "p95": 34.10474800330121,
  "mean": 27.401723947159358
 },
 "class": "prose",
 "requested_input_tokens": 2048
}
