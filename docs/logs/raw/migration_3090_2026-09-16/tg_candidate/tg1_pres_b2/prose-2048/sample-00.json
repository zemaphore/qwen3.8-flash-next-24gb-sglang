{
 "started_utc": "2026-09-17T01:02:44.966319+00:00",
 "wall_seconds": 16.479587547000847,
 "prompt_tokens": 2048,
 "completion_tokens": 512,
 "stream_events": 512,
 "ended_utc": "2026-09-17T01:03:01.446112+00:00",
 "warmup": true,
 "sample_index": 0,
 "requested_decode_tokens": 512,
 "gpu": {
  "n": 59,
  "temperature_c": {
   "min": 64.0,
   "max": 73.0,
   "mean": 70.69491525423729
  },
  "power_w": {
   "min": 120.08,
   "max": 267.75,
   "mean": 246.5028813559322
  },
  "util_pct": {
   "min": 0.0,
   "max": 96.0,
   "mean": 85.52542372881356
  },
  "clock_sm_mhz": {
   "min": 1875.0,
   "max": 1935.0,
   "mean": 1929.915254237288
  },
  "clock_mem_mhz": {
   "min": 9501.0,
   "max": 9501.0,
   "mean": 9501.0
  },
  "memory_free_mib": {
   "min": 2540.0
  }
 },
 "output_text_sha256": "6f0e2badd855ca927c3c46095082d6f85d2b82ca996b400461b90d8dac1dcd16",
 "output_text": ".py.py --.py --url.py --url http.py --url http://.py --url http://1.py --url http://12.py --url http://127.py --url http://127..py --url http://127.0.py --url http://127.0..py --url http://127.0.0.py --url http://127.0.0..py --url http://127.0.0.1.py --url http://127.0.0.1:.py --url http://127.0.0.1:3.py --url http://127.0.0.1:30.py --url http://127.0.0.1:300.py --url http://127.0.0.1:3000.py --url http://127.0.0.1:30000.py --url http://127.0.0.1:30000 --.py --url http://127.0.0.1:30000 --prompt.py --url http://127.0.0.1:30000 --prompt-t.py --url http://127.0.0.1:30000 --prompt-tokens.py --url http://127.0.0.1:30000 --prompt-tokens .py --url http://127.0.0.1:30000 --prompt-tokens 1.py --url http://127.0.0.1:30000 --prompt-tokens 10.py --url http://127.0.0.1:30000 --prompt-tokens 101.py --url http://127.0.0.1:30000 --prompt-tokens 101 .py --url http://127.0.0.1:30000 --prompt-tokens 101 4.py --url http://127.0.0.1:30000 --prompt-tokens 101 42.py --url http://127.0.0.1:30000 --prompt-tokens 101 421.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 17.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 170.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 68.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 682.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 100.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 1000.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-t.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 12.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers..py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches S.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGL.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`..py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`;.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 26.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,14.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAM.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPA.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:4.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:44.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448)..py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe P.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`);.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`..py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n##.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsg.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/        .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against S.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGL.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 7.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a25.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a2552.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a25520.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f).py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/       .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path).py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nk.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/       .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Trit.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_12.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/        .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets:.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the P.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/         .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/          .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/       .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n##.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sg.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/q.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-7.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a25.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a2552.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a25520.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single git.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single git patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single git patch that.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single git patch that carries.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single git patch that carries the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single git patch that carries the serving.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single git patch that carries the serving stack.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single git patch that carries the serving stack:.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single git patch that carries the serving stack: it.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single git patch that carries the serving stack: it applies.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single git patch that carries the serving stack: it applies\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single git patch that carries the serving stack: it applies\nagainst.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single git patch that carries the serving stack: it applies\nagainst S.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single git patch that carries the serving stack: it applies\nagainst SGL.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single git patch that carries the serving stack: it applies\nagainst SGLang.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single git patch that carries the serving stack: it applies\nagainst SGLang at.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single git patch that carries the serving stack: it applies\nagainst SGLang at commit.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single git patch that carries the serving stack: it applies\nagainst SGLang at commit `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single git patch that carries the serving stack: it applies\nagainst SGLang at commit `7.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single git patch that carries the serving stack: it applies\nagainst SGLang at commit `73.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single git patch that carries the serving stack: it applies\nagainst SGLang at commit `73a.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single git patch that carries the serving stack: it applies\nagainst SGLang at commit `73a2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single git patch that carries the serving stack: it applies\nagainst SGLang at commit `73a25.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single git patch that carries the serving stack: it applies\nagainst SGLang at commit `73a255.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single git patch that carries the serving stack: it applies\nagainst SGLang at commit `73a2552.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single git patch that carries the serving stack: it applies\nagainst SGLang at commit `73a25520.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single git patch that carries the serving stack: it applies\nagainst SGLang at commit `73a255206.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single git patch that carries the serving stack: it applies\nagainst SGLang at commit `73a255206f.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single git patch that carries the serving stack: it applies\nagainst SGLang at commit `73a255206f`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single git patch that carries the serving stack: it applies\nagainst SGLang at commit `73a255206f` and.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single git patch that carries the serving stack: it applies\nagainst SGLang at commit `73a255206f` and is.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single git patch that carries the serving stack: it applies\nagainst SGLang at commit `73a255206f` and is the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single git patch that carries the serving stack: it applies\nagainst SGLang at commit `73a255206f` and is the artifact.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single git patch that carries the serving stack: it applies\nagainst SGLang at commit `73a255206f` and is the artifact that.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single git patch that carries the serving stack: it applies\nagainst SGLang at commit `73a255206f` and is the artifact that `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single git patch that carries the serving stack: it applies\nagainst SGLang at commit `73a255206f` and is the artifact that `scripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single git patch that carries the serving stack: it applies\nagainst SGLang at commit `73a255206f` and is the artifact that `scripts/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single git patch that carries the serving stack: it applies\nagainst SGLang at commit `73a255206f` and is the artifact that `scripts/serve.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single git patch that carries the serving stack: it applies\nagainst SGLang at commit `73a255206f` and is the artifact that `scripts/serve.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single git patch that carries the serving stack: it applies\nagainst SGLang at commit `73a255206f` and is the artifact that `scripts/serve.sh`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single git patch that carries the serving stack: it applies\nagainst SGLang at commit `73a255206f` and is the artifact that `scripts/serve.sh` applies.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single git patch that carries the serving stack: it applies\nagainst SGLang at commit `73a255206f` and is the artifact that `scripts/serve.sh` applies..py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single git patch that carries the serving stack: it applies\nagainst SGLang at commit `73a255206f` and is the artifact that `scripts/serve.sh` applies. It.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single git patch that carries the serving stack: it applies\nagainst SGLang at commit `73a255206f` and is the artifact that `scripts/serve.sh` applies. It is.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single git patch that carries the serving stack: it applies\nagainst SGLang at commit `73a255206f` and is the artifact that `scripts/serve.sh` applies. It is the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single git patch that carries the serving stack: it applies\nagainst SGLang at commit `73a255206f` and is the artifact that `scripts/serve.sh` applies. It is the\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single git patch that carries the serving stack: it applies\nagainst SGLang at commit `73a255206f` and is the artifact that `scripts/serve.sh` applies. It is the\ncons.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single git patch that carries the serving stack: it applies\nagainst SGLang at commit `73a255206f` and is the artifact that `scripts/serve.sh` applies. It is the\nconsolid.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single git patch that carries the serving stack: it applies\nagainst SGLang at commit `73a255206f` and is the artifact that `scripts/serve.sh` applies. It is the\nconsolidated.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single git patch that carries the serving stack: it applies\nagainst SGLang at commit `73a255206f` and is the artifact that `scripts/serve.sh` applies. It is the\nconsolidated form.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single git patch that carries the serving stack: it applies\nagainst SGLang at commit `73a255206f` and is the artifact that `scripts/serve.sh` applies. It is the\nconsolidated form of.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single git patch that carries the serving stack: it applies\nagainst SGLang at commit `73a255206f` and is the artifact that `scripts/serve.sh` applies. It is the\nconsolidated form of the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single git patch that carries the serving stack: it applies\nagainst SGLang at commit `73a255206f` and is the artifact that `scripts/serve.sh` applies. It is the\nconsolidated form of the layered.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single git patch that carries the serving stack: it applies\nagainst SGLang at commit `73a255206f` and is the artifact that `scripts/serve.sh` applies. It is the\nconsolidated form of the layered patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single git patch that carries the serving stack: it applies\nagainst SGLang at commit `73a255206f` and is the artifact that `scripts/serve.sh` applies. It is the\nconsolidated form of the layered patch scripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single git patch that carries the serving stack: it applies\nagainst SGLang at commit `73a255206f` and is the artifact that `scripts/serve.sh` applies. It is the\nconsolidated form of the layered patch scripts in.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single git patch that carries the serving stack: it applies\nagainst SGLang at commit `73a255206f` and is the artifact that `scripts/serve.sh` applies. It is the\nconsolidated form of the layered patch scripts in `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single git patch that carries the serving stack: it applies\nagainst SGLang at commit `73a255206f` and is the artifact that `scripts/serve.sh` applies. It is the\nconsolidated form of the layered patch scripts in `patches.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single git patch that carries the serving stack: it applies\nagainst SGLang at commit `73a255206f` and is the artifact that `scripts/serve.sh` applies. It is the\nconsolidated form of the layered patch scripts in `patches/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single git patch that carries the serving stack: it applies\nagainst SGLang at commit `73a255206f` and is the artifact that `scripts/serve.sh` applies. It is the\nconsolidated form of the layered patch scripts in `patches/`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single git patch that carries the serving stack: it applies\nagainst SGLang at commit `73a255206f` and is the artifact that `scripts/serve.sh` applies. It is the\nconsolidated form of the layered patch scripts in `patches/` (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single git patch that carries the serving stack: it applies\nagainst SGLang at commit `73a255206f` and is the artifact that `scripts/serve.sh` applies. It is the\nconsolidated form of the layered patch scripts in `patches/` (the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single git patch that carries the serving stack: it applies\nagainst SGLang at commit `73a255206f` and is the artifact that `scripts/serve.sh` applies. It is the\nconsolidated form of the layered patch scripts in `patches/` (the build.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single git patch that carries the serving stack: it applies\nagainst SGLang at commit `73a255206f` and is the artifact that `scripts/serve.sh` applies. It is the\nconsolidated form of the layered patch scripts in `patches/` (the build path.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single git patch that carries the serving stack: it applies\nagainst SGLang at commit `73a255206f` and is the artifact that `scripts/serve.sh` applies. It is the\nconsolidated form of the layered patch scripts in `patches/` (the build path),.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single git patch that carries the serving stack: it applies\nagainst SGLang at commit `73a255206f` and is the artifact that `scripts/serve.sh` applies. It is the\nconsolidated form of the layered patch scripts in `patches/` (the build path), which.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single git patch that carries the serving stack: it applies\nagainst SGLang at commit `73a255206f` and is the artifact that `scripts/serve.sh` applies. It is the\nconsolidated form of the layered patch scripts in `patches/` (the build path), which were.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single git patch that carries the serving stack: it applies\nagainst SGLang at commit `73a255206f` and is the artifact that `scripts/serve.sh` applies. It is the\nconsolidated form of the layered patch scripts in `patches/` (the build path), which were applied.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single git patch that carries the serving stack: it applies\nagainst SGLang at commit `73a255206f` and is the artifact that `scripts/serve.sh` applies. It is the\nconsolidated form of the layered patch scripts in `patches/` (the build path), which were applied in.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single git patch that carries the serving stack: it applies\nagainst SGLang at commit `73a255206f` and is the artifact that `scripts/serve.sh` applies. It is the\nconsolidated form of the layered patch scripts in `patches/` (the build path), which were applied in order.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single git patch that carries the serving stack: it applies\nagainst SGLang at commit `73a255206f` and is the artifact that `scripts/serve.sh` applies. It is the\nconsolidated form of the layered patch scripts in `patches/` (the build path), which were applied in order\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single git patch that carries the serving stack: it applies\nagainst SGLang at commit `73a255206f` and is the artifact that `scripts/serve.sh` applies. It is the\nconsolidated form of the layered patch scripts in `patches/` (the build path), which were applied in order\nduring.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single git patch that carries the serving stack: it applies\nagainst SGLang at commit `73a255206f` and is the artifact that `scripts/serve.sh` applies. It is the\nconsolidated form of the layered patch scripts in `patches/` (the build path), which were applied in order\nduring development.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single git patch that carries the serving stack: it applies\nagainst SGLang at commit `73a255206f` and is the artifact that `scripts/serve.sh` applies. It is the\nconsolidated form of the layered patch scripts in `patches/` (the build path), which were applied in order\nduring development and.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single git patch that carries the serving stack: it applies\nagainst SGLang at commit `73a255206f` and is the artifact that `scripts/serve.sh` applies. It is the\nconsolidated form of the layered patch scripts in `patches/` (the build path), which were applied in order\nduring development and are.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single git patch that carries the serving stack: it applies\nagainst SGLang at commit `73a255206f` and is the artifact that `scripts/serve.sh` applies. It is the\nconsolidated form of the layered patch scripts in `patches/` (the build path), which were applied in order\nduring development and are kept.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single git patch that carries the serving stack: it applies\nagainst SGLang at commit `73a255206f` and is the artifact that `scripts/serve.sh` applies. It is the\nconsolidated form of the layered patch scripts in `patches/` (the build path), which were applied in order\nduring development and are kept here.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single git patch that carries the serving stack: it applies\nagainst SGLang at commit `73a255206f` and is the artifact that `scripts/serve.sh` applies. It is the\nconsolidated form of the layered patch scripts in `patches/` (the build path), which were applied in order\nduring development and are kept here as.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single git patch that carries the serving stack: it applies\nagainst SGLang at commit `73a255206f` and is the artifact that `scripts/serve.sh` applies. It is the\nconsolidated form of the layered patch scripts in `patches/` (the build path), which were applied in order\nduring development and are kept here as the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single git patch that carries the serving stack: it applies\nagainst SGLang at commit `73a255206f` and is the artifact that `scripts/serve.sh` applies. It is the\nconsolidated form of the layered patch scripts in `patches/` (the build path), which were applied in order\nduring development and are kept here as the record.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single git patch that carries the serving stack: it applies\nagainst SGLang at commit `73a255206f` and is the artifact that `scripts/serve.sh` applies. It is the\nconsolidated form of the layered patch scripts in `patches/` (the build path), which were applied in order\nduring development and are kept here as the record of.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single git patch that carries the serving stack: it applies\nagainst SGLang at commit `73a255206f` and is the artifact that `scripts/serve.sh` applies. It is the\nconsolidated form of the layered patch scripts in `patches/` (the build path), which were applied in order\nduring development and are kept here as the record of how.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single git patch that carries the serving stack: it applies\nagainst SGLang at commit `73a255206f` and is the artifact that `scripts/serve.sh` applies. It is the\nconsolidated form of the layered patch scripts in `patches/` (the build path), which were applied in order\nduring development and are kept here as the record of how the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single git patch that carries the serving stack: it applies\nagainst SGLang at commit `73a255206f` and is the artifact that `scripts/serve.sh` applies. It is the\nconsolidated form of the layered patch scripts in `patches/` (the build path), which were applied in order\nduring development and are kept here as the record of how the serving.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single git patch that carries the serving stack: it applies\nagainst SGLang at commit `73a255206f` and is the artifact that `scripts/serve.sh` applies. It is the\nconsolidated form of the layered patch scripts in `patches/` (the build path), which were applied in order\nduring development and are kept here as the record of how the serving patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single git patch that carries the serving stack: it applies\nagainst SGLang at commit `73a255206f` and is the artifact that `scripts/serve.sh` applies. It is the\nconsolidated form of the layered patch scripts in `patches/` (the build path), which were applied in order\nduring development and are kept here as the record of how the serving patch was.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single git patch that carries the serving stack: it applies\nagainst SGLang at commit `73a255206f` and is the artifact that `scripts/serve.sh` applies. It is the\nconsolidated form of the layered patch scripts in `patches/` (the build path), which were applied in order\nduring development and are kept here as the record of how the serving patch was assembled.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single git patch that carries the serving stack: it applies\nagainst SGLang at commit `73a255206f` and is the artifact that `scripts/serve.sh` applies. It is the\nconsolidated form of the layered patch scripts in `patches/` (the build path), which were applied in order\nduring development and are kept here as the record of how the serving patch was assembled..py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single git patch that carries the serving stack: it applies\nagainst SGLang at commit `73a255206f` and is the artifact that `scripts/serve.sh` applies. It is the\nconsolidated form of the layered patch scripts in `patches/` (the build path), which were applied in order\nduring development and are kept here as the record of how the serving patch was assembled. The.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single git patch that carries the serving stack: it applies\nagainst SGLang at commit `73a255206f` and is the artifact that `scripts/serve.sh` applies. It is the\nconsolidated form of the layered patch scripts in `patches/` (the build path), which were applied in order\nduring development and are kept here as the record of how the serving patch was assembled. The patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single git patch that carries the serving stack: it applies\nagainst SGLang at commit `73a255206f` and is the artifact that `scripts/serve.sh` applies. It is the\nconsolidated form of the layered patch scripts in `patches/` (the build path), which were applied in order\nduring development and are kept here as the record of how the serving patch was assembled. The patch touches.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single git patch that carries the serving stack: it applies\nagainst SGLang at commit `73a255206f` and is the artifact that `scripts/serve.sh` applies. It is the\nconsolidated form of the layered patch scripts in `patches/` (the build path), which were applied in order\nduring development and are kept here as the record of how the serving patch was assembled. The patch touches\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single git patch that carries the serving stack: it applies\nagainst SGLang at commit `73a255206f` and is the artifact that `scripts/serve.sh` applies. It is the\nconsolidated form of the layered patch scripts in `patches/` (the build path), which were applied in order\nduring development and are kept here as the record of how the serving patch was assembled. The patch touches\nthe.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single git patch that carries the serving stack: it applies\nagainst SGLang at commit `73a255206f` and is the artifact that `scripts/serve.sh` applies. It is the\nconsolidated form of the layered patch scripts in `patches/` (the build path), which were applied in order\nduring development and are kept here as the record of how the serving patch was assembled. The patch touches\nthe model.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single git patch that carries the serving stack: it applies\nagainst SGLang at commit `73a255206f` and is the artifact that `scripts/serve.sh` applies. It is the\nconsolidated form of the layered patch scripts in `patches/` (the build path), which were applied in order\nduring development and are kept here as the record of how the serving patch was assembled. The patch touches\nthe model runner.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single git patch that carries the serving stack: it applies\nagainst SGLang at commit `73a255206f` and is the artifact that `scripts/serve.sh` applies. It is the\nconsolidated form of the layered patch scripts in `patches/` (the build path), which were applied in order\nduring development and are kept here as the record of how the serving patch was assembled. The patch touches\nthe model runner,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single git patch that carries the serving stack: it applies\nagainst SGLang at commit `73a255206f` and is the artifact that `scripts/serve.sh` applies. It is the\nconsolidated form of the layered patch scripts in `patches/` (the build path), which were applied in order\nduring development and are kept here as the record of how the serving patch was assembled. The patch touches\nthe model runner, the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single git patch that carries the serving stack: it applies\nagainst SGLang at commit `73a255206f` and is the artifact that `scripts/serve.sh` applies. It is the\nconsolidated form of the layered patch scripts in `patches/` (the build path), which were applied in order\nduring development and are kept here as the record of how the serving patch was assembled. The patch touches\nthe model runner, the Mo.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single git patch that carries the serving stack: it applies\nagainst SGLang at commit `73a255206f` and is the artifact that `scripts/serve.sh` applies. It is the\nconsolidated form of the layered patch scripts in `patches/` (the build path), which were applied in order\nduring development and are kept here as the record of how the serving patch was assembled. The patch touches\nthe model runner, the MoE.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single git patch that carries the serving stack: it applies\nagainst SGLang at commit `73a255206f` and is the artifact that `scripts/serve.sh` applies. It is the\nconsolidated form of the layered patch scripts in `patches/` (the build path), which were applied in order\nduring development and are kept here as the record of how the serving patch was assembled. The patch touches\nthe model runner, the MoE layers.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single git patch that carries the serving stack: it applies\nagainst SGLang at commit `73a255206f` and is the artifact that `scripts/serve.sh` applies. It is the\nconsolidated form of the layered patch scripts in `patches/` (the build path), which were applied in order\nduring development and are kept here as the record of how the serving patch was assembled. The patch touches\nthe model runner, the MoE layers,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single git patch that carries the serving stack: it applies\nagainst SGLang at commit `73a255206f` and is the artifact that `scripts/serve.sh` applies. It is the\nconsolidated form of the layered patch scripts in `patches/` (the build path), which were applied in order\nduring development and are kept here as the record of how the serving patch was assembled. The patch touches\nthe model runner, the MoE layers, the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single git patch that carries the serving stack: it applies\nagainst SGLang at commit `73a255206f` and is the artifact that `scripts/serve.sh` applies. It is the\nconsolidated form of the layered patch scripts in `patches/` (the build path), which were applied in order\nduring development and are kept here as the record of how the serving patch was assembled. The patch touches\nthe model runner, the MoE layers, the attention.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single git patch that carries the serving stack: it applies\nagainst SGLang at commit `73a255206f` and is the artifact that `scripts/serve.sh` applies. It is the\nconsolidated form of the layered patch scripts in `patches/` (the build path), which were applied in order\nduring development and are kept here as the record of how the serving patch was assembled. The patch touches\nthe model runner, the MoE layers, the attention backend.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single git patch that carries the serving stack: it applies\nagainst SGLang at commit `73a255206f` and is the artifact that `scripts/serve.sh` applies. It is the\nconsolidated form of the layered patch scripts in `patches/` (the build path), which were applied in order\nduring development and are kept here as the record of how the serving patch was assembled. The patch touches\nthe model runner, the MoE layers, the attention backend,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single git patch that carries the serving stack: it applies\nagainst SGLang at commit `73a255206f` and is the artifact that `scripts/serve.sh` applies. It is the\nconsolidated form of the layered patch scripts in `patches/` (the build path), which were applied in order\nduring development and are kept here as the record of how the serving patch was assembled. The patch touches\nthe model runner, the MoE layers, the attention backend, the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single git patch that carries the serving stack: it applies\nagainst SGLang at commit `73a255206f` and is the artifact that `scripts/serve.sh` applies. It is the\nconsolidated form of the layered patch scripts in `patches/` (the build path), which were applied in order\nduring development and are kept here as the record of how the serving patch was assembled. The patch touches\nthe model runner, the MoE layers, the attention backend, the KV.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single git patch that carries the serving stack: it applies\nagainst SGLang at commit `73a255206f` and is the artifact that `scripts/serve.sh` applies. It is the\nconsolidated form of the layered patch scripts in `patches/` (the build path), which were applied in order\nduring development and are kept here as the record of how the serving patch was assembled. The patch touches\nthe model runner, the MoE layers, the attention backend, the KV cache.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single git patch that carries the serving stack: it applies\nagainst SGLang at commit `73a255206f` and is the artifact that `scripts/serve.sh` applies. It is the\nconsolidated form of the layered patch scripts in `patches/` (the build path), which were applied in order\nduring development and are kept here as the record of how the serving patch was assembled. The patch touches\nthe model runner, the MoE layers, the attention backend, the KV cache,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single git patch that carries the serving stack: it applies\nagainst SGLang at commit `73a255206f` and is the artifact that `scripts/serve.sh` applies. It is the\nconsolidated form of the layered patch scripts in `patches/` (the build path), which were applied in order\nduring development and are kept here as the record of how the serving patch was assembled. The patch touches\nthe model runner, the MoE layers, the attention backend, the KV cache, the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single git patch that carries the serving stack: it applies\nagainst SGLang at commit `73a255206f` and is the artifact that `scripts/serve.sh` applies. It is the\nconsolidated form of the layered patch scripts in `patches/` (the build path), which were applied in order\nduring development and are kept here as the record of how the serving patch was assembled. The patch touches\nthe model runner, the MoE layers, the attention backend, the KV cache, the P.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single git patch that carries the serving stack: it applies\nagainst SGLang at commit `73a255206f` and is the artifact that `scripts/serve.sh` applies. It is the\nconsolidated form of the layered patch scripts in `patches/` (the build path), which were applied in order\nduring development and are kept here as the record of how the serving patch was assembled. The patch touches\nthe model runner, the MoE layers, the attention backend, the KV cache, the PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single git patch that carries the serving stack: it applies\nagainst SGLang at commit `73a255206f` and is the artifact that `scripts/serve.sh` applies. It is the\nconsolidated form of the layered patch scripts in `patches/` (the build path), which were applied in order\nduring development and are kept here as the record of how the serving patch was assembled. The patch touches\nthe model runner, the MoE layers, the attention backend, the KV cache, the PLE,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single git patch that carries the serving stack: it applies\nagainst SGLang at commit `73a255206f` and is the artifact that `scripts/serve.sh` applies. It is the\nconsolidated form of the layered patch scripts in `patches/` (the build path), which were applied in order\nduring development and are kept here as the record of how the serving patch was assembled. The patch touches\nthe model runner, the MoE layers, the attention backend, the KV cache, the PLE, the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single git patch that carries the serving stack: it applies\nagainst SGLang at commit `73a255206f` and is the artifact that `scripts/serve.sh` applies. It is the\nconsolidated form of the layered patch scripts in `patches/` (the build path), which were applied in order\nduring development and are kept here as the record of how the serving patch was assembled. The patch touches\nthe model runner, the MoE layers, the attention backend, the KV cache, the PLE, the expert.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single git patch that carries the serving stack: it applies\nagainst SGLang at commit `73a255206f` and is the artifact that `scripts/serve.sh` applies. It is the\nconsolidated form of the layered patch scripts in `patches/` (the build path), which were applied in order\nduring development and are kept here as the record of how the serving patch was assembled. The patch touches\nthe model runner, the MoE layers, the attention backend, the KV cache, the PLE, the expert cache.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single git patch that carries the serving stack: it applies\nagainst SGLang at commit `73a255206f` and is the artifact that `scripts/serve.sh` applies. It is the\nconsolidated form of the layered patch scripts in `patches/` (the build path), which were applied in order\nduring development and are kept here as the record of how the serving patch was assembled. The patch touches\nthe model runner, the MoE layers, the attention backend, the KV cache, the PLE, the expert cache,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single git patch that carries the serving stack: it applies\nagainst SGLang at commit `73a255206f` and is the artifact that `scripts/serve.sh` applies. It is the\nconsolidated form of the layered patch scripts in `patches/` (the build path), which were applied in order\nduring development and are kept here as the record of how the serving patch was assembled. The patch touches\nthe model runner, the MoE layers, the attention backend, the KV cache, the PLE, the expert cache, the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single git patch that carries the serving stack: it applies\nagainst SGLang at commit `73a255206f` and is the artifact that `scripts/serve.sh` applies. It is the\nconsolidated form of the layered patch scripts in `patches/` (the build path), which were applied in order\nduring development and are kept here as the record of how the serving patch was assembled. The patch touches\nthe model runner, the MoE layers, the attention backend, the KV cache, the PLE, the expert cache, the\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single git patch that carries the serving stack: it applies\nagainst SGLang at commit `73a255206f` and is the artifact that `scripts/serve.sh` applies. It is the\nconsolidated form of the layered patch scripts in `patches/` (the build path), which were applied in order\nduring development and are kept here as the record of how the serving patch was assembled. The patch touches\nthe model runner, the MoE layers, the attention backend, the KV cache, the PLE, the expert cache, the\nscheduler.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single git patch that carries the serving stack: it applies\nagainst SGLang at commit `73a255206f` and is the artifact that `scripts/serve.sh` applies. It is the\nconsolidated form of the layered patch scripts in `patches/` (the build path), which were applied in order\nduring development and are kept here as the record of how the serving patch was assembled. The patch touches\nthe model runner, the MoE layers, the attention backend, the KV cache, the PLE, the expert cache, the\nscheduler and.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single git patch that carries the serving stack: it applies\nagainst SGLang at commit `73a255206f` and is the artifact that `scripts/serve.sh` applies. It is the\nconsolidated form of the layered patch scripts in `patches/` (the build path), which were applied in order\nduring development and are kept here as the record of how the serving patch was assembled. The patch touches\nthe model runner, the MoE layers, the attention backend, the KV cache, the PLE, the expert cache, the\nscheduler and the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single git patch that carries the serving stack: it applies\nagainst SGLang at commit `73a255206f` and is the artifact that `scripts/serve.sh` applies. It is the\nconsolidated form of the layered patch scripts in `patches/` (the build path), which were applied in order\nduring development and are kept here as the record of how the serving patch was assembled. The patch touches\nthe model runner, the MoE layers, the attention backend, the KV cache, the PLE, the expert cache, the\nscheduler and the server.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single git patch that carries the serving stack: it applies\nagainst SGLang at commit `73a255206f` and is the artifact that `scripts/serve.sh` applies. It is the\nconsolidated form of the layered patch scripts in `patches/` (the build path), which were applied in order\nduring development and are kept here as the record of how the serving patch was assembled. The patch touches\nthe model runner, the MoE layers, the attention backend, the KV cache, the PLE, the expert cache, the\nscheduler and the server arguments.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single git patch that carries the serving stack: it applies\nagainst SGLang at commit `73a255206f` and is the artifact that `scripts/serve.sh` applies. It is the\nconsolidated form of the layered patch scripts in `patches/` (the build path), which were applied in order\nduring development and are kept here as the record of how the serving patch was assembled. The patch touches\nthe model runner, the MoE layers, the attention backend, the KV cache, the PLE, the expert cache, the\nscheduler and the server arguments..py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single git patch that carries the serving stack: it applies\nagainst SGLang at commit `73a255206f` and is the artifact that `scripts/serve.sh` applies. It is the\nconsolidated form of the layered patch scripts in `patches/` (the build path), which were applied in order\nduring development and are kept here as the record of how the serving patch was assembled. The patch touches\nthe model runner, the MoE layers, the attention backend, the KV cache, the PLE, the expert cache, the\nscheduler and the server arguments.\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single git patch that carries the serving stack: it applies\nagainst SGLang at commit `73a255206f` and is the artifact that `scripts/serve.sh` applies. It is the\nconsolidated form of the layered patch scripts in `patches/` (the build path), which were applied in order\nduring development and are kept here as the record of how the serving patch was assembled. The patch touches\nthe model runner, the MoE layers, the attention backend, the KV cache, the PLE, the expert cache, the\nscheduler and the server arguments.\n\n##.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single git patch that carries the serving stack: it applies\nagainst SGLang at commit `73a255206f` and is the artifact that `scripts/serve.sh` applies. It is the\nconsolidated form of the layered patch scripts in `patches/` (the build path), which were applied in order\nduring development and are kept here as the record of how the serving patch was assembled. The patch touches\nthe model runner, the MoE layers, the attention backend, the KV cache, the PLE, the expert cache, the\nscheduler and the server arguments.\n\n## The.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single git patch that carries the serving stack: it applies\nagainst SGLang at commit `73a255206f` and is the artifact that `scripts/serve.sh` applies. It is the\nconsolidated form of the layered patch scripts in `patches/` (the build path), which were applied in order\nduring development and are kept here as the record of how the serving patch was assembled. The patch touches\nthe model runner, the MoE layers, the attention backend, the KV cache, the PLE, the expert cache, the\nscheduler and the server arguments.\n\n## The kernels.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single git patch that carries the serving stack: it applies\nagainst SGLang at commit `73a255206f` and is the artifact that `scripts/serve.sh` applies. It is the\nconsolidated form of the layered patch scripts in `patches/` (the build path), which were applied in order\nduring development and are kept here as the record of how the serving patch was assembled. The patch touches\nthe model runner, the MoE layers, the attention backend, the KV cache, the PLE, the expert cache, the\nscheduler and the server arguments.\n\n## The kernels\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single git patch that carries the serving stack: it applies\nagainst SGLang at commit `73a255206f` and is the artifact that `scripts/serve.sh` applies. It is the\nconsolidated form of the layered patch scripts in `patches/` (the build path), which were applied in order\nduring development and are kept here as the record of how the serving patch was assembled. The patch touches\nthe model runner, the MoE layers, the attention backend, the KV cache, the PLE, the expert cache, the\nscheduler and the server arguments.\n\n## The kernels\n\n`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single git patch that carries the serving stack: it applies\nagainst SGLang at commit `73a255206f` and is the artifact that `scripts/serve.sh` applies. It is the\nconsolidated form of the layered patch scripts in `patches/` (the build path), which were applied in order\nduring development and are kept here as the record of how the serving patch was assembled. The patch touches\nthe model runner, the MoE layers, the attention backend, the KV cache, the PLE, the expert cache, the\nscheduler and the server arguments.\n\n## The kernels\n\n`k.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single git patch that carries the serving stack: it applies\nagainst SGLang at commit `73a255206f` and is the artifact that `scripts/serve.sh` applies. It is the\nconsolidated form of the layered patch scripts in `patches/` (the build path), which were applied in order\nduring development and are kept here as the record of how the serving patch was assembled. The patch touches\nthe model runner, the MoE layers, the attention backend, the KV cache, the PLE, the expert cache, the\nscheduler and the server arguments.\n\n## The kernels\n\n`kernels.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single git patch that carries the serving stack: it applies\nagainst SGLang at commit `73a255206f` and is the artifact that `scripts/serve.sh` applies. It is the\nconsolidated form of the layered patch scripts in `patches/` (the build path), which were applied in order\nduring development and are kept here as the record of how the serving patch was assembled. The patch touches\nthe model runner, the MoE layers, the attention backend, the KV cache, the PLE, the expert cache, the\nscheduler and the server arguments.\n\n## The kernels\n\n`kernels/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single git patch that carries the serving stack: it applies\nagainst SGLang at commit `73a255206f` and is the artifact that `scripts/serve.sh` applies. It is the\nconsolidated form of the layered patch scripts in `patches/` (the build path), which were applied in order\nduring development and are kept here as the record of how the serving patch was assembled. The patch touches\nthe model runner, the MoE layers, the attention backend, the KV cache, the PLE, the expert cache, the\nscheduler and the server arguments.\n\n## The kernels\n\n`kernels/`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single git patch that carries the serving stack: it applies\nagainst SGLang at commit `73a255206f` and is the artifact that `scripts/serve.sh` applies. It is the\nconsolidated form of the layered patch scripts in `patches/` (the build path), which were applied in order\nduring development and are kept here as the record of how the serving patch was assembled. The patch touches\nthe model runner, the MoE layers, the attention backend, the KV cache, the PLE, the expert cache, the\nscheduler and the server arguments.\n\n## The kernels\n\n`kernels/` holds.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single git patch that carries the serving stack: it applies\nagainst SGLang at commit `73a255206f` and is the artifact that `scripts/serve.sh` applies. It is the\nconsolidated form of the layered patch scripts in `patches/` (the build path), which were applied in order\nduring development and are kept here as the record of how the serving patch was assembled. The patch touches\nthe model runner, the MoE layers, the attention backend, the KV cache, the PLE, the expert cache, the\nscheduler and the server arguments.\n\n## The kernels\n\n`kernels/` holds the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single git patch that carries the serving stack: it applies\nagainst SGLang at commit `73a255206f` and is the artifact that `scripts/serve.sh` applies. It is the\nconsolidated form of the layered patch scripts in `patches/` (the build path), which were applied in order\nduring development and are kept here as the record of how the serving patch was assembled. The patch touches\nthe model runner, the MoE layers, the attention backend, the KV cache, the PLE, the expert cache, the\nscheduler and the server arguments.\n\n## The kernels\n\n`kernels/` holds the Trit.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single git patch that carries the serving stack: it applies\nagainst SGLang at commit `73a255206f` and is the artifact that `scripts/serve.sh` applies. It is the\nconsolidated form of the layered patch scripts in `patches/` (the build path), which were applied in order\nduring development and are kept here as the record of how the serving patch was assembled. The patch touches\nthe model runner, the MoE layers, the attention backend, the KV cache, the PLE, the expert cache, the\nscheduler and the server arguments.\n\n## The kernels\n\n`kernels/` holds the Triton.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single git patch that carries the serving stack: it applies\nagainst SGLang at commit `73a255206f` and is the artifact that `scripts/serve.sh` applies. It is the\nconsolidated form of the layered patch scripts in `patches/` (the build path), which were applied in order\nduring development and are kept here as the record of how the serving patch was assembled. The patch touches\nthe model runner, the MoE layers, the attention backend, the KV cache, the PLE, the expert cache, the\nscheduler and the server arguments.\n\n## The kernels\n\n`kernels/` holds the Triton kernels.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single git patch that carries the serving stack: it applies\nagainst SGLang at commit `73a255206f` and is the artifact that `scripts/serve.sh` applies. It is the\nconsolidated form of the layered patch scripts in `patches/` (the build path), which were applied in order\nduring development and are kept here as the record of how the serving patch was assembled. The patch touches\nthe model runner, the MoE layers, the attention backend, the KV cache, the PLE, the expert cache, the\nscheduler and the server arguments.\n\n## The kernels\n\n`kernels/` holds the Triton kernels that.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single git patch that carries the serving stack: it applies\nagainst SGLang at commit `73a255206f` and is the artifact that `scripts/serve.sh` applies. It is the\nconsolidated form of the layered patch scripts in `patches/` (the build path), which were applied in order\nduring development and are kept here as the record of how the serving patch was assembled. The patch touches\nthe model runner, the MoE layers, the attention backend, the KV cache, the PLE, the expert cache, the\nscheduler and the server arguments.\n\n## The kernels\n\n`kernels/` holds the Triton kernels that the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single git patch that carries the serving stack: it applies\nagainst SGLang at commit `73a255206f` and is the artifact that `scripts/serve.sh` applies. It is the\nconsolidated form of the layered patch scripts in `patches/` (the build path), which were applied in order\nduring development and are kept here as the record of how the serving patch was assembled. The patch touches\nthe model runner, the MoE layers, the attention backend, the KV cache, the PLE, the expert cache, the\nscheduler and the server arguments.\n\n## The kernels\n\n`kernels/` holds the Triton kernels that the patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single git patch that carries the serving stack: it applies\nagainst SGLang at commit `73a255206f` and is the artifact that `scripts/serve.sh` applies. It is the\nconsolidated form of the layered patch scripts in `patches/` (the build path), which were applied in order\nduring development and are kept here as the record of how the serving patch was assembled. The patch touches\nthe model runner, the MoE layers, the attention backend, the KV cache, the PLE, the expert cache, the\nscheduler and the server arguments.\n\n## The kernels\n\n`kernels/` holds the Triton kernels that the patch loads.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single git patch that carries the serving stack: it applies\nagainst SGLang at commit `73a255206f` and is the artifact that `scripts/serve.sh` applies. It is the\nconsolidated form of the layered patch scripts in `patches/` (the build path), which were applied in order\nduring development and are kept here as the record of how the serving patch was assembled. The patch touches\nthe model runner, the MoE layers, the attention backend, the KV cache, the PLE, the expert cache, the\nscheduler and the server arguments.\n\n## The kernels\n\n`kernels/` holds the Triton kernels that the patch loads,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single git patch that carries the serving stack: it applies\nagainst SGLang at commit `73a255206f` and is the artifact that `scripts/serve.sh` applies. It is the\nconsolidated form of the layered patch scripts in `patches/` (the build path), which were applied in order\nduring development and are kept here as the record of how the serving patch was assembled. The patch touches\nthe model runner, the MoE layers, the attention backend, the KV cache, the PLE, the expert cache, the\nscheduler and the server arguments.\n\n## The kernels\n\n`kernels/` holds the Triton kernels that the patch loads, with.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single git patch that carries the serving stack: it applies\nagainst SGLang at commit `73a255206f` and is the artifact that `scripts/serve.sh` applies. It is the\nconsolidated form of the layered patch scripts in `patches/` (the build path), which were applied in order\nduring development and are kept here as the record of how the serving patch was assembled. The patch touches\nthe model runner, the MoE layers, the attention backend, the KV cache, the PLE, the expert cache, the\nscheduler and the server arguments.\n\n## The kernels\n\n`kernels/` holds the Triton kernels that the patch loads, with the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single git patch that carries the serving stack: it applies\nagainst SGLang at commit `73a255206f` and is the artifact that `scripts/serve.sh` applies. It is the\nconsolidated form of the layered patch scripts in `patches/` (the build path), which were applied in order\nduring development and are kept here as the record of how the serving patch was assembled. The patch touches\nthe model runner, the MoE layers, the attention backend, the KV cache, the PLE, the expert cache, the\nscheduler and the server arguments.\n\n## The kernels\n\n`kernels/` holds the Triton kernels that the patch loads, with the tuned.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single git patch that carries the serving stack: it applies\nagainst SGLang at commit `73a255206f` and is the artifact that `scripts/serve.sh` applies. It is the\nconsolidated form of the layered patch scripts in `patches/` (the build path), which were applied in order\nduring development and are kept here as the record of how the serving patch was assembled. The patch touches\nthe model runner, the MoE layers, the attention backend, the KV cache, the PLE, the expert cache, the\nscheduler and the server arguments.\n\n## The kernels\n\n`kernels/` holds the Triton kernels that the patch loads, with the tuned configs.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single git patch that carries the serving stack: it applies\nagainst SGLang at commit `73a255206f` and is the artifact that `scripts/serve.sh` applies. It is the\nconsolidated form of the layered patch scripts in `patches/` (the build path), which were applied in order\nduring development and are kept here as the record of how the serving patch was assembled. The patch touches\nthe model runner, the MoE layers, the attention backend, the KV cache, the PLE, the expert cache, the\nscheduler and the server arguments.\n\n## The kernels\n\n`kernels/` holds the Triton kernels that the patch loads, with the tuned configs for.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single git patch that carries the serving stack: it applies\nagainst SGLang at commit `73a255206f` and is the artifact that `scripts/serve.sh` applies. It is the\nconsolidated form of the layered patch scripts in `patches/` (the build path), which were applied in order\nduring development and are kept here as the record of how the serving patch was assembled. The patch touches\nthe model runner, the MoE layers, the attention backend, the KV cache, the PLE, the expert cache, the\nscheduler and the server arguments.\n\n## The kernels\n\n`kernels/` holds the Triton kernels that the patch loads, with the tuned configs for sm.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single git patch that carries the serving stack: it applies\nagainst SGLang at commit `73a255206f` and is the artifact that `scripts/serve.sh` applies. It is the\nconsolidated form of the layered patch scripts in `patches/` (the build path), which were applied in order\nduring development and are kept here as the record of how the serving patch was assembled. The patch touches\nthe model runner, the MoE layers, the attention backend, the KV cache, the PLE, the expert cache, the\nscheduler and the server arguments.\n\n## The kernels\n\n`kernels/` holds the Triton kernels that the patch loads, with the tuned configs for sm_.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single git patch that carries the serving stack: it applies\nagainst SGLang at commit `73a255206f` and is the artifact that `scripts/serve.sh` applies. It is the\nconsolidated form of the layered patch scripts in `patches/` (the build path), which were applied in order\nduring development and are kept here as the record of how the serving patch was assembled. The patch touches\nthe model runner, the MoE layers, the attention backend, the KV cache, the PLE, the expert cache, the\nscheduler and the server arguments.\n\n## The kernels\n\n`kernels/` holds the Triton kernels that the patch loads, with the tuned configs for sm_1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-weights-dir` is the directory that\ncontains `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch, against SGLang at 73a255206f)\npatches/        the layered patch scripts the serving patch was built from (the build path)\nkernels/        the Triton kernels, with the tuned configs for sm_120\nassets/         the two serving assets: the PLE n-gram table and the expert cache\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single git patch that carries the serving stack: it applies\nagainst SGLang at commit `73a255206f` and is the artifact that `scripts/serve.sh` applies. It is the\nconsolidated form of the layered patch scripts in `patches/` (the build path), which were applied in order\nduring development and are kept here as the record of how the serving patch was assembled. The patch touches\nthe model runner, the MoE layers, the attention backend, the KV cache, the PLE, the expert cache, the\nscheduler and the server arguments.\n\n## The kernels\n\n`kernels/` holds the Triton kernels that the patch loads, with the tuned configs for sm_12",
 "ttft_seconds": 2.680629921000218,
 "status": "ok",
 "tg_tokens_per_s": 37.03199989056764,
 "decode_seconds_per_token": 0.02700367257925782,
 "individual_token_timing": true,
 "event_intervals_ms": {
  "n": 511,
  "median": 26.357163005741313,
  "p95": 33.89051399426535,
  "mean": 27.00367257925782
 },
 "class": "prose",
 "requested_input_tokens": 2048
}
