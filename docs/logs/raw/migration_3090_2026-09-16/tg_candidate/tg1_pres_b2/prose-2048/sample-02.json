{
 "started_utc": "2026-09-17T01:03:15.920900+00:00",
 "wall_seconds": 14.758840855996823,
 "prompt_tokens": 2048,
 "completion_tokens": 512,
 "stream_events": 512,
 "ended_utc": "2026-09-17T01:03:30.679844+00:00",
 "warmup": false,
 "sample_index": 2,
 "requested_decode_tokens": 512,
 "gpu": {
  "n": 53,
  "temperature_c": {
   "min": 74.0,
   "max": 75.0,
   "mean": 74.15094339622641
  },
  "power_w": {
   "min": 255.78,
   "max": 277.23,
   "mean": 264.8390566037736
  },
  "util_pct": {
   "min": 87.0,
   "max": 100.0,
   "mean": 90.81132075471699
  },
  "clock_sm_mhz": {
   "min": 1545.0,
   "max": 1920.0,
   "mean": 1893.9622641509434
  },
  "clock_mem_mhz": {
   "min": 9501.0,
   "max": 9501.0,
   "mean": 9501.0
  },
  "memory_free_mib": {
   "min": 2540.0
  }
 },
 "output_text_sha256": "68ad4ef276137b11f0bd3cb8164e32c1bdb17cf460d9ed38675a6fcc92153476",
 "output_text": ".py.py --.py --url.py --url http.py --url http://.py --url http://1.py --url http://12.py --url http://127.py --url http://127..py --url http://127.0.py --url http://127.0..py --url http://127.0.0.py --url http://127.0.0..py --url http://127.0.0.1.py --url http://127.0.0.1:.py --url http://127.0.0.1:3.py --url http://127.0.0.1:30.py --url http://127.0.0.1:300.py --url http://127.0.0.1:3000.py --url http://127.0.0.1:30000.py --url http://127.0.0.1:30000 --.py --url http://127.0.0.1:30000 --prompt.py --url http://127.0.0.1:30000 --prompt-t.py --url http://127.0.0.1:30000 --prompt-tokens.py --url http://127.0.0.1:30000 --prompt-tokens .py --url http://127.0.0.1:30000 --prompt-tokens 1.py --url http://127.0.0.1:30000 --prompt-tokens 10.py --url http://127.0.0.1:30000 --prompt-tokens 101.py --url http://127.0.0.1:30000 --prompt-tokens 101 .py --url http://127.0.0.1:30000 --prompt-tokens 101 4.py --url http://127.0.0.1:30000 --prompt-tokens 101 42.py --url http://127.0.0.1:30000 --prompt-tokens 101 421.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 17.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 170.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 68.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 682.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 100.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 1000.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-t.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 12.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers..py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches S.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGL.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`..py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`;.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 26.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,14.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAM.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPA.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:4.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:44.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448)..py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`),.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root..py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `S.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGL.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`V.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults;.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sg.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--ven.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them..py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n##.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsg.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/        .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (q.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-7.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a25.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a2552.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a25520.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch).py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the P.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nk.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/       .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Trit.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels:.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 Mo.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gem.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gem.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, P.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/         .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools:.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py, ngram_p.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_p.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py, ngram_p.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py, ngram_ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py\nscripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py\nscripts/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py\nscripts/       .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py\nscripts/        serve.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py\nscripts/        serve.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py\nscripts/        serve.sh (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py\nscripts/        serve.sh (the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py\nscripts/        serve.sh (the launcher.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py\nscripts/        serve.sh (the launcher),.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py\nscripts/        serve.sh (the launcher), serve.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py\nscripts/        serve.sh (the launcher), serve_el.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py\nscripts/        serve.sh (the launcher), serve_elastic.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py\nscripts/        serve.sh (the launcher), serve_elastic.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py\nscripts/        serve.sh (the launcher), serve_elastic.sh,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py\nscripts/        serve.sh (the launcher), serve_elastic.sh, serve.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py\nscripts/        serve.sh (the launcher), serve_elastic.sh, serve_t.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py\nscripts/        serve.sh (the launcher), serve_elastic.sh, serve_tiers.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py\nscripts/        serve.sh (the launcher), serve_elastic.sh, serve_tiers.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py\nscripts/        serve.sh (the launcher), serve_elastic.sh, serve_tiers.sh,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py\nscripts/        serve.sh (the launcher), serve_elastic.sh, serve_tiers.sh, serve.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py\nscripts/        serve.sh (the launcher), serve_elastic.sh, serve_tiers.sh, serve_t.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py\nscripts/        serve.sh (the launcher), serve_elastic.sh, serve_tiers.sh, serve_tiers.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py\nscripts/        serve.sh (the launcher), serve_elastic.sh, serve_tiers.sh, serve_tiers_el.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py\nscripts/        serve.sh (the launcher), serve_elastic.sh, serve_tiers.sh, serve_tiers_elastic.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py\nscripts/        serve.sh (the launcher), serve_elastic.sh, serve_tiers.sh, serve_tiers_elastic.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py\nscripts/        serve.sh (the launcher), serve_elastic.sh, serve_tiers.sh, serve_tiers_elastic.sh,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py\nscripts/        serve.sh (the launcher), serve_elastic.sh, serve_tiers.sh, serve_tiers_elastic.sh,\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py\nscripts/        serve.sh (the launcher), serve_elastic.sh, serve_tiers.sh, serve_tiers_elastic.sh,\n             .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py\nscripts/        serve.sh (the launcher), serve_elastic.sh, serve_tiers.sh, serve_tiers_elastic.sh,\n              serve.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py\nscripts/        serve.sh (the launcher), serve_elastic.sh, serve_tiers.sh, serve_tiers_elastic.sh,\n              serve_t.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py\nscripts/        serve.sh (the launcher), serve_elastic.sh, serve_tiers.sh, serve_tiers_elastic.sh,\n              serve_tiers.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py\nscripts/        serve.sh (the launcher), serve_elastic.sh, serve_tiers.sh, serve_tiers_elastic.sh,\n              serve_tiers_el.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py\nscripts/        serve.sh (the launcher), serve_elastic.sh, serve_tiers.sh, serve_tiers_elastic.sh,\n              serve_tiers_elastic.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py\nscripts/        serve.sh (the launcher), serve_elastic.sh, serve_tiers.sh, serve_tiers_elastic.sh,\n              serve_tiers_elastic_.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py\nscripts/        serve.sh (the launcher), serve_elastic.sh, serve_tiers.sh, serve_tiers_elastic.sh,\n              serve_tiers_elastic_2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py\nscripts/        serve.sh (the launcher), serve_elastic.sh, serve_tiers.sh, serve_tiers_elastic.sh,\n              serve_tiers_elastic_25.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py\nscripts/        serve.sh (the launcher), serve_elastic.sh, serve_tiers.sh, serve_tiers_elastic.sh,\n              serve_tiers_elastic_258.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py\nscripts/        serve.sh (the launcher), serve_elastic.sh, serve_tiers.sh, serve_tiers_elastic.sh,\n              serve_tiers_elastic_258k.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py\nscripts/        serve.sh (the launcher), serve_elastic.sh, serve_tiers.sh, serve_tiers_elastic.sh,\n              serve_tiers_elastic_258k.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py\nscripts/        serve.sh (the launcher), serve_elastic.sh, serve_tiers.sh, serve_tiers_elastic.sh,\n              serve_tiers_elastic_258k.sh,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py\nscripts/        serve.sh (the launcher), serve_elastic.sh, serve_tiers.sh, serve_tiers_elastic.sh,\n              serve_tiers_elastic_258k.sh, serve.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py\nscripts/        serve.sh (the launcher), serve_elastic.sh, serve_tiers.sh, serve_tiers_elastic.sh,\n              serve_tiers_elastic_258k.sh, serve_t.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py\nscripts/        serve.sh (the launcher), serve_elastic.sh, serve_tiers.sh, serve_tiers_elastic.sh,\n              serve_tiers_elastic_258k.sh, serve_tiers.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py\nscripts/        serve.sh (the launcher), serve_elastic.sh, serve_tiers.sh, serve_tiers_elastic.sh,\n              serve_tiers_elastic_258k.sh, serve_tiers_el.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py\nscripts/        serve.sh (the launcher), serve_elastic.sh, serve_tiers.sh, serve_tiers_elastic.sh,\n              serve_tiers_elastic_258k.sh, serve_tiers_elastic.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py\nscripts/        serve.sh (the launcher), serve_elastic.sh, serve_tiers.sh, serve_tiers_elastic.sh,\n              serve_tiers_elastic_258k.sh, serve_tiers_elastic_.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py\nscripts/        serve.sh (the launcher), serve_elastic.sh, serve_tiers.sh, serve_tiers_elastic.sh,\n              serve_tiers_elastic_258k.sh, serve_tiers_elastic_2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py\nscripts/        serve.sh (the launcher), serve_elastic.sh, serve_tiers.sh, serve_tiers_elastic.sh,\n              serve_tiers_elastic_258k.sh, serve_tiers_elastic_25.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py\nscripts/        serve.sh (the launcher), serve_elastic.sh, serve_tiers.sh, serve_tiers_elastic.sh,\n              serve_tiers_elastic_258k.sh, serve_tiers_elastic_258.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py\nscripts/        serve.sh (the launcher), serve_elastic.sh, serve_tiers.sh, serve_tiers_elastic.sh,\n              serve_tiers_elastic_258k.sh, serve_tiers_elastic_258k.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py\nscripts/        serve.sh (the launcher), serve_elastic.sh, serve_tiers.sh, serve_tiers_elastic.sh,\n              serve_tiers_elastic_258k.sh, serve_tiers_elastic_258k_.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py\nscripts/        serve.sh (the launcher), serve_elastic.sh, serve_tiers.sh, serve_tiers_elastic.sh,\n              serve_tiers_elastic_258k.sh, serve_tiers_elastic_258k_2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py\nscripts/        serve.sh (the launcher), serve_elastic.sh, serve_tiers.sh, serve_tiers_elastic.sh,\n              serve_tiers_elastic_258k.sh, serve_tiers_elastic_258k_2.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py\nscripts/        serve.sh (the launcher), serve_elastic.sh, serve_tiers.sh, serve_tiers_elastic.sh,\n              serve_tiers_elastic_258k.sh, serve_tiers_elastic_258k_2.sh,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py\nscripts/        serve.sh (the launcher), serve_elastic.sh, serve_tiers.sh, serve_tiers_elastic.sh,\n              serve_tiers_elastic_258k.sh, serve_tiers_elastic_258k_2.sh, serve.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py\nscripts/        serve.sh (the launcher), serve_elastic.sh, serve_tiers.sh, serve_tiers_elastic.sh,\n              serve_tiers_elastic_258k.sh, serve_tiers_elastic_258k_2.sh, serve_t.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py\nscripts/        serve.sh (the launcher), serve_elastic.sh, serve_tiers.sh, serve_tiers_elastic.sh,\n              serve_tiers_elastic_258k.sh, serve_tiers_elastic_258k_2.sh, serve_tiers.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py\nscripts/        serve.sh (the launcher), serve_elastic.sh, serve_tiers.sh, serve_tiers_elastic.sh,\n              serve_tiers_elastic_258k.sh, serve_tiers_elastic_258k_2.sh, serve_tiers_el.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py\nscripts/        serve.sh (the launcher), serve_elastic.sh, serve_tiers.sh, serve_tiers_elastic.sh,\n              serve_tiers_elastic_258k.sh, serve_tiers_elastic_258k_2.sh, serve_tiers_elastic.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py\nscripts/        serve.sh (the launcher), serve_elastic.sh, serve_tiers.sh, serve_tiers_elastic.sh,\n              serve_tiers_elastic_258k.sh, serve_tiers_elastic_258k_2.sh, serve_tiers_elastic_.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py\nscripts/        serve.sh (the launcher), serve_elastic.sh, serve_tiers.sh, serve_tiers_elastic.sh,\n              serve_tiers_elastic_258k.sh, serve_tiers_elastic_258k_2.sh, serve_tiers_elastic_2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py\nscripts/        serve.sh (the launcher), serve_elastic.sh, serve_tiers.sh, serve_tiers_elastic.sh,\n              serve_tiers_elastic_258k.sh, serve_tiers_elastic_258k_2.sh, serve_tiers_elastic_25.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py\nscripts/        serve.sh (the launcher), serve_elastic.sh, serve_tiers.sh, serve_tiers_elastic.sh,\n              serve_tiers_elastic_258k.sh, serve_tiers_elastic_258k_2.sh, serve_tiers_elastic_258.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py\nscripts/        serve.sh (the launcher), serve_elastic.sh, serve_tiers.sh, serve_tiers_elastic.sh,\n              serve_tiers_elastic_258k.sh, serve_tiers_elastic_258k_2.sh, serve_tiers_elastic_258k.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py\nscripts/        serve.sh (the launcher), serve_elastic.sh, serve_tiers.sh, serve_tiers_elastic.sh,\n              serve_tiers_elastic_258k.sh, serve_tiers_elastic_258k_2.sh, serve_tiers_elastic_258k_.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py\nscripts/        serve.sh (the launcher), serve_elastic.sh, serve_tiers.sh, serve_tiers_elastic.sh,\n              serve_tiers_elastic_258k.sh, serve_tiers_elastic_258k_2.sh, serve_tiers_elastic_258k_3.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py\nscripts/        serve.sh (the launcher), serve_elastic.sh, serve_tiers.sh, serve_tiers_elastic.sh,\n              serve_tiers_elastic_258k.sh, serve_tiers_elastic_258k_2.sh, serve_tiers_elastic_258k_3.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py\nscripts/        serve.sh (the launcher), serve_elastic.sh, serve_tiers.sh, serve_tiers_elastic.sh,\n              serve_tiers_elastic_258k.sh, serve_tiers_elastic_258k_2.sh, serve_tiers_elastic_258k_3.sh,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py\nscripts/        serve.sh (the launcher), serve_elastic.sh, serve_tiers.sh, serve_tiers_elastic.sh,\n              serve_tiers_elastic_258k.sh, serve_tiers_elastic_258k_2.sh, serve_tiers_elastic_258k_3.sh,\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py\nscripts/        serve.sh (the launcher), serve_elastic.sh, serve_tiers.sh, serve_tiers_elastic.sh,\n              serve_tiers_elastic_258k.sh, serve_tiers_elastic_258k_2.sh, serve_tiers_elastic_258k_3.sh,\n             .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py\nscripts/        serve.sh (the launcher), serve_elastic.sh, serve_tiers.sh, serve_tiers_elastic.sh,\n              serve_tiers_elastic_258k.sh, serve_tiers_elastic_258k_2.sh, serve_tiers_elastic_258k_3.sh,\n              serve.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py\nscripts/        serve.sh (the launcher), serve_elastic.sh, serve_tiers.sh, serve_tiers_elastic.sh,\n              serve_tiers_elastic_258k.sh, serve_tiers_elastic_258k_2.sh, serve_tiers_elastic_258k_3.sh,\n              serve_t.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py\nscripts/        serve.sh (the launcher), serve_elastic.sh, serve_tiers.sh, serve_tiers_elastic.sh,\n              serve_tiers_elastic_258k.sh, serve_tiers_elastic_258k_2.sh, serve_tiers_elastic_258k_3.sh,\n              serve_tiers.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py\nscripts/        serve.sh (the launcher), serve_elastic.sh, serve_tiers.sh, serve_tiers_elastic.sh,\n              serve_tiers_elastic_258k.sh, serve_tiers_elastic_258k_2.sh, serve_tiers_elastic_258k_3.sh,\n              serve_tiers_el.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py\nscripts/        serve.sh (the launcher), serve_elastic.sh, serve_tiers.sh, serve_tiers_elastic.sh,\n              serve_tiers_elastic_258k.sh, serve_tiers_elastic_258k_2.sh, serve_tiers_elastic_258k_3.sh,\n              serve_tiers_elastic.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py\nscripts/        serve.sh (the launcher), serve_elastic.sh, serve_tiers.sh, serve_tiers_elastic.sh,\n              serve_tiers_elastic_258k.sh, serve_tiers_elastic_258k_2.sh, serve_tiers_elastic_258k_3.sh,\n              serve_tiers_elastic_.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py\nscripts/        serve.sh (the launcher), serve_elastic.sh, serve_tiers.sh, serve_tiers_elastic.sh,\n              serve_tiers_elastic_258k.sh, serve_tiers_elastic_258k_2.sh, serve_tiers_elastic_258k_3.sh,\n              serve_tiers_elastic_2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py\nscripts/        serve.sh (the launcher), serve_elastic.sh, serve_tiers.sh, serve_tiers_elastic.sh,\n              serve_tiers_elastic_258k.sh, serve_tiers_elastic_258k_2.sh, serve_tiers_elastic_258k_3.sh,\n              serve_tiers_elastic_25.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py\nscripts/        serve.sh (the launcher), serve_elastic.sh, serve_tiers.sh, serve_tiers_elastic.sh,\n              serve_tiers_elastic_258k.sh, serve_tiers_elastic_258k_2.sh, serve_tiers_elastic_258k_3.sh,\n              serve_tiers_elastic_258.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py\nscripts/        serve.sh (the launcher), serve_elastic.sh, serve_tiers.sh, serve_tiers_elastic.sh,\n              serve_tiers_elastic_258k.sh, serve_tiers_elastic_258k_2.sh, serve_tiers_elastic_258k_3.sh,\n              serve_tiers_elastic_258k.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py\nscripts/        serve.sh (the launcher), serve_elastic.sh, serve_tiers.sh, serve_tiers_elastic.sh,\n              serve_tiers_elastic_258k.sh, serve_tiers_elastic_258k_2.sh, serve_tiers_elastic_258k_3.sh,\n              serve_tiers_elastic_258k_.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py\nscripts/        serve.sh (the launcher), serve_elastic.sh, serve_tiers.sh, serve_tiers_elastic.sh,\n              serve_tiers_elastic_258k.sh, serve_tiers_elastic_258k_2.sh, serve_tiers_elastic_258k_3.sh,\n              serve_tiers_elastic_258k_4.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py\nscripts/        serve.sh (the launcher), serve_elastic.sh, serve_tiers.sh, serve_tiers_elastic.sh,\n              serve_tiers_elastic_258k.sh, serve_tiers_elastic_258k_2.sh, serve_tiers_elastic_258k_3.sh,\n              serve_tiers_elastic_258k_4.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py\nscripts/        serve.sh (the launcher), serve_elastic.sh, serve_tiers.sh, serve_tiers_elastic.sh,\n              serve_tiers_elastic_258k.sh, serve_tiers_elastic_258k_2.sh, serve_tiers_elastic_258k_3.sh,\n              serve_tiers_elastic_258k_4.sh,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py\nscripts/        serve.sh (the launcher), serve_elastic.sh, serve_tiers.sh, serve_tiers_elastic.sh,\n              serve_tiers_elastic_258k.sh, serve_tiers_elastic_258k_2.sh, serve_tiers_elastic_258k_3.sh,\n              serve_tiers_elastic_258k_4.sh, serve.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py\nscripts/        serve.sh (the launcher), serve_elastic.sh, serve_tiers.sh, serve_tiers_elastic.sh,\n              serve_tiers_elastic_258k.sh, serve_tiers_elastic_258k_2.sh, serve_tiers_elastic_258k_3.sh,\n              serve_tiers_elastic_258k_4.sh, serve_t.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py\nscripts/        serve.sh (the launcher), serve_elastic.sh, serve_tiers.sh, serve_tiers_elastic.sh,\n              serve_tiers_elastic_258k.sh, serve_tiers_elastic_258k_2.sh, serve_tiers_elastic_258k_3.sh,\n              serve_tiers_elastic_258k_4.sh, serve_tiers.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py\nscripts/        serve.sh (the launcher), serve_elastic.sh, serve_tiers.sh, serve_tiers_elastic.sh,\n              serve_tiers_elastic_258k.sh, serve_tiers_elastic_258k_2.sh, serve_tiers_elastic_258k_3.sh,\n              serve_tiers_elastic_258k_4.sh, serve_tiers_el.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py\nscripts/        serve.sh (the launcher), serve_elastic.sh, serve_tiers.sh, serve_tiers_elastic.sh,\n              serve_tiers_elastic_258k.sh, serve_tiers_elastic_258k_2.sh, serve_tiers_elastic_258k_3.sh,\n              serve_tiers_elastic_258k_4.sh, serve_tiers_elastic.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py\nscripts/        serve.sh (the launcher), serve_elastic.sh, serve_tiers.sh, serve_tiers_elastic.sh,\n              serve_tiers_elastic_258k.sh, serve_tiers_elastic_258k_2.sh, serve_tiers_elastic_258k_3.sh,\n              serve_tiers_elastic_258k_4.sh, serve_tiers_elastic_.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py\nscripts/        serve.sh (the launcher), serve_elastic.sh, serve_tiers.sh, serve_tiers_elastic.sh,\n              serve_tiers_elastic_258k.sh, serve_tiers_elastic_258k_2.sh, serve_tiers_elastic_258k_3.sh,\n              serve_tiers_elastic_258k_4.sh, serve_tiers_elastic_2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py\nscripts/        serve.sh (the launcher), serve_elastic.sh, serve_tiers.sh, serve_tiers_elastic.sh,\n              serve_tiers_elastic_258k.sh, serve_tiers_elastic_258k_2.sh, serve_tiers_elastic_258k_3.sh,\n              serve_tiers_elastic_258k_4.sh, serve_tiers_elastic_25.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py\nscripts/        serve.sh (the launcher), serve_elastic.sh, serve_tiers.sh, serve_tiers_elastic.sh,\n              serve_tiers_elastic_258k.sh, serve_tiers_elastic_258k_2.sh, serve_tiers_elastic_258k_3.sh,\n              serve_tiers_elastic_258k_4.sh, serve_tiers_elastic_258.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py\nscripts/        serve.sh (the launcher), serve_elastic.sh, serve_tiers.sh, serve_tiers_elastic.sh,\n              serve_tiers_elastic_258k.sh, serve_tiers_elastic_258k_2.sh, serve_tiers_elastic_258k_3.sh,\n              serve_tiers_elastic_258k_4.sh, serve_tiers_elastic_258k.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py\nscripts/        serve.sh (the launcher), serve_elastic.sh, serve_tiers.sh, serve_tiers_elastic.sh,\n              serve_tiers_elastic_258k.sh, serve_tiers_elastic_258k_2.sh, serve_tiers_elastic_258k_3.sh,\n              serve_tiers_elastic_258k_4.sh, serve_tiers_elastic_258k_.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py\nscripts/        serve.sh (the launcher), serve_elastic.sh, serve_tiers.sh, serve_tiers_elastic.sh,\n              serve_tiers_elastic_258k.sh, serve_tiers_elastic_258k_2.sh, serve_tiers_elastic_258k_3.sh,\n              serve_tiers_elastic_258k_4.sh, serve_tiers_elastic_258k_5.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py\nscripts/        serve.sh (the launcher), serve_elastic.sh, serve_tiers.sh, serve_tiers_elastic.sh,\n              serve_tiers_elastic_258k.sh, serve_tiers_elastic_258k_2.sh, serve_tiers_elastic_258k_3.sh,\n              serve_tiers_elastic_258k_4.sh, serve_tiers_elastic_258k_5.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py\nscripts/        serve.sh (the launcher), serve_elastic.sh, serve_tiers.sh, serve_tiers_elastic.sh,\n              serve_tiers_elastic_258k.sh, serve_tiers_elastic_258k_2.sh, serve_tiers_elastic_258k_3.sh,\n              serve_tiers_elastic_258k_4.sh, serve_tiers_elastic_258k_5.sh,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py\nscripts/        serve.sh (the launcher), serve_elastic.sh, serve_tiers.sh, serve_tiers_elastic.sh,\n              serve_tiers_elastic_258k.sh, serve_tiers_elastic_258k_2.sh, serve_tiers_elastic_258k_3.sh,\n              serve_tiers_elastic_258k_4.sh, serve_tiers_elastic_258k_5.sh, serve.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py\nscripts/        serve.sh (the launcher), serve_elastic.sh, serve_tiers.sh, serve_tiers_elastic.sh,\n              serve_tiers_elastic_258k.sh, serve_tiers_elastic_258k_2.sh, serve_tiers_elastic_258k_3.sh,\n              serve_tiers_elastic_258k_4.sh, serve_tiers_elastic_258k_5.sh, serve_t.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py\nscripts/        serve.sh (the launcher), serve_elastic.sh, serve_tiers.sh, serve_tiers_elastic.sh,\n              serve_tiers_elastic_258k.sh, serve_tiers_elastic_258k_2.sh, serve_tiers_elastic_258k_3.sh,\n              serve_tiers_elastic_258k_4.sh, serve_tiers_elastic_258k_5.sh, serve_tiers.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py\nscripts/        serve.sh (the launcher), serve_elastic.sh, serve_tiers.sh, serve_tiers_elastic.sh,\n              serve_tiers_elastic_258k.sh, serve_tiers_elastic_258k_2.sh, serve_tiers_elastic_258k_3.sh,\n              serve_tiers_elastic_258k_4.sh, serve_tiers_elastic_258k_5.sh, serve_tiers_el.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py\nscripts/        serve.sh (the launcher), serve_elastic.sh, serve_tiers.sh, serve_tiers_elastic.sh,\n              serve_tiers_elastic_258k.sh, serve_tiers_elastic_258k_2.sh, serve_tiers_elastic_258k_3.sh,\n              serve_tiers_elastic_258k_4.sh, serve_tiers_elastic_258k_5.sh, serve_tiers_elastic.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py\nscripts/        serve.sh (the launcher), serve_elastic.sh, serve_tiers.sh, serve_tiers_elastic.sh,\n              serve_tiers_elastic_258k.sh, serve_tiers_elastic_258k_2.sh, serve_tiers_elastic_258k_3.sh,\n              serve_tiers_elastic_258k_4.sh, serve_tiers_elastic_258k_5.sh, serve_tiers_elastic_.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py\nscripts/        serve.sh (the launcher), serve_elastic.sh, serve_tiers.sh, serve_tiers_elastic.sh,\n              serve_tiers_elastic_258k.sh, serve_tiers_elastic_258k_2.sh, serve_tiers_elastic_258k_3.sh,\n              serve_tiers_elastic_258k_4.sh, serve_tiers_elastic_258k_5.sh, serve_tiers_elastic_2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py\nscripts/        serve.sh (the launcher), serve_elastic.sh, serve_tiers.sh, serve_tiers_elastic.sh,\n              serve_tiers_elastic_258k.sh, serve_tiers_elastic_258k_2.sh, serve_tiers_elastic_258k_3.sh,\n              serve_tiers_elastic_258k_4.sh, serve_tiers_elastic_258k_5.sh, serve_tiers_elastic_25.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py\nscripts/        serve.sh (the launcher), serve_elastic.sh, serve_tiers.sh, serve_tiers_elastic.sh,\n              serve_tiers_elastic_258k.sh, serve_tiers_elastic_258k_2.sh, serve_tiers_elastic_258k_3.sh,\n              serve_tiers_elastic_258k_4.sh, serve_tiers_elastic_258k_5.sh, serve_tiers_elastic_258.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py\nscripts/        serve.sh (the launcher), serve_elastic.sh, serve_tiers.sh, serve_tiers_elastic.sh,\n              serve_tiers_elastic_258k.sh, serve_tiers_elastic_258k_2.sh, serve_tiers_elastic_258k_3.sh,\n              serve_tiers_elastic_258k_4.sh, serve_tiers_elastic_258k_5.sh, serve_tiers_elastic_258k.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py\nscripts/        serve.sh (the launcher), serve_elastic.sh, serve_tiers.sh, serve_tiers_elastic.sh,\n              serve_tiers_elastic_258k.sh, serve_tiers_elastic_258k_2.sh, serve_tiers_elastic_258k_3.sh,\n              serve_tiers_elastic_258k_4.sh, serve_tiers_elastic_258k_5.sh, serve_tiers_elastic_258k_.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py\nscripts/        serve.sh (the launcher), serve_elastic.sh, serve_tiers.sh, serve_tiers_elastic.sh,\n              serve_tiers_elastic_258k.sh, serve_tiers_elastic_258k_2.sh, serve_tiers_elastic_258k_3.sh,\n              serve_tiers_elastic_258k_4.sh, serve_tiers_elastic_258k_5.sh, serve_tiers_elastic_258k_6.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py\nscripts/        serve.sh (the launcher), serve_elastic.sh, serve_tiers.sh, serve_tiers_elastic.sh,\n              serve_tiers_elastic_258k.sh, serve_tiers_elastic_258k_2.sh, serve_tiers_elastic_258k_3.sh,\n              serve_tiers_elastic_258k_4.sh, serve_tiers_elastic_258k_5.sh, serve_tiers_elastic_258k_6.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py\nscripts/        serve.sh (the launcher), serve_elastic.sh, serve_tiers.sh, serve_tiers_elastic.sh,\n              serve_tiers_elastic_258k.sh, serve_tiers_elastic_258k_2.sh, serve_tiers_elastic_258k_3.sh,\n              serve_tiers_elastic_258k_4.sh, serve_tiers_elastic_258k_5.sh, serve_tiers_elastic_258k_6.sh,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py\nscripts/        serve.sh (the launcher), serve_elastic.sh, serve_tiers.sh, serve_tiers_elastic.sh,\n              serve_tiers_elastic_258k.sh, serve_tiers_elastic_258k_2.sh, serve_tiers_elastic_258k_3.sh,\n              serve_tiers_elastic_258k_4.sh, serve_tiers_elastic_258k_5.sh, serve_tiers_elastic_258k_6.sh,\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py\nscripts/        serve.sh (the launcher), serve_elastic.sh, serve_tiers.sh, serve_tiers_elastic.sh,\n              serve_tiers_elastic_258k.sh, serve_tiers_elastic_258k_2.sh, serve_tiers_elastic_258k_3.sh,\n              serve_tiers_elastic_258k_4.sh, serve_tiers_elastic_258k_5.sh, serve_tiers_elastic_258k_6.sh,\n             .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py\nscripts/        serve.sh (the launcher), serve_elastic.sh, serve_tiers.sh, serve_tiers_elastic.sh,\n              serve_tiers_elastic_258k.sh, serve_tiers_elastic_258k_2.sh, serve_tiers_elastic_258k_3.sh,\n              serve_tiers_elastic_258k_4.sh, serve_tiers_elastic_258k_5.sh, serve_tiers_elastic_258k_6.sh,\n              serve.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py\nscripts/        serve.sh (the launcher), serve_elastic.sh, serve_tiers.sh, serve_tiers_elastic.sh,\n              serve_tiers_elastic_258k.sh, serve_tiers_elastic_258k_2.sh, serve_tiers_elastic_258k_3.sh,\n              serve_tiers_elastic_258k_4.sh, serve_tiers_elastic_258k_5.sh, serve_tiers_elastic_258k_6.sh,\n              serve_t.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py\nscripts/        serve.sh (the launcher), serve_elastic.sh, serve_tiers.sh, serve_tiers_elastic.sh,\n              serve_tiers_elastic_258k.sh, serve_tiers_elastic_258k_2.sh, serve_tiers_elastic_258k_3.sh,\n              serve_tiers_elastic_258k_4.sh, serve_tiers_elastic_258k_5.sh, serve_tiers_elastic_258k_6.sh,\n              serve_tiers.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py\nscripts/        serve.sh (the launcher), serve_elastic.sh, serve_tiers.sh, serve_tiers_elastic.sh,\n              serve_tiers_elastic_258k.sh, serve_tiers_elastic_258k_2.sh, serve_tiers_elastic_258k_3.sh,\n              serve_tiers_elastic_258k_4.sh, serve_tiers_elastic_258k_5.sh, serve_tiers_elastic_258k_6.sh,\n              serve_tiers_el.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py\nscripts/        serve.sh (the launcher), serve_elastic.sh, serve_tiers.sh, serve_tiers_elastic.sh,\n              serve_tiers_elastic_258k.sh, serve_tiers_elastic_258k_2.sh, serve_tiers_elastic_258k_3.sh,\n              serve_tiers_elastic_258k_4.sh, serve_tiers_elastic_258k_5.sh, serve_tiers_elastic_258k_6.sh,\n              serve_tiers_elastic.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py\nscripts/        serve.sh (the launcher), serve_elastic.sh, serve_tiers.sh, serve_tiers_elastic.sh,\n              serve_tiers_elastic_258k.sh, serve_tiers_elastic_258k_2.sh, serve_tiers_elastic_258k_3.sh,\n              serve_tiers_elastic_258k_4.sh, serve_tiers_elastic_258k_5.sh, serve_tiers_elastic_258k_6.sh,\n              serve_tiers_elastic_.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py\nscripts/        serve.sh (the launcher), serve_elastic.sh, serve_tiers.sh, serve_tiers_elastic.sh,\n              serve_tiers_elastic_258k.sh, serve_tiers_elastic_258k_2.sh, serve_tiers_elastic_258k_3.sh,\n              serve_tiers_elastic_258k_4.sh, serve_tiers_elastic_258k_5.sh, serve_tiers_elastic_258k_6.sh,\n              serve_tiers_elastic_2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py\nscripts/        serve.sh (the launcher), serve_elastic.sh, serve_tiers.sh, serve_tiers_elastic.sh,\n              serve_tiers_elastic_258k.sh, serve_tiers_elastic_258k_2.sh, serve_tiers_elastic_258k_3.sh,\n              serve_tiers_elastic_258k_4.sh, serve_tiers_elastic_258k_5.sh, serve_tiers_elastic_258k_6.sh,\n              serve_tiers_elastic_25.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py\nscripts/        serve.sh (the launcher), serve_elastic.sh, serve_tiers.sh, serve_tiers_elastic.sh,\n              serve_tiers_elastic_258k.sh, serve_tiers_elastic_258k_2.sh, serve_tiers_elastic_258k_3.sh,\n              serve_tiers_elastic_258k_4.sh, serve_tiers_elastic_258k_5.sh, serve_tiers_elastic_258k_6.sh,\n              serve_tiers_elastic_258.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py\nscripts/        serve.sh (the launcher), serve_elastic.sh, serve_tiers.sh, serve_tiers_elastic.sh,\n              serve_tiers_elastic_258k.sh, serve_tiers_elastic_258k_2.sh, serve_tiers_elastic_258k_3.sh,\n              serve_tiers_elastic_258k_4.sh, serve_tiers_elastic_258k_5.sh, serve_tiers_elastic_258k_6.sh,\n              serve_tiers_elastic_258k.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py\nscripts/        serve.sh (the launcher), serve_elastic.sh, serve_tiers.sh, serve_tiers_elastic.sh,\n              serve_tiers_elastic_258k.sh, serve_tiers_elastic_258k_2.sh, serve_tiers_elastic_258k_3.sh,\n              serve_tiers_elastic_258k_4.sh, serve_tiers_elastic_258k_5.sh, serve_tiers_elastic_258k_6.sh,\n              serve_tiers_elastic_258k_.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py\nscripts/        serve.sh (the launcher), serve_elastic.sh, serve_tiers.sh, serve_tiers_elastic.sh,\n              serve_tiers_elastic_258k.sh, serve_tiers_elastic_258k_2.sh, serve_tiers_elastic_258k_3.sh,\n              serve_tiers_elastic_258k_4.sh, serve_tiers_elastic_258k_5.sh, serve_tiers_elastic_258k_6.sh,\n              serve_tiers_elastic_258k_7.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py\nscripts/        serve.sh (the launcher), serve_elastic.sh, serve_tiers.sh, serve_tiers_elastic.sh,\n              serve_tiers_elastic_258k.sh, serve_tiers_elastic_258k_2.sh, serve_tiers_elastic_258k_3.sh,\n              serve_tiers_elastic_258k_4.sh, serve_tiers_elastic_258k_5.sh, serve_tiers_elastic_258k_6.sh,\n              serve_tiers_elastic_258k_7.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py\nscripts/        serve.sh (the launcher), serve_elastic.sh, serve_tiers.sh, serve_tiers_elastic.sh,\n              serve_tiers_elastic_258k.sh, serve_tiers_elastic_258k_2.sh, serve_tiers_elastic_258k_3.sh,\n              serve_tiers_elastic_258k_4.sh, serve_tiers_elastic_258k_5.sh, serve_tiers_elastic_258k_6.sh,\n              serve_tiers_elastic_258k_7.sh,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py\nscripts/        serve.sh (the launcher), serve_elastic.sh, serve_tiers.sh, serve_tiers_elastic.sh,\n              serve_tiers_elastic_258k.sh, serve_tiers_elastic_258k_2.sh, serve_tiers_elastic_258k_3.sh,\n              serve_tiers_elastic_258k_4.sh, serve_tiers_elastic_258k_5.sh, serve_tiers_elastic_258k_6.sh,\n              serve_tiers_elastic_258k_7.sh, serve.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py\nscripts/        serve.sh (the launcher), serve_elastic.sh, serve_tiers.sh, serve_tiers_elastic.sh,\n              serve_tiers_elastic_258k.sh, serve_tiers_elastic_258k_2.sh, serve_tiers_elastic_258k_3.sh,\n              serve_tiers_elastic_258k_4.sh, serve_tiers_elastic_258k_5.sh, serve_tiers_elastic_258k_6.sh,\n              serve_tiers_elastic_258k_7.sh, serve_t.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py\nscripts/        serve.sh (the launcher), serve_elastic.sh, serve_tiers.sh, serve_tiers_elastic.sh,\n              serve_tiers_elastic_258k.sh, serve_tiers_elastic_258k_2.sh, serve_tiers_elastic_258k_3.sh,\n              serve_tiers_elastic_258k_4.sh, serve_tiers_elastic_258k_5.sh, serve_tiers_elastic_258k_6.sh,\n              serve_tiers_elastic_258k_7.sh, serve_tiers.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py\nscripts/        serve.sh (the launcher), serve_elastic.sh, serve_tiers.sh, serve_tiers_elastic.sh,\n              serve_tiers_elastic_258k.sh, serve_tiers_elastic_258k_2.sh, serve_tiers_elastic_258k_3.sh,\n              serve_tiers_elastic_258k_4.sh, serve_tiers_elastic_258k_5.sh, serve_tiers_elastic_258k_6.sh,\n              serve_tiers_elastic_258k_7.sh, serve_tiers_el.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py\nscripts/        serve.sh (the launcher), serve_elastic.sh, serve_tiers.sh, serve_tiers_elastic.sh,\n              serve_tiers_elastic_258k.sh, serve_tiers_elastic_258k_2.sh, serve_tiers_elastic_258k_3.sh,\n              serve_tiers_elastic_258k_4.sh, serve_tiers_elastic_258k_5.sh, serve_tiers_elastic_258k_6.sh,\n              serve_tiers_elastic_258k_7.sh, serve_tiers_elastic.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe `PLE` variable must point at `ple/` (the directory holding `ple.json` and `ple.f8_e4m3.bin`), not at the\nmodel root. The `SGLANG`/`VENV`/`MODEL`/`PLE` variables are the launcher's defaults; the `--sglang`/`--venv`/\n`--model`/`--ple` flags override them.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control modules, the PLE patch\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV cache, PLE embedding, int8 ring\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py\nscripts/        serve.sh (the launcher), serve_elastic.sh, serve_tiers.sh, serve_tiers_elastic.sh,\n              serve_tiers_elastic_258k.sh, serve_tiers_elastic_258k_2.sh, serve_tiers_elastic_258k_3.sh,\n              serve_tiers_elastic_258k_4.sh, serve_tiers_elastic_258k_5.sh, serve_tiers_elastic_258k_6.sh,\n              serve_tiers_elastic_258k_7.sh, serve_tiers_elastic_",
 "ttft_seconds": 1.0807740320015,
 "status": "ok",
 "tg_tokens_per_s": 37.359287960002405,
 "decode_seconds_per_token": 0.026767105440302287,
 "individual_token_timing": true,
 "event_intervals_ms": {
  "n": 511,
  "median": 26.465361996088177,
  "p95": 32.935817012912594,
  "mean": 26.767105440302288
 },
 "class": "prose",
 "requested_input_tokens": 2048
}
