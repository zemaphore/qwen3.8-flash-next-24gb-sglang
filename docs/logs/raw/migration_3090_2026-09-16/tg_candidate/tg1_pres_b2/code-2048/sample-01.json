{
 "started_utc": "2026-09-17T01:04:59.424108+00:00",
 "wall_seconds": 15.691717430992867,
 "prompt_tokens": 2048,
 "completion_tokens": 512,
 "stream_events": 512,
 "ended_utc": "2026-09-17T01:05:15.115937+00:00",
 "warmup": false,
 "sample_index": 1,
 "requested_decode_tokens": 512,
 "gpu": {
  "n": 56,
  "temperature_c": {
   "min": 77.0,
   "max": 78.0,
   "mean": 77.07142857142857
  },
  "power_w": {
   "min": 256.69,
   "max": 276.58,
   "mean": 264.3623214285714
  },
  "util_pct": {
   "min": 88.0,
   "max": 100.0,
   "mean": 91.51785714285714
  },
  "clock_sm_mhz": {
   "min": 1515.0,
   "max": 1920.0,
   "mean": 1894.2857142857142
  },
  "clock_mem_mhz": {
   "min": 9501.0,
   "max": 9501.0,
   "mean": 9501.0
  },
  "memory_free_mib": {
   "min": 2540.0
  }
 },
 "output_text_sha256": "f40ce14d07e13d3f3ca52bb3bbd45b136af875de8b93ef85f5dabd8e1c71886b",
 "output_text": "++       +        b+        b_z+        b_zp+        b_zp_num+        b_zp_num =+        b_zp_num = +        b_zp_num = 1+        b_zp_num = 12+        b_zp_num = 128+        b_zp_num = 128\n+        b_zp_num = 128\n++        b_zp_num = 128\n+   +        b_zp_num = 128\n+    elif+        b_zp_num = 128\n+    elif has+        b_zp_num = 128\n+    elif has_z+        b_zp_num = 128\n+    elif has_zp+        b_zp_num = 128\n+    elif has_zp and+        b_zp_num = 128\n+    elif has_zp and use+        b_zp_num = 128\n+    elif has_zp and use_int+        b_zp_num = 128\n+    elif has_zp and use_int2+        b_zp_num = 128\n+    elif has_zp and use_int2_w+        b_zp_num = 128\n+    elif has_zp and use_int2_w2+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_z+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_sh+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16)+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) *+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+   +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_z+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_z+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_sh+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :]+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] %+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2)+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) *+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+   +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    #+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # ------------------------------------------------+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+   +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    #+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+   +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    #+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]`+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+   +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    #+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp3+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+   +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    #+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator`+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+   +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N),+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float3+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+   +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.c+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        #+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B, +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        #+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M),+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N)+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_K+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+           +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None]+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] <+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+           +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask &+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :]+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] <+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+           +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None]+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] &+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+           +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+           +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+           +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+           +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n+        if+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n+        if use+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n+        if use_int+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n+        if use_int2+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n+        if use_int2_w+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n+        if use_int2_w2+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n+        if use_int2_w2a+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n+        if use_int2_w2a1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n+        if use_int2_w2a16+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n+        if use_int2_w2a16:+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n+        if use_int2_w2a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n+        if use_int2_w2a16:\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n+        if use_int2_w2a16:\n+           +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n+        if use_int2_w2a16:\n+            b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n+        if use_int2_w2a16:\n+            b_word+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n+        if use_int2_w2a16:\n+            b_word =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n+        if use_int2_w2a16:\n+            b_word = tl+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n+        if use_int2_w2a16:\n+            b_word = tl.load+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n+        if use_int2_w2a16:\n+            b_word = tl.load(b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n+        if use_int2_w2a16:\n+            b_word = tl.load(b_ptrs+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n+        if use_int2_w2a16:\n+            b_word = tl.load(b_ptrs,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n+        if use_int2_w2a16:\n+            b_word = tl.load(b_ptrs, other+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n+        if use_int2_w2a16:\n+            b_word = tl.load(b_ptrs, other=+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n+        if use_int2_w2a16:\n+            b_word = tl.load(b_ptrs, other=0+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n+        if use_int2_w2a16:\n+            b_word = tl.load(b_ptrs, other=0)+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n+        if use_int2_w2a16:\n+            b_word = tl.load(b_ptrs, other=0)\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n+        if use_int2_w2a16:\n+            b_word = tl.load(b_ptrs, other=0)\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n+        if use_int2_w2a16:\n+            b_word = tl.load(b_ptrs, other=0)\n+           +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n+        if use_int2_w2a16:\n+            b_word = tl.load(b_ptrs, other=0)\n+            b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n+        if use_int2_w2a16:\n+            b_word = tl.load(b_ptrs, other=0)\n+            b =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n+        if use_int2_w2a16:\n+            b_word = tl.load(b_ptrs, other=0)\n+            b = (+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n+        if use_int2_w2a16:\n+            b_word = tl.load(b_ptrs, other=0)\n+            b = (\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n+        if use_int2_w2a16:\n+            b_word = tl.load(b_ptrs, other=0)\n+            b = (\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n+        if use_int2_w2a16:\n+            b_word = tl.load(b_ptrs, other=0)\n+            b = (\n+               +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n+        if use_int2_w2a16:\n+            b_word = tl.load(b_ptrs, other=0)\n+            b = (\n+                (+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n+        if use_int2_w2a16:\n+            b_word = tl.load(b_ptrs, other=0)\n+            b = (\n+                (b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n+        if use_int2_w2a16:\n+            b_word = tl.load(b_ptrs, other=0)\n+            b = (\n+                (b_word+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n+        if use_int2_w2a16:\n+            b_word = tl.load(b_ptrs, other=0)\n+            b = (\n+                (b_word[:,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n+        if use_int2_w2a16:\n+            b_word = tl.load(b_ptrs, other=0)\n+            b = (\n+                (b_word[:, None+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n+        if use_int2_w2a16:\n+            b_word = tl.load(b_ptrs, other=0)\n+            b = (\n+                (b_word[:, None,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n+        if use_int2_w2a16:\n+            b_word = tl.load(b_ptrs, other=0)\n+            b = (\n+                (b_word[:, None, None+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n+        if use_int2_w2a16:\n+            b_word = tl.load(b_ptrs, other=0)\n+            b = (\n+                (b_word[:, None, None]+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n+        if use_int2_w2a16:\n+            b_word = tl.load(b_ptrs, other=0)\n+            b = (\n+                (b_word[:, None, None] >>+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n+        if use_int2_w2a16:\n+            b_word = tl.load(b_ptrs, other=0)\n+            b = (\n+                (b_word[:, None, None] >> b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n+        if use_int2_w2a16:\n+            b_word = tl.load(b_ptrs, other=0)\n+            b = (\n+                (b_word[:, None, None] >> b_sh+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n+        if use_int2_w2a16:\n+            b_word = tl.load(b_ptrs, other=0)\n+            b = (\n+                (b_word[:, None, None] >> b_shifter+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n+        if use_int2_w2a16:\n+            b_word = tl.load(b_ptrs, other=0)\n+            b = (\n+                (b_word[:, None, None] >> b_shifter)+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n+        if use_int2_w2a16:\n+            b_word = tl.load(b_ptrs, other=0)\n+            b = (\n+                (b_word[:, None, None] >> b_shifter) &+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n+        if use_int2_w2a16:\n+            b_word = tl.load(b_ptrs, other=0)\n+            b = (\n+                (b_word[:, None, None] >> b_shifter) & +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n+        if use_int2_w2a16:\n+            b_word = tl.load(b_ptrs, other=0)\n+            b = (\n+                (b_word[:, None, None] >> b_shifter) & 3+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n+        if use_int2_w2a16:\n+            b_word = tl.load(b_ptrs, other=0)\n+            b = (\n+                (b_word[:, None, None] >> b_shifter) & 3\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n+        if use_int2_w2a16:\n+            b_word = tl.load(b_ptrs, other=0)\n+            b = (\n+                (b_word[:, None, None] >> b_shifter) & 3\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n+        if use_int2_w2a16:\n+            b_word = tl.load(b_ptrs, other=0)\n+            b = (\n+                (b_word[:, None, None] >> b_shifter) & 3\n+           +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n+        if use_int2_w2a16:\n+            b_word = tl.load(b_ptrs, other=0)\n+            b = (\n+                (b_word[:, None, None] >> b_shifter) & 3\n+            ).+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n+        if use_int2_w2a16:\n+            b_word = tl.load(b_ptrs, other=0)\n+            b = (\n+                (b_word[:, None, None] >> b_shifter) & 3\n+            ).reshape+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n+        if use_int2_w2a16:\n+            b_word = tl.load(b_ptrs, other=0)\n+            b = (\n+                (b_word[:, None, None] >> b_shifter) & 3\n+            ).reshape(B+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n+        if use_int2_w2a16:\n+            b_word = tl.load(b_ptrs, other=0)\n+            b = (\n+                (b_word[:, None, None] >> b_shifter) & 3\n+            ).reshape(BLOCK+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n+        if use_int2_w2a16:\n+            b_word = tl.load(b_ptrs, other=0)\n+            b = (\n+                (b_word[:, None, None] >> b_shifter) & 3\n+            ).reshape(BLOCK_SIZE+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n+        if use_int2_w2a16:\n+            b_word = tl.load(b_ptrs, other=0)\n+            b = (\n+                (b_word[:, None, None] >> b_shifter) & 3\n+            ).reshape(BLOCK_SIZE_K+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n+        if use_int2_w2a16:\n+            b_word = tl.load(b_ptrs, other=0)\n+            b = (\n+                (b_word[:, None, None] >> b_shifter) & 3\n+            ).reshape(BLOCK_SIZE_K,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n+        if use_int2_w2a16:\n+            b_word = tl.load(b_ptrs, other=0)\n+            b = (\n+                (b_word[:, None, None] >> b_shifter) & 3\n+            ).reshape(BLOCK_SIZE_K, BLOCK+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n+        if use_int2_w2a16:\n+            b_word = tl.load(b_ptrs, other=0)\n+            b = (\n+                (b_word[:, None, None] >> b_shifter) & 3\n+            ).reshape(BLOCK_SIZE_K, BLOCK_SIZE+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n+        if use_int2_w2a16:\n+            b_word = tl.load(b_ptrs, other=0)\n+            b = (\n+                (b_word[:, None, None] >> b_shifter) & 3\n+            ).reshape(BLOCK_SIZE_K, BLOCK_SIZE_N+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n+        if use_int2_w2a16:\n+            b_word = tl.load(b_ptrs, other=0)\n+            b = (\n+                (b_word[:, None, None] >> b_shifter) & 3\n+            ).reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n+        if use_int2_w2a16:\n+            b_word = tl.load(b_ptrs, other=0)\n+            b = (\n+                (b_word[:, None, None] >> b_shifter) & 3\n+            ).reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n+        if use_int2_w2a16:\n+            b_word = tl.load(b_ptrs, other=0)\n+            b = (\n+                (b_word[:, None, None] >> b_shifter) & 3\n+            ).reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n+        if use_int2_w2a16:\n+            b_word = tl.load(b_ptrs, other=0)\n+            b = (\n+                (b_word[:, None, None] >> b_shifter) & 3\n+            ).reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+           +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n+        if use_int2_w2a16:\n+            b_word = tl.load(b_ptrs, other=0)\n+            b = (\n+                (b_word[:, None, None] >> b_shifter) & 3\n+            ).reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            if+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n+        if use_int2_w2a16:\n+            b_word = tl.load(b_ptrs, other=0)\n+            b = (\n+                (b_word[:, None, None] >> b_shifter) & 3\n+            ).reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            if has+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n+        if use_int2_w2a16:\n+            b_word = tl.load(b_ptrs, other=0)\n+            b = (\n+                (b_word[:, None, None] >> b_shifter) & 3\n+            ).reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            if has_z+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n+        if use_int2_w2a16:\n+            b_word = tl.load(b_ptrs, other=0)\n+            b = (\n+                (b_word[:, None, None] >> b_shifter) & 3\n+            ).reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            if has_zp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n+        if use_int2_w2a16:\n+            b_word = tl.load(b_ptrs, other=0)\n+            b = (\n+                (b_word[:, None, None] >> b_shifter) & 3\n+            ).reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            if has_zp:+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n+        if use_int2_w2a16:\n+            b_word = tl.load(b_ptrs, other=0)\n+            b = (\n+                (b_word[:, None, None] >> b_shifter) & 3\n+            ).reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            if has_zp:\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n+        if use_int2_w2a16:\n+            b_word = tl.load(b_ptrs, other=0)\n+            b = (\n+                (b_word[:, None, None] >> b_shifter) & 3\n+            ).reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            if has_zp:\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n+        if use_int2_w2a16:\n+            b_word = tl.load(b_ptrs, other=0)\n+            b = (\n+                (b_word[:, None, None] >> b_shifter) & 3\n+            ).reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            if has_zp:\n+               +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n+        if use_int2_w2a16:\n+            b_word = tl.load(b_ptrs, other=0)\n+            b = (\n+                (b_word[:, None, None] >> b_shifter) & 3\n+            ).reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            if has_zp:\n+                b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n+        if use_int2_w2a16:\n+            b_word = tl.load(b_ptrs, other=0)\n+            b = (\n+                (b_word[:, None, None] >> b_shifter) & 3\n+            ).reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            if has_zp:\n+                b =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n+        if use_int2_w2a16:\n+            b_word = tl.load(b_ptrs, other=0)\n+            b = (\n+                (b_word[:, None, None] >> b_shifter) & 3\n+            ).reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            if has_zp:\n+                b = b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n+        if use_int2_w2a16:\n+            b_word = tl.load(b_ptrs, other=0)\n+            b = (\n+                (b_word[:, None, None] >> b_shifter) & 3\n+            ).reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            if has_zp:\n+                b = b -+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n+        if use_int2_w2a16:\n+            b_word = tl.load(b_ptrs, other=0)\n+            b = (\n+                (b_word[:, None, None] >> b_shifter) & 3\n+            ).reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            if has_zp:\n+                b = b - ((+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n+        if use_int2_w2a16:\n+            b_word = tl.load(b_ptrs, other=0)\n+            b = (\n+                (b_word[:, None, None] >> b_shifter) & 3\n+            ).reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            if has_zp:\n+                b = b - ((b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n+        if use_int2_w2a16:\n+            b_word = tl.load(b_ptrs, other=0)\n+            b = (\n+                (b_word[:, None, None] >> b_shifter) & 3\n+            ).reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            if has_zp:\n+                b = b - ((b_z+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n+        if use_int2_w2a16:\n+            b_word = tl.load(b_ptrs, other=0)\n+            b = (\n+                (b_word[:, None, None] >> b_shifter) & 3\n+            ).reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            if has_zp:\n+                b = b - ((b_zp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n+        if use_int2_w2a16:\n+            b_word = tl.load(b_ptrs, other=0)\n+            b = (\n+                (b_word[:, None, None] >> b_shifter) & 3\n+            ).reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            if has_zp:\n+                b = b - ((b_zp_word+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n+        if use_int2_w2a16:\n+            b_word = tl.load(b_ptrs, other=0)\n+            b = (\n+                (b_word[:, None, None] >> b_shifter) & 3\n+            ).reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            if has_zp:\n+                b = b - ((b_zp_word[:,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n+        if use_int2_w2a16:\n+            b_word = tl.load(b_ptrs, other=0)\n+            b = (\n+                (b_word[:, None, None] >> b_shifter) & 3\n+            ).reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            if has_zp:\n+                b = b - ((b_zp_word[:, None+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n+        if use_int2_w2a16:\n+            b_word = tl.load(b_ptrs, other=0)\n+            b = (\n+                (b_word[:, None, None] >> b_shifter) & 3\n+            ).reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            if has_zp:\n+                b = b - ((b_zp_word[:, None,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n+        if use_int2_w2a16:\n+            b_word = tl.load(b_ptrs, other=0)\n+            b = (\n+                (b_word[:, None, None] >> b_shifter) & 3\n+            ).reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            if has_zp:\n+                b = b - ((b_zp_word[:, None, None+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n+        if use_int2_w2a16:\n+            b_word = tl.load(b_ptrs, other=0)\n+            b = (\n+                (b_word[:, None, None] >> b_shifter) & 3\n+            ).reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            if has_zp:\n+                b = b - ((b_zp_word[:, None, None]+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n+        if use_int2_w2a16:\n+            b_word = tl.load(b_ptrs, other=0)\n+            b = (\n+                (b_word[:, None, None] >> b_shifter) & 3\n+            ).reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            if has_zp:\n+                b = b - ((b_zp_word[:, None, None] >>+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n+        if use_int2_w2a16:\n+            b_word = tl.load(b_ptrs, other=0)\n+            b = (\n+                (b_word[:, None, None] >> b_shifter) & 3\n+            ).reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            if has_zp:\n+                b = b - ((b_zp_word[:, None, None] >> b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n+        if use_int2_w2a16:\n+            b_word = tl.load(b_ptrs, other=0)\n+            b = (\n+                (b_word[:, None, None] >> b_shifter) & 3\n+            ).reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            if has_zp:\n+                b = b - ((b_zp_word[:, None, None] >> b_z+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n+        if use_int2_w2a16:\n+            b_word = tl.load(b_ptrs, other=0)\n+            b = (\n+                (b_word[:, None, None] >> b_shifter) & 3\n+            ).reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            if has_zp:\n+                b = b - ((b_zp_word[:, None, None] >> b_zp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n+        if use_int2_w2a16:\n+            b_word = tl.load(b_ptrs, other=0)\n+            b = (\n+                (b_word[:, None, None] >> b_shifter) & 3\n+            ).reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            if has_zp:\n+                b = b - ((b_zp_word[:, None, None] >> b_zp_sh+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n+        if use_int2_w2a16:\n+            b_word = tl.load(b_ptrs, other=0)\n+            b = (\n+                (b_word[:, None, None] >> b_shifter) & 3\n+            ).reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            if has_zp:\n+                b = b - ((b_zp_word[:, None, None] >> b_zp_shifter+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n+        if use_int2_w2a16:\n+            b_word = tl.load(b_ptrs, other=0)\n+            b = (\n+                (b_word[:, None, None] >> b_shifter) & 3\n+            ).reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            if has_zp:\n+                b = b - ((b_zp_word[:, None, None] >> b_zp_shifter)+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n+        if use_int2_w2a16:\n+            b_word = tl.load(b_ptrs, other=0)\n+            b = (\n+                (b_word[:, None, None] >> b_shifter) & 3\n+            ).reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            if has_zp:\n+                b = b - ((b_zp_word[:, None, None] >> b_zp_shifter) &+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n+        if use_int2_w2a16:\n+            b_word = tl.load(b_ptrs, other=0)\n+            b = (\n+                (b_word[:, None, None] >> b_shifter) & 3\n+            ).reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            if has_zp:\n+                b = b - ((b_zp_word[:, None, None] >> b_zp_shifter) & +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n+        if use_int2_w2a16:\n+            b_word = tl.load(b_ptrs, other=0)\n+            b = (\n+                (b_word[:, None, None] >> b_shifter) & 3\n+            ).reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            if has_zp:\n+                b = b - ((b_zp_word[:, None, None] >> b_zp_shifter) & 3+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n+        if use_int2_w2a16:\n+            b_word = tl.load(b_ptrs, other=0)\n+            b = (\n+                (b_word[:, None, None] >> b_shifter) & 3\n+            ).reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            if has_zp:\n+                b = b - ((b_zp_word[:, None, None] >> b_zp_shifter) & 3)+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n+        if use_int2_w2a16:\n+            b_word = tl.load(b_ptrs, other=0)\n+            b = (\n+                (b_word[:, None, None] >> b_shifter) & 3\n+            ).reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            if has_zp:\n+                b = b - ((b_zp_word[:, None, None] >> b_zp_shifter) & 3)\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n+        if use_int2_w2a16:\n+            b_word = tl.load(b_ptrs, other=0)\n+            b = (\n+                (b_word[:, None, None] >> b_shifter) & 3\n+            ).reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            if has_zp:\n+                b = b - ((b_zp_word[:, None, None] >> b_zp_shifter) & 3)\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n+        if use_int2_w2a16:\n+            b_word = tl.load(b_ptrs, other=0)\n+            b = (\n+                (b_word[:, None, None] >> b_shifter) & 3\n+            ).reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            if has_zp:\n+                b = b - ((b_zp_word[:, None, None] >> b_zp_shifter) & 3)\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n+        if use_int2_w2a16:\n+            b_word = tl.load(b_ptrs, other=0)\n+            b = (\n+                (b_word[:, None, None] >> b_shifter) & 3\n+            ).reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            if has_zp:\n+                b = b - ((b_zp_word[:, None, None] >> b_zp_shifter) & 3)\n+        elif+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n+        if use_int2_w2a16:\n+            b_word = tl.load(b_ptrs, other=0)\n+            b = (\n+                (b_word[:, None, None] >> b_shifter) & 3\n+            ).reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            if has_zp:\n+                b = b - ((b_zp_word[:, None, None] >> b_zp_shifter) & 3)\n+        elif use+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n+        if use_int2_w2a16:\n+            b_word = tl.load(b_ptrs, other=0)\n+            b = (\n+                (b_word[:, None, None] >> b_shifter) & 3\n+            ).reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            if has_zp:\n+                b = b - ((b_zp_word[:, None, None] >> b_zp_shifter) & 3)\n+        elif use_int+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n+        if use_int2_w2a16:\n+            b_word = tl.load(b_ptrs, other=0)\n+            b = (\n+                (b_word[:, None, None] >> b_shifter) & 3\n+            ).reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            if has_zp:\n+                b = b - ((b_zp_word[:, None, None] >> b_zp_shifter) & 3)\n+        elif use_int4+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n+        if use_int2_w2a16:\n+            b_word = tl.load(b_ptrs, other=0)\n+            b = (\n+                (b_word[:, None, None] >> b_shifter) & 3\n+            ).reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            if has_zp:\n+                b = b - ((b_zp_word[:, None, None] >> b_zp_shifter) & 3)\n+        elif use_int4_w+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n+        if use_int2_w2a16:\n+            b_word = tl.load(b_ptrs, other=0)\n+            b = (\n+                (b_word[:, None, None] >> b_shifter) & 3\n+            ).reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            if has_zp:\n+                b = b - ((b_zp_word[:, None, None] >> b_zp_shifter) & 3)\n+        elif use_int4_w4+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n+        if use_int2_w2a16:\n+            b_word = tl.load(b_ptrs, other=0)\n+            b = (\n+                (b_word[:, None, None] >> b_shifter) & 3\n+            ).reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            if has_zp:\n+                b = b - ((b_zp_word[:, None, None] >> b_zp_shifter) & 3)\n+        elif use_int4_w4a+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n+        if use_int2_w2a16:\n+            b_word = tl.load(b_ptrs, other=0)\n+            b = (\n+                (b_word[:, None, None] >> b_shifter) & 3\n+            ).reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            if has_zp:\n+                b = b - ((b_zp_word[:, None, None] >> b_zp_shifter) & 3)\n+        elif use_int4_w4a1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n+        if use_int2_w2a16:\n+            b_word = tl.load(b_ptrs, other=0)\n+            b = (\n+                (b_word[:, None, None] >> b_shifter) & 3\n+            ).reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            if has_zp:\n+                b = b - ((b_zp_word[:, None, None] >> b_zp_shifter) & 3)\n+        elif use_int4_w4a16+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n+        if use_int2_w2a16:\n+            b_word = tl.load(b_ptrs, other=0)\n+            b = (\n+                (b_word[:, None, None] >> b_shifter) & 3\n+            ).reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            if has_zp:\n+                b = b - ((b_zp_word[:, None, None] >> b_zp_shifter) & 3)\n+        elif use_int4_w4a16:+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n+        if use_int2_w2a16:\n+            b_word = tl.load(b_ptrs, other=0)\n+            b = (\n+                (b_word[:, None, None] >> b_shifter) & 3\n+            ).reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            if has_zp:\n+                b = b - ((b_zp_word[:, None, None] >> b_zp_shifter) & 3)\n+        elif use_int4_w4a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n+        if use_int2_w2a16:\n+            b_word = tl.load(b_ptrs, other=0)\n+            b = (\n+                (b_word[:, None, None] >> b_shifter) & 3\n+            ).reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            if has_zp:\n+                b = b - ((b_zp_word[:, None, None] >> b_zp_shifter) & 3)\n+        elif use_int4_w4a16:\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n+        if use_int2_w2a16:\n+            b_word = tl.load(b_ptrs, other=0)\n+            b = (\n+                (b_word[:, None, None] >> b_shifter) & 3\n+            ).reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            if has_zp:\n+                b = b - ((b_zp_word[:, None, None] >> b_zp_shifter) & 3)\n+        elif use_int4_w4a16:\n+           +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n+        if use_int2_w2a16:\n+            b_word = tl.load(b_ptrs, other=0)\n+            b = (\n+                (b_word[:, None, None] >> b_shifter) & 3\n+            ).reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            if has_zp:\n+                b = b - ((b_zp_word[:, None, None] >> b_zp_shifter) & 3)\n+        elif use_int4_w4a16:\n+            b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n+        if use_int2_w2a16:\n+            b_word = tl.load(b_ptrs, other=0)\n+            b = (\n+                (b_word[:, None, None] >> b_shifter) & 3\n+            ).reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            if has_zp:\n+                b = b - ((b_zp_word[:, None, None] >> b_zp_shifter) & 3)\n+        elif use_int4_w4a16:\n+            b =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n+        if use_int2_w2a16:\n+            b_word = tl.load(b_ptrs, other=0)\n+            b = (\n+                (b_word[:, None, None] >> b_shifter) & 3\n+            ).reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            if has_zp:\n+                b = b - ((b_zp_word[:, None, None] >> b_zp_shifter) & 3)\n+        elif use_int4_w4a16:\n+            b = tl+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n+        if use_int2_w2a16:\n+            b_word = tl.load(b_ptrs, other=0)\n+            b = (\n+                (b_word[:, None, None] >> b_shifter) & 3\n+            ).reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            if has_zp:\n+                b = b - ((b_zp_word[:, None, None] >> b_zp_shifter) & 3)\n+        elif use_int4_w4a16:\n+            b = tl.load+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n+        if use_int2_w2a16:\n+            b_word = tl.load(b_ptrs, other=0)\n+            b = (\n+                (b_word[:, None, None] >> b_shifter) & 3\n+            ).reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            if has_zp:\n+                b = b - ((b_zp_word[:, None, None] >> b_zp_shifter) & 3)\n+        elif use_int4_w4a16:\n+            b = tl.load(b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n+        if use_int2_w2a16:\n+            b_word = tl.load(b_ptrs, other=0)\n+            b = (\n+                (b_word[:, None, None] >> b_shifter) & 3\n+            ).reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            if has_zp:\n+                b = b - ((b_zp_word[:, None, None] >> b_zp_shifter) & 3)\n+        elif use_int4_w4a16:\n+            b = tl.load(b_ptrs+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n+        if use_int2_w2a16:\n+            b_word = tl.load(b_ptrs, other=0)\n+            b = (\n+                (b_word[:, None, None] >> b_shifter) & 3\n+            ).reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            if has_zp:\n+                b = b - ((b_zp_word[:, None, None] >> b_zp_shifter) & 3)\n+        elif use_int4_w4a16:\n+            b = tl.load(b_ptrs,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n+        if use_int2_w2a16:\n+            b_word = tl.load(b_ptrs, other=0)\n+            b = (\n+                (b_word[:, None, None] >> b_shifter) & 3\n+            ).reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            if has_zp:\n+                b = b - ((b_zp_word[:, None, None] >> b_zp_shifter) & 3)\n+        elif use_int4_w4a16:\n+            b = tl.load(b_ptrs, mask+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n+        if use_int2_w2a16:\n+            b_word = tl.load(b_ptrs, other=0)\n+            b = (\n+                (b_word[:, None, None] >> b_shifter) & 3\n+            ).reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            if has_zp:\n+                b = b - ((b_zp_word[:, None, None] >> b_zp_shifter) & 3)\n+        elif use_int4_w4a16:\n+            b = tl.load(b_ptrs, mask=k+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n+        if use_int2_w2a16:\n+            b_word = tl.load(b_ptrs, other=0)\n+            b = (\n+                (b_word[:, None, None] >> b_shifter) & 3\n+            ).reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            if has_zp:\n+                b = b - ((b_zp_word[:, None, None] >> b_zp_shifter) & 3)\n+        elif use_int4_w4a16:\n+            b = tl.load(b_ptrs, mask=k_mask+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n+        if use_int2_w2a16:\n+            b_word = tl.load(b_ptrs, other=0)\n+            b = (\n+                (b_word[:, None, None] >> b_shifter) & 3\n+            ).reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            if has_zp:\n+                b = b - ((b_zp_word[:, None, None] >> b_zp_shifter) & 3)\n+        elif use_int4_w4a16:\n+            b = tl.load(b_ptrs, mask=k_mask,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n+        if use_int2_w2a16:\n+            b_word = tl.load(b_ptrs, other=0)\n+            b = (\n+                (b_word[:, None, None] >> b_shifter) & 3\n+            ).reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            if has_zp:\n+                b = b - ((b_zp_word[:, None, None] >> b_zp_shifter) & 3)\n+        elif use_int4_w4a16:\n+            b = tl.load(b_ptrs, mask=k_mask, other+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n+        if use_int2_w2a16:\n+            b_word = tl.load(b_ptrs, other=0)\n+            b = (\n+                (b_word[:, None, None] >> b_shifter) & 3\n+            ).reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            if has_zp:\n+                b = b - ((b_zp_word[:, None, None] >> b_zp_shifter) & 3)\n+        elif use_int4_w4a16:\n+            b = tl.load(b_ptrs, mask=k_mask, other=+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n+        if use_int2_w2a16:\n+            b_word = tl.load(b_ptrs, other=0)\n+            b = (\n+                (b_word[:, None, None] >> b_shifter) & 3\n+            ).reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            if has_zp:\n+                b = b - ((b_zp_word[:, None, None] >> b_zp_shifter) & 3)\n+        elif use_int4_w4a16:\n+            b = tl.load(b_ptrs, mask=k_mask, other=0+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n+        if use_int2_w2a16:\n+            b_word = tl.load(b_ptrs, other=0)\n+            b = (\n+                (b_word[:, None, None] >> b_shifter) & 3\n+            ).reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            if has_zp:\n+                b = b - ((b_zp_word[:, None, None] >> b_zp_shifter) & 3)\n+        elif use_int4_w4a16:\n+            b = tl.load(b_ptrs, mask=k_mask, other=0)+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n+        if use_int2_w2a16:\n+            b_word = tl.load(b_ptrs, other=0)\n+            b = (\n+                (b_word[:, None, None] >> b_shifter) & 3\n+            ).reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            if has_zp:\n+                b = b - ((b_zp_word[:, None, None] >> b_zp_shifter) & 3)\n+        elif use_int4_w4a16:\n+            b = tl.load(b_ptrs, mask=k_mask, other=0)\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n+        if use_int2_w2a16:\n+            b_word = tl.load(b_ptrs, other=0)\n+            b = (\n+                (b_word[:, None, None] >> b_shifter) & 3\n+            ).reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            if has_zp:\n+                b = b - ((b_zp_word[:, None, None] >> b_zp_shifter) & 3)\n+        elif use_int4_w4a16:\n+            b = tl.load(b_ptrs, mask=k_mask, other=0)\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n+        if use_int2_w2a16:\n+            b_word = tl.load(b_ptrs, other=0)\n+            b = (\n+                (b_word[:, None, None] >> b_shifter) & 3\n+            ).reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            if has_zp:\n+                b = b - ((b_zp_word[:, None, None] >> b_zp_shifter) & 3)\n+        elif use_int4_w4a16:\n+            b = tl.load(b_ptrs, mask=k_mask, other=0)\n+           +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n+        if use_int2_w2a16:\n+            b_word = tl.load(b_ptrs, other=0)\n+            b = (\n+                (b_word[:, None, None] >> b_shifter) & 3\n+            ).reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            if has_zp:\n+                b = b - ((b_zp_word[:, None, None] >> b_zp_shifter) & 3)\n+        elif use_int4_w4a16:\n+            b = tl.load(b_ptrs, mask=k_mask, other=0)\n+            b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n+        if use_int2_w2a16:\n+            b_word = tl.load(b_ptrs, other=0)\n+            b = (\n+                (b_word[:, None, None] >> b_shifter) & 3\n+            ).reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            if has_zp:\n+                b = b - ((b_zp_word[:, None, None] >> b_zp_shifter) & 3)\n+        elif use_int4_w4a16:\n+            b = tl.load(b_ptrs, mask=k_mask, other=0)\n+            b =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n+        if use_int2_w2a16:\n+            b_word = tl.load(b_ptrs, other=0)\n+            b = (\n+                (b_word[:, None, None] >> b_shifter) & 3\n+            ).reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            if has_zp:\n+                b = b - ((b_zp_word[:, None, None] >> b_zp_shifter) & 3)\n+        elif use_int4_w4a16:\n+            b = tl.load(b_ptrs, mask=k_mask, other=0)\n+            b = ((+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n+        if use_int2_w2a16:\n+            b_word = tl.load(b_ptrs, other=0)\n+            b = (\n+                (b_word[:, None, None] >> b_shifter) & 3\n+            ).reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            if has_zp:\n+                b = b - ((b_zp_word[:, None, None] >> b_zp_shifter) & 3)\n+        elif use_int4_w4a16:\n+            b = tl.load(b_ptrs, mask=k_mask, other=0)\n+            b = ((b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n+        if use_int2_w2a16:\n+            b_word = tl.load(b_ptrs, other=0)\n+            b = (\n+                (b_word[:, None, None] >> b_shifter) & 3\n+            ).reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            if has_zp:\n+                b = b - ((b_zp_word[:, None, None] >> b_zp_shifter) & 3)\n+        elif use_int4_w4a16:\n+            b = tl.load(b_ptrs, mask=k_mask, other=0)\n+            b = ((b >>+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n+        if use_int2_w2a16:\n+            b_word = tl.load(b_ptrs, other=0)\n+            b = (\n+                (b_word[:, None, None] >> b_shifter) & 3\n+            ).reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            if has_zp:\n+                b = b - ((b_zp_word[:, None, None] >> b_zp_shifter) & 3)\n+        elif use_int4_w4a16:\n+            b = tl.load(b_ptrs, mask=k_mask, other=0)\n+            b = ((b >> b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n+        if use_int2_w2a16:\n+            b_word = tl.load(b_ptrs, other=0)\n+            b = (\n+                (b_word[:, None, None] >> b_shifter) & 3\n+            ).reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            if has_zp:\n+                b = b - ((b_zp_word[:, None, None] >> b_zp_shifter) & 3)\n+        elif use_int4_w4a16:\n+            b = tl.load(b_ptrs, mask=k_mask, other=0)\n+            b = ((b >> b_sh+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n+        if use_int2_w2a16:\n+            b_word = tl.load(b_ptrs, other=0)\n+            b = (\n+                (b_word[:, None, None] >> b_shifter) & 3\n+            ).reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            if has_zp:\n+                b = b - ((b_zp_word[:, None, None] >> b_zp_shifter) & 3)\n+        elif use_int4_w4a16:\n+            b = tl.load(b_ptrs, mask=k_mask, other=0)\n+            b = ((b >> b_shifter+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n+        if use_int2_w2a16:\n+            b_word = tl.load(b_ptrs, other=0)\n+            b = (\n+                (b_word[:, None, None] >> b_shifter) & 3\n+            ).reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            if has_zp:\n+                b = b - ((b_zp_word[:, None, None] >> b_zp_shifter) & 3)\n+        elif use_int4_w4a16:\n+            b = tl.load(b_ptrs, mask=k_mask, other=0)\n+            b = ((b >> b_shifter)+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n+        if use_int2_w2a16:\n+            b_word = tl.load(b_ptrs, other=0)\n+            b = (\n+                (b_word[:, None, None] >> b_shifter) & 3\n+            ).reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            if has_zp:\n+                b = b - ((b_zp_word[:, None, None] >> b_zp_shifter) & 3)\n+        elif use_int4_w4a16:\n+            b = tl.load(b_ptrs, mask=k_mask, other=0)\n+            b = ((b >> b_shifter) &+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n+        if use_int2_w2a16:\n+            b_word = tl.load(b_ptrs, other=0)\n+            b = (\n+                (b_word[:, None, None] >> b_shifter) & 3\n+            ).reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            if has_zp:\n+                b = b - ((b_zp_word[:, None, None] >> b_zp_shifter) & 3)\n+        elif use_int4_w4a16:\n+            b = tl.load(b_ptrs, mask=k_mask, other=0)\n+            b = ((b >> b_shifter) & +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n+        if use_int2_w2a16:\n+            b_word = tl.load(b_ptrs, other=0)\n+            b = (\n+                (b_word[:, None, None] >> b_shifter) & 3\n+            ).reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            if has_zp:\n+                b = b - ((b_zp_word[:, None, None] >> b_zp_shifter) & 3)\n+        elif use_int4_w4a16:\n+            b = tl.load(b_ptrs, mask=k_mask, other=0)\n+            b = ((b >> b_shifter) & 1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n+        if use_int2_w2a16:\n+            b_word = tl.load(b_ptrs, other=0)\n+            b = (\n+                (b_word[:, None, None] >> b_shifter) & 3\n+            ).reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            if has_zp:\n+                b = b - ((b_zp_word[:, None, None] >> b_zp_shifter) & 3)\n+        elif use_int4_w4a16:\n+            b = tl.load(b_ptrs, mask=k_mask, other=0)\n+            b = ((b >> b_shifter) & 15+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n+        if use_int2_w2a16:\n+            b_word = tl.load(b_ptrs, other=0)\n+            b = (\n+                (b_word[:, None, None] >> b_shifter) & 3\n+            ).reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            if has_zp:\n+                b = b - ((b_zp_word[:, None, None] >> b_zp_shifter) & 3)\n+        elif use_int4_w4a16:\n+            b = tl.load(b_ptrs, mask=k_mask, other=0)\n+            b = ((b >> b_shifter) & 15)+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n+        if use_int2_w2a16:\n+            b_word = tl.load(b_ptrs, other=0)\n+            b = (\n+                (b_word[:, None, None] >> b_shifter) & 3\n+            ).reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            if has_zp:\n+                b = b - ((b_zp_word[:, None, None] >> b_zp_shifter) & 3)\n+        elif use_int4_w4a16:\n+            b = tl.load(b_ptrs, mask=k_mask, other=0)\n+            b = ((b >> b_shifter) & 15) -+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n+        if use_int2_w2a16:\n+            b_word = tl.load(b_ptrs, other=0)\n+            b = (\n+                (b_word[:, None, None] >> b_shifter) & 3\n+            ).reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            if has_zp:\n+                b = b - ((b_zp_word[:, None, None] >> b_zp_shifter) & 3)\n+        elif use_int4_w4a16:\n+            b = tl.load(b_ptrs, mask=k_mask, other=0)\n+            b = ((b >> b_shifter) & 15) - b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n+        if use_int2_w2a16:\n+            b_word = tl.load(b_ptrs, other=0)\n+            b = (\n+                (b_word[:, None, None] >> b_shifter) & 3\n+            ).reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            if has_zp:\n+                b = b - ((b_zp_word[:, None, None] >> b_zp_shifter) & 3)\n+        elif use_int4_w4a16:\n+            b = tl.load(b_ptrs, mask=k_mask, other=0)\n+            b = ((b >> b_shifter) & 15) - b_z+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) dimensions.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token[None, :] < num_valid_tokens)\n+            a_mask = token_mask[:, None] & k_mask\n+        else:\n+            a_mask = token_mask[:, None]\n+\n+        a = tl.load(\n+            a_ptrs,\n+            mask=a_mask,\n+            other=0.0,\n+        )\n+        if use_int2_w2a16:\n+            b_word = tl.load(b_ptrs, other=0)\n+            b = (\n+                (b_word[:, None, None] >> b_shifter) & 3\n+            ).reshape(BLOCK_SIZE_K, BLOCK_SIZE_N)\n+            if has_zp:\n+                b = b - ((b_zp_word[:, None, None] >> b_zp_shifter) & 3)\n+        elif use_int4_w4a16:\n+            b = tl.load(b_ptrs, mask=k_mask, other=0)\n+            b = ((b >> b_shifter) & 15) - b_zp",
 "ttft_seconds": 1.0816168819874292,
 "status": "ok",
 "tg_tokens_per_s": 34.975990544428505,
 "decode_seconds_per_token": 0.02859104158121677,
 "individual_token_timing": true,
 "event_intervals_ms": {
  "n": 511,
  "median": 28.111394989537075,
  "p95": 35.79699400870595,
  "mean": 28.59104158121677
 },
 "class": "code",
 "requested_input_tokens": 2048
}
