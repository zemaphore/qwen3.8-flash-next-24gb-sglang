{
 "started_utc": "2026-09-16T23:10:14.278342+00:00",
 "wall_seconds": 15.419895181999891,
 "prompt_tokens": 2048,
 "completion_tokens": 512,
 "stream_events": 512,
 "ended_utc": "2026-09-16T23:10:29.698362+00:00",
 "warmup": false,
 "sample_index": 1,
 "requested_decode_tokens": 512,
 "gpu": {
  "n": 56,
  "temperature_c": {
   "min": 72.0,
   "max": 74.0,
   "mean": 73.55357142857143
  },
  "power_w": {
   "min": 247.79,
   "max": 272.13,
   "mean": 260.0701785714286
  },
  "util_pct": {
   "min": 71.0,
   "max": 98.0,
   "mean": 89.42857142857143
  },
  "clock_sm_mhz": {
   "min": 1680.0,
   "max": 1920.0,
   "mean": 1909.5535714285713
  },
  "clock_mem_mhz": {
   "min": 9501.0,
   "max": 9501.0,
   "mean": 9501.0
  },
  "memory_free_mib": {
   "min": 2540.0
  }
 },
 "output_text_sha256": "bbd9913cf7f55e7df4c7babef850ac75cd8979bc526b58131848dc2711599ef4",
 "output_text": ".py.py --.py --url.py --url http.py --url http://.py --url http://1.py --url http://12.py --url http://127.py --url http://127..py --url http://127.0.py --url http://127.0..py --url http://127.0.0.py --url http://127.0.0..py --url http://127.0.0.1.py --url http://127.0.0.1:.py --url http://127.0.0.1:3.py --url http://127.0.0.1:30.py --url http://127.0.0.1:300.py --url http://127.0.0.1:3000.py --url http://127.0.0.1:30000.py --url http://127.0.0.1:30000 --.py --url http://127.0.0.1:30000 --prompt.py --url http://127.0.0.1:30000 --prompt-t.py --url http://127.0.0.1:30000 --prompt-tokens.py --url http://127.0.0.1:30000 --prompt-tokens .py --url http://127.0.0.1:30000 --prompt-tokens 1.py --url http://127.0.0.1:30000 --prompt-tokens 10.py --url http://127.0.0.1:30000 --prompt-tokens 101.py --url http://127.0.0.1:30000 --prompt-tokens 101 .py --url http://127.0.0.1:30000 --prompt-tokens 101 4.py --url http://127.0.0.1:30000 --prompt-tokens 101 42.py --url http://127.0.0.1:30000 --prompt-tokens 101 421.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 17.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 170.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 68.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 682.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 100.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 1000.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-t.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 12.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers..py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches S.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGL.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`..py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(C.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAM.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPA.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:4.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:41.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413);.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthrough.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#con.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency))..py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The P.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NV.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`mem.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window;.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`..py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n##.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsg.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/        .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-p.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against S.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGL.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 7.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a25.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a2552.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a25520.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f).py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/       .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (0.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01...py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..0.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_t.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py).py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nk.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/       .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Trit.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels:.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 Mo.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gem.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gem.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, P.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/        .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets:.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the P.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/         .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools:.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_p.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_p.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n               .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_p.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_p.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_p.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh\ndocs.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh\ndocs/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh\ndocs/          .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh\ndocs/           the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh\ndocs/           the design.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh\ndocs/           the design documents.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh\ndocs/           the design documents and.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh\ndocs/           the design documents and the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh\ndocs/           the design documents and the engineering.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh\ndocs/           the design documents and the engineering log.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh\ndocs/           the design documents and the engineering log\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh\ndocs/           the design documents and the engineering log\ndocs.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh\ndocs/           the design documents and the engineering log\ndocs/C.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh\ndocs/           the design documents and the engineering log\ndocs/CAM.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh\ndocs/           the design documents and the engineering log\ndocs/CAMPA.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh\ndocs/           the design documents and the engineering log\ndocs/CAMPAIGN.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh\ndocs/           the design documents and the engineering log\ndocs/CAMPAIGN.md.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh\ndocs/           the design documents and the engineering log\ndocs/CAMPAIGN.md  .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh\ndocs/           the design documents and the engineering log\ndocs/CAMPAIGN.md   the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh\ndocs/           the design documents and the engineering log\ndocs/CAMPAIGN.md   the append.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh\ndocs/           the design documents and the engineering log\ndocs/CAMPAIGN.md   the append-only.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh\ndocs/           the design documents and the engineering log\ndocs/CAMPAIGN.md   the append-only engineering.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh\ndocs/           the design documents and the engineering log\ndocs/CAMPAIGN.md   the append-only engineering log.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh\ndocs/           the design documents and the engineering log\ndocs/CAMPAIGN.md   the append-only engineering log (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh\ndocs/           the design documents and the engineering log\ndocs/CAMPAIGN.md   the append-only engineering log (dated.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh\ndocs/           the design documents and the engineering log\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh\ndocs/           the design documents and the engineering log\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh\ndocs/           the design documents and the engineering log\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, newest.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh\ndocs/           the design documents and the engineering log\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, newest first.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh\ndocs/           the design documents and the engineering log\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, newest first).py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh\ndocs/           the design documents and the engineering log\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, newest first)\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh\ndocs/           the design documents and the engineering log\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, newest first)\ndocs.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh\ndocs/           the design documents and the engineering log\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, newest first)\ndocs/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh\ndocs/           the design documents and the engineering log\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, newest first)\ndocs/DES.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh\ndocs/           the design documents and the engineering log\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, newest first)\ndocs/DESIGN.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh\ndocs/           the design documents and the engineering log\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, newest first)\ndocs/DESIGN.md.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh\ndocs/           the design documents and the engineering log\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, newest first)\ndocs/DESIGN.md    .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh\ndocs/           the design documents and the engineering log\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, newest first)\ndocs/DESIGN.md     the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh\ndocs/           the design documents and the engineering log\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, newest first)\ndocs/DESIGN.md     the design.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh\ndocs/           the design documents and the engineering log\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, newest first)\ndocs/DESIGN.md     the design record.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh\ndocs/           the design documents and the engineering log\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, newest first)\ndocs/DESIGN.md     the design record:.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh\ndocs/           the design documents and the engineering log\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, newest first)\ndocs/DESIGN.md     the design record: the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh\ndocs/           the design documents and the engineering log\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, newest first)\ndocs/DESIGN.md     the design record: the architecture.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh\ndocs/           the design documents and the engineering log\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, newest first)\ndocs/DESIGN.md     the design record: the architecture,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh\ndocs/           the design documents and the engineering log\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, newest first)\ndocs/DESIGN.md     the design record: the architecture, the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh\ndocs/           the design documents and the engineering log\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, newest first)\ndocs/DESIGN.md     the design record: the architecture, the quant.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh\ndocs/           the design documents and the engineering log\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, newest first)\ndocs/DESIGN.md     the design record: the architecture, the quantization.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh\ndocs/           the design documents and the engineering log\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, newest first)\ndocs/DESIGN.md     the design record: the architecture, the quantization,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh\ndocs/           the design documents and the engineering log\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, newest first)\ndocs/DESIGN.md     the design record: the architecture, the quantization, the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh\ndocs/           the design documents and the engineering log\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, newest first)\ndocs/DESIGN.md     the design record: the architecture, the quantization, the serving.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh\ndocs/           the design documents and the engineering log\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, newest first)\ndocs/DESIGN.md     the design record: the architecture, the quantization, the serving stack.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh\ndocs/           the design documents and the engineering log\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, newest first)\ndocs/DESIGN.md     the design record: the architecture, the quantization, the serving stack,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh\ndocs/           the design documents and the engineering log\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, newest first)\ndocs/DESIGN.md     the design record: the architecture, the quantization, the serving stack,\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh\ndocs/           the design documents and the engineering log\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, newest first)\ndocs/DESIGN.md     the design record: the architecture, the quantization, the serving stack,\n                  .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh\ndocs/           the design documents and the engineering log\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, newest first)\ndocs/DESIGN.md     the design record: the architecture, the quantization, the serving stack,\n                   the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh\ndocs/           the design documents and the engineering log\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, newest first)\ndocs/DESIGN.md     the design record: the architecture, the quantization, the serving stack,\n                   the KV.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh\ndocs/           the design documents and the engineering log\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, newest first)\ndocs/DESIGN.md     the design record: the architecture, the quantization, the serving stack,\n                   the KV cache.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh\ndocs/           the design documents and the engineering log\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, newest first)\ndocs/DESIGN.md     the design record: the architecture, the quantization, the serving stack,\n                   the KV cache,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh\ndocs/           the design documents and the engineering log\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, newest first)\ndocs/DESIGN.md     the design record: the architecture, the quantization, the serving stack,\n                   the KV cache, the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh\ndocs/           the design documents and the engineering log\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, newest first)\ndocs/DESIGN.md     the design record: the architecture, the quantization, the serving stack,\n                   the KV cache, the P.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh\ndocs/           the design documents and the engineering log\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, newest first)\ndocs/DESIGN.md     the design record: the architecture, the quantization, the serving stack,\n                   the KV cache, the PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh\ndocs/           the design documents and the engineering log\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, newest first)\ndocs/DESIGN.md     the design record: the architecture, the quantization, the serving stack,\n                   the KV cache, the PLE,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh\ndocs/           the design documents and the engineering log\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, newest first)\ndocs/DESIGN.md     the design record: the architecture, the quantization, the serving stack,\n                   the KV cache, the PLE, the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh\ndocs/           the design documents and the engineering log\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, newest first)\ndocs/DESIGN.md     the design record: the architecture, the quantization, the serving stack,\n                   the KV cache, the PLE, the outcomes.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh\ndocs/           the design documents and the engineering log\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, newest first)\ndocs/DESIGN.md     the design record: the architecture, the quantization, the serving stack,\n                   the KV cache, the PLE, the outcomes and.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh\ndocs/           the design documents and the engineering log\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, newest first)\ndocs/DESIGN.md     the design record: the architecture, the quantization, the serving stack,\n                   the KV cache, the PLE, the outcomes and the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh\ndocs/           the design documents and the engineering log\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, newest first)\ndocs/DESIGN.md     the design record: the architecture, the quantization, the serving stack,\n                   the KV cache, the PLE, the outcomes and the open.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh\ndocs/           the design documents and the engineering log\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, newest first)\ndocs/DESIGN.md     the design record: the architecture, the quantization, the serving stack,\n                   the KV cache, the PLE, the outcomes and the open questions.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh\ndocs/           the design documents and the engineering log\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, newest first)\ndocs/DESIGN.md     the design record: the architecture, the quantization, the serving stack,\n                   the KV cache, the PLE, the outcomes and the open questions\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh\ndocs/           the design documents and the engineering log\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, newest first)\ndocs/DESIGN.md     the design record: the architecture, the quantization, the serving stack,\n                   the KV cache, the PLE, the outcomes and the open questions\ndocs.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh\ndocs/           the design documents and the engineering log\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, newest first)\ndocs/DESIGN.md     the design record: the architecture, the quantization, the serving stack,\n                   the KV cache, the PLE, the outcomes and the open questions\ndocs/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh\ndocs/           the design documents and the engineering log\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, newest first)\ndocs/DESIGN.md     the design record: the architecture, the quantization, the serving stack,\n                   the KV cache, the PLE, the outcomes and the open questions\ndocs/PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh\ndocs/           the design documents and the engineering log\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, newest first)\ndocs/DESIGN.md     the design record: the architecture, the quantization, the serving stack,\n                   the KV cache, the PLE, the outcomes and the open questions\ndocs/PLE.md.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh\ndocs/           the design documents and the engineering log\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, newest first)\ndocs/DESIGN.md     the design record: the architecture, the quantization, the serving stack,\n                   the KV cache, the PLE, the outcomes and the open questions\ndocs/PLE.md       .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh\ndocs/           the design documents and the engineering log\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, newest first)\ndocs/DESIGN.md     the design record: the architecture, the quantization, the serving stack,\n                   the KV cache, the PLE, the outcomes and the open questions\ndocs/PLE.md        the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh\ndocs/           the design documents and the engineering log\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, newest first)\ndocs/DESIGN.md     the design record: the architecture, the quantization, the serving stack,\n                   the KV cache, the PLE, the outcomes and the open questions\ndocs/PLE.md        the n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh\ndocs/           the design documents and the engineering log\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, newest first)\ndocs/DESIGN.md     the design record: the architecture, the quantization, the serving stack,\n                   the KV cache, the PLE, the outcomes and the open questions\ndocs/PLE.md        the n-.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh\ndocs/           the design documents and the engineering log\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, newest first)\ndocs/DESIGN.md     the design record: the architecture, the quantization, the serving stack,\n                   the KV cache, the PLE, the outcomes and the open questions\ndocs/PLE.md        the n-gram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh\ndocs/           the design documents and the engineering log\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, newest first)\ndocs/DESIGN.md     the design record: the architecture, the quantization, the serving stack,\n                   the KV cache, the PLE, the outcomes and the open questions\ndocs/PLE.md        the n-gram P.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh\ndocs/           the design documents and the engineering log\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, newest first)\ndocs/DESIGN.md     the design record: the architecture, the quantization, the serving stack,\n                   the KV cache, the PLE, the outcomes and the open questions\ndocs/PLE.md        the n-gram PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh\ndocs/           the design documents and the engineering log\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, newest first)\ndocs/DESIGN.md     the design record: the architecture, the quantization, the serving stack,\n                   the KV cache, the PLE, the outcomes and the open questions\ndocs/PLE.md        the n-gram PLE design.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh\ndocs/           the design documents and the engineering log\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, newest first)\ndocs/DESIGN.md     the design record: the architecture, the quantization, the serving stack,\n                   the KV cache, the PLE, the outcomes and the open questions\ndocs/PLE.md        the n-gram PLE design record.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh\ndocs/           the design documents and the engineering log\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, newest first)\ndocs/DESIGN.md     the design record: the architecture, the quantization, the serving stack,\n                   the KV cache, the PLE, the outcomes and the open questions\ndocs/PLE.md        the n-gram PLE design record\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh\ndocs/           the design documents and the engineering log\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, newest first)\ndocs/DESIGN.md     the design record: the architecture, the quantization, the serving stack,\n                   the KV cache, the PLE, the outcomes and the open questions\ndocs/PLE.md        the n-gram PLE design record\ndocs.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh\ndocs/           the design documents and the engineering log\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, newest first)\ndocs/DESIGN.md     the design record: the architecture, the quantization, the serving stack,\n                   the KV cache, the PLE, the outcomes and the open questions\ndocs/PLE.md        the n-gram PLE design record\ndocs/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh\ndocs/           the design documents and the engineering log\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, newest first)\ndocs/DESIGN.md     the design record: the architecture, the quantization, the serving stack,\n                   the KV cache, the PLE, the outcomes and the open questions\ndocs/PLE.md        the n-gram PLE design record\ndocs/README.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh\ndocs/           the design documents and the engineering log\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, newest first)\ndocs/DESIGN.md     the design record: the architecture, the quantization, the serving stack,\n                   the KV cache, the PLE, the outcomes and the open questions\ndocs/PLE.md        the n-gram PLE design record\ndocs/README.md.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh\ndocs/           the design documents and the engineering log\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, newest first)\ndocs/DESIGN.md     the design record: the architecture, the quantization, the serving stack,\n                   the KV cache, the PLE, the outcomes and the open questions\ndocs/PLE.md        the n-gram PLE design record\ndocs/README.md    .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh\ndocs/           the design documents and the engineering log\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, newest first)\ndocs/DESIGN.md     the design record: the architecture, the quantization, the serving stack,\n                   the KV cache, the PLE, the outcomes and the open questions\ndocs/PLE.md        the n-gram PLE design record\ndocs/README.md     how.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh\ndocs/           the design documents and the engineering log\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, newest first)\ndocs/DESIGN.md     the design record: the architecture, the quantization, the serving stack,\n                   the KV cache, the PLE, the outcomes and the open questions\ndocs/PLE.md        the n-gram PLE design record\ndocs/README.md     how to.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh\ndocs/           the design documents and the engineering log\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, newest first)\ndocs/DESIGN.md     the design record: the architecture, the quantization, the serving stack,\n                   the KV cache, the PLE, the outcomes and the open questions\ndocs/PLE.md        the n-gram PLE design record\ndocs/README.md     how to read.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh\ndocs/           the design documents and the engineering log\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, newest first)\ndocs/DESIGN.md     the design record: the architecture, the quantization, the serving stack,\n                   the KV cache, the PLE, the outcomes and the open questions\ndocs/PLE.md        the n-gram PLE design record\ndocs/README.md     how to read the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh\ndocs/           the design documents and the engineering log\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, newest first)\ndocs/DESIGN.md     the design record: the architecture, the quantization, the serving stack,\n                   the KV cache, the PLE, the outcomes and the open questions\ndocs/PLE.md        the n-gram PLE design record\ndocs/README.md     how to read the documents.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh\ndocs/           the design documents and the engineering log\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, newest first)\ndocs/DESIGN.md     the design record: the architecture, the quantization, the serving stack,\n                   the KV cache, the PLE, the outcomes and the open questions\ndocs/PLE.md        the n-gram PLE design record\ndocs/README.md     how to read the documents\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh\ndocs/           the design documents and the engineering log\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, newest first)\ndocs/DESIGN.md     the design record: the architecture, the quantization, the serving stack,\n                   the KV cache, the PLE, the outcomes and the open questions\ndocs/PLE.md        the n-gram PLE design record\ndocs/README.md     how to read the documents\ndocs.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh\ndocs/           the design documents and the engineering log\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, newest first)\ndocs/DESIGN.md     the design record: the architecture, the quantization, the serving stack,\n                   the KV cache, the PLE, the outcomes and the open questions\ndocs/PLE.md        the n-gram PLE design record\ndocs/README.md     how to read the documents\ndocs/logs.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh\ndocs/           the design documents and the engineering log\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, newest first)\ndocs/DESIGN.md     the design record: the architecture, the quantization, the serving stack,\n                   the KV cache, the PLE, the outcomes and the open questions\ndocs/PLE.md        the n-gram PLE design record\ndocs/README.md     how to read the documents\ndocs/logs/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh\ndocs/           the design documents and the engineering log\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, newest first)\ndocs/DESIGN.md     the design record: the architecture, the quantization, the serving stack,\n                   the KV cache, the PLE, the outcomes and the open questions\ndocs/PLE.md        the n-gram PLE design record\ndocs/README.md     how to read the documents\ndocs/logs/      .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh\ndocs/           the design documents and the engineering log\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, newest first)\ndocs/DESIGN.md     the design record: the architecture, the quantization, the serving stack,\n                   the KV cache, the PLE, the outcomes and the open questions\ndocs/PLE.md        the n-gram PLE design record\ndocs/README.md     how to read the documents\ndocs/logs/       the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh\ndocs/           the design documents and the engineering log\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, newest first)\ndocs/DESIGN.md     the design record: the architecture, the quantization, the serving stack,\n                   the KV cache, the PLE, the outcomes and the open questions\ndocs/PLE.md        the n-gram PLE design record\ndocs/README.md     how to read the documents\ndocs/logs/       the measurement.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh\ndocs/           the design documents and the engineering log\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, newest first)\ndocs/DESIGN.md     the design record: the architecture, the quantization, the serving stack,\n                   the KV cache, the PLE, the outcomes and the open questions\ndocs/PLE.md        the n-gram PLE design record\ndocs/README.md     how to read the documents\ndocs/logs/       the measurement logs.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh\ndocs/           the design documents and the engineering log\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, newest first)\ndocs/DESIGN.md     the design record: the architecture, the quantization, the serving stack,\n                   the KV cache, the PLE, the outcomes and the open questions\ndocs/PLE.md        the n-gram PLE design record\ndocs/README.md     how to read the documents\ndocs/logs/       the measurement logs (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh\ndocs/           the design documents and the engineering log\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, newest first)\ndocs/DESIGN.md     the design record: the architecture, the quantization, the serving stack,\n                   the KV cache, the PLE, the outcomes and the open questions\ndocs/PLE.md        the n-gram PLE design record\ndocs/README.md     how to read the documents\ndocs/logs/       the measurement logs (night.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh\ndocs/           the design documents and the engineering log\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, newest first)\ndocs/DESIGN.md     the design record: the architecture, the quantization, the serving stack,\n                   the KV cache, the PLE, the outcomes and the open questions\ndocs/PLE.md        the n-gram PLE design record\ndocs/README.md     how to read the documents\ndocs/logs/       the measurement logs (night.log.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh\ndocs/           the design documents and the engineering log\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, newest first)\ndocs/DESIGN.md     the design record: the architecture, the quantization, the serving stack,\n                   the KV cache, the PLE, the outcomes and the open questions\ndocs/PLE.md        the n-gram PLE design record\ndocs/README.md     how to read the documents\ndocs/logs/       the measurement logs (night.log,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh\ndocs/           the design documents and the engineering log\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, newest first)\ndocs/DESIGN.md     the design record: the architecture, the quantization, the serving stack,\n                   the KV cache, the PLE, the outcomes and the open questions\ndocs/PLE.md        the n-gram PLE design record\ndocs/README.md     how to read the documents\ndocs/logs/       the measurement logs (night.log, tiers.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh\ndocs/           the design documents and the engineering log\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, newest first)\ndocs/DESIGN.md     the design record: the architecture, the quantization, the serving stack,\n                   the KV cache, the PLE, the outcomes and the open questions\ndocs/PLE.md        the n-gram PLE design record\ndocs/README.md     how to read the documents\ndocs/logs/       the measurement logs (night.log, tiers-validate.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh\ndocs/           the design documents and the engineering log\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, newest first)\ndocs/DESIGN.md     the design record: the architecture, the quantization, the serving stack,\n                   the KV cache, the PLE, the outcomes and the open questions\ndocs/PLE.md        the n-gram PLE design record\ndocs/README.md     how to read the documents\ndocs/logs/       the measurement logs (night.log, tiers-validate.log.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh\ndocs/           the design documents and the engineering log\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, newest first)\ndocs/DESIGN.md     the design record: the architecture, the quantization, the serving stack,\n                   the KV cache, the PLE, the outcomes and the open questions\ndocs/PLE.md        the n-gram PLE design record\ndocs/README.md     how to read the documents\ndocs/logs/       the measurement logs (night.log, tiers-validate.log,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh\ndocs/           the design documents and the engineering log\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, newest first)\ndocs/DESIGN.md     the design record: the architecture, the quantization, the serving stack,\n                   the KV cache, the PLE, the outcomes and the open questions\ndocs/PLE.md        the n-gram PLE design record\ndocs/README.md     how to read the documents\ndocs/logs/       the measurement logs (night.log, tiers-validate.log, elastic.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh\ndocs/           the design documents and the engineering log\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, newest first)\ndocs/DESIGN.md     the design record: the architecture, the quantization, the serving stack,\n                   the KV cache, the PLE, the outcomes and the open questions\ndocs/PLE.md        the n-gram PLE design record\ndocs/README.md     how to read the documents\ndocs/logs/       the measurement logs (night.log, tiers-validate.log, elastic..py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh\ndocs/           the design documents and the engineering log\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, newest first)\ndocs/DESIGN.md     the design record: the architecture, the quantization, the serving stack,\n                   the KV cache, the PLE, the outcomes and the open questions\ndocs/PLE.md        the n-gram PLE design record\ndocs/README.md     how to read the documents\ndocs/logs/       the measurement logs (night.log, tiers-validate.log, elastic.ctl.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh\ndocs/           the design documents and the engineering log\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, newest first)\ndocs/DESIGN.md     the design record: the architecture, the quantization, the serving stack,\n                   the KV cache, the PLE, the outcomes and the open questions\ndocs/PLE.md        the n-gram PLE design record\ndocs/README.md     how to read the documents\ndocs/logs/       the measurement logs (night.log, tiers-validate.log, elastic.ctl.status.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh\ndocs/           the design documents and the engineering log\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, newest first)\ndocs/DESIGN.md     the design record: the architecture, the quantization, the serving stack,\n                   the KV cache, the PLE, the outcomes and the open questions\ndocs/PLE.md        the n-gram PLE design record\ndocs/README.md     how to read the documents\ndocs/logs/       the measurement logs (night.log, tiers-validate.log, elastic.ctl.status,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh\ndocs/           the design documents and the engineering log\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, newest first)\ndocs/DESIGN.md     the design record: the architecture, the quantization, the serving stack,\n                   the KV cache, the PLE, the outcomes and the open questions\ndocs/PLE.md        the n-gram PLE design record\ndocs/README.md     how to read the documents\ndocs/logs/       the measurement logs (night.log, tiers-validate.log, elastic.ctl.status,\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh\ndocs/           the design documents and the engineering log\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, newest first)\ndocs/DESIGN.md     the design record: the architecture, the quantization, the serving stack,\n                   the KV cache, the PLE, the outcomes and the open questions\ndocs/PLE.md        the n-gram PLE design record\ndocs/README.md     how to read the documents\ndocs/logs/       the measurement logs (night.log, tiers-validate.log, elastic.ctl.status,\n                .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh\ndocs/           the design documents and the engineering log\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, newest first)\ndocs/DESIGN.md     the design record: the architecture, the quantization, the serving stack,\n                   the KV cache, the PLE, the outcomes and the open questions\ndocs/PLE.md        the n-gram PLE design record\ndocs/README.md     how to read the documents\ndocs/logs/       the measurement logs (night.log, tiers-validate.log, elastic.ctl.status,\n                 elastic.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh\ndocs/           the design documents and the engineering log\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, newest first)\ndocs/DESIGN.md     the design record: the architecture, the quantization, the serving stack,\n                   the KV cache, the PLE, the outcomes and the open questions\ndocs/PLE.md        the n-gram PLE design record\ndocs/README.md     how to read the documents\ndocs/logs/       the measurement logs (night.log, tiers-validate.log, elastic.ctl.status,\n                 elastic..py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh\ndocs/           the design documents and the engineering log\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, newest first)\ndocs/DESIGN.md     the design record: the architecture, the quantization, the serving stack,\n                   the KV cache, the PLE, the outcomes and the open questions\ndocs/PLE.md        the n-gram PLE design record\ndocs/README.md     how to read the documents\ndocs/logs/       the measurement logs (night.log, tiers-validate.log, elastic.ctl.status,\n                 elastic.ctl.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh\ndocs/           the design documents and the engineering log\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, newest first)\ndocs/DESIGN.md     the design record: the architecture, the quantization, the serving stack,\n                   the KV cache, the PLE, the outcomes and the open questions\ndocs/PLE.md        the n-gram PLE design record\ndocs/README.md     how to read the documents\ndocs/logs/       the measurement logs (night.log, tiers-validate.log, elastic.ctl.status,\n                 elastic.ctl.log.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh\ndocs/           the design documents and the engineering log\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, newest first)\ndocs/DESIGN.md     the design record: the architecture, the quantization, the serving stack,\n                   the KV cache, the PLE, the outcomes and the open questions\ndocs/PLE.md        the n-gram PLE design record\ndocs/README.md     how to read the documents\ndocs/logs/       the measurement logs (night.log, tiers-validate.log, elastic.ctl.status,\n                 elastic.ctl.log,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh\ndocs/           the design documents and the engineering log\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, newest first)\ndocs/DESIGN.md     the design record: the architecture, the quantization, the serving stack,\n                   the KV cache, the PLE, the outcomes and the open questions\ndocs/PLE.md        the n-gram PLE design record\ndocs/README.md     how to read the documents\ndocs/logs/       the measurement logs (night.log, tiers-validate.log, elastic.ctl.status,\n                 elastic.ctl.log, elastic.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh\ndocs/           the design documents and the engineering log\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, newest first)\ndocs/DESIGN.md     the design record: the architecture, the quantization, the serving stack,\n                   the KV cache, the PLE, the outcomes and the open questions\ndocs/PLE.md        the n-gram PLE design record\ndocs/README.md     how to read the documents\ndocs/logs/       the measurement logs (night.log, tiers-validate.log, elastic.ctl.status,\n                 elastic.ctl.log, elastic..py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh\ndocs/           the design documents and the engineering log\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, newest first)\ndocs/DESIGN.md     the design record: the architecture, the quantization, the serving stack,\n                   the KV cache, the PLE, the outcomes and the open questions\ndocs/PLE.md        the n-gram PLE design record\ndocs/README.md     how to read the documents\ndocs/logs/       the measurement logs (night.log, tiers-validate.log, elastic.ctl.status,\n                 elastic.ctl.log, elastic.ctl.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh\ndocs/           the design documents and the engineering log\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, newest first)\ndocs/DESIGN.md     the design record: the architecture, the quantization, the serving stack,\n                   the KV cache, the PLE, the outcomes and the open questions\ndocs/PLE.md        the n-gram PLE design record\ndocs/README.md     how to read the documents\ndocs/logs/       the measurement logs (night.log, tiers-validate.log, elastic.ctl.status,\n                 elastic.ctl.log, elastic.ctl.log.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh\ndocs/           the design documents and the engineering log\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, newest first)\ndocs/DESIGN.md     the design record: the architecture, the quantization, the serving stack,\n                   the KV cache, the PLE, the outcomes and the open questions\ndocs/PLE.md        the n-gram PLE design record\ndocs/README.md     how to read the documents\ndocs/logs/       the measurement logs (night.log, tiers-validate.log, elastic.ctl.status,\n                 elastic.ctl.log, elastic.ctl.log..py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh\ndocs/           the design documents and the engineering log\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, newest first)\ndocs/DESIGN.md     the design record: the architecture, the quantization, the serving stack,\n                   the KV cache, the PLE, the outcomes and the open questions\ndocs/PLE.md        the n-gram PLE design record\ndocs/README.md     how to read the documents\ndocs/logs/       the measurement logs (night.log, tiers-validate.log, elastic.ctl.status,\n                 elastic.ctl.log, elastic.ctl.log.1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh\ndocs/           the design documents and the engineering log\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, newest first)\ndocs/DESIGN.md     the design record: the architecture, the quantization, the serving stack,\n                   the KV cache, the PLE, the outcomes and the open questions\ndocs/PLE.md        the n-gram PLE design record\ndocs/README.md     how to read the documents\ndocs/logs/       the measurement logs (night.log, tiers-validate.log, elastic.ctl.status,\n                 elastic.ctl.log, elastic.ctl.log.1,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh\ndocs/           the design documents and the engineering log\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, newest first)\ndocs/DESIGN.md     the design record: the architecture, the quantization, the serving stack,\n                   the KV cache, the PLE, the outcomes and the open questions\ndocs/PLE.md        the n-gram PLE design record\ndocs/README.md     how to read the documents\ndocs/logs/       the measurement logs (night.log, tiers-validate.log, elastic.ctl.status,\n                 elastic.ctl.log, elastic.ctl.log.1, elastic.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh\ndocs/           the design documents and the engineering log\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, newest first)\ndocs/DESIGN.md     the design record: the architecture, the quantization, the serving stack,\n                   the KV cache, the PLE, the outcomes and the open questions\ndocs/PLE.md        the n-gram PLE design record\ndocs/README.md     how to read the documents\ndocs/logs/       the measurement logs (night.log, tiers-validate.log, elastic.ctl.status,\n                 elastic.ctl.log, elastic.ctl.log.1, elastic..py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh\ndocs/           the design documents and the engineering log\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, newest first)\ndocs/DESIGN.md     the design record: the architecture, the quantization, the serving stack,\n                   the KV cache, the PLE, the outcomes and the open questions\ndocs/PLE.md        the n-gram PLE design record\ndocs/README.md     how to read the documents\ndocs/logs/       the measurement logs (night.log, tiers-validate.log, elastic.ctl.status,\n                 elastic.ctl.log, elastic.ctl.log.1, elastic.ctl.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh\ndocs/           the design documents and the engineering log\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, newest first)\ndocs/DESIGN.md     the design record: the architecture, the quantization, the serving stack,\n                   the KV cache, the PLE, the outcomes and the open questions\ndocs/PLE.md        the n-gram PLE design record\ndocs/README.md     how to read the documents\ndocs/logs/       the measurement logs (night.log, tiers-validate.log, elastic.ctl.status,\n                 elastic.ctl.log, elastic.ctl.log.1, elastic.ctl.log.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh\ndocs/           the design documents and the engineering log\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, newest first)\ndocs/DESIGN.md     the design record: the architecture, the quantization, the serving stack,\n                   the KV cache, the PLE, the outcomes and the open questions\ndocs/PLE.md        the n-gram PLE design record\ndocs/README.md     how to read the documents\ndocs/logs/       the measurement logs (night.log, tiers-validate.log, elastic.ctl.status,\n                 elastic.ctl.log, elastic.ctl.log.1, elastic.ctl.log..py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh\ndocs/           the design documents and the engineering log\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, newest first)\ndocs/DESIGN.md     the design record: the architecture, the quantization, the serving stack,\n                   the KV cache, the PLE, the outcomes and the open questions\ndocs/PLE.md        the n-gram PLE design record\ndocs/README.md     how to read the documents\ndocs/logs/       the measurement logs (night.log, tiers-validate.log, elastic.ctl.status,\n                 elastic.ctl.log, elastic.ctl.log.1, elastic.ctl.log.2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh\ndocs/           the design documents and the engineering log\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, newest first)\ndocs/DESIGN.md     the design record: the architecture, the quantization, the serving stack,\n                   the KV cache, the PLE, the outcomes and the open questions\ndocs/PLE.md        the n-gram PLE design record\ndocs/README.md     how to read the documents\ndocs/logs/       the measurement logs (night.log, tiers-validate.log, elastic.ctl.status,\n                 elastic.ctl.log, elastic.ctl.log.1, elastic.ctl.log.2,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh\ndocs/           the design documents and the engineering log\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, newest first)\ndocs/DESIGN.md     the design record: the architecture, the quantization, the serving stack,\n                   the KV cache, the PLE, the outcomes and the open questions\ndocs/PLE.md        the n-gram PLE design record\ndocs/README.md     how to read the documents\ndocs/logs/       the measurement logs (night.log, tiers-validate.log, elastic.ctl.status,\n                 elastic.ctl.log, elastic.ctl.log.1, elastic.ctl.log.2, elastic.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh\ndocs/           the design documents and the engineering log\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, newest first)\ndocs/DESIGN.md     the design record: the architecture, the quantization, the serving stack,\n                   the KV cache, the PLE, the outcomes and the open questions\ndocs/PLE.md        the n-gram PLE design record\ndocs/README.md     how to read the documents\ndocs/logs/       the measurement logs (night.log, tiers-validate.log, elastic.ctl.status,\n                 elastic.ctl.log, elastic.ctl.log.1, elastic.ctl.log.2, elastic..py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh\ndocs/           the design documents and the engineering log\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, newest first)\ndocs/DESIGN.md     the design record: the architecture, the quantization, the serving stack,\n                   the KV cache, the PLE, the outcomes and the open questions\ndocs/PLE.md        the n-gram PLE design record\ndocs/README.md     how to read the documents\ndocs/logs/       the measurement logs (night.log, tiers-validate.log, elastic.ctl.status,\n                 elastic.ctl.log, elastic.ctl.log.1, elastic.ctl.log.2, elastic.ctl.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh\ndocs/           the design documents and the engineering log\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, newest first)\ndocs/DESIGN.md     the design record: the architecture, the quantization, the serving stack,\n                   the KV cache, the PLE, the outcomes and the open questions\ndocs/PLE.md        the n-gram PLE design record\ndocs/README.md     how to read the documents\ndocs/logs/       the measurement logs (night.log, tiers-validate.log, elastic.ctl.status,\n                 elastic.ctl.log, elastic.ctl.log.1, elastic.ctl.log.2, elastic.ctl.log.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh\ndocs/           the design documents and the engineering log\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, newest first)\ndocs/DESIGN.md     the design record: the architecture, the quantization, the serving stack,\n                   the KV cache, the PLE, the outcomes and the open questions\ndocs/PLE.md        the n-gram PLE design record\ndocs/README.md     how to read the documents\ndocs/logs/       the measurement logs (night.log, tiers-validate.log, elastic.ctl.status,\n                 elastic.ctl.log, elastic.ctl.log.1, elastic.ctl.log.2, elastic.ctl.log..py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh\ndocs/           the design documents and the engineering log\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, newest first)\ndocs/DESIGN.md     the design record: the architecture, the quantization, the serving stack,\n                   the KV cache, the PLE, the outcomes and the open questions\ndocs/PLE.md        the n-gram PLE design record\ndocs/README.md     how to read the documents\ndocs/logs/       the measurement logs (night.log, tiers-validate.log, elastic.ctl.status,\n                 elastic.ctl.log, elastic.ctl.log.1, elastic.ctl.log.2, elastic.ctl.log.3.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh\ndocs/           the design documents and the engineering log\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, newest first)\ndocs/DESIGN.md     the design record: the architecture, the quantization, the serving stack,\n                   the KV cache, the PLE, the outcomes and the open questions\ndocs/PLE.md        the n-gram PLE design record\ndocs/README.md     how to read the documents\ndocs/logs/       the measurement logs (night.log, tiers-validate.log, elastic.ctl.status,\n                 elastic.ctl.log, elastic.ctl.log.1, elastic.ctl.log.2, elastic.ctl.log.3,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh\ndocs/           the design documents and the engineering log\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, newest first)\ndocs/DESIGN.md     the design record: the architecture, the quantization, the serving stack,\n                   the KV cache, the PLE, the outcomes and the open questions\ndocs/PLE.md        the n-gram PLE design record\ndocs/README.md     how to read the documents\ndocs/logs/       the measurement logs (night.log, tiers-validate.log, elastic.ctl.status,\n                 elastic.ctl.log, elastic.ctl.log.1, elastic.ctl.log.2, elastic.ctl.log.3,\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh\ndocs/           the design documents and the engineering log\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, newest first)\ndocs/DESIGN.md     the design record: the architecture, the quantization, the serving stack,\n                   the KV cache, the PLE, the outcomes and the open questions\ndocs/PLE.md        the n-gram PLE design record\ndocs/README.md     how to read the documents\ndocs/logs/       the measurement logs (night.log, tiers-validate.log, elastic.ctl.status,\n                 elastic.ctl.log, elastic.ctl.log.1, elastic.ctl.log.2, elastic.ctl.log.3,\n                .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh\ndocs/           the design documents and the engineering log\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, newest first)\ndocs/DESIGN.md     the design record: the architecture, the quantization, the serving stack,\n                   the KV cache, the PLE, the outcomes and the open questions\ndocs/PLE.md        the n-gram PLE design record\ndocs/README.md     how to read the documents\ndocs/logs/       the measurement logs (night.log, tiers-validate.log, elastic.ctl.status,\n                 elastic.ctl.log, elastic.ctl.log.1, elastic.ctl.log.2, elastic.ctl.log.3,\n                 elastic.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh\ndocs/           the design documents and the engineering log\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, newest first)\ndocs/DESIGN.md     the design record: the architecture, the quantization, the serving stack,\n                   the KV cache, the PLE, the outcomes and the open questions\ndocs/PLE.md        the n-gram PLE design record\ndocs/README.md     how to read the documents\ndocs/logs/       the measurement logs (night.log, tiers-validate.log, elastic.ctl.status,\n                 elastic.ctl.log, elastic.ctl.log.1, elastic.ctl.log.2, elastic.ctl.log.3,\n                 elastic..py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh\ndocs/           the design documents and the engineering log\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, newest first)\ndocs/DESIGN.md     the design record: the architecture, the quantization, the serving stack,\n                   the KV cache, the PLE, the outcomes and the open questions\ndocs/PLE.md        the n-gram PLE design record\ndocs/README.md     how to read the documents\ndocs/logs/       the measurement logs (night.log, tiers-validate.log, elastic.ctl.status,\n                 elastic.ctl.log, elastic.ctl.log.1, elastic.ctl.log.2, elastic.ctl.log.3,\n                 elastic.ctl.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh\ndocs/           the design documents and the engineering log\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, newest first)\ndocs/DESIGN.md     the design record: the architecture, the quantization, the serving stack,\n                   the KV cache, the PLE, the outcomes and the open questions\ndocs/PLE.md        the n-gram PLE design record\ndocs/README.md     how to read the documents\ndocs/logs/       the measurement logs (night.log, tiers-validate.log, elastic.ctl.status,\n                 elastic.ctl.log, elastic.ctl.log.1, elastic.ctl.log.2, elastic.ctl.log.3,\n                 elastic.ctl.log.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh\ndocs/           the design documents and the engineering log\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, newest first)\ndocs/DESIGN.md     the design record: the architecture, the quantization, the serving stack,\n                   the KV cache, the PLE, the outcomes and the open questions\ndocs/PLE.md        the n-gram PLE design record\ndocs/README.md     how to read the documents\ndocs/logs/       the measurement logs (night.log, tiers-validate.log, elastic.ctl.status,\n                 elastic.ctl.log, elastic.ctl.log.1, elastic.ctl.log.2, elastic.ctl.log.3,\n                 elastic.ctl.log..py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh\ndocs/           the design documents and the engineering log\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, newest first)\ndocs/DESIGN.md     the design record: the architecture, the quantization, the serving stack,\n                   the KV cache, the PLE, the outcomes and the open questions\ndocs/PLE.md        the n-gram PLE design record\ndocs/README.md     how to read the documents\ndocs/logs/       the measurement logs (night.log, tiers-validate.log, elastic.ctl.status,\n                 elastic.ctl.log, elastic.ctl.log.1, elastic.ctl.log.2, elastic.ctl.log.3,\n                 elastic.ctl.log.4.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh\ndocs/           the design documents and the engineering log\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, newest first)\ndocs/DESIGN.md     the design record: the architecture, the quantization, the serving stack,\n                   the KV cache, the PLE, the outcomes and the open questions\ndocs/PLE.md        the n-gram PLE design record\ndocs/README.md     how to read the documents\ndocs/logs/       the measurement logs (night.log, tiers-validate.log, elastic.ctl.status,\n                 elastic.ctl.log, elastic.ctl.log.1, elastic.ctl.log.2, elastic.ctl.log.3,\n                 elastic.ctl.log.4,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh\ndocs/           the design documents and the engineering log\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, newest first)\ndocs/DESIGN.md     the design record: the architecture, the quantization, the serving stack,\n                   the KV cache, the PLE, the outcomes and the open questions\ndocs/PLE.md        the n-gram PLE design record\ndocs/README.md     how to read the documents\ndocs/logs/       the measurement logs (night.log, tiers-validate.log, elastic.ctl.status,\n                 elastic.ctl.log, elastic.ctl.log.1, elastic.ctl.log.2, elastic.ctl.log.3,\n                 elastic.ctl.log.4, elastic.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe and is read through a 1 GB\n`memmap` window; `PLE` must point at a directory containing `ple.json` and `ple.f8_e4m3.bin`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (git format-patch against SGLang 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, plus kv_tiers.py)\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,\n                ngram_ple_validate.py, ngram_ple_validate_test.py, ngram_ple_validate_test.sh\ndocs/           the design documents and the engineering log\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, newest first)\ndocs/DESIGN.md     the design record: the architecture, the quantization, the serving stack,\n                   the KV cache, the PLE, the outcomes and the open questions\ndocs/PLE.md        the n-gram PLE design record\ndocs/README.md     how to read the documents\ndocs/logs/       the measurement logs (night.log, tiers-validate.log, elastic.ctl.status,\n                 elastic.ctl.log, elastic.ctl.log.1, elastic.ctl.log.2, elastic.ctl.log.3,\n                 elastic.ctl.log.4, elastic.",
 "ttft_seconds": 1.4145813719951548,
 "status": "ok",
 "tg_tokens_per_s": 36.48636735096734,
 "decode_seconds_per_token": 0.0274074968982487,
 "individual_token_timing": true,
 "event_intervals_ms": {
  "n": 511,
  "median": 26.75916301086545,
  "p95": 34.84920199844055,
  "mean": 27.407496898248702
 },
 "class": "prose",
 "requested_input_tokens": 2048
}
