{
 "started_utc": "2026-09-16T23:09:57.631462+00:00",
 "wall_seconds": 16.642934715986485,
 "prompt_tokens": 2048,
 "completion_tokens": 512,
 "stream_events": 512,
 "ended_utc": "2026-09-16T23:10:14.274524+00:00",
 "warmup": true,
 "sample_index": 0,
 "requested_decode_tokens": 512,
 "gpu": {
  "n": 60,
  "temperature_c": {
   "min": 65.0,
   "max": 73.0,
   "mean": 71.11666666666666
  },
  "power_w": {
   "min": 136.03,
   "max": 274.87,
   "mean": 245.2655
  },
  "util_pct": {
   "min": 0.0,
   "max": 97.0,
   "mean": 84.33333333333333
  },
  "clock_sm_mhz": {
   "min": 1875.0,
   "max": 1935.0,
   "mean": 1924.25
  },
  "clock_mem_mhz": {
   "min": 9501.0,
   "max": 9501.0,
   "mean": 9501.0
  },
  "memory_free_mib": {
   "min": 2540.0
  }
 },
 "output_text_sha256": "9cc4874884295e4785376551408519fde99f3eac20d6a776a9d5509af1ba3c59",
 "output_text": ".py.py --.py --url.py --url http.py --url http://.py --url http://1.py --url http://12.py --url http://127.py --url http://127..py --url http://127.0.py --url http://127.0..py --url http://127.0.0.py --url http://127.0.0..py --url http://127.0.0.1.py --url http://127.0.0.1:.py --url http://127.0.0.1:3.py --url http://127.0.0.1:30.py --url http://127.0.0.1:300.py --url http://127.0.0.1:3000.py --url http://127.0.0.1:30000.py --url http://127.0.0.1:30000 --.py --url http://127.0.0.1:30000 --prompt.py --url http://127.0.0.1:30000 --prompt-t.py --url http://127.0.0.1:30000 --prompt-tokens.py --url http://127.0.0.1:30000 --prompt-tokens .py --url http://127.0.0.1:30000 --prompt-tokens 1.py --url http://127.0.0.1:30000 --prompt-tokens 10.py --url http://127.0.0.1:30000 --prompt-tokens 101.py --url http://127.0.0.1:30000 --prompt-tokens 101 .py --url http://127.0.0.1:30000 --prompt-tokens 101 4.py --url http://127.0.0.1:30000 --prompt-tokens 101 42.py --url http://127.0.0.1:30000 --prompt-tokens 101 421.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 17.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 170.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 68.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 682.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 100.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 1000.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-t.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 12.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state:.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`),.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`..py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAM.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPA.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:4.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:41.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413);.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(C.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAM.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPA.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:3.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:32.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320)..py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NV.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe:.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the P.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 5.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51..py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint..py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n##.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsg.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/        .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against S.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGL.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 7.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a25.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a2552.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a25520.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f).py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/       .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (0.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (00.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001...py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..0.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..01.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order).py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nk.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/       .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Trit.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert G.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEM.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n               .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, P.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, P.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense G.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEM.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, P.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 G.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEM.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, P.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 G.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEM.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM).py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/       .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state),.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n               .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/        .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the two.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the two serving.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the two serving assets.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the two serving assets:.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the two serving assets: the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the two serving assets: the Auto.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the two serving assets: the AutoRound.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the two serving assets: the AutoRound calibration.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the two serving assets: the AutoRound calibration set.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the two serving assets: the AutoRound calibration set and.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the two serving assets: the AutoRound calibration set and the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the two serving assets: the AutoRound calibration set and the P.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the two serving assets: the AutoRound calibration set and the PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the two serving assets: the AutoRound calibration set and the PLE n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the two serving assets: the AutoRound calibration set and the PLE n-.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the two serving assets: the AutoRound calibration set and the PLE n-gram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the two serving assets: the AutoRound calibration set and the PLE n-gram table.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the two serving assets: the AutoRound calibration set and the PLE n-gram table\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the two serving assets: the AutoRound calibration set and the PLE n-gram table\ntools.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the two serving assets: the AutoRound calibration set and the PLE n-gram table\ntools/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the two serving assets: the AutoRound calibration set and the PLE n-gram table\ntools/         .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the two serving assets: the AutoRound calibration set and the PLE n-gram table\ntools/          the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the two serving assets: the AutoRound calibration set and the PLE n-gram table\ntools/          the measurement.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the two serving assets: the AutoRound calibration set and the PLE n-gram table\ntools/          the measurement tools.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the two serving assets: the AutoRound calibration set and the PLE n-gram table\ntools/          the measurement tools (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the two serving assets: the AutoRound calibration set and the PLE n-gram table\ntools/          the measurement tools (bench.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the two serving assets: the AutoRound calibration set and the PLE n-gram table\ntools/          the measurement tools (bench.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the two serving assets: the AutoRound calibration set and the PLE n-gram table\ntools/          the measurement tools (bench.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the two serving assets: the AutoRound calibration set and the PLE n-gram table\ntools/          the measurement tools (bench.py, the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the two serving assets: the AutoRound calibration set and the PLE n-gram table\ntools/          the measurement tools (bench.py, the KV.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the two serving assets: the AutoRound calibration set and the PLE n-gram table\ntools/          the measurement tools (bench.py, the KV-cache.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the two serving assets: the AutoRound calibration set and the PLE n-gram table\ntools/          the measurement tools (bench.py, the KV-cache quality.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the two serving assets: the AutoRound calibration set and the PLE n-gram table\ntools/          the measurement tools (bench.py, the KV-cache quality harness.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the two serving assets: the AutoRound calibration set and the PLE n-gram table\ntools/          the measurement tools (bench.py, the KV-cache quality harness,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the two serving assets: the AutoRound calibration set and the PLE n-gram table\ntools/          the measurement tools (bench.py, the KV-cache quality harness, the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the two serving assets: the AutoRound calibration set and the PLE n-gram table\ntools/          the measurement tools (bench.py, the KV-cache quality harness, the P.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the two serving assets: the AutoRound calibration set and the PLE n-gram table\ntools/          the measurement tools (bench.py, the KV-cache quality harness, the PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the two serving assets: the AutoRound calibration set and the PLE n-gram table\ntools/          the measurement tools (bench.py, the KV-cache quality harness, the PLE table.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the two serving assets: the AutoRound calibration set and the PLE n-gram table\ntools/          the measurement tools (bench.py, the KV-cache quality harness, the PLE table builder.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the two serving assets: the AutoRound calibration set and the PLE n-gram table\ntools/          the measurement tools (bench.py, the KV-cache quality harness, the PLE table builder).py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the two serving assets: the AutoRound calibration set and the PLE n-gram table\ntools/          the measurement tools (bench.py, the KV-cache quality harness, the PLE table builder)\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the two serving assets: the AutoRound calibration set and the PLE n-gram table\ntools/          the measurement tools (bench.py, the KV-cache quality harness, the PLE table builder)\ndocs.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the two serving assets: the AutoRound calibration set and the PLE n-gram table\ntools/          the measurement tools (bench.py, the KV-cache quality harness, the PLE table builder)\ndocs/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the two serving assets: the AutoRound calibration set and the PLE n-gram table\ntools/          the measurement tools (bench.py, the KV-cache quality harness, the PLE table builder)\ndocs/          .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the two serving assets: the AutoRound calibration set and the PLE n-gram table\ntools/          the measurement tools (bench.py, the KV-cache quality harness, the PLE table builder)\ndocs/           CAM.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the two serving assets: the AutoRound calibration set and the PLE n-gram table\ntools/          the measurement tools (bench.py, the KV-cache quality harness, the PLE table builder)\ndocs/           CAMPA.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the two serving assets: the AutoRound calibration set and the PLE n-gram table\ntools/          the measurement tools (bench.py, the KV-cache quality harness, the PLE table builder)\ndocs/           CAMPAIGN.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the two serving assets: the AutoRound calibration set and the PLE n-gram table\ntools/          the measurement tools (bench.py, the KV-cache quality harness, the PLE table builder)\ndocs/           CAMPAIGN.md.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the two serving assets: the AutoRound calibration set and the PLE n-gram table\ntools/          the measurement tools (bench.py, the KV-cache quality harness, the PLE table builder)\ndocs/           CAMPAIGN.md (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the two serving assets: the AutoRound calibration set and the PLE n-gram table\ntools/          the measurement tools (bench.py, the KV-cache quality harness, the PLE table builder)\ndocs/           CAMPAIGN.md (the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the two serving assets: the AutoRound calibration set and the PLE n-gram table\ntools/          the measurement tools (bench.py, the KV-cache quality harness, the PLE table builder)\ndocs/           CAMPAIGN.md (the engineering.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the two serving assets: the AutoRound calibration set and the PLE n-gram table\ntools/          the measurement tools (bench.py, the KV-cache quality harness, the PLE table builder)\ndocs/           CAMPAIGN.md (the engineering log.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the two serving assets: the AutoRound calibration set and the PLE n-gram table\ntools/          the measurement tools (bench.py, the KV-cache quality harness, the PLE table builder)\ndocs/           CAMPAIGN.md (the engineering log),.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the two serving assets: the AutoRound calibration set and the PLE n-gram table\ntools/          the measurement tools (bench.py, the KV-cache quality harness, the PLE table builder)\ndocs/           CAMPAIGN.md (the engineering log), the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the two serving assets: the AutoRound calibration set and the PLE n-gram table\ntools/          the measurement tools (bench.py, the KV-cache quality harness, the PLE table builder)\ndocs/           CAMPAIGN.md (the engineering log), the design.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the two serving assets: the AutoRound calibration set and the PLE n-gram table\ntools/          the measurement tools (bench.py, the KV-cache quality harness, the PLE table builder)\ndocs/           CAMPAIGN.md (the engineering log), the design documents.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the two serving assets: the AutoRound calibration set and the PLE n-gram table\ntools/          the measurement tools (bench.py, the KV-cache quality harness, the PLE table builder)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the two serving assets: the AutoRound calibration set and the PLE n-gram table\ntools/          the measurement tools (bench.py, the KV-cache quality harness, the PLE table builder)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the two serving assets: the AutoRound calibration set and the PLE n-gram table\ntools/          the measurement tools (bench.py, the KV-cache quality harness, the PLE table builder)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the two serving assets: the AutoRound calibration set and the PLE n-gram table\ntools/          the measurement tools (bench.py, the KV-cache quality harness, the PLE table builder)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the two serving assets: the AutoRound calibration set and the PLE n-gram table\ntools/          the measurement tools (bench.py, the KV-cache quality harness, the PLE table builder)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes,\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the two serving assets: the AutoRound calibration set and the PLE n-gram table\ntools/          the measurement tools (bench.py, the KV-cache quality harness, the PLE table builder)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes,\n               .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the two serving assets: the AutoRound calibration set and the PLE n-gram table\ntools/          the measurement tools (bench.py, the KV-cache quality harness, the PLE table builder)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes,\n                the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the two serving assets: the AutoRound calibration set and the PLE n-gram table\ntools/          the measurement tools (bench.py, the KV-cache quality harness, the PLE table builder)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes,\n                the measurement.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the two serving assets: the AutoRound calibration set and the PLE n-gram table\ntools/          the measurement tools (bench.py, the KV-cache quality harness, the PLE table builder)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes,\n                the measurement logs.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the two serving assets: the AutoRound calibration set and the PLE n-gram table\ntools/          the measurement tools (bench.py, the KV-cache quality harness, the PLE table builder)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes,\n                the measurement logs,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the two serving assets: the AutoRound calibration set and the PLE n-gram table\ntools/          the measurement tools (bench.py, the KV-cache quality harness, the PLE table builder)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes,\n                the measurement logs, the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the two serving assets: the AutoRound calibration set and the PLE n-gram table\ntools/          the measurement tools (bench.py, the KV-cache quality harness, the PLE table builder)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes,\n                the measurement logs, the model.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the two serving assets: the AutoRound calibration set and the PLE n-gram table\ntools/          the measurement tools (bench.py, the KV-cache quality harness, the PLE table builder)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes,\n                the measurement logs, the model card.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the two serving assets: the AutoRound calibration set and the PLE n-gram table\ntools/          the measurement tools (bench.py, the KV-cache quality harness, the PLE table builder)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes,\n                the measurement logs, the model card\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the two serving assets: the AutoRound calibration set and the PLE n-gram table\ntools/          the measurement tools (bench.py, the KV-cache quality harness, the PLE table builder)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes,\n                the measurement logs, the model card\ndocs.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the two serving assets: the AutoRound calibration set and the PLE n-gram table\ntools/          the measurement tools (bench.py, the KV-cache quality harness, the PLE table builder)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes,\n                the measurement logs, the model card\ndocs/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the two serving assets: the AutoRound calibration set and the PLE n-gram table\ntools/          the measurement tools (bench.py, the KV-cache quality harness, the PLE table builder)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes,\n                the measurement logs, the model card\ndocs/quant.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the two serving assets: the AutoRound calibration set and the PLE n-gram table\ntools/          the measurement tools (bench.py, the KV-cache quality harness, the PLE table builder)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes,\n                the measurement logs, the model card\ndocs/quant/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the two serving assets: the AutoRound calibration set and the PLE n-gram table\ntools/          the measurement tools (bench.py, the KV-cache quality harness, the PLE table builder)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes,\n                the measurement logs, the model card\ndocs/quant/    .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the two serving assets: the AutoRound calibration set and the PLE n-gram table\ntools/          the measurement tools (bench.py, the KV-cache quality harness, the PLE table builder)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes,\n                the measurement logs, the model card\ndocs/quant/     the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the two serving assets: the AutoRound calibration set and the PLE n-gram table\ntools/          the measurement tools (bench.py, the KV-cache quality harness, the PLE table builder)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes,\n                the measurement logs, the model card\ndocs/quant/     the quant.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the two serving assets: the AutoRound calibration set and the PLE n-gram table\ntools/          the measurement tools (bench.py, the KV-cache quality harness, the PLE table builder)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes,\n                the measurement logs, the model card\ndocs/quant/     the quantization.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the two serving assets: the AutoRound calibration set and the PLE n-gram table\ntools/          the measurement tools (bench.py, the KV-cache quality harness, the PLE table builder)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes,\n                the measurement logs, the model card\ndocs/quant/     the quantization code.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the two serving assets: the AutoRound calibration set and the PLE n-gram table\ntools/          the measurement tools (bench.py, the KV-cache quality harness, the PLE table builder)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes,\n                the measurement logs, the model card\ndocs/quant/     the quantization code (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the two serving assets: the AutoRound calibration set and the PLE n-gram table\ntools/          the measurement tools (bench.py, the KV-cache quality harness, the PLE table builder)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes,\n                the measurement logs, the model card\ndocs/quant/     the quantization code (the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the two serving assets: the AutoRound calibration set and the PLE n-gram table\ntools/          the measurement tools (bench.py, the KV-cache quality harness, the PLE table builder)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes,\n                the measurement logs, the model card\ndocs/quant/     the quantization code (the Auto.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the two serving assets: the AutoRound calibration set and the PLE n-gram table\ntools/          the measurement tools (bench.py, the KV-cache quality harness, the PLE table builder)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes,\n                the measurement logs, the model card\ndocs/quant/     the quantization code (the AutoRound.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the two serving assets: the AutoRound calibration set and the PLE n-gram table\ntools/          the measurement tools (bench.py, the KV-cache quality harness, the PLE table builder)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes,\n                the measurement logs, the model card\ndocs/quant/     the quantization code (the AutoRound recipe.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the two serving assets: the AutoRound calibration set and the PLE n-gram table\ntools/          the measurement tools (bench.py, the KV-cache quality harness, the PLE table builder)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes,\n                the measurement logs, the model card\ndocs/quant/     the quantization code (the AutoRound recipe,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the two serving assets: the AutoRound calibration set and the PLE n-gram table\ntools/          the measurement tools (bench.py, the KV-cache quality harness, the PLE table builder)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes,\n                the measurement logs, the model card\ndocs/quant/     the quantization code (the AutoRound recipe, the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the two serving assets: the AutoRound calibration set and the PLE n-gram table\ntools/          the measurement tools (bench.py, the KV-cache quality harness, the PLE table builder)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes,\n                the measurement logs, the model card\ndocs/quant/     the quantization code (the AutoRound recipe, the calibration.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the two serving assets: the AutoRound calibration set and the PLE n-gram table\ntools/          the measurement tools (bench.py, the KV-cache quality harness, the PLE table builder)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes,\n                the measurement logs, the model card\ndocs/quant/     the quantization code (the AutoRound recipe, the calibration,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the two serving assets: the AutoRound calibration set and the PLE n-gram table\ntools/          the measurement tools (bench.py, the KV-cache quality harness, the PLE table builder)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes,\n                the measurement logs, the model card\ndocs/quant/     the quantization code (the AutoRound recipe, the calibration, the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the two serving assets: the AutoRound calibration set and the PLE n-gram table\ntools/          the measurement tools (bench.py, the KV-cache quality harness, the PLE table builder)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes,\n                the measurement logs, the model card\ndocs/quant/     the quantization code (the AutoRound recipe, the calibration, the quality.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the two serving assets: the AutoRound calibration set and the PLE n-gram table\ntools/          the measurement tools (bench.py, the KV-cache quality harness, the PLE table builder)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes,\n                the measurement logs, the model card\ndocs/quant/     the quantization code (the AutoRound recipe, the calibration, the quality harness.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the two serving assets: the AutoRound calibration set and the PLE n-gram table\ntools/          the measurement tools (bench.py, the KV-cache quality harness, the PLE table builder)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes,\n                the measurement logs, the model card\ndocs/quant/     the quantization code (the AutoRound recipe, the calibration, the quality harness).py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the two serving assets: the AutoRound calibration set and the PLE n-gram table\ntools/          the measurement tools (bench.py, the KV-cache quality harness, the PLE table builder)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes,\n                the measurement logs, the model card\ndocs/quant/     the quantization code (the AutoRound recipe, the calibration, the quality harness)\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the two serving assets: the AutoRound calibration set and the PLE n-gram table\ntools/          the measurement tools (bench.py, the KV-cache quality harness, the PLE table builder)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes,\n                the measurement logs, the model card\ndocs/quant/     the quantization code (the AutoRound recipe, the calibration, the quality harness)\ndocs.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the two serving assets: the AutoRound calibration set and the PLE n-gram table\ntools/          the measurement tools (bench.py, the KV-cache quality harness, the PLE table builder)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes,\n                the measurement logs, the model card\ndocs/quant/     the quantization code (the AutoRound recipe, the calibration, the quality harness)\ndocs/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the two serving assets: the AutoRound calibration set and the PLE n-gram table\ntools/          the measurement tools (bench.py, the KV-cache quality harness, the PLE table builder)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes,\n                the measurement logs, the model card\ndocs/quant/     the quantization code (the AutoRound recipe, the calibration, the quality harness)\ndocs/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the two serving assets: the AutoRound calibration set and the PLE n-gram table\ntools/          the measurement tools (bench.py, the KV-cache quality harness, the PLE table builder)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes,\n                the measurement logs, the model card\ndocs/quant/     the quantization code (the AutoRound recipe, the calibration, the quality harness)\ndocs/ple/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the two serving assets: the AutoRound calibration set and the PLE n-gram table\ntools/          the measurement tools (bench.py, the KV-cache quality harness, the PLE table builder)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes,\n                the measurement logs, the model card\ndocs/quant/     the quantization code (the AutoRound recipe, the calibration, the quality harness)\ndocs/ple/      .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the two serving assets: the AutoRound calibration set and the PLE n-gram table\ntools/          the measurement tools (bench.py, the KV-cache quality harness, the PLE table builder)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes,\n                the measurement logs, the model card\ndocs/quant/     the quantization code (the AutoRound recipe, the calibration, the quality harness)\ndocs/ple/       the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the two serving assets: the AutoRound calibration set and the PLE n-gram table\ntools/          the measurement tools (bench.py, the KV-cache quality harness, the PLE table builder)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes,\n                the measurement logs, the model card\ndocs/quant/     the quantization code (the AutoRound recipe, the calibration, the quality harness)\ndocs/ple/       the P.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the two serving assets: the AutoRound calibration set and the PLE n-gram table\ntools/          the measurement tools (bench.py, the KV-cache quality harness, the PLE table builder)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes,\n                the measurement logs, the model card\ndocs/quant/     the quantization code (the AutoRound recipe, the calibration, the quality harness)\ndocs/ple/       the PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the two serving assets: the AutoRound calibration set and the PLE n-gram table\ntools/          the measurement tools (bench.py, the KV-cache quality harness, the PLE table builder)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes,\n                the measurement logs, the model card\ndocs/quant/     the quantization code (the AutoRound recipe, the calibration, the quality harness)\ndocs/ple/       the PLE design.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the two serving assets: the AutoRound calibration set and the PLE n-gram table\ntools/          the measurement tools (bench.py, the KV-cache quality harness, the PLE table builder)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes,\n                the measurement logs, the model card\ndocs/quant/     the quantization code (the AutoRound recipe, the calibration, the quality harness)\ndocs/ple/       the PLE design and.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the two serving assets: the AutoRound calibration set and the PLE n-gram table\ntools/          the measurement tools (bench.py, the KV-cache quality harness, the PLE table builder)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes,\n                the measurement logs, the model card\ndocs/quant/     the quantization code (the AutoRound recipe, the calibration, the quality harness)\ndocs/ple/       the PLE design and the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the two serving assets: the AutoRound calibration set and the PLE n-gram table\ntools/          the measurement tools (bench.py, the KV-cache quality harness, the PLE table builder)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes,\n                the measurement logs, the model card\ndocs/quant/     the quantization code (the AutoRound recipe, the calibration, the quality harness)\ndocs/ple/       the PLE design and the P.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the two serving assets: the AutoRound calibration set and the PLE n-gram table\ntools/          the measurement tools (bench.py, the KV-cache quality harness, the PLE table builder)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes,\n                the measurement logs, the model card\ndocs/quant/     the quantization code (the AutoRound recipe, the calibration, the quality harness)\ndocs/ple/       the PLE design and the PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the two serving assets: the AutoRound calibration set and the PLE n-gram table\ntools/          the measurement tools (bench.py, the KV-cache quality harness, the PLE table builder)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes,\n                the measurement logs, the model card\ndocs/quant/     the quantization code (the AutoRound recipe, the calibration, the quality harness)\ndocs/ple/       the PLE design and the PLE table.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the two serving assets: the AutoRound calibration set and the PLE n-gram table\ntools/          the measurement tools (bench.py, the KV-cache quality harness, the PLE table builder)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes,\n                the measurement logs, the model card\ndocs/quant/     the quantization code (the AutoRound recipe, the calibration, the quality harness)\ndocs/ple/       the PLE design and the PLE table builder.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the two serving assets: the AutoRound calibration set and the PLE n-gram table\ntools/          the measurement tools (bench.py, the KV-cache quality harness, the PLE table builder)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes,\n                the measurement logs, the model card\ndocs/quant/     the quantization code (the AutoRound recipe, the calibration, the quality harness)\ndocs/ple/       the PLE design and the PLE table builder\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the two serving assets: the AutoRound calibration set and the PLE n-gram table\ntools/          the measurement tools (bench.py, the KV-cache quality harness, the PLE table builder)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes,\n                the measurement logs, the model card\ndocs/quant/     the quantization code (the AutoRound recipe, the calibration, the quality harness)\ndocs/ple/       the PLE design and the PLE table builder\ndocs.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the two serving assets: the AutoRound calibration set and the PLE n-gram table\ntools/          the measurement tools (bench.py, the KV-cache quality harness, the PLE table builder)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes,\n                the measurement logs, the model card\ndocs/quant/     the quantization code (the AutoRound recipe, the calibration, the quality harness)\ndocs/ple/       the PLE design and the PLE table builder\ndocs/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the two serving assets: the AutoRound calibration set and the PLE n-gram table\ntools/          the measurement tools (bench.py, the KV-cache quality harness, the PLE table builder)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes,\n                the measurement logs, the model card\ndocs/quant/     the quantization code (the AutoRound recipe, the calibration, the quality harness)\ndocs/ple/       the PLE design and the PLE table builder\ndocs/sg.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the two serving assets: the AutoRound calibration set and the PLE n-gram table\ntools/          the measurement tools (bench.py, the KV-cache quality harness, the PLE table builder)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes,\n                the measurement logs, the model card\ndocs/quant/     the quantization code (the AutoRound recipe, the calibration, the quality harness)\ndocs/ple/       the PLE design and the PLE table builder\ndocs/sglang.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the two serving assets: the AutoRound calibration set and the PLE n-gram table\ntools/          the measurement tools (bench.py, the KV-cache quality harness, the PLE table builder)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes,\n                the measurement logs, the model card\ndocs/quant/     the quantization code (the AutoRound recipe, the calibration, the quality harness)\ndocs/ple/       the PLE design and the PLE table builder\ndocs/sglang/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the two serving assets: the AutoRound calibration set and the PLE n-gram table\ntools/          the measurement tools (bench.py, the KV-cache quality harness, the PLE table builder)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes,\n                the measurement logs, the model card\ndocs/quant/     the quantization code (the AutoRound recipe, the calibration, the quality harness)\ndocs/ple/       the PLE design and the PLE table builder\ndocs/sglang/   .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the two serving assets: the AutoRound calibration set and the PLE n-gram table\ntools/          the measurement tools (bench.py, the KV-cache quality harness, the PLE table builder)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes,\n                the measurement logs, the model card\ndocs/quant/     the quantization code (the AutoRound recipe, the calibration, the quality harness)\ndocs/ple/       the PLE design and the PLE table builder\ndocs/sglang/    the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the two serving assets: the AutoRound calibration set and the PLE n-gram table\ntools/          the measurement tools (bench.py, the KV-cache quality harness, the PLE table builder)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes,\n                the measurement logs, the model card\ndocs/quant/     the quantization code (the AutoRound recipe, the calibration, the quality harness)\ndocs/ple/       the PLE design and the PLE table builder\ndocs/sglang/    the S.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the two serving assets: the AutoRound calibration set and the PLE n-gram table\ntools/          the measurement tools (bench.py, the KV-cache quality harness, the PLE table builder)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes,\n                the measurement logs, the model card\ndocs/quant/     the quantization code (the AutoRound recipe, the calibration, the quality harness)\ndocs/ple/       the PLE design and the PLE table builder\ndocs/sglang/    the SGL.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the two serving assets: the AutoRound calibration set and the PLE n-gram table\ntools/          the measurement tools (bench.py, the KV-cache quality harness, the PLE table builder)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes,\n                the measurement logs, the model card\ndocs/quant/     the quantization code (the AutoRound recipe, the calibration, the quality harness)\ndocs/ple/       the PLE design and the PLE table builder\ndocs/sglang/    the SGLang.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the two serving assets: the AutoRound calibration set and the PLE n-gram table\ntools/          the measurement tools (bench.py, the KV-cache quality harness, the PLE table builder)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes,\n                the measurement logs, the model card\ndocs/quant/     the quantization code (the AutoRound recipe, the calibration, the quality harness)\ndocs/ple/       the PLE design and the PLE table builder\ndocs/sglang/    the SGLang design.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the two serving assets: the AutoRound calibration set and the PLE n-gram table\ntools/          the measurement tools (bench.py, the KV-cache quality harness, the PLE table builder)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes,\n                the measurement logs, the model card\ndocs/quant/     the quantization code (the AutoRound recipe, the calibration, the quality harness)\ndocs/ple/       the PLE design and the PLE table builder\ndocs/sglang/    the SGLang design documents.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the two serving assets: the AutoRound calibration set and the PLE n-gram table\ntools/          the measurement tools (bench.py, the KV-cache quality harness, the PLE table builder)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes,\n                the measurement logs, the model card\ndocs/quant/     the quantization code (the AutoRound recipe, the calibration, the quality harness)\ndocs/ple/       the PLE design and the PLE table builder\ndocs/sglang/    the SGLang design documents (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the two serving assets: the AutoRound calibration set and the PLE n-gram table\ntools/          the measurement tools (bench.py, the KV-cache quality harness, the PLE table builder)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes,\n                the measurement logs, the model card\ndocs/quant/     the quantization code (the AutoRound recipe, the calibration, the quality harness)\ndocs/ple/       the PLE design and the PLE table builder\ndocs/sglang/    the SGLang design documents (the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the two serving assets: the AutoRound calibration set and the PLE n-gram table\ntools/          the measurement tools (bench.py, the KV-cache quality harness, the PLE table builder)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes,\n                the measurement logs, the model card\ndocs/quant/     the quantization code (the AutoRound recipe, the calibration, the quality harness)\ndocs/ple/       the PLE design and the PLE table builder\ndocs/sglang/    the SGLang design documents (the serving.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the two serving assets: the AutoRound calibration set and the PLE n-gram table\ntools/          the measurement tools (bench.py, the KV-cache quality harness, the PLE table builder)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes,\n                the measurement logs, the model card\ndocs/quant/     the quantization code (the AutoRound recipe, the calibration, the quality harness)\ndocs/ple/       the PLE design and the PLE table builder\ndocs/sglang/    the SGLang design documents (the serving patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the two serving assets: the AutoRound calibration set and the PLE n-gram table\ntools/          the measurement tools (bench.py, the KV-cache quality harness, the PLE table builder)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes,\n                the measurement logs, the model card\ndocs/quant/     the quantization code (the AutoRound recipe, the calibration, the quality harness)\ndocs/ple/       the PLE design and the PLE table builder\ndocs/sglang/    the SGLang design documents (the serving patch,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the two serving assets: the AutoRound calibration set and the PLE n-gram table\ntools/          the measurement tools (bench.py, the KV-cache quality harness, the PLE table builder)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes,\n                the measurement logs, the model card\ndocs/quant/     the quantization code (the AutoRound recipe, the calibration, the quality harness)\ndocs/ple/       the PLE design and the PLE table builder\ndocs/sglang/    the SGLang design documents (the serving patch, the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the two serving assets: the AutoRound calibration set and the PLE n-gram table\ntools/          the measurement tools (bench.py, the KV-cache quality harness, the PLE table builder)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes,\n                the measurement logs, the model card\ndocs/quant/     the quantization code (the AutoRound recipe, the calibration, the quality harness)\ndocs/ple/       the PLE design and the PLE table builder\ndocs/sglang/    the SGLang design documents (the serving patch, the elastic.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the two serving assets: the AutoRound calibration set and the PLE n-gram table\ntools/          the measurement tools (bench.py, the KV-cache quality harness, the PLE table builder)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes,\n                the measurement logs, the model card\ndocs/quant/     the quantization code (the AutoRound recipe, the calibration, the quality harness)\ndocs/ple/       the PLE design and the PLE table builder\ndocs/sglang/    the SGLang design documents (the serving patch, the elastic controller.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the two serving assets: the AutoRound calibration set and the PLE n-gram table\ntools/          the measurement tools (bench.py, the KV-cache quality harness, the PLE table builder)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes,\n                the measurement logs, the model card\ndocs/quant/     the quantization code (the AutoRound recipe, the calibration, the quality harness)\ndocs/ple/       the PLE design and the PLE table builder\ndocs/sglang/    the SGLang design documents (the serving patch, the elastic controller,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the two serving assets: the AutoRound calibration set and the PLE n-gram table\ntools/          the measurement tools (bench.py, the KV-cache quality harness, the PLE table builder)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes,\n                the measurement logs, the model card\ndocs/quant/     the quantization code (the AutoRound recipe, the calibration, the quality harness)\ndocs/ple/       the PLE design and the PLE table builder\ndocs/sglang/    the SGLang design documents (the serving patch, the elastic controller, the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the two serving assets: the AutoRound calibration set and the PLE n-gram table\ntools/          the measurement tools (bench.py, the KV-cache quality harness, the PLE table builder)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes,\n                the measurement logs, the model card\ndocs/quant/     the quantization code (the AutoRound recipe, the calibration, the quality harness)\ndocs/ple/       the PLE design and the PLE table builder\ndocs/sglang/    the SGLang design documents (the serving patch, the elastic controller, the KV.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the two serving assets: the AutoRound calibration set and the PLE n-gram table\ntools/          the measurement tools (bench.py, the KV-cache quality harness, the PLE table builder)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes,\n                the measurement logs, the model card\ndocs/quant/     the quantization code (the AutoRound recipe, the calibration, the quality harness)\ndocs/ple/       the PLE design and the PLE table builder\ndocs/sglang/    the SGLang design documents (the serving patch, the elastic controller, the KV tiers.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the two serving assets: the AutoRound calibration set and the PLE n-gram table\ntools/          the measurement tools (bench.py, the KV-cache quality harness, the PLE table builder)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes,\n                the measurement logs, the model card\ndocs/quant/     the quantization code (the AutoRound recipe, the calibration, the quality harness)\ndocs/ple/       the PLE design and the PLE table builder\ndocs/sglang/    the SGLang design documents (the serving patch, the elastic controller, the KV tiers).py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the two serving assets: the AutoRound calibration set and the PLE n-gram table\ntools/          the measurement tools (bench.py, the KV-cache quality harness, the PLE table builder)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes,\n                the measurement logs, the model card\ndocs/quant/     the quantization code (the AutoRound recipe, the calibration, the quality harness)\ndocs/ple/       the PLE design and the PLE table builder\ndocs/sglang/    the SGLang design documents (the serving patch, the elastic controller, the KV tiers)\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the two serving assets: the AutoRound calibration set and the PLE n-gram table\ntools/          the measurement tools (bench.py, the KV-cache quality harness, the PLE table builder)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes,\n                the measurement logs, the model card\ndocs/quant/     the quantization code (the AutoRound recipe, the calibration, the quality harness)\ndocs/ple/       the PLE design and the PLE table builder\ndocs/sglang/    the SGLang design documents (the serving patch, the elastic controller, the KV tiers)\ndocs.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the two serving assets: the AutoRound calibration set and the PLE n-gram table\ntools/          the measurement tools (bench.py, the KV-cache quality harness, the PLE table builder)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes,\n                the measurement logs, the model card\ndocs/quant/     the quantization code (the AutoRound recipe, the calibration, the quality harness)\ndocs/ple/       the PLE design and the PLE table builder\ndocs/sglang/    the SGLang design documents (the serving patch, the elastic controller, the KV tiers)\ndocs/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the two serving assets: the AutoRound calibration set and the PLE n-gram table\ntools/          the measurement tools (bench.py, the KV-cache quality harness, the PLE table builder)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes,\n                the measurement logs, the model card\ndocs/quant/     the quantization code (the AutoRound recipe, the calibration, the quality harness)\ndocs/ple/       the PLE design and the PLE table builder\ndocs/sglang/    the SGLang design documents (the serving patch, the elastic controller, the KV tiers)\ndocs/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the two serving assets: the AutoRound calibration set and the PLE n-gram table\ntools/          the measurement tools (bench.py, the KV-cache quality harness, the PLE table builder)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes,\n                the measurement logs, the model card\ndocs/quant/     the quantization code (the AutoRound recipe, the calibration, the quality harness)\ndocs/ple/       the PLE design and the PLE table builder\ndocs/sglang/    the SGLang design documents (the serving patch, the elastic controller, the KV tiers)\ndocs/ple/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the two serving assets: the AutoRound calibration set and the PLE n-gram table\ntools/          the measurement tools (bench.py, the KV-cache quality harness, the PLE table builder)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes,\n                the measurement logs, the model card\ndocs/quant/     the quantization code (the AutoRound recipe, the calibration, the quality harness)\ndocs/ple/       the PLE design and the PLE table builder\ndocs/sglang/    the SGLang design documents (the serving patch, the elastic controller, the KV tiers)\ndocs/ple/      .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the two serving assets: the AutoRound calibration set and the PLE n-gram table\ntools/          the measurement tools (bench.py, the KV-cache quality harness, the PLE table builder)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes,\n                the measurement logs, the model card\ndocs/quant/     the quantization code (the AutoRound recipe, the calibration, the quality harness)\ndocs/ple/       the PLE design and the PLE table builder\ndocs/sglang/    the SGLang design documents (the serving patch, the elastic controller, the KV tiers)\ndocs/ple/       the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the two serving assets: the AutoRound calibration set and the PLE n-gram table\ntools/          the measurement tools (bench.py, the KV-cache quality harness, the PLE table builder)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes,\n                the measurement logs, the model card\ndocs/quant/     the quantization code (the AutoRound recipe, the calibration, the quality harness)\ndocs/ple/       the PLE design and the PLE table builder\ndocs/sglang/    the SGLang design documents (the serving patch, the elastic controller, the KV tiers)\ndocs/ple/       the P.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the two serving assets: the AutoRound calibration set and the PLE n-gram table\ntools/          the measurement tools (bench.py, the KV-cache quality harness, the PLE table builder)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes,\n                the measurement logs, the model card\ndocs/quant/     the quantization code (the AutoRound recipe, the calibration, the quality harness)\ndocs/ple/       the PLE design and the PLE table builder\ndocs/sglang/    the SGLang design documents (the serving patch, the elastic controller, the KV tiers)\ndocs/ple/       the PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the two serving assets: the AutoRound calibration set and the PLE n-gram table\ntools/          the measurement tools (bench.py, the KV-cache quality harness, the PLE table builder)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes,\n                the measurement logs, the model card\ndocs/quant/     the quantization code (the AutoRound recipe, the calibration, the quality harness)\ndocs/ple/       the PLE design and the PLE table builder\ndocs/sglang/    the SGLang design documents (the serving patch, the elastic controller, the KV tiers)\ndocs/ple/       the PLE design.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the two serving assets: the AutoRound calibration set and the PLE n-gram table\ntools/          the measurement tools (bench.py, the KV-cache quality harness, the PLE table builder)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes,\n                the measurement logs, the model card\ndocs/quant/     the quantization code (the AutoRound recipe, the calibration, the quality harness)\ndocs/ple/       the PLE design and the PLE table builder\ndocs/sglang/    the SGLang design documents (the serving patch, the elastic controller, the KV tiers)\ndocs/ple/       the PLE design and.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the two serving assets: the AutoRound calibration set and the PLE n-gram table\ntools/          the measurement tools (bench.py, the KV-cache quality harness, the PLE table builder)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes,\n                the measurement logs, the model card\ndocs/quant/     the quantization code (the AutoRound recipe, the calibration, the quality harness)\ndocs/ple/       the PLE design and the PLE table builder\ndocs/sglang/    the SGLang design documents (the serving patch, the elastic controller, the KV tiers)\ndocs/ple/       the PLE design and the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the two serving assets: the AutoRound calibration set and the PLE n-gram table\ntools/          the measurement tools (bench.py, the KV-cache quality harness, the PLE table builder)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes,\n                the measurement logs, the model card\ndocs/quant/     the quantization code (the AutoRound recipe, the calibration, the quality harness)\ndocs/ple/       the PLE design and the PLE table builder\ndocs/sglang/    the SGLang design documents (the serving patch, the elastic controller, the KV tiers)\ndocs/ple/       the PLE design and the P.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the two serving assets: the AutoRound calibration set and the PLE n-gram table\ntools/          the measurement tools (bench.py, the KV-cache quality harness, the PLE table builder)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes,\n                the measurement logs, the model card\ndocs/quant/     the quantization code (the AutoRound recipe, the calibration, the quality harness)\ndocs/ple/       the PLE design and the PLE table builder\ndocs/sglang/    the SGLang design documents (the serving patch, the elastic controller, the KV tiers)\ndocs/ple/       the PLE design and the PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the two serving assets: the AutoRound calibration set and the PLE n-gram table\ntools/          the measurement tools (bench.py, the KV-cache quality harness, the PLE table builder)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes,\n                the measurement logs, the model card\ndocs/quant/     the quantization code (the AutoRound recipe, the calibration, the quality harness)\ndocs/ple/       the PLE design and the PLE table builder\ndocs/sglang/    the SGLang design documents (the serving patch, the elastic controller, the KV tiers)\ndocs/ple/       the PLE design and the PLE table.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the two serving assets: the AutoRound calibration set and the PLE n-gram table\ntools/          the measurement tools (bench.py, the KV-cache quality harness, the PLE table builder)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes,\n                the measurement logs, the model card\ndocs/quant/     the quantization code (the AutoRound recipe, the calibration, the quality harness)\ndocs/ple/       the PLE design and the PLE table builder\ndocs/sglang/    the SGLang design documents (the serving patch, the elastic controller, the KV tiers)\ndocs/ple/       the PLE design and the PLE table builder.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the two serving assets: the AutoRound calibration set and the PLE n-gram table\ntools/          the measurement tools (bench.py, the KV-cache quality harness, the PLE table builder)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes,\n                the measurement logs, the model card\ndocs/quant/     the quantization code (the AutoRound recipe, the calibration, the quality harness)\ndocs/ple/       the PLE design and the PLE table builder\ndocs/sglang/    the SGLang design documents (the serving patch, the elastic controller, the KV tiers)\ndocs/ple/       the PLE design and the PLE table builder\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the two serving assets: the AutoRound calibration set and the PLE n-gram table\ntools/          the measurement tools (bench.py, the KV-cache quality harness, the PLE table builder)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes,\n                the measurement logs, the model card\ndocs/quant/     the quantization code (the AutoRound recipe, the calibration, the quality harness)\ndocs/ple/       the PLE design and the PLE table builder\ndocs/sglang/    the SGLang design documents (the serving patch, the elastic controller, the KV tiers)\ndocs/ple/       the PLE design and the PLE table builder\ndocs.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the two serving assets: the AutoRound calibration set and the PLE n-gram table\ntools/          the measurement tools (bench.py, the KV-cache quality harness, the PLE table builder)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes,\n                the measurement logs, the model card\ndocs/quant/     the quantization code (the AutoRound recipe, the calibration, the quality harness)\ndocs/ple/       the PLE design and the PLE table builder\ndocs/sglang/    the SGLang design documents (the serving patch, the elastic controller, the KV tiers)\ndocs/ple/       the PLE design and the PLE table builder\ndocs/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of the accepted state: it sets the environment, applies the patch list\n(`patches/`), starts the server and waits for `/health`. The published numbers were measured with\n`--max-running-requests 1` (CAMPAIGN.md:413); `--max-running-requests 4` is the elastic-controller default\n(CAMPAIGN.md:320). The server's `--ple-table-path` must be on NVMe: the PLE table is 51.2 GB and the\n`ple/` directory is part of the checkpoint.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (001..010, in build order)\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV pool, INT4 KV pool,\n                INT8 KV ring, PLE n-gram lookup, PLE dense GEMM, PLE int8 GEMM, PLE int4 GEMM)\nscripts/        serve.sh (the launcher of the accepted state), build.sh (the build of the assets),\n                bench.sh, the CUDA 13 build scripts\nassets/         the two serving assets: the AutoRound calibration set and the PLE n-gram table\ntools/          the measurement tools (bench.py, the KV-cache quality harness, the PLE table builder)\ndocs/           CAMPAIGN.md (the engineering log), the design documents with their outcomes,\n                the measurement logs, the model card\ndocs/quant/     the quantization code (the AutoRound recipe, the calibration, the quality harness)\ndocs/ple/       the PLE design and the PLE table builder\ndocs/sglang/    the SGLang design documents (the serving patch, the elastic controller, the KV tiers)\ndocs/ple/       the PLE design and the PLE table builder\ndocs/sg",
 "ttft_seconds": 3.0016119909996632,
 "status": "ok",
 "tg_tokens_per_s": 37.45991418615947,
 "decode_seconds_per_token": 0.026695202637956807,
 "individual_token_timing": true,
 "event_intervals_ms": {
  "n": 511,
  "median": 26.245815999573097,
  "p95": 33.77977899799589,
  "mean": 26.695202637956807
 },
 "class": "prose",
 "requested_input_tokens": 2048
}
