{
 "started_utc": "2026-09-16T23:10:44.459670+00:00",
 "wall_seconds": 14.538537084998097,
 "prompt_tokens": 2048,
 "completion_tokens": 512,
 "stream_events": 512,
 "ended_utc": "2026-09-16T23:10:58.998280+00:00",
 "warmup": false,
 "sample_index": 3,
 "requested_decode_tokens": 512,
 "gpu": {
  "n": 52,
  "temperature_c": {
   "min": 75.0,
   "max": 76.0,
   "mean": 75.73076923076923
  },
  "power_w": {
   "min": 255.52,
   "max": 275.21,
   "mean": 267.8675
  },
  "util_pct": {
   "min": 88.0,
   "max": 100.0,
   "mean": 90.26923076923077
  },
  "clock_sm_mhz": {
   "min": 1755.0,
   "max": 1920.0,
   "mean": 1907.3076923076924
  },
  "clock_mem_mhz": {
   "min": 9501.0,
   "max": 9501.0,
   "mean": 9501.0
  },
  "memory_free_mib": {
   "min": 2540.0
  }
 },
 "output_text_sha256": "01f26f740779eb57343ead82fb21ed7e6eb76fc3458819096d6867e0b960c4f2",
 "output_text": ".py.py --.py --url.py --url http.py --url http://.py --url http://1.py --url http://12.py --url http://127.py --url http://127..py --url http://127.0.py --url http://127.0..py --url http://127.0.0.py --url http://127.0.0..py --url http://127.0.0.1.py --url http://127.0.0.1:.py --url http://127.0.0.1:3.py --url http://127.0.0.1:30.py --url http://127.0.0.1:300.py --url http://127.0.0.1:3000.py --url http://127.0.0.1:30000.py --url http://127.0.0.1:30000 --.py --url http://127.0.0.1:30000 --prompt.py --url http://127.0.0.1:30000 --prompt-t.py --url http://127.0.0.1:30000 --prompt-tokens.py --url http://127.0.0.1:30000 --prompt-tokens .py --url http://127.0.0.1:30000 --prompt-tokens 1.py --url http://127.0.0.1:30000 --prompt-tokens 10.py --url http://127.0.0.1:30000 --prompt-tokens 101.py --url http://127.0.0.1:30000 --prompt-tokens 101 .py --url http://127.0.0.1:30000 --prompt-tokens 101 4.py --url http://127.0.0.1:30000 --prompt-tokens 101 42.py --url http://127.0.0.1:30000 --prompt-tokens 101 421.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 17.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 170.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 68.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 682.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 100.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 1000.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-t.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 12.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record:.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nS.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGL.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`..py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time),.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-t.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 26.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 2621.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 26214.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context),.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tier.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-d.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`..py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NV.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk:.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the P.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode..py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the P.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table;.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-d.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`..py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n##.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsg.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/        .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (q.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-7.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a25.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a2552.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a25520.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch).py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nk.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/       .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Trit.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels:.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_m.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 Mo.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE),.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_m.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_p.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 Mo.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE +.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + P.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE),.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_m.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_p.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n               .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (the pre.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (the pre-p.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (the pre-patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (the pre-patch copy.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (the pre-patch copy used.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (the pre-patch copy used by.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (the pre-patch copy used by the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (the pre-patch copy used by the P.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (the pre-patch copy used by the PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (the pre-patch copy used by the PLE patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (the pre-patch copy used by the PLE patch),.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (the pre-patch copy used by the PLE patch), int.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (the pre-patch copy used by the PLE patch), int2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (the pre-patch copy used by the PLE patch), int2_m.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (the pre-patch copy used by the PLE patch), int2_moe.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (the pre-patch copy used by the PLE patch), int2_moe_p.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (the pre-patch copy used by the PLE patch), int2_moe_ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (the pre-patch copy used by the PLE patch), int2_moe_ple_patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (the pre-patch copy used by the PLE patch), int2_moe_ple_patch.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (the pre-patch copy used by the PLE patch), int2_moe_ple_patch.py (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (the pre-patch copy used by the PLE patch), int2_moe_ple_patch.py (the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (the pre-patch copy used by the PLE patch), int2_moe_ple_patch.py (the P.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (the pre-patch copy used by the PLE patch), int2_moe_ple_patch.py (the PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (the pre-patch copy used by the PLE patch), int2_moe_ple_patch.py (the PLE patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (the pre-patch copy used by the PLE patch), int2_moe_ple_patch.py (the PLE patch itself.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (the pre-patch copy used by the PLE patch), int2_moe_ple_patch.py (the PLE patch itself).py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (the pre-patch copy used by the PLE patch), int2_moe_ple_patch.py (the PLE patch itself)\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (the pre-patch copy used by the PLE patch), int2_moe_ple_patch.py (the PLE patch itself)\nassets.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (the pre-patch copy used by the PLE patch), int2_moe_ple_patch.py (the PLE patch itself)\nassets/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (the pre-patch copy used by the PLE patch), int2_moe_ple_patch.py (the PLE patch itself)\nassets/        .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (the pre-patch copy used by the PLE patch), int2_moe_ple_patch.py (the PLE patch itself)\nassets/         the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (the pre-patch copy used by the PLE patch), int2_moe_ple_patch.py (the PLE patch itself)\nassets/         the two.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (the pre-patch copy used by the PLE patch), int2_moe_ple_patch.py (the PLE patch itself)\nassets/         the two serving.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (the pre-patch copy used by the PLE patch), int2_moe_ple_patch.py (the PLE patch itself)\nassets/         the two serving assets.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (the pre-patch copy used by the PLE patch), int2_moe_ple_patch.py (the PLE patch itself)\nassets/         the two serving assets:.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (the pre-patch copy used by the PLE patch), int2_moe_ple_patch.py (the PLE patch itself)\nassets/         the two serving assets: the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (the pre-patch copy used by the PLE patch), int2_moe_ple_patch.py (the PLE patch itself)\nassets/         the two serving assets: the P.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (the pre-patch copy used by the PLE patch), int2_moe_ple_patch.py (the PLE patch itself)\nassets/         the two serving assets: the PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (the pre-patch copy used by the PLE patch), int2_moe_ple_patch.py (the PLE patch itself)\nassets/         the two serving assets: the PLE n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (the pre-patch copy used by the PLE patch), int2_moe_ple_patch.py (the PLE patch itself)\nassets/         the two serving assets: the PLE n-.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (the pre-patch copy used by the PLE patch), int2_moe_ple_patch.py (the PLE patch itself)\nassets/         the two serving assets: the PLE n-gram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (the pre-patch copy used by the PLE patch), int2_moe_ple_patch.py (the PLE patch itself)\nassets/         the two serving assets: the PLE n-gram table.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (the pre-patch copy used by the PLE patch), int2_moe_ple_patch.py (the PLE patch itself)\nassets/         the two serving assets: the PLE n-gram table (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (the pre-patch copy used by the PLE patch), int2_moe_ple_patch.py (the PLE patch itself)\nassets/         the two serving assets: the PLE n-gram table (ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (the pre-patch copy used by the PLE patch), int2_moe_ple_patch.py (the PLE patch itself)\nassets/         the two serving assets: the PLE n-gram table (ple/).py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (the pre-patch copy used by the PLE patch), int2_moe_ple_patch.py (the PLE patch itself)\nassets/         the two serving assets: the PLE n-gram table (ple/) and.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (the pre-patch copy used by the PLE patch), int2_moe_ple_patch.py (the PLE patch itself)\nassets/         the two serving assets: the PLE n-gram table (ple/) and the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (the pre-patch copy used by the PLE patch), int2_moe_ple_patch.py (the PLE patch itself)\nassets/         the two serving assets: the PLE n-gram table (ple/) and the int.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (the pre-patch copy used by the PLE patch), int2_moe_ple_patch.py (the PLE patch itself)\nassets/         the two serving assets: the PLE n-gram table (ple/) and the int2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (the pre-patch copy used by the PLE patch), int2_moe_ple_patch.py (the PLE patch itself)\nassets/         the two serving assets: the PLE n-gram table (ple/) and the int2 checkpoint.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (the pre-patch copy used by the PLE patch), int2_moe_ple_patch.py (the PLE patch itself)\nassets/         the two serving assets: the PLE n-gram table (ple/) and the int2 checkpoint (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (the pre-patch copy used by the PLE patch), int2_moe_ple_patch.py (the PLE patch itself)\nassets/         the two serving assets: the PLE n-gram table (ple/) and the int2 checkpoint (model.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (the pre-patch copy used by the PLE patch), int2_moe_ple_patch.py (the PLE patch itself)\nassets/         the two serving assets: the PLE n-gram table (ple/) and the int2 checkpoint (model/).py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (the pre-patch copy used by the PLE patch), int2_moe_ple_patch.py (the PLE patch itself)\nassets/         the two serving assets: the PLE n-gram table (ple/) and the int2 checkpoint (model/)\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (the pre-patch copy used by the PLE patch), int2_moe_ple_patch.py (the PLE patch itself)\nassets/         the two serving assets: the PLE n-gram table (ple/) and the int2 checkpoint (model/)\ntools.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (the pre-patch copy used by the PLE patch), int2_moe_ple_patch.py (the PLE patch itself)\nassets/         the two serving assets: the PLE n-gram table (ple/) and the int2 checkpoint (model/)\ntools/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (the pre-patch copy used by the PLE patch), int2_moe_ple_patch.py (the PLE patch itself)\nassets/         the two serving assets: the PLE n-gram table (ple/) and the int2 checkpoint (model/)\ntools/         .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (the pre-patch copy used by the PLE patch), int2_moe_ple_patch.py (the PLE patch itself)\nassets/         the two serving assets: the PLE n-gram table (ple/) and the int2 checkpoint (model/)\ntools/          the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (the pre-patch copy used by the PLE patch), int2_moe_ple_patch.py (the PLE patch itself)\nassets/         the two serving assets: the PLE n-gram table (ple/) and the int2 checkpoint (model/)\ntools/          the measurement.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (the pre-patch copy used by the PLE patch), int2_moe_ple_patch.py (the PLE patch itself)\nassets/         the two serving assets: the PLE n-gram table (ple/) and the int2 checkpoint (model/)\ntools/          the measurement tools.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (the pre-patch copy used by the PLE patch), int2_moe_ple_patch.py (the PLE patch itself)\nassets/         the two serving assets: the PLE n-gram table (ple/) and the int2 checkpoint (model/)\ntools/          the measurement tools:.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (the pre-patch copy used by the PLE patch), int2_moe_ple_patch.py (the PLE patch itself)\nassets/         the two serving assets: the PLE n-gram table (ple/) and the int2 checkpoint (model/)\ntools/          the measurement tools: bench.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (the pre-patch copy used by the PLE patch), int2_moe_ple_patch.py (the PLE patch itself)\nassets/         the two serving assets: the PLE n-gram table (ple/) and the int2 checkpoint (model/)\ntools/          the measurement tools: bench.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (the pre-patch copy used by the PLE patch), int2_moe_ple_patch.py (the PLE patch itself)\nassets/         the two serving assets: the PLE n-gram table (ple/) and the int2 checkpoint (model/)\ntools/          the measurement tools: bench.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (the pre-patch copy used by the PLE patch), int2_moe_ple_patch.py (the PLE patch itself)\nassets/         the two serving assets: the PLE n-gram table (ple/) and the int2 checkpoint (model/)\ntools/          the measurement tools: bench.py, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (the pre-patch copy used by the PLE patch), int2_moe_ple_patch.py (the PLE patch itself)\nassets/         the two serving assets: the PLE n-gram table (ple/) and the int2 checkpoint (model/)\ntools/          the measurement tools: bench.py, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (the pre-patch copy used by the PLE patch), int2_moe_ple_patch.py (the PLE patch itself)\nassets/         the two serving assets: the PLE n-gram table (ple/) and the int2 checkpoint (model/)\ntools/          the measurement tools: bench.py, ngram.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (the pre-patch copy used by the PLE patch), int2_moe_ple_patch.py (the PLE patch itself)\nassets/         the two serving assets: the PLE n-gram table (ple/) and the int2 checkpoint (model/)\ntools/          the measurement tools: bench.py, ngram.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (the pre-patch copy used by the PLE patch), int2_moe_ple_patch.py (the PLE patch itself)\nassets/         the two serving assets: the PLE n-gram table (ple/) and the int2 checkpoint (model/)\ntools/          the measurement tools: bench.py, ngram.py, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (the pre-patch copy used by the PLE patch), int2_moe_ple_patch.py (the PLE patch itself)\nassets/         the two serving assets: the PLE n-gram table (ple/) and the int2 checkpoint (model/)\ntools/          the measurement tools: bench.py, ngram.py, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (the pre-patch copy used by the PLE patch), int2_moe_ple_patch.py (the PLE patch itself)\nassets/         the two serving assets: the PLE n-gram table (ple/) and the int2 checkpoint (model/)\ntools/          the measurement tools: bench.py, ngram.py, ngram_p.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (the pre-patch copy used by the PLE patch), int2_moe_ple_patch.py (the PLE patch itself)\nassets/         the two serving assets: the PLE n-gram table (ple/) and the int2 checkpoint (model/)\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (the pre-patch copy used by the PLE patch), int2_moe_ple_patch.py (the PLE patch itself)\nassets/         the two serving assets: the PLE n-gram table (ple/) and the int2 checkpoint (model/)\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (the pre-patch copy used by the PLE patch), int2_moe_ple_patch.py (the PLE patch itself)\nassets/         the two serving assets: the PLE n-gram table (ple/) and the int2 checkpoint (model/)\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (the pre-patch copy used by the PLE patch), int2_moe_ple_patch.py (the PLE patch itself)\nassets/         the two serving assets: the PLE n-gram table (ple/) and the int2 checkpoint (model/)\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (the pre-patch copy used by the PLE patch), int2_moe_ple_patch.py (the PLE patch itself)\nassets/         the two serving assets: the PLE n-gram table (ple/) and the int2 checkpoint (model/)\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (the pre-patch copy used by the PLE patch), int2_moe_ple_patch.py (the PLE patch itself)\nassets/         the two serving assets: the PLE n-gram table (ple/) and the int2 checkpoint (model/)\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_p.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (the pre-patch copy used by the PLE patch), int2_moe_ple_patch.py (the PLE patch itself)\nassets/         the two serving assets: the PLE n-gram table (ple/) and the int2 checkpoint (model/)\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (the pre-patch copy used by the PLE patch), int2_moe_ple_patch.py (the PLE patch itself)\nassets/         the two serving assets: the PLE n-gram table (ple/) and the int2 checkpoint (model/)\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_p.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (the pre-patch copy used by the PLE patch), int2_moe_ple_patch.py (the PLE patch itself)\nassets/         the two serving assets: the PLE n-gram table (ple/) and the int2 checkpoint (model/)\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_patched.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (the pre-patch copy used by the PLE patch), int2_moe_ple_patch.py (the PLE patch itself)\nassets/         the two serving assets: the PLE n-gram table (ple/) and the int2 checkpoint (model/)\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_patched.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (the pre-patch copy used by the PLE patch), int2_moe_ple_patch.py (the PLE patch itself)\nassets/         the two serving assets: the PLE n-gram table (ple/) and the int2 checkpoint (model/)\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_patched.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (the pre-patch copy used by the PLE patch), int2_moe_ple_patch.py (the PLE patch itself)\nassets/         the two serving assets: the PLE n-gram table (ple/) and the int2 checkpoint (model/)\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_patched.py, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (the pre-patch copy used by the PLE patch), int2_moe_ple_patch.py (the PLE patch itself)\nassets/         the two serving assets: the PLE n-gram table (ple/) and the int2 checkpoint (model/)\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_patched.py, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (the pre-patch copy used by the PLE patch), int2_moe_ple_patch.py (the PLE patch itself)\nassets/         the two serving assets: the PLE n-gram table (ple/) and the int2 checkpoint (model/)\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_patched.py, ngram_p.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (the pre-patch copy used by the PLE patch), int2_moe_ple_patch.py (the PLE patch itself)\nassets/         the two serving assets: the PLE n-gram table (ple/) and the int2 checkpoint (model/)\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_patched.py, ngram_ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (the pre-patch copy used by the PLE patch), int2_moe_ple_patch.py (the PLE patch itself)\nassets/         the two serving assets: the PLE n-gram table (ple/) and the int2 checkpoint (model/)\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_patched.py, ngram_ple_p.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (the pre-patch copy used by the PLE patch), int2_moe_ple_patch.py (the PLE patch itself)\nassets/         the two serving assets: the PLE n-gram table (ple/) and the int2 checkpoint (model/)\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_patched.py, ngram_ple_ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (the pre-patch copy used by the PLE patch), int2_moe_ple_patch.py (the PLE patch itself)\nassets/         the two serving assets: the PLE n-gram table (ple/) and the int2 checkpoint (model/)\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_patched.py, ngram_ple_ple.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (the pre-patch copy used by the PLE patch), int2_moe_ple_patch.py (the PLE patch itself)\nassets/         the two serving assets: the PLE n-gram table (ple/) and the int2 checkpoint (model/)\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_patched.py, ngram_ple_ple.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (the pre-patch copy used by the PLE patch), int2_moe_ple_patch.py (the PLE patch itself)\nassets/         the two serving assets: the PLE n-gram table (ple/) and the int2 checkpoint (model/)\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_patched.py, ngram_ple_ple.py,\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (the pre-patch copy used by the PLE patch), int2_moe_ple_patch.py (the PLE patch itself)\nassets/         the two serving assets: the PLE n-gram table (ple/) and the int2 checkpoint (model/)\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_patched.py, ngram_ple_ple.py,\n               .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (the pre-patch copy used by the PLE patch), int2_moe_ple_patch.py (the PLE patch itself)\nassets/         the two serving assets: the PLE n-gram table (ple/) and the int2 checkpoint (model/)\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_patched.py, ngram_ple_ple.py,\n                n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (the pre-patch copy used by the PLE patch), int2_moe_ple_patch.py (the PLE patch itself)\nassets/         the two serving assets: the PLE n-gram table (ple/) and the int2 checkpoint (model/)\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_patched.py, ngram_ple_ple.py,\n                ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (the pre-patch copy used by the PLE patch), int2_moe_ple_patch.py (the PLE patch itself)\nassets/         the two serving assets: the PLE n-gram table (ple/) and the int2 checkpoint (model/)\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_patched.py, ngram_ple_ple.py,\n                ngram_p.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (the pre-patch copy used by the PLE patch), int2_moe_ple_patch.py (the PLE patch itself)\nassets/         the two serving assets: the PLE n-gram table (ple/) and the int2 checkpoint (model/)\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_patched.py, ngram_ple_ple.py,\n                ngram_ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (the pre-patch copy used by the PLE patch), int2_moe_ple_patch.py (the PLE patch itself)\nassets/         the two serving assets: the PLE n-gram table (ple/) and the int2 checkpoint (model/)\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_patched.py, ngram_ple_ple.py,\n                ngram_ple_p.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (the pre-patch copy used by the PLE patch), int2_moe_ple_patch.py (the PLE patch itself)\nassets/         the two serving assets: the PLE n-gram table (ple/) and the int2 checkpoint (model/)\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_patched.py, ngram_ple_ple.py,\n                ngram_ple_ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (the pre-patch copy used by the PLE patch), int2_moe_ple_patch.py (the PLE patch itself)\nassets/         the two serving assets: the PLE n-gram table (ple/) and the int2 checkpoint (model/)\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_patched.py, ngram_ple_ple.py,\n                ngram_ple_ple_p.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (the pre-patch copy used by the PLE patch), int2_moe_ple_patch.py (the PLE patch itself)\nassets/         the two serving assets: the PLE n-gram table (ple/) and the int2 checkpoint (model/)\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_patched.py, ngram_ple_ple.py,\n                ngram_ple_ple_patched.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (the pre-patch copy used by the PLE patch), int2_moe_ple_patch.py (the PLE patch itself)\nassets/         the two serving assets: the PLE n-gram table (ple/) and the int2 checkpoint (model/)\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_patched.py, ngram_ple_ple.py,\n                ngram_ple_ple_patched.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (the pre-patch copy used by the PLE patch), int2_moe_ple_patch.py (the PLE patch itself)\nassets/         the two serving assets: the PLE n-gram table (ple/) and the int2 checkpoint (model/)\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_patched.py, ngram_ple_ple.py,\n                ngram_ple_ple_patched.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (the pre-patch copy used by the PLE patch), int2_moe_ple_patch.py (the PLE patch itself)\nassets/         the two serving assets: the PLE n-gram table (ple/) and the int2 checkpoint (model/)\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_patched.py, ngram_ple_ple.py,\n                ngram_ple_ple_patched.py, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (the pre-patch copy used by the PLE patch), int2_moe_ple_patch.py (the PLE patch itself)\nassets/         the two serving assets: the PLE n-gram table (ple/) and the int2 checkpoint (model/)\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_patched.py, ngram_ple_ple.py,\n                ngram_ple_ple_patched.py, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (the pre-patch copy used by the PLE patch), int2_moe_ple_patch.py (the PLE patch itself)\nassets/         the two serving assets: the PLE n-gram table (ple/) and the int2 checkpoint (model/)\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_patched.py, ngram_ple_ple.py,\n                ngram_ple_ple_patched.py, ngram_p.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (the pre-patch copy used by the PLE patch), int2_moe_ple_patch.py (the PLE patch itself)\nassets/         the two serving assets: the PLE n-gram table (ple/) and the int2 checkpoint (model/)\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_patched.py, ngram_ple_ple.py,\n                ngram_ple_ple_patched.py, ngram_ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (the pre-patch copy used by the PLE patch), int2_moe_ple_patch.py (the PLE patch itself)\nassets/         the two serving assets: the PLE n-gram table (ple/) and the int2 checkpoint (model/)\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_patched.py, ngram_ple_ple.py,\n                ngram_ple_ple_patched.py, ngram_ple_p.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (the pre-patch copy used by the PLE patch), int2_moe_ple_patch.py (the PLE patch itself)\nassets/         the two serving assets: the PLE n-gram table (ple/) and the int2 checkpoint (model/)\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_patched.py, ngram_ple_ple.py,\n                ngram_ple_ple_patched.py, ngram_ple_ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (the pre-patch copy used by the PLE patch), int2_moe_ple_patch.py (the PLE patch itself)\nassets/         the two serving assets: the PLE n-gram table (ple/) and the int2 checkpoint (model/)\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_patched.py, ngram_ple_ple.py,\n                ngram_ple_ple_patched.py, ngram_ple_ple_p.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (the pre-patch copy used by the PLE patch), int2_moe_ple_patch.py (the PLE patch itself)\nassets/         the two serving assets: the PLE n-gram table (ple/) and the int2 checkpoint (model/)\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_patched.py, ngram_ple_ple.py,\n                ngram_ple_ple_patched.py, ngram_ple_ple_patched.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (the pre-patch copy used by the PLE patch), int2_moe_ple_patch.py (the PLE patch itself)\nassets/         the two serving assets: the PLE n-gram table (ple/) and the int2 checkpoint (model/)\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_patched.py, ngram_ple_ple.py,\n                ngram_ple_ple_patched.py, ngram_ple_ple_patched_p.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (the pre-patch copy used by the PLE patch), int2_moe_ple_patch.py (the PLE patch itself)\nassets/         the two serving assets: the PLE n-gram table (ple/) and the int2 checkpoint (model/)\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_patched.py, ngram_ple_ple.py,\n                ngram_ple_ple_patched.py, ngram_ple_ple_patched_ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (the pre-patch copy used by the PLE patch), int2_moe_ple_patch.py (the PLE patch itself)\nassets/         the two serving assets: the PLE n-gram table (ple/) and the int2 checkpoint (model/)\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_patched.py, ngram_ple_ple.py,\n                ngram_ple_ple_patched.py, ngram_ple_ple_patched_ple.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (the pre-patch copy used by the PLE patch), int2_moe_ple_patch.py (the PLE patch itself)\nassets/         the two serving assets: the PLE n-gram table (ple/) and the int2 checkpoint (model/)\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_patched.py, ngram_ple_ple.py,\n                ngram_ple_ple_patched.py, ngram_ple_ple_patched_ple.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (the pre-patch copy used by the PLE patch), int2_moe_ple_patch.py (the PLE patch itself)\nassets/         the two serving assets: the PLE n-gram table (ple/) and the int2 checkpoint (model/)\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_patched.py, ngram_ple_ple.py,\n                ngram_ple_ple_patched.py, ngram_ple_ple_patched_ple.py, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (the pre-patch copy used by the PLE patch), int2_moe_ple_patch.py (the PLE patch itself)\nassets/         the two serving assets: the PLE n-gram table (ple/) and the int2 checkpoint (model/)\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_patched.py, ngram_ple_ple.py,\n                ngram_ple_ple_patched.py, ngram_ple_ple_patched_ple.py, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (the pre-patch copy used by the PLE patch), int2_moe_ple_patch.py (the PLE patch itself)\nassets/         the two serving assets: the PLE n-gram table (ple/) and the int2 checkpoint (model/)\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_patched.py, ngram_ple_ple.py,\n                ngram_ple_ple_patched.py, ngram_ple_ple_patched_ple.py, ngram_p.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (the pre-patch copy used by the PLE patch), int2_moe_ple_patch.py (the PLE patch itself)\nassets/         the two serving assets: the PLE n-gram table (ple/) and the int2 checkpoint (model/)\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_patched.py, ngram_ple_ple.py,\n                ngram_ple_ple_patched.py, ngram_ple_ple_patched_ple.py, ngram_ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (the pre-patch copy used by the PLE patch), int2_moe_ple_patch.py (the PLE patch itself)\nassets/         the two serving assets: the PLE n-gram table (ple/) and the int2 checkpoint (model/)\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_patched.py, ngram_ple_ple.py,\n                ngram_ple_ple_patched.py, ngram_ple_ple_patched_ple.py, ngram_ple_p.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (the pre-patch copy used by the PLE patch), int2_moe_ple_patch.py (the PLE patch itself)\nassets/         the two serving assets: the PLE n-gram table (ple/) and the int2 checkpoint (model/)\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_patched.py, ngram_ple_ple.py,\n                ngram_ple_ple_patched.py, ngram_ple_ple_patched_ple.py, ngram_ple_ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (the pre-patch copy used by the PLE patch), int2_moe_ple_patch.py (the PLE patch itself)\nassets/         the two serving assets: the PLE n-gram table (ple/) and the int2 checkpoint (model/)\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_patched.py, ngram_ple_ple.py,\n                ngram_ple_ple_patched.py, ngram_ple_ple_patched_ple.py, ngram_ple_ple_p.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (the pre-patch copy used by the PLE patch), int2_moe_ple_patch.py (the PLE patch itself)\nassets/         the two serving assets: the PLE n-gram table (ple/) and the int2 checkpoint (model/)\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_patched.py, ngram_ple_ple.py,\n                ngram_ple_ple_patched.py, ngram_ple_ple_patched_ple.py, ngram_ple_ple_patched.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (the pre-patch copy used by the PLE patch), int2_moe_ple_patch.py (the PLE patch itself)\nassets/         the two serving assets: the PLE n-gram table (ple/) and the int2 checkpoint (model/)\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_patched.py, ngram_ple_ple.py,\n                ngram_ple_ple_patched.py, ngram_ple_ple_patched_ple.py, ngram_ple_ple_patched_p.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (the pre-patch copy used by the PLE patch), int2_moe_ple_patch.py (the PLE patch itself)\nassets/         the two serving assets: the PLE n-gram table (ple/) and the int2 checkpoint (model/)\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_patched.py, ngram_ple_ple.py,\n                ngram_ple_ple_patched.py, ngram_ple_ple_patched_ple.py, ngram_ple_ple_patched_ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (the pre-patch copy used by the PLE patch), int2_moe_ple_patch.py (the PLE patch itself)\nassets/         the two serving assets: the PLE n-gram table (ple/) and the int2 checkpoint (model/)\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_patched.py, ngram_ple_ple.py,\n                ngram_ple_ple_patched.py, ngram_ple_ple_patched_ple.py, ngram_ple_ple_patched_ple_p.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (the pre-patch copy used by the PLE patch), int2_moe_ple_patch.py (the PLE patch itself)\nassets/         the two serving assets: the PLE n-gram table (ple/) and the int2 checkpoint (model/)\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_patched.py, ngram_ple_ple.py,\n                ngram_ple_ple_patched.py, ngram_ple_ple_patched_ple.py, ngram_ple_ple_patched_ple_patched.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (the pre-patch copy used by the PLE patch), int2_moe_ple_patch.py (the PLE patch itself)\nassets/         the two serving assets: the PLE n-gram table (ple/) and the int2 checkpoint (model/)\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_patched.py, ngram_ple_ple.py,\n                ngram_ple_ple_patched.py, ngram_ple_ple_patched_ple.py, ngram_ple_ple_patched_ple_patched.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (the pre-patch copy used by the PLE patch), int2_moe_ple_patch.py (the PLE patch itself)\nassets/         the two serving assets: the PLE n-gram table (ple/) and the int2 checkpoint (model/)\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_patched.py, ngram_ple_ple.py,\n                ngram_ple_ple_patched.py, ngram_ple_ple_patched_ple.py, ngram_ple_ple_patched_ple_patched.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (the pre-patch copy used by the PLE patch), int2_moe_ple_patch.py (the PLE patch itself)\nassets/         the two serving assets: the PLE n-gram table (ple/) and the int2 checkpoint (model/)\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_patched.py, ngram_ple_ple.py,\n                ngram_ple_ple_patched.py, ngram_ple_ple_patched_ple.py, ngram_ple_ple_patched_ple_patched.py,\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (the pre-patch copy used by the PLE patch), int2_moe_ple_patch.py (the PLE patch itself)\nassets/         the two serving assets: the PLE n-gram table (ple/) and the int2 checkpoint (model/)\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_patched.py, ngram_ple_ple.py,\n                ngram_ple_ple_patched.py, ngram_ple_ple_patched_ple.py, ngram_ple_ple_patched_ple_patched.py,\n               .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (the pre-patch copy used by the PLE patch), int2_moe_ple_patch.py (the PLE patch itself)\nassets/         the two serving assets: the PLE n-gram table (ple/) and the int2 checkpoint (model/)\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_patched.py, ngram_ple_ple.py,\n                ngram_ple_ple_patched.py, ngram_ple_ple_patched_ple.py, ngram_ple_ple_patched_ple_patched.py,\n                n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (the pre-patch copy used by the PLE patch), int2_moe_ple_patch.py (the PLE patch itself)\nassets/         the two serving assets: the PLE n-gram table (ple/) and the int2 checkpoint (model/)\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_patched.py, ngram_ple_ple.py,\n                ngram_ple_ple_patched.py, ngram_ple_ple_patched_ple.py, ngram_ple_ple_patched_ple_patched.py,\n                ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (the pre-patch copy used by the PLE patch), int2_moe_ple_patch.py (the PLE patch itself)\nassets/         the two serving assets: the PLE n-gram table (ple/) and the int2 checkpoint (model/)\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_patched.py, ngram_ple_ple.py,\n                ngram_ple_ple_patched.py, ngram_ple_ple_patched_ple.py, ngram_ple_ple_patched_ple_patched.py,\n                ngram_p.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (the pre-patch copy used by the PLE patch), int2_moe_ple_patch.py (the PLE patch itself)\nassets/         the two serving assets: the PLE n-gram table (ple/) and the int2 checkpoint (model/)\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_patched.py, ngram_ple_ple.py,\n                ngram_ple_ple_patched.py, ngram_ple_ple_patched_ple.py, ngram_ple_ple_patched_ple_patched.py,\n                ngram_ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (the pre-patch copy used by the PLE patch), int2_moe_ple_patch.py (the PLE patch itself)\nassets/         the two serving assets: the PLE n-gram table (ple/) and the int2 checkpoint (model/)\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_patched.py, ngram_ple_ple.py,\n                ngram_ple_ple_patched.py, ngram_ple_ple_patched_ple.py, ngram_ple_ple_patched_ple_patched.py,\n                ngram_ple_p.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (the pre-patch copy used by the PLE patch), int2_moe_ple_patch.py (the PLE patch itself)\nassets/         the two serving assets: the PLE n-gram table (ple/) and the int2 checkpoint (model/)\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_patched.py, ngram_ple_ple.py,\n                ngram_ple_ple_patched.py, ngram_ple_ple_patched_ple.py, ngram_ple_ple_patched_ple_patched.py,\n                ngram_ple_ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (the pre-patch copy used by the PLE patch), int2_moe_ple_patch.py (the PLE patch itself)\nassets/         the two serving assets: the PLE n-gram table (ple/) and the int2 checkpoint (model/)\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_patched.py, ngram_ple_ple.py,\n                ngram_ple_ple_patched.py, ngram_ple_ple_patched_ple.py, ngram_ple_ple_patched_ple_patched.py,\n                ngram_ple_ple_p.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (the pre-patch copy used by the PLE patch), int2_moe_ple_patch.py (the PLE patch itself)\nassets/         the two serving assets: the PLE n-gram table (ple/) and the int2 checkpoint (model/)\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_patched.py, ngram_ple_ple.py,\n                ngram_ple_ple_patched.py, ngram_ple_ple_patched_ple.py, ngram_ple_ple_patched_ple_patched.py,\n                ngram_ple_ple_patched.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (the pre-patch copy used by the PLE patch), int2_moe_ple_patch.py (the PLE patch itself)\nassets/         the two serving assets: the PLE n-gram table (ple/) and the int2 checkpoint (model/)\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_patched.py, ngram_ple_ple.py,\n                ngram_ple_ple_patched.py, ngram_ple_ple_patched_ple.py, ngram_ple_ple_patched_ple_patched.py,\n                ngram_ple_ple_patched_p.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (the pre-patch copy used by the PLE patch), int2_moe_ple_patch.py (the PLE patch itself)\nassets/         the two serving assets: the PLE n-gram table (ple/) and the int2 checkpoint (model/)\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_patched.py, ngram_ple_ple.py,\n                ngram_ple_ple_patched.py, ngram_ple_ple_patched_ple.py, ngram_ple_ple_patched_ple_patched.py,\n                ngram_ple_ple_patched_ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (the pre-patch copy used by the PLE patch), int2_moe_ple_patch.py (the PLE patch itself)\nassets/         the two serving assets: the PLE n-gram table (ple/) and the int2 checkpoint (model/)\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_patched.py, ngram_ple_ple.py,\n                ngram_ple_ple_patched.py, ngram_ple_ple_patched_ple.py, ngram_ple_ple_patched_ple_patched.py,\n                ngram_ple_ple_patched_ple_p.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (the pre-patch copy used by the PLE patch), int2_moe_ple_patch.py (the PLE patch itself)\nassets/         the two serving assets: the PLE n-gram table (ple/) and the int2 checkpoint (model/)\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_patched.py, ngram_ple_ple.py,\n                ngram_ple_ple_patched.py, ngram_ple_ple_patched_ple.py, ngram_ple_ple_patched_ple_patched.py,\n                ngram_ple_ple_patched_ple_patched.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (the pre-patch copy used by the PLE patch), int2_moe_ple_patch.py (the PLE patch itself)\nassets/         the two serving assets: the PLE n-gram table (ple/) and the int2 checkpoint (model/)\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_patched.py, ngram_ple_ple.py,\n                ngram_ple_ple_patched.py, ngram_ple_ple_patched_ple.py, ngram_ple_ple_patched_ple_patched.py,\n                ngram_ple_ple_patched_ple_patched_p.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (the pre-patch copy used by the PLE patch), int2_moe_ple_patch.py (the PLE patch itself)\nassets/         the two serving assets: the PLE n-gram table (ple/) and the int2 checkpoint (model/)\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_patched.py, ngram_ple_ple.py,\n                ngram_ple_ple_patched.py, ngram_ple_ple_patched_ple.py, ngram_ple_ple_patched_ple_patched.py,\n                ngram_ple_ple_patched_ple_patched_ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (the pre-patch copy used by the PLE patch), int2_moe_ple_patch.py (the PLE patch itself)\nassets/         the two serving assets: the PLE n-gram table (ple/) and the int2 checkpoint (model/)\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_patched.py, ngram_ple_ple.py,\n                ngram_ple_ple_patched.py, ngram_ple_ple_patched_ple.py, ngram_ple_ple_patched_ple_patched.py,\n                ngram_ple_ple_patched_ple_patched_ple.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (the pre-patch copy used by the PLE patch), int2_moe_ple_patch.py (the PLE patch itself)\nassets/         the two serving assets: the PLE n-gram table (ple/) and the int2 checkpoint (model/)\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_patched.py, ngram_ple_ple.py,\n                ngram_ple_ple_patched.py, ngram_ple_ple_patched_ple.py, ngram_ple_ple_patched_ple_patched.py,\n                ngram_ple_ple_patched_ple_patched_ple.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (the pre-patch copy used by the PLE patch), int2_moe_ple_patch.py (the PLE patch itself)\nassets/         the two serving assets: the PLE n-gram table (ple/) and the int2 checkpoint (model/)\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_patched.py, ngram_ple_ple.py,\n                ngram_ple_ple_patched.py, ngram_ple_ple_patched_ple.py, ngram_ple_ple_patched_ple_patched.py,\n                ngram_ple_ple_patched_ple_patched_ple.py, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (the pre-patch copy used by the PLE patch), int2_moe_ple_patch.py (the PLE patch itself)\nassets/         the two serving assets: the PLE n-gram table (ple/) and the int2 checkpoint (model/)\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_patched.py, ngram_ple_ple.py,\n                ngram_ple_ple_patched.py, ngram_ple_ple_patched_ple.py, ngram_ple_ple_patched_ple_patched.py,\n                ngram_ple_ple_patched_ple_patched_ple.py, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (the pre-patch copy used by the PLE patch), int2_moe_ple_patch.py (the PLE patch itself)\nassets/         the two serving assets: the PLE n-gram table (ple/) and the int2 checkpoint (model/)\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_patched.py, ngram_ple_ple.py,\n                ngram_ple_ple_patched.py, ngram_ple_ple_patched_ple.py, ngram_ple_ple_patched_ple_patched.py,\n                ngram_ple_ple_patched_ple_patched_ple.py, ngram_p.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (the pre-patch copy used by the PLE patch), int2_moe_ple_patch.py (the PLE patch itself)\nassets/         the two serving assets: the PLE n-gram table (ple/) and the int2 checkpoint (model/)\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_patched.py, ngram_ple_ple.py,\n                ngram_ple_ple_patched.py, ngram_ple_ple_patched_ple.py, ngram_ple_ple_patched_ple_patched.py,\n                ngram_ple_ple_patched_ple_patched_ple.py, ngram_ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (the pre-patch copy used by the PLE patch), int2_moe_ple_patch.py (the PLE patch itself)\nassets/         the two serving assets: the PLE n-gram table (ple/) and the int2 checkpoint (model/)\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_patched.py, ngram_ple_ple.py,\n                ngram_ple_ple_patched.py, ngram_ple_ple_patched_ple.py, ngram_ple_ple_patched_ple_patched.py,\n                ngram_ple_ple_patched_ple_patched_ple.py, ngram_ple_p.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (the pre-patch copy used by the PLE patch), int2_moe_ple_patch.py (the PLE patch itself)\nassets/         the two serving assets: the PLE n-gram table (ple/) and the int2 checkpoint (model/)\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_patched.py, ngram_ple_ple.py,\n                ngram_ple_ple_patched.py, ngram_ple_ple_patched_ple.py, ngram_ple_ple_patched_ple_patched.py,\n                ngram_ple_ple_patched_ple_patched_ple.py, ngram_ple_ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher of record: it sets the environment, applies the patch list, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at\na time), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),\n`--ple-table-path` pointing at `ple/ple.json` and `--ple-table-dtype float8_e4m3fn`. The `PLE` variable\nmust be a path on an NVMe disk: the PLE table is read through mmap and a spinning disk stalls decode.\n`--ple-table-path` is the only flag that reaches the PLE table; the table dtype is set by `--ple-table-dtype`\nand the table itself is read from the file named by `ple.json`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, one per layer, in build order\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n                (the pre-patch copy used by the PLE patch), int2_moe_ple_patch.py (the PLE patch itself)\nassets/         the two serving assets: the PLE n-gram table (ple/) and the int2 checkpoint (model/)\ntools/          the measurement tools: bench.py, ngram.py, ngram_ple.py, ngram_ple_patched.py, ngram_ple_ple.py,\n                ngram_ple_ple_patched.py, ngram_ple_ple_patched_ple.py, ngram_ple_ple_patched_ple_patched.py,\n                ngram_ple_ple_patched_ple_patched_ple.py, ngram_ple_ple_p",
 "ttft_seconds": 1.1236043099925155,
 "status": "ok",
 "tg_tokens_per_s": 38.09206719103316,
 "decode_seconds_per_token": 0.026252185133060962,
 "individual_token_timing": true,
 "event_intervals_ms": {
  "n": 511,
  "median": 25.826826997217722,
  "p95": 32.13246898667421,
  "mean": 26.252185133060962
 },
 "class": "prose",
 "requested_input_tokens": 2048
}
