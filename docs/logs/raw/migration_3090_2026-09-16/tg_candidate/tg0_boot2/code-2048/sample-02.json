{
 "started_utc": "2026-09-16T23:12:26.226404+00:00",
 "wall_seconds": 15.746223465001094,
 "prompt_tokens": 2048,
 "completion_tokens": 512,
 "stream_events": 512,
 "ended_utc": "2026-09-16T23:12:41.972816+00:00",
 "warmup": false,
 "sample_index": 2,
 "requested_decode_tokens": 512,
 "gpu": {
  "n": 57,
  "temperature_c": {
   "min": 77.0,
   "max": 79.0,
   "mean": 78.19298245614036
  },
  "power_w": {
   "min": 254.97,
   "max": 277.59,
   "mean": 264.7338596491228
  },
  "util_pct": {
   "min": 75.0,
   "max": 100.0,
   "mean": 90.12280701754386
  },
  "clock_sm_mhz": {
   "min": 1605.0,
   "max": 1905.0,
   "mean": 1879.7368421052631
  },
  "clock_mem_mhz": {
   "min": 9501.0,
   "max": 9501.0,
   "mean": 9501.0
  },
  "memory_free_mib": {
   "min": 2540.0
  }
 },
 "output_text_sha256": "236951eaa4f33bb3b22287ad11d63559ad990b2ebba64d89bce715d129f8b3af",
 "output_text": "++       +        b+        b_z+        b_zp+        b_zp_num+        b_zp_num =+        b_zp_num = +        b_zp_num = 1+        b_zp_num = 12+        b_zp_num = 128+        b_zp_num = 128\n+        b_zp_num = 128\n++        b_zp_num = 128\n+   +        b_zp_num = 128\n+    elif+        b_zp_num = 128\n+    elif has+        b_zp_num = 128\n+    elif has_z+        b_zp_num = 128\n+    elif has_zp+        b_zp_num = 128\n+    elif has_zp and+        b_zp_num = 128\n+    elif has_zp and use+        b_zp_num = 128\n+    elif has_zp and use_int+        b_zp_num = 128\n+    elif has_zp and use_int2+        b_zp_num = 128\n+    elif has_zp and use_int2_w+        b_zp_num = 128\n+    elif has_zp and use_int2_w2+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_z+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_sh+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16)+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) *+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+   +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_exp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts *+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn *+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr +=+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off ++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+   +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_z+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+           +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_z+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_exp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts *+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+   +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N),+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float3+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+   +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        #+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2a+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+           +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+               +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                #+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block:+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+               +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None]+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] <+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K //+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+               +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                #+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values:+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+               +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None]+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >>+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_sh+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter)+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) &+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+               +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+                #+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+                # De+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+                # Dequant+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+                # Dequantize+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+                # Dequantize along+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+                # Dequantize along K+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+                # Dequantize along K (+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+                # Dequantize along K (scale+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+                # Dequantize along K (scale is+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+                # Dequantize along K (scale is per+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+                # Dequantize along K (scale is per-column+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+                # Dequantize along K (scale is per-column,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+                # Dequantize along K (scale is per-column, applied+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+                # Dequantize along K (scale is per-column, applied after+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+                # Dequantize along K (scale is per-column, applied after the+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+                # Dequantize along K (scale is per-column, applied after the dot+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+                # Dequantize along K (scale is per-column, applied after the dot):+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+                # Dequantize along K (scale is per-column, applied after the dot):\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+                # Dequantize along K (scale is per-column, applied after the dot):\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+                # Dequantize along K (scale is per-column, applied after the dot):\n+               +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+                # Dequantize along K (scale is per-column, applied after the dot):\n+                if+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+                # Dequantize along K (scale is per-column, applied after the dot):\n+                if has+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+                # Dequantize along K (scale is per-column, applied after the dot):\n+                if has_z+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+                # Dequantize along K (scale is per-column, applied after the dot):\n+                if has_zp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+                # Dequantize along K (scale is per-column, applied after the dot):\n+                if has_zp:+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+                # Dequantize along K (scale is per-column, applied after the dot):\n+                if has_zp:\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+                # Dequantize along K (scale is per-column, applied after the dot):\n+                if has_zp:\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+                # Dequantize along K (scale is per-column, applied after the dot):\n+                if has_zp:\n+                   +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+                # Dequantize along K (scale is per-column, applied after the dot):\n+                if has_zp:\n+                    b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+                # Dequantize along K (scale is per-column, applied after the dot):\n+                if has_zp:\n+                    b_z+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+                # Dequantize along K (scale is per-column, applied after the dot):\n+                if has_zp:\n+                    b_zp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+                # Dequantize along K (scale is per-column, applied after the dot):\n+                if has_zp:\n+                    b_zp_words+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+                # Dequantize along K (scale is per-column, applied after the dot):\n+                if has_zp:\n+                    b_zp_words =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+                # Dequantize along K (scale is per-column, applied after the dot):\n+                if has_zp:\n+                    b_zp_words = tl+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+                # Dequantize along K (scale is per-column, applied after the dot):\n+                if has_zp:\n+                    b_zp_words = tl.load+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+                # Dequantize along K (scale is per-column, applied after the dot):\n+                if has_zp:\n+                    b_zp_words = tl.load(+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+                # Dequantize along K (scale is per-column, applied after the dot):\n+                if has_zp:\n+                    b_zp_words = tl.load(\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+                # Dequantize along K (scale is per-column, applied after the dot):\n+                if has_zp:\n+                    b_zp_words = tl.load(\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+                # Dequantize along K (scale is per-column, applied after the dot):\n+                if has_zp:\n+                    b_zp_words = tl.load(\n+                       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+                # Dequantize along K (scale is per-column, applied after the dot):\n+                if has_zp:\n+                    b_zp_words = tl.load(\n+                        b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+                # Dequantize along K (scale is per-column, applied after the dot):\n+                if has_zp:\n+                    b_zp_words = tl.load(\n+                        b_z+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+                # Dequantize along K (scale is per-column, applied after the dot):\n+                if has_zp:\n+                    b_zp_words = tl.load(\n+                        b_zp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+                # Dequantize along K (scale is per-column, applied after the dot):\n+                if has_zp:\n+                    b_zp_words = tl.load(\n+                        b_zp_ptr+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+                # Dequantize along K (scale is per-column, applied after the dot):\n+                if has_zp:\n+                    b_zp_words = tl.load(\n+                        b_zp_ptr\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+                # Dequantize along K (scale is per-column, applied after the dot):\n+                if has_zp:\n+                    b_zp_words = tl.load(\n+                        b_zp_ptr\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+                # Dequantize along K (scale is per-column, applied after the dot):\n+                if has_zp:\n+                    b_zp_words = tl.load(\n+                        b_zp_ptr\n+                       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+                # Dequantize along K (scale is per-column, applied after the dot):\n+                if has_zp:\n+                    b_zp_words = tl.load(\n+                        b_zp_ptr\n+                        ++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+                # Dequantize along K (scale is per-column, applied after the dot):\n+                if has_zp:\n+                    b_zp_words = tl.load(\n+                        b_zp_ptr\n+                        + b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+                # Dequantize along K (scale is per-column, applied after the dot):\n+                if has_zp:\n+                    b_zp_words = tl.load(\n+                        b_zp_ptr\n+                        + b_z+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+                # Dequantize along K (scale is per-column, applied after the dot):\n+                if has_zp:\n+                    b_zp_words = tl.load(\n+                        b_zp_ptr\n+                        + b_zp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+                # Dequantize along K (scale is per-column, applied after the dot):\n+                if has_zp:\n+                    b_zp_words = tl.load(\n+                        b_zp_ptr\n+                        + b_zp_src+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+                # Dequantize along K (scale is per-column, applied after the dot):\n+                if has_zp:\n+                    b_zp_words = tl.load(\n+                        b_zp_ptr\n+                        + b_zp_src_off+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+                # Dequantize along K (scale is per-column, applied after the dot):\n+                if has_zp:\n+                    b_zp_words = tl.load(\n+                        b_zp_ptr\n+                        + b_zp_src_off\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+                # Dequantize along K (scale is per-column, applied after the dot):\n+                if has_zp:\n+                    b_zp_words = tl.load(\n+                        b_zp_ptr\n+                        + b_zp_src_off\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+                # Dequantize along K (scale is per-column, applied after the dot):\n+                if has_zp:\n+                    b_zp_words = tl.load(\n+                        b_zp_ptr\n+                        + b_zp_src_off\n+                       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+                # Dequantize along K (scale is per-column, applied after the dot):\n+                if has_zp:\n+                    b_zp_words = tl.load(\n+                        b_zp_ptr\n+                        + b_zp_src_off\n+                        ++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+                # Dequantize along K (scale is per-column, applied after the dot):\n+                if has_zp:\n+                    b_zp_words = tl.load(\n+                        b_zp_ptr\n+                        + b_zp_src_off\n+                        + offs+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+                # Dequantize along K (scale is per-column, applied after the dot):\n+                if has_zp:\n+                    b_zp_words = tl.load(\n+                        b_zp_ptr\n+                        + b_zp_src_off\n+                        + offs_kw+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+                # Dequantize along K (scale is per-column, applied after the dot):\n+                if has_zp:\n+                    b_zp_words = tl.load(\n+                        b_zp_ptr\n+                        + b_zp_src_off\n+                        + offs_kw[:,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+                # Dequantize along K (scale is per-column, applied after the dot):\n+                if has_zp:\n+                    b_zp_words = tl.load(\n+                        b_zp_ptr\n+                        + b_zp_src_off\n+                        + offs_kw[:, None+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+                # Dequantize along K (scale is per-column, applied after the dot):\n+                if has_zp:\n+                    b_zp_words = tl.load(\n+                        b_zp_ptr\n+                        + b_zp_src_off\n+                        + offs_kw[:, None]+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+                # Dequantize along K (scale is per-column, applied after the dot):\n+                if has_zp:\n+                    b_zp_words = tl.load(\n+                        b_zp_ptr\n+                        + b_zp_src_off\n+                        + offs_kw[:, None] *+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+                # Dequantize along K (scale is per-column, applied after the dot):\n+                if has_zp:\n+                    b_zp_words = tl.load(\n+                        b_zp_ptr\n+                        + b_zp_src_off\n+                        + offs_kw[:, None] * stride+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+                # Dequantize along K (scale is per-column, applied after the dot):\n+                if has_zp:\n+                    b_zp_words = tl.load(\n+                        b_zp_ptr\n+                        + b_zp_src_off\n+                        + offs_kw[:, None] * stride_b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+                # Dequantize along K (scale is per-column, applied after the dot):\n+                if has_zp:\n+                    b_zp_words = tl.load(\n+                        b_zp_ptr\n+                        + b_zp_src_off\n+                        + offs_kw[:, None] * stride_bzk+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+                # Dequantize along K (scale is per-column, applied after the dot):\n+                if has_zp:\n+                    b_zp_words = tl.load(\n+                        b_zp_ptr\n+                        + b_zp_src_off\n+                        + offs_kw[:, None] * stride_bzk\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+                # Dequantize along K (scale is per-column, applied after the dot):\n+                if has_zp:\n+                    b_zp_words = tl.load(\n+                        b_zp_ptr\n+                        + b_zp_src_off\n+                        + offs_kw[:, None] * stride_bzk\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+                # Dequantize along K (scale is per-column, applied after the dot):\n+                if has_zp:\n+                    b_zp_words = tl.load(\n+                        b_zp_ptr\n+                        + b_zp_src_off\n+                        + offs_kw[:, None] * stride_bzk\n+                       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+                # Dequantize along K (scale is per-column, applied after the dot):\n+                if has_zp:\n+                    b_zp_words = tl.load(\n+                        b_zp_ptr\n+                        + b_zp_src_off\n+                        + offs_kw[:, None] * stride_bzk\n+                        ++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+                # Dequantize along K (scale is per-column, applied after the dot):\n+                if has_zp:\n+                    b_zp_words = tl.load(\n+                        b_zp_ptr\n+                        + b_zp_src_off\n+                        + offs_kw[:, None] * stride_bzk\n+                        + offs+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+                # Dequantize along K (scale is per-column, applied after the dot):\n+                if has_zp:\n+                    b_zp_words = tl.load(\n+                        b_zp_ptr\n+                        + b_zp_src_off\n+                        + offs_kw[:, None] * stride_bzk\n+                        + offs_bn+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+                # Dequantize along K (scale is per-column, applied after the dot):\n+                if has_zp:\n+                    b_zp_words = tl.load(\n+                        b_zp_ptr\n+                        + b_zp_src_off\n+                        + offs_kw[:, None] * stride_bzk\n+                        + offs_bn[+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+                # Dequantize along K (scale is per-column, applied after the dot):\n+                if has_zp:\n+                    b_zp_words = tl.load(\n+                        b_zp_ptr\n+                        + b_zp_src_off\n+                        + offs_kw[:, None] * stride_bzk\n+                        + offs_bn[None+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+                # Dequantize along K (scale is per-column, applied after the dot):\n+                if has_zp:\n+                    b_zp_words = tl.load(\n+                        b_zp_ptr\n+                        + b_zp_src_off\n+                        + offs_kw[:, None] * stride_bzk\n+                        + offs_bn[None,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+                # Dequantize along K (scale is per-column, applied after the dot):\n+                if has_zp:\n+                    b_zp_words = tl.load(\n+                        b_zp_ptr\n+                        + b_zp_src_off\n+                        + offs_kw[:, None] * stride_bzk\n+                        + offs_bn[None, :]+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+                # Dequantize along K (scale is per-column, applied after the dot):\n+                if has_zp:\n+                    b_zp_words = tl.load(\n+                        b_zp_ptr\n+                        + b_zp_src_off\n+                        + offs_kw[:, None] * stride_bzk\n+                        + offs_bn[None, :] *+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+                # Dequantize along K (scale is per-column, applied after the dot):\n+                if has_zp:\n+                    b_zp_words = tl.load(\n+                        b_zp_ptr\n+                        + b_zp_src_off\n+                        + offs_kw[:, None] * stride_bzk\n+                        + offs_bn[None, :] * stride+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+                # Dequantize along K (scale is per-column, applied after the dot):\n+                if has_zp:\n+                    b_zp_words = tl.load(\n+                        b_zp_ptr\n+                        + b_zp_src_off\n+                        + offs_kw[:, None] * stride_bzk\n+                        + offs_bn[None, :] * stride_b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+                # Dequantize along K (scale is per-column, applied after the dot):\n+                if has_zp:\n+                    b_zp_words = tl.load(\n+                        b_zp_ptr\n+                        + b_zp_src_off\n+                        + offs_kw[:, None] * stride_bzk\n+                        + offs_bn[None, :] * stride_bzn+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+                # Dequantize along K (scale is per-column, applied after the dot):\n+                if has_zp:\n+                    b_zp_words = tl.load(\n+                        b_zp_ptr\n+                        + b_zp_src_off\n+                        + offs_kw[:, None] * stride_bzk\n+                        + offs_bn[None, :] * stride_bzn,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+                # Dequantize along K (scale is per-column, applied after the dot):\n+                if has_zp:\n+                    b_zp_words = tl.load(\n+                        b_zp_ptr\n+                        + b_zp_src_off\n+                        + offs_kw[:, None] * stride_bzk\n+                        + offs_bn[None, :] * stride_bzn,\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+                # Dequantize along K (scale is per-column, applied after the dot):\n+                if has_zp:\n+                    b_zp_words = tl.load(\n+                        b_zp_ptr\n+                        + b_zp_src_off\n+                        + offs_kw[:, None] * stride_bzk\n+                        + offs_bn[None, :] * stride_bzn,\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+                # Dequantize along K (scale is per-column, applied after the dot):\n+                if has_zp:\n+                    b_zp_words = tl.load(\n+                        b_zp_ptr\n+                        + b_zp_src_off\n+                        + offs_kw[:, None] * stride_bzk\n+                        + offs_bn[None, :] * stride_bzn,\n+                       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+                # Dequantize along K (scale is per-column, applied after the dot):\n+                if has_zp:\n+                    b_zp_words = tl.load(\n+                        b_zp_ptr\n+                        + b_zp_src_off\n+                        + offs_kw[:, None] * stride_bzk\n+                        + offs_bn[None, :] * stride_bzn,\n+                        mask+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+                # Dequantize along K (scale is per-column, applied after the dot):\n+                if has_zp:\n+                    b_zp_words = tl.load(\n+                        b_zp_ptr\n+                        + b_zp_src_off\n+                        + offs_kw[:, None] * stride_bzk\n+                        + offs_bn[None, :] * stride_bzn,\n+                        mask=+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+                # Dequantize along K (scale is per-column, applied after the dot):\n+                if has_zp:\n+                    b_zp_words = tl.load(\n+                        b_zp_ptr\n+                        + b_zp_src_off\n+                        + offs_kw[:, None] * stride_bzk\n+                        + offs_bn[None, :] * stride_bzn,\n+                        mask=offs+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+                # Dequantize along K (scale is per-column, applied after the dot):\n+                if has_zp:\n+                    b_zp_words = tl.load(\n+                        b_zp_ptr\n+                        + b_zp_src_off\n+                        + offs_kw[:, None] * stride_bzk\n+                        + offs_bn[None, :] * stride_bzn,\n+                        mask=offs_kw+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+                # Dequantize along K (scale is per-column, applied after the dot):\n+                if has_zp:\n+                    b_zp_words = tl.load(\n+                        b_zp_ptr\n+                        + b_zp_src_off\n+                        + offs_kw[:, None] * stride_bzk\n+                        + offs_bn[None, :] * stride_bzn,\n+                        mask=offs_kw[:,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+                # Dequantize along K (scale is per-column, applied after the dot):\n+                if has_zp:\n+                    b_zp_words = tl.load(\n+                        b_zp_ptr\n+                        + b_zp_src_off\n+                        + offs_kw[:, None] * stride_bzk\n+                        + offs_bn[None, :] * stride_bzn,\n+                        mask=offs_kw[:, None+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+                # Dequantize along K (scale is per-column, applied after the dot):\n+                if has_zp:\n+                    b_zp_words = tl.load(\n+                        b_zp_ptr\n+                        + b_zp_src_off\n+                        + offs_kw[:, None] * stride_bzk\n+                        + offs_bn[None, :] * stride_bzn,\n+                        mask=offs_kw[:, None]+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+                # Dequantize along K (scale is per-column, applied after the dot):\n+                if has_zp:\n+                    b_zp_words = tl.load(\n+                        b_zp_ptr\n+                        + b_zp_src_off\n+                        + offs_kw[:, None] * stride_bzk\n+                        + offs_bn[None, :] * stride_bzn,\n+                        mask=offs_kw[:, None] <+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+                # Dequantize along K (scale is per-column, applied after the dot):\n+                if has_zp:\n+                    b_zp_words = tl.load(\n+                        b_zp_ptr\n+                        + b_zp_src_off\n+                        + offs_kw[:, None] * stride_bzk\n+                        + offs_bn[None, :] * stride_bzn,\n+                        mask=offs_kw[:, None] < K+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+                # Dequantize along K (scale is per-column, applied after the dot):\n+                if has_zp:\n+                    b_zp_words = tl.load(\n+                        b_zp_ptr\n+                        + b_zp_src_off\n+                        + offs_kw[:, None] * stride_bzk\n+                        + offs_bn[None, :] * stride_bzn,\n+                        mask=offs_kw[:, None] < K //+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+                # Dequantize along K (scale is per-column, applied after the dot):\n+                if has_zp:\n+                    b_zp_words = tl.load(\n+                        b_zp_ptr\n+                        + b_zp_src_off\n+                        + offs_kw[:, None] * stride_bzk\n+                        + offs_bn[None, :] * stride_bzn,\n+                        mask=offs_kw[:, None] < K // +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+                # Dequantize along K (scale is per-column, applied after the dot):\n+                if has_zp:\n+                    b_zp_words = tl.load(\n+                        b_zp_ptr\n+                        + b_zp_src_off\n+                        + offs_kw[:, None] * stride_bzk\n+                        + offs_bn[None, :] * stride_bzn,\n+                        mask=offs_kw[:, None] < K // 1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+                # Dequantize along K (scale is per-column, applied after the dot):\n+                if has_zp:\n+                    b_zp_words = tl.load(\n+                        b_zp_ptr\n+                        + b_zp_src_off\n+                        + offs_kw[:, None] * stride_bzk\n+                        + offs_bn[None, :] * stride_bzn,\n+                        mask=offs_kw[:, None] < K // 16+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+                # Dequantize along K (scale is per-column, applied after the dot):\n+                if has_zp:\n+                    b_zp_words = tl.load(\n+                        b_zp_ptr\n+                        + b_zp_src_off\n+                        + offs_kw[:, None] * stride_bzk\n+                        + offs_bn[None, :] * stride_bzn,\n+                        mask=offs_kw[:, None] < K // 16,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+                # Dequantize along K (scale is per-column, applied after the dot):\n+                if has_zp:\n+                    b_zp_words = tl.load(\n+                        b_zp_ptr\n+                        + b_zp_src_off\n+                        + offs_kw[:, None] * stride_bzk\n+                        + offs_bn[None, :] * stride_bzn,\n+                        mask=offs_kw[:, None] < K // 16,\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+                # Dequantize along K (scale is per-column, applied after the dot):\n+                if has_zp:\n+                    b_zp_words = tl.load(\n+                        b_zp_ptr\n+                        + b_zp_src_off\n+                        + offs_kw[:, None] * stride_bzk\n+                        + offs_bn[None, :] * stride_bzn,\n+                        mask=offs_kw[:, None] < K // 16,\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+                # Dequantize along K (scale is per-column, applied after the dot):\n+                if has_zp:\n+                    b_zp_words = tl.load(\n+                        b_zp_ptr\n+                        + b_zp_src_off\n+                        + offs_kw[:, None] * stride_bzk\n+                        + offs_bn[None, :] * stride_bzn,\n+                        mask=offs_kw[:, None] < K // 16,\n+                       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+                # Dequantize along K (scale is per-column, applied after the dot):\n+                if has_zp:\n+                    b_zp_words = tl.load(\n+                        b_zp_ptr\n+                        + b_zp_src_off\n+                        + offs_kw[:, None] * stride_bzk\n+                        + offs_bn[None, :] * stride_bzn,\n+                        mask=offs_kw[:, None] < K // 16,\n+                        other+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+                # Dequantize along K (scale is per-column, applied after the dot):\n+                if has_zp:\n+                    b_zp_words = tl.load(\n+                        b_zp_ptr\n+                        + b_zp_src_off\n+                        + offs_kw[:, None] * stride_bzk\n+                        + offs_bn[None, :] * stride_bzn,\n+                        mask=offs_kw[:, None] < K // 16,\n+                        other=+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+                # Dequantize along K (scale is per-column, applied after the dot):\n+                if has_zp:\n+                    b_zp_words = tl.load(\n+                        b_zp_ptr\n+                        + b_zp_src_off\n+                        + offs_kw[:, None] * stride_bzk\n+                        + offs_bn[None, :] * stride_bzn,\n+                        mask=offs_kw[:, None] < K // 16,\n+                        other=0+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+                # Dequantize along K (scale is per-column, applied after the dot):\n+                if has_zp:\n+                    b_zp_words = tl.load(\n+                        b_zp_ptr\n+                        + b_zp_src_off\n+                        + offs_kw[:, None] * stride_bzk\n+                        + offs_bn[None, :] * stride_bzn,\n+                        mask=offs_kw[:, None] < K // 16,\n+                        other=0,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+                # Dequantize along K (scale is per-column, applied after the dot):\n+                if has_zp:\n+                    b_zp_words = tl.load(\n+                        b_zp_ptr\n+                        + b_zp_src_off\n+                        + offs_kw[:, None] * stride_bzk\n+                        + offs_bn[None, :] * stride_bzn,\n+                        mask=offs_kw[:, None] < K // 16,\n+                        other=0,\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+                # Dequantize along K (scale is per-column, applied after the dot):\n+                if has_zp:\n+                    b_zp_words = tl.load(\n+                        b_zp_ptr\n+                        + b_zp_src_off\n+                        + offs_kw[:, None] * stride_bzk\n+                        + offs_bn[None, :] * stride_bzn,\n+                        mask=offs_kw[:, None] < K // 16,\n+                        other=0,\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+                # Dequantize along K (scale is per-column, applied after the dot):\n+                if has_zp:\n+                    b_zp_words = tl.load(\n+                        b_zp_ptr\n+                        + b_zp_src_off\n+                        + offs_kw[:, None] * stride_bzk\n+                        + offs_bn[None, :] * stride_bzn,\n+                        mask=offs_kw[:, None] < K // 16,\n+                        other=0,\n+                   +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+                # Dequantize along K (scale is per-column, applied after the dot):\n+                if has_zp:\n+                    b_zp_words = tl.load(\n+                        b_zp_ptr\n+                        + b_zp_src_off\n+                        + offs_kw[:, None] * stride_bzk\n+                        + offs_bn[None, :] * stride_bzn,\n+                        mask=offs_kw[:, None] < K // 16,\n+                        other=0,\n+                    )+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+                # Dequantize along K (scale is per-column, applied after the dot):\n+                if has_zp:\n+                    b_zp_words = tl.load(\n+                        b_zp_ptr\n+                        + b_zp_src_off\n+                        + offs_kw[:, None] * stride_bzk\n+                        + offs_bn[None, :] * stride_bzn,\n+                        mask=offs_kw[:, None] < K // 16,\n+                        other=0,\n+                    )\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+                # Dequantize along K (scale is per-column, applied after the dot):\n+                if has_zp:\n+                    b_zp_words = tl.load(\n+                        b_zp_ptr\n+                        + b_zp_src_off\n+                        + offs_kw[:, None] * stride_bzk\n+                        + offs_bn[None, :] * stride_bzn,\n+                        mask=offs_kw[:, None] < K // 16,\n+                        other=0,\n+                    )\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+                # Dequantize along K (scale is per-column, applied after the dot):\n+                if has_zp:\n+                    b_zp_words = tl.load(\n+                        b_zp_ptr\n+                        + b_zp_src_off\n+                        + offs_kw[:, None] * stride_bzk\n+                        + offs_bn[None, :] * stride_bzn,\n+                        mask=offs_kw[:, None] < K // 16,\n+                        other=0,\n+                    )\n+                   +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+                # Dequantize along K (scale is per-column, applied after the dot):\n+                if has_zp:\n+                    b_zp_words = tl.load(\n+                        b_zp_ptr\n+                        + b_zp_src_off\n+                        + offs_kw[:, None] * stride_bzk\n+                        + offs_bn[None, :] * stride_bzn,\n+                        mask=offs_kw[:, None] < K // 16,\n+                        other=0,\n+                    )\n+                    b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+                # Dequantize along K (scale is per-column, applied after the dot):\n+                if has_zp:\n+                    b_zp_words = tl.load(\n+                        b_zp_ptr\n+                        + b_zp_src_off\n+                        + offs_kw[:, None] * stride_bzk\n+                        + offs_bn[None, :] * stride_bzn,\n+                        mask=offs_kw[:, None] < K // 16,\n+                        other=0,\n+                    )\n+                    b_z+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+                # Dequantize along K (scale is per-column, applied after the dot):\n+                if has_zp:\n+                    b_zp_words = tl.load(\n+                        b_zp_ptr\n+                        + b_zp_src_off\n+                        + offs_kw[:, None] * stride_bzk\n+                        + offs_bn[None, :] * stride_bzn,\n+                        mask=offs_kw[:, None] < K // 16,\n+                        other=0,\n+                    )\n+                    b_zp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+                # Dequantize along K (scale is per-column, applied after the dot):\n+                if has_zp:\n+                    b_zp_words = tl.load(\n+                        b_zp_ptr\n+                        + b_zp_src_off\n+                        + offs_kw[:, None] * stride_bzk\n+                        + offs_bn[None, :] * stride_bzn,\n+                        mask=offs_kw[:, None] < K // 16,\n+                        other=0,\n+                    )\n+                    b_zp_vals+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+                # Dequantize along K (scale is per-column, applied after the dot):\n+                if has_zp:\n+                    b_zp_words = tl.load(\n+                        b_zp_ptr\n+                        + b_zp_src_off\n+                        + offs_kw[:, None] * stride_bzk\n+                        + offs_bn[None, :] * stride_bzn,\n+                        mask=offs_kw[:, None] < K // 16,\n+                        other=0,\n+                    )\n+                    b_zp_vals =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+                # Dequantize along K (scale is per-column, applied after the dot):\n+                if has_zp:\n+                    b_zp_words = tl.load(\n+                        b_zp_ptr\n+                        + b_zp_src_off\n+                        + offs_kw[:, None] * stride_bzk\n+                        + offs_bn[None, :] * stride_bzn,\n+                        mask=offs_kw[:, None] < K // 16,\n+                        other=0,\n+                    )\n+                    b_zp_vals = (+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+                # Dequantize along K (scale is per-column, applied after the dot):\n+                if has_zp:\n+                    b_zp_words = tl.load(\n+                        b_zp_ptr\n+                        + b_zp_src_off\n+                        + offs_kw[:, None] * stride_bzk\n+                        + offs_bn[None, :] * stride_bzn,\n+                        mask=offs_kw[:, None] < K // 16,\n+                        other=0,\n+                    )\n+                    b_zp_vals = (b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+                # Dequantize along K (scale is per-column, applied after the dot):\n+                if has_zp:\n+                    b_zp_words = tl.load(\n+                        b_zp_ptr\n+                        + b_zp_src_off\n+                        + offs_kw[:, None] * stride_bzk\n+                        + offs_bn[None, :] * stride_bzn,\n+                        mask=offs_kw[:, None] < K // 16,\n+                        other=0,\n+                    )\n+                    b_zp_vals = (b_z+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+                # Dequantize along K (scale is per-column, applied after the dot):\n+                if has_zp:\n+                    b_zp_words = tl.load(\n+                        b_zp_ptr\n+                        + b_zp_src_off\n+                        + offs_kw[:, None] * stride_bzk\n+                        + offs_bn[None, :] * stride_bzn,\n+                        mask=offs_kw[:, None] < K // 16,\n+                        other=0,\n+                    )\n+                    b_zp_vals = (b_zp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+                # Dequantize along K (scale is per-column, applied after the dot):\n+                if has_zp:\n+                    b_zp_words = tl.load(\n+                        b_zp_ptr\n+                        + b_zp_src_off\n+                        + offs_kw[:, None] * stride_bzk\n+                        + offs_bn[None, :] * stride_bzn,\n+                        mask=offs_kw[:, None] < K // 16,\n+                        other=0,\n+                    )\n+                    b_zp_vals = (b_zp_words+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+                # Dequantize along K (scale is per-column, applied after the dot):\n+                if has_zp:\n+                    b_zp_words = tl.load(\n+                        b_zp_ptr\n+                        + b_zp_src_off\n+                        + offs_kw[:, None] * stride_bzk\n+                        + offs_bn[None, :] * stride_bzn,\n+                        mask=offs_kw[:, None] < K // 16,\n+                        other=0,\n+                    )\n+                    b_zp_vals = (b_zp_words[:,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+                # Dequantize along K (scale is per-column, applied after the dot):\n+                if has_zp:\n+                    b_zp_words = tl.load(\n+                        b_zp_ptr\n+                        + b_zp_src_off\n+                        + offs_kw[:, None] * stride_bzk\n+                        + offs_bn[None, :] * stride_bzn,\n+                        mask=offs_kw[:, None] < K // 16,\n+                        other=0,\n+                    )\n+                    b_zp_vals = (b_zp_words[:, None+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+                # Dequantize along K (scale is per-column, applied after the dot):\n+                if has_zp:\n+                    b_zp_words = tl.load(\n+                        b_zp_ptr\n+                        + b_zp_src_off\n+                        + offs_kw[:, None] * stride_bzk\n+                        + offs_bn[None, :] * stride_bzn,\n+                        mask=offs_kw[:, None] < K // 16,\n+                        other=0,\n+                    )\n+                    b_zp_vals = (b_zp_words[:, None,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+                # Dequantize along K (scale is per-column, applied after the dot):\n+                if has_zp:\n+                    b_zp_words = tl.load(\n+                        b_zp_ptr\n+                        + b_zp_src_off\n+                        + offs_kw[:, None] * stride_bzk\n+                        + offs_bn[None, :] * stride_bzn,\n+                        mask=offs_kw[:, None] < K // 16,\n+                        other=0,\n+                    )\n+                    b_zp_vals = (b_zp_words[:, None, None+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+                # Dequantize along K (scale is per-column, applied after the dot):\n+                if has_zp:\n+                    b_zp_words = tl.load(\n+                        b_zp_ptr\n+                        + b_zp_src_off\n+                        + offs_kw[:, None] * stride_bzk\n+                        + offs_bn[None, :] * stride_bzn,\n+                        mask=offs_kw[:, None] < K // 16,\n+                        other=0,\n+                    )\n+                    b_zp_vals = (b_zp_words[:, None, None]+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+                # Dequantize along K (scale is per-column, applied after the dot):\n+                if has_zp:\n+                    b_zp_words = tl.load(\n+                        b_zp_ptr\n+                        + b_zp_src_off\n+                        + offs_kw[:, None] * stride_bzk\n+                        + offs_bn[None, :] * stride_bzn,\n+                        mask=offs_kw[:, None] < K // 16,\n+                        other=0,\n+                    )\n+                    b_zp_vals = (b_zp_words[:, None, None] >>+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+                # Dequantize along K (scale is per-column, applied after the dot):\n+                if has_zp:\n+                    b_zp_words = tl.load(\n+                        b_zp_ptr\n+                        + b_zp_src_off\n+                        + offs_kw[:, None] * stride_bzk\n+                        + offs_bn[None, :] * stride_bzn,\n+                        mask=offs_kw[:, None] < K // 16,\n+                        other=0,\n+                    )\n+                    b_zp_vals = (b_zp_words[:, None, None] >> b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+                # Dequantize along K (scale is per-column, applied after the dot):\n+                if has_zp:\n+                    b_zp_words = tl.load(\n+                        b_zp_ptr\n+                        + b_zp_src_off\n+                        + offs_kw[:, None] * stride_bzk\n+                        + offs_bn[None, :] * stride_bzn,\n+                        mask=offs_kw[:, None] < K // 16,\n+                        other=0,\n+                    )\n+                    b_zp_vals = (b_zp_words[:, None, None] >> b_z+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+                # Dequantize along K (scale is per-column, applied after the dot):\n+                if has_zp:\n+                    b_zp_words = tl.load(\n+                        b_zp_ptr\n+                        + b_zp_src_off\n+                        + offs_kw[:, None] * stride_bzk\n+                        + offs_bn[None, :] * stride_bzn,\n+                        mask=offs_kw[:, None] < K // 16,\n+                        other=0,\n+                    )\n+                    b_zp_vals = (b_zp_words[:, None, None] >> b_zp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+                # Dequantize along K (scale is per-column, applied after the dot):\n+                if has_zp:\n+                    b_zp_words = tl.load(\n+                        b_zp_ptr\n+                        + b_zp_src_off\n+                        + offs_kw[:, None] * stride_bzk\n+                        + offs_bn[None, :] * stride_bzn,\n+                        mask=offs_kw[:, None] < K // 16,\n+                        other=0,\n+                    )\n+                    b_zp_vals = (b_zp_words[:, None, None] >> b_zp_sh+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+                # Dequantize along K (scale is per-column, applied after the dot):\n+                if has_zp:\n+                    b_zp_words = tl.load(\n+                        b_zp_ptr\n+                        + b_zp_src_off\n+                        + offs_kw[:, None] * stride_bzk\n+                        + offs_bn[None, :] * stride_bzn,\n+                        mask=offs_kw[:, None] < K // 16,\n+                        other=0,\n+                    )\n+                    b_zp_vals = (b_zp_words[:, None, None] >> b_zp_shifter+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+                # Dequantize along K (scale is per-column, applied after the dot):\n+                if has_zp:\n+                    b_zp_words = tl.load(\n+                        b_zp_ptr\n+                        + b_zp_src_off\n+                        + offs_kw[:, None] * stride_bzk\n+                        + offs_bn[None, :] * stride_bzn,\n+                        mask=offs_kw[:, None] < K // 16,\n+                        other=0,\n+                    )\n+                    b_zp_vals = (b_zp_words[:, None, None] >> b_zp_shifter)+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+                # Dequantize along K (scale is per-column, applied after the dot):\n+                if has_zp:\n+                    b_zp_words = tl.load(\n+                        b_zp_ptr\n+                        + b_zp_src_off\n+                        + offs_kw[:, None] * stride_bzk\n+                        + offs_bn[None, :] * stride_bzn,\n+                        mask=offs_kw[:, None] < K // 16,\n+                        other=0,\n+                    )\n+                    b_zp_vals = (b_zp_words[:, None, None] >> b_zp_shifter) &+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+                # Dequantize along K (scale is per-column, applied after the dot):\n+                if has_zp:\n+                    b_zp_words = tl.load(\n+                        b_zp_ptr\n+                        + b_zp_src_off\n+                        + offs_kw[:, None] * stride_bzk\n+                        + offs_bn[None, :] * stride_bzn,\n+                        mask=offs_kw[:, None] < K // 16,\n+                        other=0,\n+                    )\n+                    b_zp_vals = (b_zp_words[:, None, None] >> b_zp_shifter) & +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+                # Dequantize along K (scale is per-column, applied after the dot):\n+                if has_zp:\n+                    b_zp_words = tl.load(\n+                        b_zp_ptr\n+                        + b_zp_src_off\n+                        + offs_kw[:, None] * stride_bzk\n+                        + offs_bn[None, :] * stride_bzn,\n+                        mask=offs_kw[:, None] < K // 16,\n+                        other=0,\n+                    )\n+                    b_zp_vals = (b_zp_words[:, None, None] >> b_zp_shifter) & 3+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+                # Dequantize along K (scale is per-column, applied after the dot):\n+                if has_zp:\n+                    b_zp_words = tl.load(\n+                        b_zp_ptr\n+                        + b_zp_src_off\n+                        + offs_kw[:, None] * stride_bzk\n+                        + offs_bn[None, :] * stride_bzn,\n+                        mask=offs_kw[:, None] < K // 16,\n+                        other=0,\n+                    )\n+                    b_zp_vals = (b_zp_words[:, None, None] >> b_zp_shifter) & 3\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+                # Dequantize along K (scale is per-column, applied after the dot):\n+                if has_zp:\n+                    b_zp_words = tl.load(\n+                        b_zp_ptr\n+                        + b_zp_src_off\n+                        + offs_kw[:, None] * stride_bzk\n+                        + offs_bn[None, :] * stride_bzn,\n+                        mask=offs_kw[:, None] < K // 16,\n+                        other=0,\n+                    )\n+                    b_zp_vals = (b_zp_words[:, None, None] >> b_zp_shifter) & 3\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+                # Dequantize along K (scale is per-column, applied after the dot):\n+                if has_zp:\n+                    b_zp_words = tl.load(\n+                        b_zp_ptr\n+                        + b_zp_src_off\n+                        + offs_kw[:, None] * stride_bzk\n+                        + offs_bn[None, :] * stride_bzn,\n+                        mask=offs_kw[:, None] < K // 16,\n+                        other=0,\n+                    )\n+                    b_zp_vals = (b_zp_words[:, None, None] >> b_zp_shifter) & 3\n+                   +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+                # Dequantize along K (scale is per-column, applied after the dot):\n+                if has_zp:\n+                    b_zp_words = tl.load(\n+                        b_zp_ptr\n+                        + b_zp_src_off\n+                        + offs_kw[:, None] * stride_bzk\n+                        + offs_bn[None, :] * stride_bzn,\n+                        mask=offs_kw[:, None] < K // 16,\n+                        other=0,\n+                    )\n+                    b_zp_vals = (b_zp_words[:, None, None] >> b_zp_shifter) & 3\n+                    b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+                # Dequantize along K (scale is per-column, applied after the dot):\n+                if has_zp:\n+                    b_zp_words = tl.load(\n+                        b_zp_ptr\n+                        + b_zp_src_off\n+                        + offs_kw[:, None] * stride_bzk\n+                        + offs_bn[None, :] * stride_bzn,\n+                        mask=offs_kw[:, None] < K // 16,\n+                        other=0,\n+                    )\n+                    b_zp_vals = (b_zp_words[:, None, None] >> b_zp_shifter) & 3\n+                    b =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+                # Dequantize along K (scale is per-column, applied after the dot):\n+                if has_zp:\n+                    b_zp_words = tl.load(\n+                        b_zp_ptr\n+                        + b_zp_src_off\n+                        + offs_kw[:, None] * stride_bzk\n+                        + offs_bn[None, :] * stride_bzn,\n+                        mask=offs_kw[:, None] < K // 16,\n+                        other=0,\n+                    )\n+                    b_zp_vals = (b_zp_words[:, None, None] >> b_zp_shifter) & 3\n+                    b = ((+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+                # Dequantize along K (scale is per-column, applied after the dot):\n+                if has_zp:\n+                    b_zp_words = tl.load(\n+                        b_zp_ptr\n+                        + b_zp_src_off\n+                        + offs_kw[:, None] * stride_bzk\n+                        + offs_bn[None, :] * stride_bzn,\n+                        mask=offs_kw[:, None] < K // 16,\n+                        other=0,\n+                    )\n+                    b_zp_vals = (b_zp_words[:, None, None] >> b_zp_shifter) & 3\n+                    b = ((vals+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+                # Dequantize along K (scale is per-column, applied after the dot):\n+                if has_zp:\n+                    b_zp_words = tl.load(\n+                        b_zp_ptr\n+                        + b_zp_src_off\n+                        + offs_kw[:, None] * stride_bzk\n+                        + offs_bn[None, :] * stride_bzn,\n+                        mask=offs_kw[:, None] < K // 16,\n+                        other=0,\n+                    )\n+                    b_zp_vals = (b_zp_words[:, None, None] >> b_zp_shifter) & 3\n+                    b = ((vals -+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+                # Dequantize along K (scale is per-column, applied after the dot):\n+                if has_zp:\n+                    b_zp_words = tl.load(\n+                        b_zp_ptr\n+                        + b_zp_src_off\n+                        + offs_kw[:, None] * stride_bzk\n+                        + offs_bn[None, :] * stride_bzn,\n+                        mask=offs_kw[:, None] < K // 16,\n+                        other=0,\n+                    )\n+                    b_zp_vals = (b_zp_words[:, None, None] >> b_zp_shifter) & 3\n+                    b = ((vals - b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+                # Dequantize along K (scale is per-column, applied after the dot):\n+                if has_zp:\n+                    b_zp_words = tl.load(\n+                        b_zp_ptr\n+                        + b_zp_src_off\n+                        + offs_kw[:, None] * stride_bzk\n+                        + offs_bn[None, :] * stride_bzn,\n+                        mask=offs_kw[:, None] < K // 16,\n+                        other=0,\n+                    )\n+                    b_zp_vals = (b_zp_words[:, None, None] >> b_zp_shifter) & 3\n+                    b = ((vals - b_z+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+                # Dequantize along K (scale is per-column, applied after the dot):\n+                if has_zp:\n+                    b_zp_words = tl.load(\n+                        b_zp_ptr\n+                        + b_zp_src_off\n+                        + offs_kw[:, None] * stride_bzk\n+                        + offs_bn[None, :] * stride_bzn,\n+                        mask=offs_kw[:, None] < K // 16,\n+                        other=0,\n+                    )\n+                    b_zp_vals = (b_zp_words[:, None, None] >> b_zp_shifter) & 3\n+                    b = ((vals - b_zp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+                # Dequantize along K (scale is per-column, applied after the dot):\n+                if has_zp:\n+                    b_zp_words = tl.load(\n+                        b_zp_ptr\n+                        + b_zp_src_off\n+                        + offs_kw[:, None] * stride_bzk\n+                        + offs_bn[None, :] * stride_bzn,\n+                        mask=offs_kw[:, None] < K // 16,\n+                        other=0,\n+                    )\n+                    b_zp_vals = (b_zp_words[:, None, None] >> b_zp_shifter) & 3\n+                    b = ((vals - b_zp_vals+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+                # Dequantize along K (scale is per-column, applied after the dot):\n+                if has_zp:\n+                    b_zp_words = tl.load(\n+                        b_zp_ptr\n+                        + b_zp_src_off\n+                        + offs_kw[:, None] * stride_bzk\n+                        + offs_bn[None, :] * stride_bzn,\n+                        mask=offs_kw[:, None] < K // 16,\n+                        other=0,\n+                    )\n+                    b_zp_vals = (b_zp_words[:, None, None] >> b_zp_shifter) & 3\n+                    b = ((vals - b_zp_vals).+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+                # Dequantize along K (scale is per-column, applied after the dot):\n+                if has_zp:\n+                    b_zp_words = tl.load(\n+                        b_zp_ptr\n+                        + b_zp_src_off\n+                        + offs_kw[:, None] * stride_bzk\n+                        + offs_bn[None, :] * stride_bzn,\n+                        mask=offs_kw[:, None] < K // 16,\n+                        other=0,\n+                    )\n+                    b_zp_vals = (b_zp_words[:, None, None] >> b_zp_shifter) & 3\n+                    b = ((vals - b_zp_vals).to+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+                # Dequantize along K (scale is per-column, applied after the dot):\n+                if has_zp:\n+                    b_zp_words = tl.load(\n+                        b_zp_ptr\n+                        + b_zp_src_off\n+                        + offs_kw[:, None] * stride_bzk\n+                        + offs_bn[None, :] * stride_bzn,\n+                        mask=offs_kw[:, None] < K // 16,\n+                        other=0,\n+                    )\n+                    b_zp_vals = (b_zp_words[:, None, None] >> b_zp_shifter) & 3\n+                    b = ((vals - b_zp_vals).to(+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+                # Dequantize along K (scale is per-column, applied after the dot):\n+                if has_zp:\n+                    b_zp_words = tl.load(\n+                        b_zp_ptr\n+                        + b_zp_src_off\n+                        + offs_kw[:, None] * stride_bzk\n+                        + offs_bn[None, :] * stride_bzn,\n+                        mask=offs_kw[:, None] < K // 16,\n+                        other=0,\n+                    )\n+                    b_zp_vals = (b_zp_words[:, None, None] >> b_zp_shifter) & 3\n+                    b = ((vals - b_zp_vals).to(accumulator+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+                # Dequantize along K (scale is per-column, applied after the dot):\n+                if has_zp:\n+                    b_zp_words = tl.load(\n+                        b_zp_ptr\n+                        + b_zp_src_off\n+                        + offs_kw[:, None] * stride_bzk\n+                        + offs_bn[None, :] * stride_bzn,\n+                        mask=offs_kw[:, None] < K // 16,\n+                        other=0,\n+                    )\n+                    b_zp_vals = (b_zp_words[:, None, None] >> b_zp_shifter) & 3\n+                    b = ((vals - b_zp_vals).to(accumulator.dtype+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u4e00\u4e2a if\uff0c \u7136\u540e\u51fa\u73b0if-else\u90fd\u591a\u4e2a\u62ec\u53f7\u7684\u95ee\u9898\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                words = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16, other=0)\n+                # Broadcast each word across its 16 K values: [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                vals = (words[:, None, None] >> b_shifter) & 3\n+                # Dequantize along K (scale is per-column, applied after the dot):\n+                if has_zp:\n+                    b_zp_words = tl.load(\n+                        b_zp_ptr\n+                        + b_zp_src_off\n+                        + offs_kw[:, None] * stride_bzk\n+                        + offs_bn[None, :] * stride_bzn,\n+                        mask=offs_kw[:, None] < K // 16,\n+                        other=0,\n+                    )\n+                    b_zp_vals = (b_zp_words[:, None, None] >> b_zp_shifter) & 3\n+                    b = ((vals - b_zp_vals).to(accumulator.dtype))",
 "ttft_seconds": 1.1444044839881826,
 "status": "ok",
 "tg_tokens_per_s": 34.99586378371139,
 "decode_seconds_per_token": 0.028574805473595533,
 "individual_token_timing": true,
 "event_intervals_ms": {
  "n": 511,
  "median": 28.082812001230195,
  "p95": 36.5088489925256,
  "mean": 28.574805473595532
 },
 "class": "code",
 "requested_input_tokens": 2048
}
