{
 "started_utc": "2026-09-16T23:55:29.613380+00:00",
 "wall_seconds": 100.10029220300203,
 "prompt_tokens": 131072,
 "completion_tokens": 512,
 "stream_events": 512,
 "ended_utc": "2026-09-16T23:57:09.713954+00:00",
 "warmup": false,
 "sample_index": 3,
 "requested_decode_tokens": 512,
 "gpu": {
  "n": 360,
  "temperature_c": {
   "min": 72.0,
   "max": 78.0,
   "mean": 76.01388888888889
  },
  "power_w": {
   "min": 152.38,
   "max": 283.54,
   "mean": 234.54422222222223
  },
  "util_pct": {
   "min": 0.0,
   "max": 100.0,
   "mean": 64.63333333333334
  },
  "clock_sm_mhz": {
   "min": 615.0,
   "max": 1905.0,
   "mean": 1678.75
  },
  "clock_mem_mhz": {
   "min": 9501.0,
   "max": 9501.0,
   "mean": 9501.0
  },
  "memory_free_mib": {
   "min": 736.0
  }
 },
 "output_text_sha256": "99345dbfd95385533787606022e6b2b3fb57676c81126d6ddfe454504e430554",
 "output_text": "00 tokens0 tokens in0 tokens in 0 tokens in 10 tokens in 170 tokens in 17.0 tokens in 17.80 tokens in 17.8 s0 tokens in 17.8 s           0 tokens in 17.8 s            ->0 tokens in 17.8 s            -> ~0 tokens in 17.8 s            -> ~20 tokens in 17.8 s            -> ~260 tokens in 17.8 s            -> ~26 GB0 tokens in 17.8 s            -> ~26 GB per0 tokens in 17.8 s            -> ~26 GB per token0 tokens in 17.8 s            -> ~26 GB per token\n0 tokens in 17.8 s            -> ~26 GB per token\n```0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-off0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-g0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb`0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict`0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\n0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model.0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 50 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 510 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 5120 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-exp0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert Mo0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-100 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 50 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 510 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 5120 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 100 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n|0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass |0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes |0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n|0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert |0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 10 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.30 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.310 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB |0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n|0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 100 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d70 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 40 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 480 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers |0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 00 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.60 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.630 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB |0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n|0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer |0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 20 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 260 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.000 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB |0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 40 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 410 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.50 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d70 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n###0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The stream0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\n0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes,0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small,0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral**0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u20140 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint.0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\n0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading,0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VR0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full;0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the O0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer.0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\n0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\ninter0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n10 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1.0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.**0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `off0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`,0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n  0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`.0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the off0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n  0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n20 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2.0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Off0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.**0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host,0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n  0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   G0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u20140 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every*0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token.0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\n0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `Expert0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer`0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\n0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nst0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (60 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (660 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (6680 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB)0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and re0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it.0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Trit0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u20140 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u2014 it0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u2014 it\n0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u2014 it\nstill0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u2014 it\nstill sees0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u2014 it\nstill sees one0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u2014 it\nstill sees one contiguous0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u2014 it\nstill sees one contiguous tensor0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u2014 it\nstill sees one contiguous tensor.0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u2014 it\nstill sees one contiguous tensor.\n\n0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u2014 it\nstill sees one contiguous tensor.\n\nThe0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u2014 it\nstill sees one contiguous tensor.\n\nThe gather0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u2014 it\nstill sees one contiguous tensor.\n\nThe gather is0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u2014 it\nstill sees one contiguous tensor.\n\nThe gather is a0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u2014 it\nstill sees one contiguous tensor.\n\nThe gather is a Trit0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u2014 it\nstill sees one contiguous tensor.\n\nThe gather is a Triton0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u2014 it\nstill sees one contiguous tensor.\n\nThe gather is a Triton kernel0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u2014 it\nstill sees one contiguous tensor.\n\nThe gather is a Triton kernel reading0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u2014 it\nstill sees one contiguous tensor.\n\nThe gather is a Triton kernel reading directly0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u2014 it\nstill sees one contiguous tensor.\n\nThe gather is a Triton kernel reading directly from0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u2014 it\nstill sees one contiguous tensor.\n\nThe gather is a Triton kernel reading directly from pinned0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u2014 it\nstill sees one contiguous tensor.\n\nThe gather is a Triton kernel reading directly from pinned host0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u2014 it\nstill sees one contiguous tensor.\n\nThe gather is a Triton kernel reading directly from pinned host memory0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u2014 it\nstill sees one contiguous tensor.\n\nThe gather is a Triton kernel reading directly from pinned host memory with0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u2014 it\nstill sees one contiguous tensor.\n\nThe gather is a Triton kernel reading directly from pinned host memory with GPU0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u2014 it\nstill sees one contiguous tensor.\n\nThe gather is a Triton kernel reading directly from pinned host memory with GPU-side0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u2014 it\nstill sees one contiguous tensor.\n\nThe gather is a Triton kernel reading directly from pinned host memory with GPU-side indices0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u2014 it\nstill sees one contiguous tensor.\n\nThe gather is a Triton kernel reading directly from pinned host memory with GPU-side indices.0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u2014 it\nstill sees one contiguous tensor.\n\nThe gather is a Triton kernel reading directly from pinned host memory with GPU-side indices. The0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u2014 it\nstill sees one contiguous tensor.\n\nThe gather is a Triton kernel reading directly from pinned host memory with GPU-side indices. The\n0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u2014 it\nstill sees one contiguous tensor.\n\nThe gather is a Triton kernel reading directly from pinned host memory with GPU-side indices. The\nna0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u2014 it\nstill sees one contiguous tensor.\n\nThe gather is a Triton kernel reading directly from pinned host memory with GPU-side indices. The\nnaive0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u2014 it\nstill sees one contiguous tensor.\n\nThe gather is a Triton kernel reading directly from pinned host memory with GPU-side indices. The\nnaive route0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u2014 it\nstill sees one contiguous tensor.\n\nThe gather is a Triton kernel reading directly from pinned host memory with GPU-side indices. The\nnaive route (`0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u2014 it\nstill sees one contiguous tensor.\n\nThe gather is a Triton kernel reading directly from pinned host memory with GPU-side indices. The\nnaive route (`uniq0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u2014 it\nstill sees one contiguous tensor.\n\nThe gather is a Triton kernel reading directly from pinned host memory with GPU-side indices. The\nnaive route (`uniq.to0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u2014 it\nstill sees one contiguous tensor.\n\nThe gather is a Triton kernel reading directly from pinned host memory with GPU-side indices. The\nnaive route (`uniq.to(\"0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u2014 it\nstill sees one contiguous tensor.\n\nThe gather is a Triton kernel reading directly from pinned host memory with GPU-side indices. The\nnaive route (`uniq.to(\"cpu0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u2014 it\nstill sees one contiguous tensor.\n\nThe gather is a Triton kernel reading directly from pinned host memory with GPU-side indices. The\nnaive route (`uniq.to(\"cpu\")0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u2014 it\nstill sees one contiguous tensor.\n\nThe gather is a Triton kernel reading directly from pinned host memory with GPU-side indices. The\nnaive route (`uniq.to(\"cpu\")`0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u2014 it\nstill sees one contiguous tensor.\n\nThe gather is a Triton kernel reading directly from pinned host memory with GPU-side indices. The\nnaive route (`uniq.to(\"cpu\")` then0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u2014 it\nstill sees one contiguous tensor.\n\nThe gather is a Triton kernel reading directly from pinned host memory with GPU-side indices. The\nnaive route (`uniq.to(\"cpu\")` then one0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u2014 it\nstill sees one contiguous tensor.\n\nThe gather is a Triton kernel reading directly from pinned host memory with GPU-side indices. The\nnaive route (`uniq.to(\"cpu\")` then one copy0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u2014 it\nstill sees one contiguous tensor.\n\nThe gather is a Triton kernel reading directly from pinned host memory with GPU-side indices. The\nnaive route (`uniq.to(\"cpu\")` then one copy per0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u2014 it\nstill sees one contiguous tensor.\n\nThe gather is a Triton kernel reading directly from pinned host memory with GPU-side indices. The\nnaive route (`uniq.to(\"cpu\")` then one copy per row0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u2014 it\nstill sees one contiguous tensor.\n\nThe gather is a Triton kernel reading directly from pinned host memory with GPU-side indices. The\nnaive route (`uniq.to(\"cpu\")` then one copy per row)0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u2014 it\nstill sees one contiguous tensor.\n\nThe gather is a Triton kernel reading directly from pinned host memory with GPU-side indices. The\nnaive route (`uniq.to(\"cpu\")` then one copy per row) forces0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u2014 it\nstill sees one contiguous tensor.\n\nThe gather is a Triton kernel reading directly from pinned host memory with GPU-side indices. The\nnaive route (`uniq.to(\"cpu\")` then one copy per row) forces a0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u2014 it\nstill sees one contiguous tensor.\n\nThe gather is a Triton kernel reading directly from pinned host memory with GPU-side indices. The\nnaive route (`uniq.to(\"cpu\")` then one copy per row) forces a device0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u2014 it\nstill sees one contiguous tensor.\n\nThe gather is a Triton kernel reading directly from pinned host memory with GPU-side indices. The\nnaive route (`uniq.to(\"cpu\")` then one copy per row) forces a device synchronization0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u2014 it\nstill sees one contiguous tensor.\n\nThe gather is a Triton kernel reading directly from pinned host memory with GPU-side indices. The\nnaive route (`uniq.to(\"cpu\")` then one copy per row) forces a device synchronization per0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u2014 it\nstill sees one contiguous tensor.\n\nThe gather is a Triton kernel reading directly from pinned host memory with GPU-side indices. The\nnaive route (`uniq.to(\"cpu\")` then one copy per row) forces a device synchronization per layer0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u2014 it\nstill sees one contiguous tensor.\n\nThe gather is a Triton kernel reading directly from pinned host memory with GPU-side indices. The\nnaive route (`uniq.to(\"cpu\")` then one copy per row) forces a device synchronization per layer.0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u2014 it\nstill sees one contiguous tensor.\n\nThe gather is a Triton kernel reading directly from pinned host memory with GPU-side indices. The\nnaive route (`uniq.to(\"cpu\")` then one copy per row) forces a device synchronization per layer.\n0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u2014 it\nstill sees one contiguous tensor.\n\nThe gather is a Triton kernel reading directly from pinned host memory with GPU-side indices. The\nnaive route (`uniq.to(\"cpu\")` then one copy per row) forces a device synchronization per layer.\nDuring0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u2014 it\nstill sees one contiguous tensor.\n\nThe gather is a Triton kernel reading directly from pinned host memory with GPU-side indices. The\nnaive route (`uniq.to(\"cpu\")` then one copy per row) forces a device synchronization per layer.\nDuring decode0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u2014 it\nstill sees one contiguous tensor.\n\nThe gather is a Triton kernel reading directly from pinned host memory with GPU-side indices. The\nnaive route (`uniq.to(\"cpu\")` then one copy per row) forces a device synchronization per layer.\nDuring decode we0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u2014 it\nstill sees one contiguous tensor.\n\nThe gather is a Triton kernel reading directly from pinned host memory with GPU-side indices. The\nnaive route (`uniq.to(\"cpu\")` then one copy per row) forces a device synchronization per layer.\nDuring decode we skip0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u2014 it\nstill sees one contiguous tensor.\n\nThe gather is a Triton kernel reading directly from pinned host memory with GPU-side indices. The\nnaive route (`uniq.to(\"cpu\")` then one copy per row) forces a device synchronization per layer.\nDuring decode we skip ded0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u2014 it\nstill sees one contiguous tensor.\n\nThe gather is a Triton kernel reading directly from pinned host memory with GPU-side indices. The\nnaive route (`uniq.to(\"cpu\")` then one copy per row) forces a device synchronization per layer.\nDuring decode we skip dedup0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u2014 it\nstill sees one contiguous tensor.\n\nThe gather is a Triton kernel reading directly from pinned host memory with GPU-side indices. The\nnaive route (`uniq.to(\"cpu\")` then one copy per row) forces a device synchronization per layer.\nDuring decode we skip deduplication0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u2014 it\nstill sees one contiguous tensor.\n\nThe gather is a Triton kernel reading directly from pinned host memory with GPU-side indices. The\nnaive route (`uniq.to(\"cpu\")` then one copy per row) forces a device synchronization per layer.\nDuring decode we skip deduplication entirely0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u2014 it\nstill sees one contiguous tensor.\n\nThe gather is a Triton kernel reading directly from pinned host memory with GPU-side indices. The\nnaive route (`uniq.to(\"cpu\")` then one copy per row) forces a device synchronization per layer.\nDuring decode we skip deduplication entirely:0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u2014 it\nstill sees one contiguous tensor.\n\nThe gather is a Triton kernel reading directly from pinned host memory with GPU-side indices. The\nnaive route (`uniq.to(\"cpu\")` then one copy per row) forces a device synchronization per layer.\nDuring decode we skip deduplication entirely: with0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u2014 it\nstill sees one contiguous tensor.\n\nThe gather is a Triton kernel reading directly from pinned host memory with GPU-side indices. The\nnaive route (`uniq.to(\"cpu\")` then one copy per row) forces a device synchronization per layer.\nDuring decode we skip deduplication entirely: with 0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u2014 it\nstill sees one contiguous tensor.\n\nThe gather is a Triton kernel reading directly from pinned host memory with GPU-side indices. The\nnaive route (`uniq.to(\"cpu\")` then one copy per row) forces a device synchronization per layer.\nDuring decode we skip deduplication entirely: with 10 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u2014 it\nstill sees one contiguous tensor.\n\nThe gather is a Triton kernel reading directly from pinned host memory with GPU-side indices. The\nnaive route (`uniq.to(\"cpu\")` then one copy per row) forces a device synchronization per layer.\nDuring decode we skip deduplication entirely: with 100 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u2014 it\nstill sees one contiguous tensor.\n\nThe gather is a Triton kernel reading directly from pinned host memory with GPU-side indices. The\nnaive route (`uniq.to(\"cpu\")` then one copy per row) forces a device synchronization per layer.\nDuring decode we skip deduplication entirely: with 10 ids0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u2014 it\nstill sees one contiguous tensor.\n\nThe gather is a Triton kernel reading directly from pinned host memory with GPU-side indices. The\nnaive route (`uniq.to(\"cpu\")` then one copy per row) forces a device synchronization per layer.\nDuring decode we skip deduplication entirely: with 10 ids,0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u2014 it\nstill sees one contiguous tensor.\n\nThe gather is a Triton kernel reading directly from pinned host memory with GPU-side indices. The\nnaive route (`uniq.to(\"cpu\")` then one copy per row) forces a device synchronization per layer.\nDuring decode we skip deduplication entirely: with 10 ids, copying0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u2014 it\nstill sees one contiguous tensor.\n\nThe gather is a Triton kernel reading directly from pinned host memory with GPU-side indices. The\nnaive route (`uniq.to(\"cpu\")` then one copy per row) forces a device synchronization per layer.\nDuring decode we skip deduplication entirely: with 10 ids, copying a0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u2014 it\nstill sees one contiguous tensor.\n\nThe gather is a Triton kernel reading directly from pinned host memory with GPU-side indices. The\nnaive route (`uniq.to(\"cpu\")` then one copy per row) forces a device synchronization per layer.\nDuring decode we skip deduplication entirely: with 10 ids, copying a duplicate0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u2014 it\nstill sees one contiguous tensor.\n\nThe gather is a Triton kernel reading directly from pinned host memory with GPU-side indices. The\nnaive route (`uniq.to(\"cpu\")` then one copy per row) forces a device synchronization per layer.\nDuring decode we skip deduplication entirely: with 10 ids, copying a duplicate row0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u2014 it\nstill sees one contiguous tensor.\n\nThe gather is a Triton kernel reading directly from pinned host memory with GPU-side indices. The\nnaive route (`uniq.to(\"cpu\")` then one copy per row) forces a device synchronization per layer.\nDuring decode we skip deduplication entirely: with 10 ids, copying a duplicate row is0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u2014 it\nstill sees one contiguous tensor.\n\nThe gather is a Triton kernel reading directly from pinned host memory with GPU-side indices. The\nnaive route (`uniq.to(\"cpu\")` then one copy per row) forces a device synchronization per layer.\nDuring decode we skip deduplication entirely: with 10 ids, copying a duplicate row is cheaper0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u2014 it\nstill sees one contiguous tensor.\n\nThe gather is a Triton kernel reading directly from pinned host memory with GPU-side indices. The\nnaive route (`uniq.to(\"cpu\")` then one copy per row) forces a device synchronization per layer.\nDuring decode we skip deduplication entirely: with 10 ids, copying a duplicate row is cheaper than0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u2014 it\nstill sees one contiguous tensor.\n\nThe gather is a Triton kernel reading directly from pinned host memory with GPU-side indices. The\nnaive route (`uniq.to(\"cpu\")` then one copy per row) forces a device synchronization per layer.\nDuring decode we skip deduplication entirely: with 10 ids, copying a duplicate row is cheaper than\n0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u2014 it\nstill sees one contiguous tensor.\n\nThe gather is a Triton kernel reading directly from pinned host memory with GPU-side indices. The\nnaive route (`uniq.to(\"cpu\")` then one copy per row) forces a device synchronization per layer.\nDuring decode we skip deduplication entirely: with 10 ids, copying a duplicate row is cheaper than\nthe0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u2014 it\nstill sees one contiguous tensor.\n\nThe gather is a Triton kernel reading directly from pinned host memory with GPU-side indices. The\nnaive route (`uniq.to(\"cpu\")` then one copy per row) forces a device synchronization per layer.\nDuring decode we skip deduplication entirely: with 10 ids, copying a duplicate row is cheaper than\nthe sync0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u2014 it\nstill sees one contiguous tensor.\n\nThe gather is a Triton kernel reading directly from pinned host memory with GPU-side indices. The\nnaive route (`uniq.to(\"cpu\")` then one copy per row) forces a device synchronization per layer.\nDuring decode we skip deduplication entirely: with 10 ids, copying a duplicate row is cheaper than\nthe sync `0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u2014 it\nstill sees one contiguous tensor.\n\nThe gather is a Triton kernel reading directly from pinned host memory with GPU-side indices. The\nnaive route (`uniq.to(\"cpu\")` then one copy per row) forces a device synchronization per layer.\nDuring decode we skip deduplication entirely: with 10 ids, copying a duplicate row is cheaper than\nthe sync `torch0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u2014 it\nstill sees one contiguous tensor.\n\nThe gather is a Triton kernel reading directly from pinned host memory with GPU-side indices. The\nnaive route (`uniq.to(\"cpu\")` then one copy per row) forces a device synchronization per layer.\nDuring decode we skip deduplication entirely: with 10 ids, copying a duplicate row is cheaper than\nthe sync `torch.unique0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u2014 it\nstill sees one contiguous tensor.\n\nThe gather is a Triton kernel reading directly from pinned host memory with GPU-side indices. The\nnaive route (`uniq.to(\"cpu\")` then one copy per row) forces a device synchronization per layer.\nDuring decode we skip deduplication entirely: with 10 ids, copying a duplicate row is cheaper than\nthe sync `torch.unique`0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u2014 it\nstill sees one contiguous tensor.\n\nThe gather is a Triton kernel reading directly from pinned host memory with GPU-side indices. The\nnaive route (`uniq.to(\"cpu\")` then one copy per row) forces a device synchronization per layer.\nDuring decode we skip deduplication entirely: with 10 ids, copying a duplicate row is cheaper than\nthe sync `torch.unique` forces0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u2014 it\nstill sees one contiguous tensor.\n\nThe gather is a Triton kernel reading directly from pinned host memory with GPU-side indices. The\nnaive route (`uniq.to(\"cpu\")` then one copy per row) forces a device synchronization per layer.\nDuring decode we skip deduplication entirely: with 10 ids, copying a duplicate row is cheaper than\nthe sync `torch.unique` forces because0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u2014 it\nstill sees one contiguous tensor.\n\nThe gather is a Triton kernel reading directly from pinned host memory with GPU-side indices. The\nnaive route (`uniq.to(\"cpu\")` then one copy per row) forces a device synchronization per layer.\nDuring decode we skip deduplication entirely: with 10 ids, copying a duplicate row is cheaper than\nthe sync `torch.unique` forces because its0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u2014 it\nstill sees one contiguous tensor.\n\nThe gather is a Triton kernel reading directly from pinned host memory with GPU-side indices. The\nnaive route (`uniq.to(\"cpu\")` then one copy per row) forces a device synchronization per layer.\nDuring decode we skip deduplication entirely: with 10 ids, copying a duplicate row is cheaper than\nthe sync `torch.unique` forces because its output0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u2014 it\nstill sees one contiguous tensor.\n\nThe gather is a Triton kernel reading directly from pinned host memory with GPU-side indices. The\nnaive route (`uniq.to(\"cpu\")` then one copy per row) forces a device synchronization per layer.\nDuring decode we skip deduplication entirely: with 10 ids, copying a duplicate row is cheaper than\nthe sync `torch.unique` forces because its output size0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u2014 it\nstill sees one contiguous tensor.\n\nThe gather is a Triton kernel reading directly from pinned host memory with GPU-side indices. The\nnaive route (`uniq.to(\"cpu\")` then one copy per row) forces a device synchronization per layer.\nDuring decode we skip deduplication entirely: with 10 ids, copying a duplicate row is cheaper than\nthe sync `torch.unique` forces because its output size is0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u2014 it\nstill sees one contiguous tensor.\n\nThe gather is a Triton kernel reading directly from pinned host memory with GPU-side indices. The\nnaive route (`uniq.to(\"cpu\")` then one copy per row) forces a device synchronization per layer.\nDuring decode we skip deduplication entirely: with 10 ids, copying a duplicate row is cheaper than\nthe sync `torch.unique` forces because its output size is data0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u2014 it\nstill sees one contiguous tensor.\n\nThe gather is a Triton kernel reading directly from pinned host memory with GPU-side indices. The\nnaive route (`uniq.to(\"cpu\")` then one copy per row) forces a device synchronization per layer.\nDuring decode we skip deduplication entirely: with 10 ids, copying a duplicate row is cheaper than\nthe sync `torch.unique` forces because its output size is data-dependent0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u2014 it\nstill sees one contiguous tensor.\n\nThe gather is a Triton kernel reading directly from pinned host memory with GPU-side indices. The\nnaive route (`uniq.to(\"cpu\")` then one copy per row) forces a device synchronization per layer.\nDuring decode we skip deduplication entirely: with 10 ids, copying a duplicate row is cheaper than\nthe sync `torch.unique` forces because its output size is data-dependent.0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u2014 it\nstill sees one contiguous tensor.\n\nThe gather is a Triton kernel reading directly from pinned host memory with GPU-side indices. The\nnaive route (`uniq.to(\"cpu\")` then one copy per row) forces a device synchronization per layer.\nDuring decode we skip deduplication entirely: with 10 ids, copying a duplicate row is cheaper than\nthe sync `torch.unique` forces because its output size is data-dependent.\n\n0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u2014 it\nstill sees one contiguous tensor.\n\nThe gather is a Triton kernel reading directly from pinned host memory with GPU-side indices. The\nnaive route (`uniq.to(\"cpu\")` then one copy per row) forces a device synchronization per layer.\nDuring decode we skip deduplication entirely: with 10 ids, copying a duplicate row is cheaper than\nthe sync `torch.unique` forces because its output size is data-dependent.\n\nSide0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u2014 it\nstill sees one contiguous tensor.\n\nThe gather is a Triton kernel reading directly from pinned host memory with GPU-side indices. The\nnaive route (`uniq.to(\"cpu\")` then one copy per row) forces a device synchronization per layer.\nDuring decode we skip deduplication entirely: with 10 ids, copying a duplicate row is cheaper than\nthe sync `torch.unique` forces because its output size is data-dependent.\n\nSide effect0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u2014 it\nstill sees one contiguous tensor.\n\nThe gather is a Triton kernel reading directly from pinned host memory with GPU-side indices. The\nnaive route (`uniq.to(\"cpu\")` then one copy per row) forces a device synchronization per layer.\nDuring decode we skip deduplication entirely: with 10 ids, copying a duplicate row is cheaper than\nthe sync `torch.unique` forces because its output size is data-dependent.\n\nSide effect:0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u2014 it\nstill sees one contiguous tensor.\n\nThe gather is a Triton kernel reading directly from pinned host memory with GPU-side indices. The\nnaive route (`uniq.to(\"cpu\")` then one copy per row) forces a device synchronization per layer.\nDuring decode we skip deduplication entirely: with 10 ids, copying a duplicate row is cheaper than\nthe sync `torch.unique` forces because its output size is data-dependent.\n\nSide effect: with0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u2014 it\nstill sees one contiguous tensor.\n\nThe gather is a Triton kernel reading directly from pinned host memory with GPU-side indices. The\nnaive route (`uniq.to(\"cpu\")` then one copy per row) forces a device synchronization per layer.\nDuring decode we skip deduplication entirely: with 10 ids, copying a duplicate row is cheaper than\nthe sync `torch.unique` forces because its output size is data-dependent.\n\nSide effect: with non0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u2014 it\nstill sees one contiguous tensor.\n\nThe gather is a Triton kernel reading directly from pinned host memory with GPU-side indices. The\nnaive route (`uniq.to(\"cpu\")` then one copy per row) forces a device synchronization per layer.\nDuring decode we skip deduplication entirely: with 10 ids, copying a duplicate row is cheaper than\nthe sync `torch.unique` forces because its output size is data-dependent.\n\nSide effect: with non-exp0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u2014 it\nstill sees one contiguous tensor.\n\nThe gather is a Triton kernel reading directly from pinned host memory with GPU-side indices. The\nnaive route (`uniq.to(\"cpu\")` then one copy per row) forces a device synchronization per layer.\nDuring decode we skip deduplication entirely: with 10 ids, copying a duplicate row is cheaper than\nthe sync `torch.unique` forces because its output size is data-dependent.\n\nSide effect: with non-expert0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u2014 it\nstill sees one contiguous tensor.\n\nThe gather is a Triton kernel reading directly from pinned host memory with GPU-side indices. The\nnaive route (`uniq.to(\"cpu\")` then one copy per row) forces a device synchronization per layer.\nDuring decode we skip deduplication entirely: with 10 ids, copying a duplicate row is cheaper than\nthe sync `torch.unique` forces because its output size is data-dependent.\n\nSide effect: with non-expert weights0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u2014 it\nstill sees one contiguous tensor.\n\nThe gather is a Triton kernel reading directly from pinned host memory with GPU-side indices. The\nnaive route (`uniq.to(\"cpu\")` then one copy per row) forces a device synchronization per layer.\nDuring decode we skip deduplication entirely: with 10 ids, copying a duplicate row is cheaper than\nthe sync `torch.unique` forces because its output size is data-dependent.\n\nSide effect: with non-expert weights no0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u2014 it\nstill sees one contiguous tensor.\n\nThe gather is a Triton kernel reading directly from pinned host memory with GPU-side indices. The\nnaive route (`uniq.to(\"cpu\")` then one copy per row) forces a device synchronization per layer.\nDuring decode we skip deduplication entirely: with 10 ids, copying a duplicate row is cheaper than\nthe sync `torch.unique` forces because its output size is data-dependent.\n\nSide effect: with non-expert weights no longer0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u2014 it\nstill sees one contiguous tensor.\n\nThe gather is a Triton kernel reading directly from pinned host memory with GPU-side indices. The\nnaive route (`uniq.to(\"cpu\")` then one copy per row) forces a device synchronization per layer.\nDuring decode we skip deduplication entirely: with 10 ids, copying a duplicate row is cheaper than\nthe sync `torch.unique` forces because its output size is data-dependent.\n\nSide effect: with non-expert weights no longer shut0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u2014 it\nstill sees one contiguous tensor.\n\nThe gather is a Triton kernel reading directly from pinned host memory with GPU-side indices. The\nnaive route (`uniq.to(\"cpu\")` then one copy per row) forces a device synchronization per layer.\nDuring decode we skip deduplication entirely: with 10 ids, copying a duplicate row is cheaper than\nthe sync `torch.unique` forces because its output size is data-dependent.\n\nSide effect: with non-expert weights no longer shutt0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u2014 it\nstill sees one contiguous tensor.\n\nThe gather is a Triton kernel reading directly from pinned host memory with GPU-side indices. The\nnaive route (`uniq.to(\"cpu\")` then one copy per row) forces a device synchronization per layer.\nDuring decode we skip deduplication entirely: with 10 ids, copying a duplicate row is cheaper than\nthe sync `torch.unique` forces because its output size is data-dependent.\n\nSide effect: with non-expert weights no longer shuttling0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u2014 it\nstill sees one contiguous tensor.\n\nThe gather is a Triton kernel reading directly from pinned host memory with GPU-side indices. The\nnaive route (`uniq.to(\"cpu\")` then one copy per row) forces a device synchronization per layer.\nDuring decode we skip deduplication entirely: with 10 ids, copying a duplicate row is cheaper than\nthe sync `torch.unique` forces because its output size is data-dependent.\n\nSide effect: with non-expert weights no longer shuttling back0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u2014 it\nstill sees one contiguous tensor.\n\nThe gather is a Triton kernel reading directly from pinned host memory with GPU-side indices. The\nnaive route (`uniq.to(\"cpu\")` then one copy per row) forces a device synchronization per layer.\nDuring decode we skip deduplication entirely: with 10 ids, copying a duplicate row is cheaper than\nthe sync `torch.unique` forces because its output size is data-dependent.\n\nSide effect: with non-expert weights no longer shuttling back and0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u2014 it\nstill sees one contiguous tensor.\n\nThe gather is a Triton kernel reading directly from pinned host memory with GPU-side indices. The\nnaive route (`uniq.to(\"cpu\")` then one copy per row) forces a device synchronization per layer.\nDuring decode we skip deduplication entirely: with 10 ids, copying a duplicate row is cheaper than\nthe sync `torch.unique` forces because its output size is data-dependent.\n\nSide effect: with non-expert weights no longer shuttling back and forth0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u2014 it\nstill sees one contiguous tensor.\n\nThe gather is a Triton kernel reading directly from pinned host memory with GPU-side indices. The\nnaive route (`uniq.to(\"cpu\")` then one copy per row) forces a device synchronization per layer.\nDuring decode we skip deduplication entirely: with 10 ids, copying a duplicate row is cheaper than\nthe sync `torch.unique` forces because its output size is data-dependent.\n\nSide effect: with non-expert weights no longer shuttling back and forth,0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u2014 it\nstill sees one contiguous tensor.\n\nThe gather is a Triton kernel reading directly from pinned host memory with GPU-side indices. The\nnaive route (`uniq.to(\"cpu\")` then one copy per row) forces a device synchronization per layer.\nDuring decode we skip deduplication entirely: with 10 ids, copying a duplicate row is cheaper than\nthe sync `torch.unique` forces because its output size is data-dependent.\n\nSide effect: with non-expert weights no longer shuttling back and forth, resident0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u2014 it\nstill sees one contiguous tensor.\n\nThe gather is a Triton kernel reading directly from pinned host memory with GPU-side indices. The\nnaive route (`uniq.to(\"cpu\")` then one copy per row) forces a device synchronization per layer.\nDuring decode we skip deduplication entirely: with 10 ids, copying a duplicate row is cheaper than\nthe sync `torch.unique` forces because its output size is data-dependent.\n\nSide effect: with non-expert weights no longer shuttling back and forth, resident usage0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u2014 it\nstill sees one contiguous tensor.\n\nThe gather is a Triton kernel reading directly from pinned host memory with GPU-side indices. The\nnaive route (`uniq.to(\"cpu\")` then one copy per row) forces a device synchronization per layer.\nDuring decode we skip deduplication entirely: with 10 ids, copying a duplicate row is cheaper than\nthe sync `torch.unique` forces because its output size is data-dependent.\n\nSide effect: with non-expert weights no longer shuttling back and forth, resident usage drops0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u2014 it\nstill sees one contiguous tensor.\n\nThe gather is a Triton kernel reading directly from pinned host memory with GPU-side indices. The\nnaive route (`uniq.to(\"cpu\")` then one copy per row) forces a device synchronization per layer.\nDuring decode we skip deduplication entirely: with 10 ids, copying a duplicate row is cheaper than\nthe sync `torch.unique` forces because its output size is data-dependent.\n\nSide effect: with non-expert weights no longer shuttling back and forth, resident usage drops from0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u2014 it\nstill sees one contiguous tensor.\n\nThe gather is a Triton kernel reading directly from pinned host memory with GPU-side indices. The\nnaive route (`uniq.to(\"cpu\")` then one copy per row) forces a device synchronization per layer.\nDuring decode we skip deduplication entirely: with 10 ids, copying a duplicate row is cheaper than\nthe sync `torch.unique` forces because its output size is data-dependent.\n\nSide effect: with non-expert weights no longer shuttling back and forth, resident usage drops from\n0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u2014 it\nstill sees one contiguous tensor.\n\nThe gather is a Triton kernel reading directly from pinned host memory with GPU-side indices. The\nnaive route (`uniq.to(\"cpu\")` then one copy per row) forces a device synchronization per layer.\nDuring decode we skip deduplication entirely: with 10 ids, copying a duplicate row is cheaper than\nthe sync `torch.unique` forces because its output size is data-dependent.\n\nSide effect: with non-expert weights no longer shuttling back and forth, resident usage drops from\n10 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u2014 it\nstill sees one contiguous tensor.\n\nThe gather is a Triton kernel reading directly from pinned host memory with GPU-side indices. The\nnaive route (`uniq.to(\"cpu\")` then one copy per row) forces a device synchronization per layer.\nDuring decode we skip deduplication entirely: with 10 ids, copying a duplicate row is cheaper than\nthe sync `torch.unique` forces because its output size is data-dependent.\n\nSide effect: with non-expert weights no longer shuttling back and forth, resident usage drops from\n190 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u2014 it\nstill sees one contiguous tensor.\n\nThe gather is a Triton kernel reading directly from pinned host memory with GPU-side indices. The\nnaive route (`uniq.to(\"cpu\")` then one copy per row) forces a device synchronization per layer.\nDuring decode we skip deduplication entirely: with 10 ids, copying a duplicate row is cheaper than\nthe sync `torch.unique` forces because its output size is data-dependent.\n\nSide effect: with non-expert weights no longer shuttling back and forth, resident usage drops from\n19.0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u2014 it\nstill sees one contiguous tensor.\n\nThe gather is a Triton kernel reading directly from pinned host memory with GPU-side indices. The\nnaive route (`uniq.to(\"cpu\")` then one copy per row) forces a device synchronization per layer.\nDuring decode we skip deduplication entirely: with 10 ids, copying a duplicate row is cheaper than\nthe sync `torch.unique` forces because its output size is data-dependent.\n\nSide effect: with non-expert weights no longer shuttling back and forth, resident usage drops from\n19.70 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u2014 it\nstill sees one contiguous tensor.\n\nThe gather is a Triton kernel reading directly from pinned host memory with GPU-side indices. The\nnaive route (`uniq.to(\"cpu\")` then one copy per row) forces a device synchronization per layer.\nDuring decode we skip deduplication entirely: with 10 ids, copying a duplicate row is cheaper than\nthe sync `torch.unique` forces because its output size is data-dependent.\n\nSide effect: with non-expert weights no longer shuttling back and forth, resident usage drops from\n19.760 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u2014 it\nstill sees one contiguous tensor.\n\nThe gather is a Triton kernel reading directly from pinned host memory with GPU-side indices. The\nnaive route (`uniq.to(\"cpu\")` then one copy per row) forces a device synchronization per layer.\nDuring decode we skip deduplication entirely: with 10 ids, copying a duplicate row is cheaper than\nthe sync `torch.unique` forces because its output size is data-dependent.\n\nSide effect: with non-expert weights no longer shuttling back and forth, resident usage drops from\n19.76 to0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u2014 it\nstill sees one contiguous tensor.\n\nThe gather is a Triton kernel reading directly from pinned host memory with GPU-side indices. The\nnaive route (`uniq.to(\"cpu\")` then one copy per row) forces a device synchronization per layer.\nDuring decode we skip deduplication entirely: with 10 ids, copying a duplicate row is cheaper than\nthe sync `torch.unique` forces because its output size is data-dependent.\n\nSide effect: with non-expert weights no longer shuttling back and forth, resident usage drops from\n19.76 to **0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u2014 it\nstill sees one contiguous tensor.\n\nThe gather is a Triton kernel reading directly from pinned host memory with GPU-side indices. The\nnaive route (`uniq.to(\"cpu\")` then one copy per row) forces a device synchronization per layer.\nDuring decode we skip deduplication entirely: with 10 ids, copying a duplicate row is cheaper than\nthe sync `torch.unique` forces because its output size is data-dependent.\n\nSide effect: with non-expert weights no longer shuttling back and forth, resident usage drops from\n19.76 to **10 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u2014 it\nstill sees one contiguous tensor.\n\nThe gather is a Triton kernel reading directly from pinned host memory with GPU-side indices. The\nnaive route (`uniq.to(\"cpu\")` then one copy per row) forces a device synchronization per layer.\nDuring decode we skip deduplication entirely: with 10 ids, copying a duplicate row is cheaper than\nthe sync `torch.unique` forces because its output size is data-dependent.\n\nSide effect: with non-expert weights no longer shuttling back and forth, resident usage drops from\n19.76 to **180 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u2014 it\nstill sees one contiguous tensor.\n\nThe gather is a Triton kernel reading directly from pinned host memory with GPU-side indices. The\nnaive route (`uniq.to(\"cpu\")` then one copy per row) forces a device synchronization per layer.\nDuring decode we skip deduplication entirely: with 10 ids, copying a duplicate row is cheaper than\nthe sync `torch.unique` forces because its output size is data-dependent.\n\nSide effect: with non-expert weights no longer shuttling back and forth, resident usage drops from\n19.76 to **18.0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u2014 it\nstill sees one contiguous tensor.\n\nThe gather is a Triton kernel reading directly from pinned host memory with GPU-side indices. The\nnaive route (`uniq.to(\"cpu\")` then one copy per row) forces a device synchronization per layer.\nDuring decode we skip deduplication entirely: with 10 ids, copying a duplicate row is cheaper than\nthe sync `torch.unique` forces because its output size is data-dependent.\n\nSide effect: with non-expert weights no longer shuttling back and forth, resident usage drops from\n19.76 to **18.60 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u2014 it\nstill sees one contiguous tensor.\n\nThe gather is a Triton kernel reading directly from pinned host memory with GPU-side indices. The\nnaive route (`uniq.to(\"cpu\")` then one copy per row) forces a device synchronization per layer.\nDuring decode we skip deduplication entirely: with 10 ids, copying a duplicate row is cheaper than\nthe sync `torch.unique` forces because its output size is data-dependent.\n\nSide effect: with non-expert weights no longer shuttling back and forth, resident usage drops from\n19.76 to **18.6 GB0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u2014 it\nstill sees one contiguous tensor.\n\nThe gather is a Triton kernel reading directly from pinned host memory with GPU-side indices. The\nnaive route (`uniq.to(\"cpu\")` then one copy per row) forces a device synchronization per layer.\nDuring decode we skip deduplication entirely: with 10 ids, copying a duplicate row is cheaper than\nthe sync `torch.unique` forces because its output size is data-dependent.\n\nSide effect: with non-expert weights no longer shuttling back and forth, resident usage drops from\n19.76 to **18.6 GB**.0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u2014 it\nstill sees one contiguous tensor.\n\nThe gather is a Triton kernel reading directly from pinned host memory with GPU-side indices. The\nnaive route (`uniq.to(\"cpu\")` then one copy per row) forces a device synchronization per layer.\nDuring decode we skip deduplication entirely: with 10 ids, copying a duplicate row is cheaper than\nthe sync `torch.unique` forces because its output size is data-dependent.\n\nSide effect: with non-expert weights no longer shuttling back and forth, resident usage drops from\n19.76 to **18.6 GB**. The0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u2014 it\nstill sees one contiguous tensor.\n\nThe gather is a Triton kernel reading directly from pinned host memory with GPU-side indices. The\nnaive route (`uniq.to(\"cpu\")` then one copy per row) forces a device synchronization per layer.\nDuring decode we skip deduplication entirely: with 10 ids, copying a duplicate row is cheaper than\nthe sync `torch.unique` forces because its output size is data-dependent.\n\nSide effect: with non-expert weights no longer shuttling back and forth, resident usage drops from\n19.76 to **18.6 GB**. The freed0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u2014 it\nstill sees one contiguous tensor.\n\nThe gather is a Triton kernel reading directly from pinned host memory with GPU-side indices. The\nnaive route (`uniq.to(\"cpu\")` then one copy per row) forces a device synchronization per layer.\nDuring decode we skip deduplication entirely: with 10 ids, copying a duplicate row is cheaper than\nthe sync `torch.unique` forces because its output size is data-dependent.\n\nSide effect: with non-expert weights no longer shuttling back and forth, resident usage drops from\n19.76 to **18.6 GB**. The freed 0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u2014 it\nstill sees one contiguous tensor.\n\nThe gather is a Triton kernel reading directly from pinned host memory with GPU-side indices. The\nnaive route (`uniq.to(\"cpu\")` then one copy per row) forces a device synchronization per layer.\nDuring decode we skip deduplication entirely: with 10 ids, copying a duplicate row is cheaper than\nthe sync `torch.unique` forces because its output size is data-dependent.\n\nSide effect: with non-expert weights no longer shuttling back and forth, resident usage drops from\n19.76 to **18.6 GB**. The freed 30 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u2014 it\nstill sees one contiguous tensor.\n\nThe gather is a Triton kernel reading directly from pinned host memory with GPU-side indices. The\nnaive route (`uniq.to(\"cpu\")` then one copy per row) forces a device synchronization per layer.\nDuring decode we skip deduplication entirely: with 10 ids, copying a duplicate row is cheaper than\nthe sync `torch.unique` forces because its output size is data-dependent.\n\nSide effect: with non-expert weights no longer shuttling back and forth, resident usage drops from\n19.76 to **18.6 GB**. The freed 3.0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u2014 it\nstill sees one contiguous tensor.\n\nThe gather is a Triton kernel reading directly from pinned host memory with GPU-side indices. The\nnaive route (`uniq.to(\"cpu\")` then one copy per row) forces a device synchronization per layer.\nDuring decode we skip deduplication entirely: with 10 ids, copying a duplicate row is cheaper than\nthe sync `torch.unique` forces because its output size is data-dependent.\n\nSide effect: with non-expert weights no longer shuttling back and forth, resident usage drops from\n19.76 to **18.6 GB**. The freed 3.70 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u2014 it\nstill sees one contiguous tensor.\n\nThe gather is a Triton kernel reading directly from pinned host memory with GPU-side indices. The\nnaive route (`uniq.to(\"cpu\")` then one copy per row) forces a device synchronization per layer.\nDuring decode we skip deduplication entirely: with 10 ids, copying a duplicate row is cheaper than\nthe sync `torch.unique` forces because its output size is data-dependent.\n\nSide effect: with non-expert weights no longer shuttling back and forth, resident usage drops from\n19.76 to **18.6 GB**. The freed 3.750 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u2014 it\nstill sees one contiguous tensor.\n\nThe gather is a Triton kernel reading directly from pinned host memory with GPU-side indices. The\nnaive route (`uniq.to(\"cpu\")` then one copy per row) forces a device synchronization per layer.\nDuring decode we skip deduplication entirely: with 10 ids, copying a duplicate row is cheaper than\nthe sync `torch.unique` forces because its output size is data-dependent.\n\nSide effect: with non-expert weights no longer shuttling back and forth, resident usage drops from\n19.76 to **18.6 GB**. The freed 3.75 GB0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u2014 it\nstill sees one contiguous tensor.\n\nThe gather is a Triton kernel reading directly from pinned host memory with GPU-side indices. The\nnaive route (`uniq.to(\"cpu\")` then one copy per row) forces a device synchronization per layer.\nDuring decode we skip deduplication entirely: with 10 ids, copying a duplicate row is cheaper than\nthe sync `torch.unique` forces because its output size is data-dependent.\n\nSide effect: with non-expert weights no longer shuttling back and forth, resident usage drops from\n19.76 to **18.6 GB**. The freed 3.75 GB goes0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u2014 it\nstill sees one contiguous tensor.\n\nThe gather is a Triton kernel reading directly from pinned host memory with GPU-side indices. The\nnaive route (`uniq.to(\"cpu\")` then one copy per row) forces a device synchronization per layer.\nDuring decode we skip deduplication entirely: with 10 ids, copying a duplicate row is cheaper than\nthe sync `torch.unique` forces because its output size is data-dependent.\n\nSide effect: with non-expert weights no longer shuttling back and forth, resident usage drops from\n19.76 to **18.6 GB**. The freed 3.75 GB goes to0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u2014 it\nstill sees one contiguous tensor.\n\nThe gather is a Triton kernel reading directly from pinned host memory with GPU-side indices. The\nnaive route (`uniq.to(\"cpu\")` then one copy per row) forces a device synchronization per layer.\nDuring decode we skip deduplication entirely: with 10 ids, copying a duplicate row is cheaper than\nthe sync `torch.unique` forces because its output size is data-dependent.\n\nSide effect: with non-expert weights no longer shuttling back and forth, resident usage drops from\n19.76 to **18.6 GB**. The freed 3.75 GB goes to the0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u2014 it\nstill sees one contiguous tensor.\n\nThe gather is a Triton kernel reading directly from pinned host memory with GPU-side indices. The\nnaive route (`uniq.to(\"cpu\")` then one copy per row) forces a device synchronization per layer.\nDuring decode we skip deduplication entirely: with 10 ids, copying a duplicate row is cheaper than\nthe sync `torch.unique` forces because its output size is data-dependent.\n\nSide effect: with non-expert weights no longer shuttling back and forth, resident usage drops from\n19.76 to **18.6 GB**. The freed 3.75 GB goes to the KV0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u2014 it\nstill sees one contiguous tensor.\n\nThe gather is a Triton kernel reading directly from pinned host memory with GPU-side indices. The\nnaive route (`uniq.to(\"cpu\")` then one copy per row) forces a device synchronization per layer.\nDuring decode we skip deduplication entirely: with 10 ids, copying a duplicate row is cheaper than\nthe sync `torch.unique` forces because its output size is data-dependent.\n\nSide effect: with non-expert weights no longer shuttling back and forth, resident usage drops from\n19.76 to **18.6 GB**. The freed 3.75 GB goes to the KV cache0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u2014 it\nstill sees one contiguous tensor.\n\nThe gather is a Triton kernel reading directly from pinned host memory with GPU-side indices. The\nnaive route (`uniq.to(\"cpu\")` then one copy per row) forces a device synchronization per layer.\nDuring decode we skip deduplication entirely: with 10 ids, copying a duplicate row is cheaper than\nthe sync `torch.unique` forces because its output size is data-dependent.\n\nSide effect: with non-expert weights no longer shuttling back and forth, resident usage drops from\n19.76 to **18.6 GB**. The freed 3.75 GB goes to the KV cache,0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u2014 it\nstill sees one contiguous tensor.\n\nThe gather is a Triton kernel reading directly from pinned host memory with GPU-side indices. The\nnaive route (`uniq.to(\"cpu\")` then one copy per row) forces a device synchronization per layer.\nDuring decode we skip deduplication entirely: with 10 ids, copying a duplicate row is cheaper than\nthe sync `torch.unique` forces because its output size is data-dependent.\n\nSide effect: with non-expert weights no longer shuttling back and forth, resident usage drops from\n19.76 to **18.6 GB**. The freed 3.75 GB goes to the KV cache, which0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u2014 it\nstill sees one contiguous tensor.\n\nThe gather is a Triton kernel reading directly from pinned host memory with GPU-side indices. The\nnaive route (`uniq.to(\"cpu\")` then one copy per row) forces a device synchronization per layer.\nDuring decode we skip deduplication entirely: with 10 ids, copying a duplicate row is cheaper than\nthe sync `torch.unique` forces because its output size is data-dependent.\n\nSide effect: with non-expert weights no longer shuttling back and forth, resident usage drops from\n19.76 to **18.6 GB**. The freed 3.75 GB goes to the KV cache, which grows0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u2014 it\nstill sees one contiguous tensor.\n\nThe gather is a Triton kernel reading directly from pinned host memory with GPU-side indices. The\nnaive route (`uniq.to(\"cpu\")` then one copy per row) forces a device synchronization per layer.\nDuring decode we skip deduplication entirely: with 10 ids, copying a duplicate row is cheaper than\nthe sync `torch.unique` forces because its output size is data-dependent.\n\nSide effect: with non-expert weights no longer shuttling back and forth, resident usage drops from\n19.76 to **18.6 GB**. The freed 3.75 GB goes to the KV cache, which grows from0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u2014 it\nstill sees one contiguous tensor.\n\nThe gather is a Triton kernel reading directly from pinned host memory with GPU-side indices. The\nnaive route (`uniq.to(\"cpu\")` then one copy per row) forces a device synchronization per layer.\nDuring decode we skip deduplication entirely: with 10 ids, copying a duplicate row is cheaper than\nthe sync `torch.unique` forces because its output size is data-dependent.\n\nSide effect: with non-expert weights no longer shuttling back and forth, resident usage drops from\n19.76 to **18.6 GB**. The freed 3.75 GB goes to the KV cache, which grows from 0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u2014 it\nstill sees one contiguous tensor.\n\nThe gather is a Triton kernel reading directly from pinned host memory with GPU-side indices. The\nnaive route (`uniq.to(\"cpu\")` then one copy per row) forces a device synchronization per layer.\nDuring decode we skip deduplication entirely: with 10 ids, copying a duplicate row is cheaper than\nthe sync `torch.unique` forces because its output size is data-dependent.\n\nSide effect: with non-expert weights no longer shuttling back and forth, resident usage drops from\n19.76 to **18.6 GB**. The freed 3.75 GB goes to the KV cache, which grows from 10 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u2014 it\nstill sees one contiguous tensor.\n\nThe gather is a Triton kernel reading directly from pinned host memory with GPU-side indices. The\nnaive route (`uniq.to(\"cpu\")` then one copy per row) forces a device synchronization per layer.\nDuring decode we skip deduplication entirely: with 10 ids, copying a duplicate row is cheaper than\nthe sync `torch.unique` forces because its output size is data-dependent.\n\nSide effect: with non-expert weights no longer shuttling back and forth, resident usage drops from\n19.76 to **18.6 GB**. The freed 3.75 GB goes to the KV cache, which grows from 130 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u2014 it\nstill sees one contiguous tensor.\n\nThe gather is a Triton kernel reading directly from pinned host memory with GPU-side indices. The\nnaive route (`uniq.to(\"cpu\")` then one copy per row) forces a device synchronization per layer.\nDuring decode we skip deduplication entirely: with 10 ids, copying a duplicate row is cheaper than\nthe sync `torch.unique` forces because its output size is data-dependent.\n\nSide effect: with non-expert weights no longer shuttling back and forth, resident usage drops from\n19.76 to **18.6 GB**. The freed 3.75 GB goes to the KV cache, which grows from 13,0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u2014 it\nstill sees one contiguous tensor.\n\nThe gather is a Triton kernel reading directly from pinned host memory with GPU-side indices. The\nnaive route (`uniq.to(\"cpu\")` then one copy per row) forces a device synchronization per layer.\nDuring decode we skip deduplication entirely: with 10 ids, copying a duplicate row is cheaper than\nthe sync `torch.unique` forces because its output size is data-dependent.\n\nSide effect: with non-expert weights no longer shuttling back and forth, resident usage drops from\n19.76 to **18.6 GB**. The freed 3.75 GB goes to the KV cache, which grows from 13,80 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u2014 it\nstill sees one contiguous tensor.\n\nThe gather is a Triton kernel reading directly from pinned host memory with GPU-side indices. The\nnaive route (`uniq.to(\"cpu\")` then one copy per row) forces a device synchronization per layer.\nDuring decode we skip deduplication entirely: with 10 ids, copying a duplicate row is cheaper than\nthe sync `torch.unique` forces because its output size is data-dependent.\n\nSide effect: with non-expert weights no longer shuttling back and forth, resident usage drops from\n19.76 to **18.6 GB**. The freed 3.75 GB goes to the KV cache, which grows from 13,820 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u2014 it\nstill sees one contiguous tensor.\n\nThe gather is a Triton kernel reading directly from pinned host memory with GPU-side indices. The\nnaive route (`uniq.to(\"cpu\")` then one copy per row) forces a device synchronization per layer.\nDuring decode we skip deduplication entirely: with 10 ids, copying a duplicate row is cheaper than\nthe sync `torch.unique` forces because its output size is data-dependent.\n\nSide effect: with non-expert weights no longer shuttling back and forth, resident usage drops from\n19.76 to **18.6 GB**. The freed 3.75 GB goes to the KV cache, which grows from 13,8240 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u2014 it\nstill sees one contiguous tensor.\n\nThe gather is a Triton kernel reading directly from pinned host memory with GPU-side indices. The\nnaive route (`uniq.to(\"cpu\")` then one copy per row) forces a device synchronization per layer.\nDuring decode we skip deduplication entirely: with 10 ids, copying a duplicate row is cheaper than\nthe sync `torch.unique` forces because its output size is data-dependent.\n\nSide effect: with non-expert weights no longer shuttling back and forth, resident usage drops from\n19.76 to **18.6 GB**. The freed 3.75 GB goes to the KV cache, which grows from 13,824 to0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u2014 it\nstill sees one contiguous tensor.\n\nThe gather is a Triton kernel reading directly from pinned host memory with GPU-side indices. The\nnaive route (`uniq.to(\"cpu\")` then one copy per row) forces a device synchronization per layer.\nDuring decode we skip deduplication entirely: with 10 ids, copying a duplicate row is cheaper than\nthe sync `torch.unique` forces because its output size is data-dependent.\n\nSide effect: with non-expert weights no longer shuttling back and forth, resident usage drops from\n19.76 to **18.6 GB**. The freed 3.75 GB goes to the KV cache, which grows from 13,824 to the0 tokens in 17.8 s            -> ~26 GB per token\n```\n\n`--cpu-offload-gb` copies a module's entire `state_dict` to the device on every forward pass.\nCorrect for a dense model. For a 512-expert MoE with top-10 it transfers 512 experts to use 10:\n\n| Per forward pass | bytes | |\n|---|---:|---|\n| one expert | 1.31 MB | |\n| 10 experts \u00d7 48 layers | 0.63 GB | what is needed |\n| measured transfer | 26.00 GB | 41.5\u00d7 too much |\n\n### The streamer\n\nTwo changes, both small, both **memory-neutral** \u2014 and that is the binding constraint. After\nloading, both VRAM and host RAM are full; any additional copy trips the OOM killer. Three\nintermediate designs died exactly there before this one worked.\n\n1. **Exclude experts from the bulk transfer.** In `offloader.py`, expert tensors are skipped when\n   building `device_state`. They stay in the pinned host memory the offloader already put them in.\n   Nothing is moved or copied.\n2. **Offload only experts.** Previously whole decoder layers went to the host, including attention,\n   GDN and norms \u2014 which are needed on *every* token. Those now stay resident on the GPU.\n\nOn each forward pass `ExpertStreamer` gathers exactly the selected experts into a single reused\nstaging buffer (668 MB) and renumbers the top-k ids onto it. The Triton kernel is unchanged \u2014 it\nstill sees one contiguous tensor.\n\nThe gather is a Triton kernel reading directly from pinned host memory with GPU-side indices. The\nnaive route (`uniq.to(\"cpu\")` then one copy per row) forces a device synchronization per layer.\nDuring decode we skip deduplication entirely: with 10 ids, copying a duplicate row is cheaper than\nthe sync `torch.unique` forces because its output size is data-dependent.\n\nSide effect: with non-expert weights no longer shuttling back and forth, resident usage drops from\n19.76 to **18.6 GB**. The freed 3.75 GB goes to the KV cache, which grows from 13,824 to the full",
 "ttft_seconds": 82.9421941920009,
 "status": "ok",
 "tg_tokens_per_s": 29.78200869495653,
 "decode_seconds_per_token": 0.03357731878472476,
 "individual_token_timing": true,
 "event_intervals_ms": {
  "n": 511,
  "median": 33.590285005629994,
  "p95": 40.475250003510155,
  "mean": 33.57731878472476
 },
 "class": "prose",
 "requested_input_tokens": 131072
}
