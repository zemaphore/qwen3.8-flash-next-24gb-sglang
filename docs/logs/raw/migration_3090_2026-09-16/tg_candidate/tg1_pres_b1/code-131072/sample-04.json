{
 "started_utc": "2026-09-16T23:47:14.385001+00:00",
 "wall_seconds": 95.34438927099109,
 "prompt_tokens": 131072,
 "completion_tokens": 512,
 "stream_events": 512,
 "ended_utc": "2026-09-16T23:48:49.729481+00:00",
 "warmup": false,
 "sample_index": 4,
 "requested_decode_tokens": 512,
 "gpu": {
  "n": 343,
  "temperature_c": {
   "min": 73.0,
   "max": 79.0,
   "mean": 76.55685131195335
  },
  "power_w": {
   "min": 152.82,
   "max": 283.1,
   "mean": 238.50650145772593
  },
  "util_pct": {
   "min": 0.0,
   "max": 100.0,
   "mean": 71.17784256559767
  },
  "clock_sm_mhz": {
   "min": 1215.0,
   "max": 1905.0,
   "mean": 1696.9679300291546
  },
  "clock_mem_mhz": {
   "min": 9501.0,
   "max": 9501.0,
   "mean": 9501.0
  },
  "memory_free_mib": {
   "min": 736.0
  }
 },
 "output_text_sha256": "74daaa724c5b5645d08513c5a20619e9fe76ee5855ab7ec33eb930feb6e2f432",
 "output_text": "\"\"   \"    LOG\"    LOGPRO\"    LOGPROB\"    LOGPROB_MAX\"    LOGPROB_MAX={\"    LOGPROB_MAX={overall\"    LOGPROB_MAX={overall['\"    LOGPROB_MAX={overall['max\"    LOGPROB_MAX={overall['max']\"    LOGPROB_MAX={overall['max']:.\"    LOGPROB_MAX={overall['max']:.6\"    LOGPROB_MAX={overall['max']:.6f\"    LOGPROB_MAX={overall['max']:.6f}\"    LOGPROB_MAX={overall['max']:.6f} LOG\"    LOGPROB_MAX={overall['max']:.6f} LOGPRO\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_ME\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main()\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() ->\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n   \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv)\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) <\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n       \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n       \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n   \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd =\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n   \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd ==\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n       \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name =\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n       \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path =\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D /\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n       \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(col\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(),\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2)\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) +\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n       \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\" \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n   \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd ==\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n       \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name =\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n       \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current =\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n       \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference =\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D /\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n       \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report =\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference,\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n       \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name}\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\",\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n   \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd ==\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n       \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name,\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name =\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2],\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n       \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a =\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D /\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n       \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b =\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D /\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n       \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report =\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a,\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n       \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name}\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\",\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n   \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n       \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\" \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n       \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ ==\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n   \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl``\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n``\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n``elastic\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n``elastic ctl\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n``elastic ctl done\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n``elastic ctl done``\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n``elastic ctl done`` lines\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n``elastic ctl done`` lines),\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n``elastic ctl done`` lines), so\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n``elastic ctl done`` lines), so this\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n``elastic ctl done`` lines), so this tool\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n``elastic ctl done`` lines), so this tool needs\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n``elastic ctl done`` lines), so this tool needs no\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n``elastic ctl done`` lines), so this tool needs no extra\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n``elastic ctl done`` lines), so this tool needs no extra instrumentation\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n``elastic ctl done`` lines), so this tool needs no extra instrumentation.\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n``elastic ctl done`` lines), so this tool needs no extra instrumentation. \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n``elastic ctl done`` lines), so this tool needs no extra instrumentation.  It\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n``elastic ctl done`` lines), so this tool needs no extra instrumentation.  It\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n``elastic ctl done`` lines), so this tool needs no extra instrumentation.  It\npars\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n``elastic ctl done`` lines), so this tool needs no extra instrumentation.  It\nparses\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n``elastic ctl done`` lines), so this tool needs no extra instrumentation.  It\nparses the\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n``elastic ctl done`` lines), so this tool needs no extra instrumentation.  It\nparses the log\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n``elastic ctl done`` lines), so this tool needs no extra instrumentation.  It\nparses the log,\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n``elastic ctl done`` lines), so this tool needs no extra instrumentation.  It\nparses the log, records\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n``elastic ctl done`` lines), so this tool needs no extra instrumentation.  It\nparses the log, records the\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n``elastic ctl done`` lines), so this tool needs no extra instrumentation.  It\nparses the log, records the requested\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n``elastic ctl done`` lines), so this tool needs no extra instrumentation.  It\nparses the log, records the requested and\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n``elastic ctl done`` lines), so this tool needs no extra instrumentation.  It\nparses the log, records the requested and resulting\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n``elastic ctl done`` lines), so this tool needs no extra instrumentation.  It\nparses the log, records the requested and resulting S\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n``elastic ctl done`` lines), so this tool needs no extra instrumentation.  It\nparses the log, records the requested and resulting S values\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n``elastic ctl done`` lines), so this tool needs no extra instrumentation.  It\nparses the log, records the requested and resulting S values,\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n``elastic ctl done`` lines), so this tool needs no extra instrumentation.  It\nparses the log, records the requested and resulting S values, the\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n``elastic ctl done`` lines), so this tool needs no extra instrumentation.  It\nparses the log, records the requested and resulting S values, the arena\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n``elastic ctl done`` lines), so this tool needs no extra instrumentation.  It\nparses the log, records the requested and resulting S values, the arena size\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n``elastic ctl done`` lines), so this tool needs no extra instrumentation.  It\nparses the log, records the requested and resulting S values, the arena size,\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n``elastic ctl done`` lines), so this tool needs no extra instrumentation.  It\nparses the log, records the requested and resulting S values, the arena size,\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n``elastic ctl done`` lines), so this tool needs no extra instrumentation.  It\nparses the log, records the requested and resulting S values, the arena size,\nfree\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n``elastic ctl done`` lines), so this tool needs no extra instrumentation.  It\nparses the log, records the requested and resulting S values, the arena size,\nfree VR\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n``elastic ctl done`` lines), so this tool needs no extra instrumentation.  It\nparses the log, records the requested and resulting S values, the arena size,\nfree VRAM\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n``elastic ctl done`` lines), so this tool needs no extra instrumentation.  It\nparses the log, records the requested and resulting S values, the arena size,\nfree VRAM,\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n``elastic ctl done`` lines), so this tool needs no extra instrumentation.  It\nparses the log, records the requested and resulting S values, the arena size,\nfree VRAM, and\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n``elastic ctl done`` lines), so this tool needs no extra instrumentation.  It\nparses the log, records the requested and resulting S values, the arena size,\nfree VRAM, and the\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n``elastic ctl done`` lines), so this tool needs no extra instrumentation.  It\nparses the log, records the requested and resulting S values, the arena size,\nfree VRAM, and the routing\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n``elastic ctl done`` lines), so this tool needs no extra instrumentation.  It\nparses the log, records the requested and resulting S values, the arena size,\nfree VRAM, and the routing mass\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n``elastic ctl done`` lines), so this tool needs no extra instrumentation.  It\nparses the log, records the requested and resulting S values, the arena size,\nfree VRAM, and the routing mass covered\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n``elastic ctl done`` lines), so this tool needs no extra instrumentation.  It\nparses the log, records the requested and resulting S values, the arena size,\nfree VRAM, and the routing mass covered,\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n``elastic ctl done`` lines), so this tool needs no extra instrumentation.  It\nparses the log, records the requested and resulting S values, the arena size,\nfree VRAM, and the routing mass covered, and\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n``elastic ctl done`` lines), so this tool needs no extra instrumentation.  It\nparses the log, records the requested and resulting S values, the arena size,\nfree VRAM, and the routing mass covered, and pairs\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n``elastic ctl done`` lines), so this tool needs no extra instrumentation.  It\nparses the log, records the requested and resulting S values, the arena size,\nfree VRAM, and the routing mass covered, and pairs them\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n``elastic ctl done`` lines), so this tool needs no extra instrumentation.  It\nparses the log, records the requested and resulting S values, the arena size,\nfree VRAM, and the routing mass covered, and pairs them with\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n``elastic ctl done`` lines), so this tool needs no extra instrumentation.  It\nparses the log, records the requested and resulting S values, the arena size,\nfree VRAM, and the routing mass covered, and pairs them with the\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n``elastic ctl done`` lines), so this tool needs no extra instrumentation.  It\nparses the log, records the requested and resulting S values, the arena size,\nfree VRAM, and the routing mass covered, and pairs them with the benchmark\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n``elastic ctl done`` lines), so this tool needs no extra instrumentation.  It\nparses the log, records the requested and resulting S values, the arena size,\nfree VRAM, and the routing mass covered, and pairs them with the benchmark rows\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n``elastic ctl done`` lines), so this tool needs no extra instrumentation.  It\nparses the log, records the requested and resulting S values, the arena size,\nfree VRAM, and the routing mass covered, and pairs them with the benchmark rows\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n``elastic ctl done`` lines), so this tool needs no extra instrumentation.  It\nparses the log, records the requested and resulting S values, the arena size,\nfree VRAM, and the routing mass covered, and pairs them with the benchmark rows\nfrom\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n``elastic ctl done`` lines), so this tool needs no extra instrumentation.  It\nparses the log, records the requested and resulting S values, the arena size,\nfree VRAM, and the routing mass covered, and pairs them with the benchmark rows\nfrom ``\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n``elastic ctl done`` lines), so this tool needs no extra instrumentation.  It\nparses the log, records the requested and resulting S values, the arena size,\nfree VRAM, and the routing mass covered, and pairs them with the benchmark rows\nfrom ``bench\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n``elastic ctl done`` lines), so this tool needs no extra instrumentation.  It\nparses the log, records the requested and resulting S values, the arena size,\nfree VRAM, and the routing mass covered, and pairs them with the benchmark rows\nfrom ``bench_ag\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n``elastic ctl done`` lines), so this tool needs no extra instrumentation.  It\nparses the log, records the requested and resulting S values, the arena size,\nfree VRAM, and the routing mass covered, and pairs them with the benchmark rows\nfrom ``bench_agentic\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n``elastic ctl done`` lines), so this tool needs no extra instrumentation.  It\nparses the log, records the requested and resulting S values, the arena size,\nfree VRAM, and the routing mass covered, and pairs them with the benchmark rows\nfrom ``bench_agentic.py\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n``elastic ctl done`` lines), so this tool needs no extra instrumentation.  It\nparses the log, records the requested and resulting S values, the arena size,\nfree VRAM, and the routing mass covered, and pairs them with the benchmark rows\nfrom ``bench_agentic.py``\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n``elastic ctl done`` lines), so this tool needs no extra instrumentation.  It\nparses the log, records the requested and resulting S values, the arena size,\nfree VRAM, and the routing mass covered, and pairs them with the benchmark rows\nfrom ``bench_agentic.py``.\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n``elastic ctl done`` lines), so this tool needs no extra instrumentation.  It\nparses the log, records the requested and resulting S values, the arena size,\nfree VRAM, and the routing mass covered, and pairs them with the benchmark rows\nfrom ``bench_agentic.py``.\n\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n``elastic ctl done`` lines), so this tool needs no extra instrumentation.  It\nparses the log, records the requested and resulting S values, the arena size,\nfree VRAM, and the routing mass covered, and pairs them with the benchmark rows\nfrom ``bench_agentic.py``.\n\n \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n``elastic ctl done`` lines), so this tool needs no extra instrumentation.  It\nparses the log, records the requested and resulting S values, the arena size,\nfree VRAM, and the routing mass covered, and pairs them with the benchmark rows\nfrom ``bench_agentic.py``.\n\n  python\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n``elastic ctl done`` lines), so this tool needs no extra instrumentation.  It\nparses the log, records the requested and resulting S values, the arena size,\nfree VRAM, and the routing mass covered, and pairs them with the benchmark rows\nfrom ``bench_agentic.py``.\n\n  python3\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n``elastic ctl done`` lines), so this tool needs no extra instrumentation.  It\nparses the log, records the requested and resulting S values, the arena size,\nfree VRAM, and the routing mass covered, and pairs them with the benchmark rows\nfrom ``bench_agentic.py``.\n\n  python3 tools\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n``elastic ctl done`` lines), so this tool needs no extra instrumentation.  It\nparses the log, records the requested and resulting S values, the arena size,\nfree VRAM, and the routing mass covered, and pairs them with the benchmark rows\nfrom ``bench_agentic.py``.\n\n  python3 tools/\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n``elastic ctl done`` lines), so this tool needs no extra instrumentation.  It\nparses the log, records the requested and resulting S values, the arena size,\nfree VRAM, and the routing mass covered, and pairs them with the benchmark rows\nfrom ``bench_agentic.py``.\n\n  python3 tools/elastic\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n``elastic ctl done`` lines), so this tool needs no extra instrumentation.  It\nparses the log, records the requested and resulting S values, the arena size,\nfree VRAM, and the routing mass covered, and pairs them with the benchmark rows\nfrom ``bench_agentic.py``.\n\n  python3 tools/elastic_state\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n``elastic ctl done`` lines), so this tool needs no extra instrumentation.  It\nparses the log, records the requested and resulting S values, the arena size,\nfree VRAM, and the routing mass covered, and pairs them with the benchmark rows\nfrom ``bench_agentic.py``.\n\n  python3 tools/elastic_state.py\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n``elastic ctl done`` lines), so this tool needs no extra instrumentation.  It\nparses the log, records the requested and resulting S values, the arena size,\nfree VRAM, and the routing mass covered, and pairs them with the benchmark rows\nfrom ``bench_agentic.py``.\n\n  python3 tools/elastic_state.py docs\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n``elastic ctl done`` lines), so this tool needs no extra instrumentation.  It\nparses the log, records the requested and resulting S values, the arena size,\nfree VRAM, and the routing mass covered, and pairs them with the benchmark rows\nfrom ``bench_agentic.py``.\n\n  python3 tools/elastic_state.py docs/logs\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n``elastic ctl done`` lines), so this tool needs no extra instrumentation.  It\nparses the log, records the requested and resulting S values, the arena size,\nfree VRAM, and the routing mass covered, and pairs them with the benchmark rows\nfrom ``bench_agentic.py``.\n\n  python3 tools/elastic_state.py docs/logs/raw\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n``elastic ctl done`` lines), so this tool needs no extra instrumentation.  It\nparses the log, records the requested and resulting S values, the arena size,\nfree VRAM, and the routing mass covered, and pairs them with the benchmark rows\nfrom ``bench_agentic.py``.\n\n  python3 tools/elastic_state.py docs/logs/raw/\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n``elastic ctl done`` lines), so this tool needs no extra instrumentation.  It\nparses the log, records the requested and resulting S values, the arena size,\nfree VRAM, and the routing mass covered, and pairs them with the benchmark rows\nfrom ``bench_agentic.py``.\n\n  python3 tools/elastic_state.py docs/logs/raw/...\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n``elastic ctl done`` lines), so this tool needs no extra instrumentation.  It\nparses the log, records the requested and resulting S values, the arena size,\nfree VRAM, and the routing mass covered, and pairs them with the benchmark rows\nfrom ``bench_agentic.py``.\n\n  python3 tools/elastic_state.py docs/logs/raw/.../\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n``elastic ctl done`` lines), so this tool needs no extra instrumentation.  It\nparses the log, records the requested and resulting S values, the arena size,\nfree VRAM, and the routing mass covered, and pairs them with the benchmark rows\nfrom ``bench_agentic.py``.\n\n  python3 tools/elastic_state.py docs/logs/raw/.../server\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n``elastic ctl done`` lines), so this tool needs no extra instrumentation.  It\nparses the log, records the requested and resulting S values, the arena size,\nfree VRAM, and the routing mass covered, and pairs them with the benchmark rows\nfrom ``bench_agentic.py``.\n\n  python3 tools/elastic_state.py docs/logs/raw/.../server.log\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n``elastic ctl done`` lines), so this tool needs no extra instrumentation.  It\nparses the log, records the requested and resulting S values, the arena size,\nfree VRAM, and the routing mass covered, and pairs them with the benchmark rows\nfrom ``bench_agentic.py``.\n\n  python3 tools/elastic_state.py docs/logs/raw/.../server.log --\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n``elastic ctl done`` lines), so this tool needs no extra instrumentation.  It\nparses the log, records the requested and resulting S values, the arena size,\nfree VRAM, and the routing mass covered, and pairs them with the benchmark rows\nfrom ``bench_agentic.py``.\n\n  python3 tools/elastic_state.py docs/logs/raw/.../server.log --bench\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n``elastic ctl done`` lines), so this tool needs no extra instrumentation.  It\nparses the log, records the requested and resulting S values, the arena size,\nfree VRAM, and the routing mass covered, and pairs them with the benchmark rows\nfrom ``bench_agentic.py``.\n\n  python3 tools/elastic_state.py docs/logs/raw/.../server.log --bench docs\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n``elastic ctl done`` lines), so this tool needs no extra instrumentation.  It\nparses the log, records the requested and resulting S values, the arena size,\nfree VRAM, and the routing mass covered, and pairs them with the benchmark rows\nfrom ``bench_agentic.py``.\n\n  python3 tools/elastic_state.py docs/logs/raw/.../server.log --bench docs/logs\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n``elastic ctl done`` lines), so this tool needs no extra instrumentation.  It\nparses the log, records the requested and resulting S values, the arena size,\nfree VRAM, and the routing mass covered, and pairs them with the benchmark rows\nfrom ``bench_agentic.py``.\n\n  python3 tools/elastic_state.py docs/logs/raw/.../server.log --bench docs/logs/raw\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n``elastic ctl done`` lines), so this tool needs no extra instrumentation.  It\nparses the log, records the requested and resulting S values, the arena size,\nfree VRAM, and the routing mass covered, and pairs them with the benchmark rows\nfrom ``bench_agentic.py``.\n\n  python3 tools/elastic_state.py docs/logs/raw/.../server.log --bench docs/logs/raw/\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n``elastic ctl done`` lines), so this tool needs no extra instrumentation.  It\nparses the log, records the requested and resulting S values, the arena size,\nfree VRAM, and the routing mass covered, and pairs them with the benchmark rows\nfrom ``bench_agentic.py``.\n\n  python3 tools/elastic_state.py docs/logs/raw/.../server.log --bench docs/logs/raw/...\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n``elastic ctl done`` lines), so this tool needs no extra instrumentation.  It\nparses the log, records the requested and resulting S values, the arena size,\nfree VRAM, and the routing mass covered, and pairs them with the benchmark rows\nfrom ``bench_agentic.py``.\n\n  python3 tools/elastic_state.py docs/logs/raw/.../server.log --bench docs/logs/raw/.../\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n``elastic ctl done`` lines), so this tool needs no extra instrumentation.  It\nparses the log, records the requested and resulting S values, the arena size,\nfree VRAM, and the routing mass covered, and pairs them with the benchmark rows\nfrom ``bench_agentic.py``.\n\n  python3 tools/elastic_state.py docs/logs/raw/.../server.log --bench docs/logs/raw/.../bench\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n``elastic ctl done`` lines), so this tool needs no extra instrumentation.  It\nparses the log, records the requested and resulting S values, the arena size,\nfree VRAM, and the routing mass covered, and pairs them with the benchmark rows\nfrom ``bench_agentic.py``.\n\n  python3 tools/elastic_state.py docs/logs/raw/.../server.log --bench docs/logs/raw/.../bench-\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n``elastic ctl done`` lines), so this tool needs no extra instrumentation.  It\nparses the log, records the requested and resulting S values, the arena size,\nfree VRAM, and the routing mass covered, and pairs them with the benchmark rows\nfrom ``bench_agentic.py``.\n\n  python3 tools/elastic_state.py docs/logs/raw/.../server.log --bench docs/logs/raw/.../bench-*.\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n``elastic ctl done`` lines), so this tool needs no extra instrumentation.  It\nparses the log, records the requested and resulting S values, the arena size,\nfree VRAM, and the routing mass covered, and pairs them with the benchmark rows\nfrom ``bench_agentic.py``.\n\n  python3 tools/elastic_state.py docs/logs/raw/.../server.log --bench docs/logs/raw/.../bench-*.txt\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n``elastic ctl done`` lines), so this tool needs no extra instrumentation.  It\nparses the log, records the requested and resulting S values, the arena size,\nfree VRAM, and the routing mass covered, and pairs them with the benchmark rows\nfrom ``bench_agentic.py``.\n\n  python3 tools/elastic_state.py docs/logs/raw/.../server.log --bench docs/logs/raw/.../bench-*.txt\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n``elastic ctl done`` lines), so this tool needs no extra instrumentation.  It\nparses the log, records the requested and resulting S values, the arena size,\nfree VRAM, and the routing mass covered, and pairs them with the benchmark rows\nfrom ``bench_agentic.py``.\n\n  python3 tools/elastic_state.py docs/logs/raw/.../server.log --bench docs/logs/raw/.../bench-*.txt\n\"\"\"\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n``elastic ctl done`` lines), so this tool needs no extra instrumentation.  It\nparses the log, records the requested and resulting S values, the arena size,\nfree VRAM, and the routing mass covered, and pairs them with the benchmark rows\nfrom ``bench_agentic.py``.\n\n  python3 tools/elastic_state.py docs/logs/raw/.../server.log --bench docs/logs/raw/.../bench-*.txt\n\"\"\"\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n``elastic ctl done`` lines), so this tool needs no extra instrumentation.  It\nparses the log, records the requested and resulting S values, the arena size,\nfree VRAM, and the routing mass covered, and pairs them with the benchmark rows\nfrom ``bench_agentic.py``.\n\n  python3 tools/elastic_state.py docs/logs/raw/.../server.log --bench docs/logs/raw/.../bench-*.txt\n\"\"\"\nfrom\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n``elastic ctl done`` lines), so this tool needs no extra instrumentation.  It\nparses the log, records the requested and resulting S values, the arena size,\nfree VRAM, and the routing mass covered, and pairs them with the benchmark rows\nfrom ``bench_agentic.py``.\n\n  python3 tools/elastic_state.py docs/logs/raw/.../server.log --bench docs/logs/raw/.../bench-*.txt\n\"\"\"\nfrom __\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n``elastic ctl done`` lines), so this tool needs no extra instrumentation.  It\nparses the log, records the requested and resulting S values, the arena size,\nfree VRAM, and the routing mass covered, and pairs them with the benchmark rows\nfrom ``bench_agentic.py``.\n\n  python3 tools/elastic_state.py docs/logs/raw/.../server.log --bench docs/logs/raw/.../bench-*.txt\n\"\"\"\nfrom __future\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n``elastic ctl done`` lines), so this tool needs no extra instrumentation.  It\nparses the log, records the requested and resulting S values, the arena size,\nfree VRAM, and the routing mass covered, and pairs them with the benchmark rows\nfrom ``bench_agentic.py``.\n\n  python3 tools/elastic_state.py docs/logs/raw/.../server.log --bench docs/logs/raw/.../bench-*.txt\n\"\"\"\nfrom __future__\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n``elastic ctl done`` lines), so this tool needs no extra instrumentation.  It\nparses the log, records the requested and resulting S values, the arena size,\nfree VRAM, and the routing mass covered, and pairs them with the benchmark rows\nfrom ``bench_agentic.py``.\n\n  python3 tools/elastic_state.py docs/logs/raw/.../server.log --bench docs/logs/raw/.../bench-*.txt\n\"\"\"\nfrom __future__ import\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n``elastic ctl done`` lines), so this tool needs no extra instrumentation.  It\nparses the log, records the requested and resulting S values, the arena size,\nfree VRAM, and the routing mass covered, and pairs them with the benchmark rows\nfrom ``bench_agentic.py``.\n\n  python3 tools/elastic_state.py docs/logs/raw/.../server.log --bench docs/logs/raw/.../bench-*.txt\n\"\"\"\nfrom __future__ import annotations\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n``elastic ctl done`` lines), so this tool needs no extra instrumentation.  It\nparses the log, records the requested and resulting S values, the arena size,\nfree VRAM, and the routing mass covered, and pairs them with the benchmark rows\nfrom ``bench_agentic.py``.\n\n  python3 tools/elastic_state.py docs/logs/raw/.../server.log --bench docs/logs/raw/.../bench-*.txt\n\"\"\"\nfrom __future__ import annotations\n\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n``elastic ctl done`` lines), so this tool needs no extra instrumentation.  It\nparses the log, records the requested and resulting S values, the arena size,\nfree VRAM, and the routing mass covered, and pairs them with the benchmark rows\nfrom ``bench_agentic.py``.\n\n  python3 tools/elastic_state.py docs/logs/raw/.../server.log --bench docs/logs/raw/.../bench-*.txt\n\"\"\"\nfrom __future__ import annotations\n\nimport\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n``elastic ctl done`` lines), so this tool needs no extra instrumentation.  It\nparses the log, records the requested and resulting S values, the arena size,\nfree VRAM, and the routing mass covered, and pairs them with the benchmark rows\nfrom ``bench_agentic.py``.\n\n  python3 tools/elastic_state.py docs/logs/raw/.../server.log --bench docs/logs/raw/.../bench-*.txt\n\"\"\"\nfrom __future__ import annotations\n\nimport argparse\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n``elastic ctl done`` lines), so this tool needs no extra instrumentation.  It\nparses the log, records the requested and resulting S values, the arena size,\nfree VRAM, and the routing mass covered, and pairs them with the benchmark rows\nfrom ``bench_agentic.py``.\n\n  python3 tools/elastic_state.py docs/logs/raw/.../server.log --bench docs/logs/raw/.../bench-*.txt\n\"\"\"\nfrom __future__ import annotations\n\nimport argparse\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n``elastic ctl done`` lines), so this tool needs no extra instrumentation.  It\nparses the log, records the requested and resulting S values, the arena size,\nfree VRAM, and the routing mass covered, and pairs them with the benchmark rows\nfrom ``bench_agentic.py``.\n\n  python3 tools/elastic_state.py docs/logs/raw/.../server.log --bench docs/logs/raw/.../bench-*.txt\n\"\"\"\nfrom __future__ import annotations\n\nimport argparse\nimport\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n``elastic ctl done`` lines), so this tool needs no extra instrumentation.  It\nparses the log, records the requested and resulting S values, the arena size,\nfree VRAM, and the routing mass covered, and pairs them with the benchmark rows\nfrom ``bench_agentic.py``.\n\n  python3 tools/elastic_state.py docs/logs/raw/.../server.log --bench docs/logs/raw/.../bench-*.txt\n\"\"\"\nfrom __future__ import annotations\n\nimport argparse\nimport glob\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n``elastic ctl done`` lines), so this tool needs no extra instrumentation.  It\nparses the log, records the requested and resulting S values, the arena size,\nfree VRAM, and the routing mass covered, and pairs them with the benchmark rows\nfrom ``bench_agentic.py``.\n\n  python3 tools/elastic_state.py docs/logs/raw/.../server.log --bench docs/logs/raw/.../bench-*.txt\n\"\"\"\nfrom __future__ import annotations\n\nimport argparse\nimport glob\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n``elastic ctl done`` lines), so this tool needs no extra instrumentation.  It\nparses the log, records the requested and resulting S values, the arena size,\nfree VRAM, and the routing mass covered, and pairs them with the benchmark rows\nfrom ``bench_agentic.py``.\n\n  python3 tools/elastic_state.py docs/logs/raw/.../server.log --bench docs/logs/raw/.../bench-*.txt\n\"\"\"\nfrom __future__ import annotations\n\nimport argparse\nimport glob\nimport\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n``elastic ctl done`` lines), so this tool needs no extra instrumentation.  It\nparses the log, records the requested and resulting S values, the arena size,\nfree VRAM, and the routing mass covered, and pairs them with the benchmark rows\nfrom ``bench_agentic.py``.\n\n  python3 tools/elastic_state.py docs/logs/raw/.../server.log --bench docs/logs/raw/.../bench-*.txt\n\"\"\"\nfrom __future__ import annotations\n\nimport argparse\nimport glob\nimport json\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n``elastic ctl done`` lines), so this tool needs no extra instrumentation.  It\nparses the log, records the requested and resulting S values, the arena size,\nfree VRAM, and the routing mass covered, and pairs them with the benchmark rows\nfrom ``bench_agentic.py``.\n\n  python3 tools/elastic_state.py docs/logs/raw/.../server.log --bench docs/logs/raw/.../bench-*.txt\n\"\"\"\nfrom __future__ import annotations\n\nimport argparse\nimport glob\nimport json\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n``elastic ctl done`` lines), so this tool needs no extra instrumentation.  It\nparses the log, records the requested and resulting S values, the arena size,\nfree VRAM, and the routing mass covered, and pairs them with the benchmark rows\nfrom ``bench_agentic.py``.\n\n  python3 tools/elastic_state.py docs/logs/raw/.../server.log --bench docs/logs/raw/.../bench-*.txt\n\"\"\"\nfrom __future__ import annotations\n\nimport argparse\nimport glob\nimport json\nimport\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n``elastic ctl done`` lines), so this tool needs no extra instrumentation.  It\nparses the log, records the requested and resulting S values, the arena size,\nfree VRAM, and the routing mass covered, and pairs them with the benchmark rows\nfrom ``bench_agentic.py``.\n\n  python3 tools/elastic_state.py docs/logs/raw/.../server.log --bench docs/logs/raw/.../bench-*.txt\n\"\"\"\nfrom __future__ import annotations\n\nimport argparse\nimport glob\nimport json\nimport os\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n``elastic ctl done`` lines), so this tool needs no extra instrumentation.  It\nparses the log, records the requested and resulting S values, the arena size,\nfree VRAM, and the routing mass covered, and pairs them with the benchmark rows\nfrom ``bench_agentic.py``.\n\n  python3 tools/elastic_state.py docs/logs/raw/.../server.log --bench docs/logs/raw/.../bench-*.txt\n\"\"\"\nfrom __future__ import annotations\n\nimport argparse\nimport glob\nimport json\nimport os\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n``elastic ctl done`` lines), so this tool needs no extra instrumentation.  It\nparses the log, records the requested and resulting S values, the arena size,\nfree VRAM, and the routing mass covered, and pairs them with the benchmark rows\nfrom ``bench_agentic.py``.\n\n  python3 tools/elastic_state.py docs/logs/raw/.../server.log --bench docs/logs/raw/.../bench-*.txt\n\"\"\"\nfrom __future__ import annotations\n\nimport argparse\nimport glob\nimport json\nimport os\nimport\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n``elastic ctl done`` lines), so this tool needs no extra instrumentation.  It\nparses the log, records the requested and resulting S values, the arena size,\nfree VRAM, and the routing mass covered, and pairs them with the benchmark rows\nfrom ``bench_agentic.py``.\n\n  python3 tools/elastic_state.py docs/logs/raw/.../server.log --bench docs/logs/raw/.../bench-*.txt\n\"\"\"\nfrom __future__ import annotations\n\nimport argparse\nimport glob\nimport json\nimport os\nimport re\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n``elastic ctl done`` lines), so this tool needs no extra instrumentation.  It\nparses the log, records the requested and resulting S values, the arena size,\nfree VRAM, and the routing mass covered, and pairs them with the benchmark rows\nfrom ``bench_agentic.py``.\n\n  python3 tools/elastic_state.py docs/logs/raw/.../server.log --bench docs/logs/raw/.../bench-*.txt\n\"\"\"\nfrom __future__ import annotations\n\nimport argparse\nimport glob\nimport json\nimport os\nimport re\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n``elastic ctl done`` lines), so this tool needs no extra instrumentation.  It\nparses the log, records the requested and resulting S values, the arena size,\nfree VRAM, and the routing mass covered, and pairs them with the benchmark rows\nfrom ``bench_agentic.py``.\n\n  python3 tools/elastic_state.py docs/logs/raw/.../server.log --bench docs/logs/raw/.../bench-*.txt\n\"\"\"\nfrom __future__ import annotations\n\nimport argparse\nimport glob\nimport json\nimport os\nimport re\nimport\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n``elastic ctl done`` lines), so this tool needs no extra instrumentation.  It\nparses the log, records the requested and resulting S values, the arena size,\nfree VRAM, and the routing mass covered, and pairs them with the benchmark rows\nfrom ``bench_agentic.py``.\n\n  python3 tools/elastic_state.py docs/logs/raw/.../server.log --bench docs/logs/raw/.../bench-*.txt\n\"\"\"\nfrom __future__ import annotations\n\nimport argparse\nimport glob\nimport json\nimport os\nimport re\nimport sys\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n``elastic ctl done`` lines), so this tool needs no extra instrumentation.  It\nparses the log, records the requested and resulting S values, the arena size,\nfree VRAM, and the routing mass covered, and pairs them with the benchmark rows\nfrom ``bench_agentic.py``.\n\n  python3 tools/elastic_state.py docs/logs/raw/.../server.log --bench docs/logs/raw/.../bench-*.txt\n\"\"\"\nfrom __future__ import annotations\n\nimport argparse\nimport glob\nimport json\nimport os\nimport re\nimport sys\n\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n``elastic ctl done`` lines), so this tool needs no extra instrumentation.  It\nparses the log, records the requested and resulting S values, the arena size,\nfree VRAM, and the routing mass covered, and pairs them with the benchmark rows\nfrom ``bench_agentic.py``.\n\n  python3 tools/elastic_state.py docs/logs/raw/.../server.log --bench docs/logs/raw/.../bench-*.txt\n\"\"\"\nfrom __future__ import annotations\n\nimport argparse\nimport glob\nimport json\nimport os\nimport re\nimport sys\n\nfrom\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n``elastic ctl done`` lines), so this tool needs no extra instrumentation.  It\nparses the log, records the requested and resulting S values, the arena size,\nfree VRAM, and the routing mass covered, and pairs them with the benchmark rows\nfrom ``bench_agentic.py``.\n\n  python3 tools/elastic_state.py docs/logs/raw/.../server.log --bench docs/logs/raw/.../bench-*.txt\n\"\"\"\nfrom __future__ import annotations\n\nimport argparse\nimport glob\nimport json\nimport os\nimport re\nimport sys\n\nfrom bench\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n``elastic ctl done`` lines), so this tool needs no extra instrumentation.  It\nparses the log, records the requested and resulting S values, the arena size,\nfree VRAM, and the routing mass covered, and pairs them with the benchmark rows\nfrom ``bench_agentic.py``.\n\n  python3 tools/elastic_state.py docs/logs/raw/.../server.log --bench docs/logs/raw/.../bench-*.txt\n\"\"\"\nfrom __future__ import annotations\n\nimport argparse\nimport glob\nimport json\nimport os\nimport re\nimport sys\n\nfrom bench_ag\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n``elastic ctl done`` lines), so this tool needs no extra instrumentation.  It\nparses the log, records the requested and resulting S values, the arena size,\nfree VRAM, and the routing mass covered, and pairs them with the benchmark rows\nfrom ``bench_agentic.py``.\n\n  python3 tools/elastic_state.py docs/logs/raw/.../server.log --bench docs/logs/raw/.../bench-*.txt\n\"\"\"\nfrom __future__ import annotations\n\nimport argparse\nimport glob\nimport json\nimport os\nimport re\nimport sys\n\nfrom bench_agentic\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n``elastic ctl done`` lines), so this tool needs no extra instrumentation.  It\nparses the log, records the requested and resulting S values, the arena size,\nfree VRAM, and the routing mass covered, and pairs them with the benchmark rows\nfrom ``bench_agentic.py``.\n\n  python3 tools/elastic_state.py docs/logs/raw/.../server.log --bench docs/logs/raw/.../bench-*.txt\n\"\"\"\nfrom __future__ import annotations\n\nimport argparse\nimport glob\nimport json\nimport os\nimport re\nimport sys\n\nfrom bench_agentic import\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n``elastic ctl done`` lines), so this tool needs no extra instrumentation.  It\nparses the log, records the requested and resulting S values, the arena size,\nfree VRAM, and the routing mass covered, and pairs them with the benchmark rows\nfrom ``bench_agentic.py``.\n\n  python3 tools/elastic_state.py docs/logs/raw/.../server.log --bench docs/logs/raw/.../bench-*.txt\n\"\"\"\nfrom __future__ import annotations\n\nimport argparse\nimport glob\nimport json\nimport os\nimport re\nimport sys\n\nfrom bench_agentic import parse\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n``elastic ctl done`` lines), so this tool needs no extra instrumentation.  It\nparses the log, records the requested and resulting S values, the arena size,\nfree VRAM, and the routing mass covered, and pairs them with the benchmark rows\nfrom ``bench_agentic.py``.\n\n  python3 tools/elastic_state.py docs/logs/raw/.../server.log --bench docs/logs/raw/.../bench-*.txt\n\"\"\"\nfrom __future__ import annotations\n\nimport argparse\nimport glob\nimport json\nimport os\nimport re\nimport sys\n\nfrom bench_agentic import parse_row\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n``elastic ctl done`` lines), so this tool needs no extra instrumentation.  It\nparses the log, records the requested and resulting S values, the arena size,\nfree VRAM, and the routing mass covered, and pairs them with the benchmark rows\nfrom ``bench_agentic.py``.\n\n  python3 tools/elastic_state.py docs/logs/raw/.../server.log --bench docs/logs/raw/.../bench-*.txt\n\"\"\"\nfrom __future__ import annotations\n\nimport argparse\nimport glob\nimport json\nimport os\nimport re\nimport sys\n\nfrom bench_agentic import parse_row\n\n\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n``elastic ctl done`` lines), so this tool needs no extra instrumentation.  It\nparses the log, records the requested and resulting S values, the arena size,\nfree VRAM, and the routing mass covered, and pairs them with the benchmark rows\nfrom ``bench_agentic.py``.\n\n  python3 tools/elastic_state.py docs/logs/raw/.../server.log --bench docs/logs/raw/.../bench-*.txt\n\"\"\"\nfrom __future__ import annotations\n\nimport argparse\nimport glob\nimport json\nimport os\nimport re\nimport sys\n\nfrom bench_agentic import parse_row\n\n\nCTL\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n``elastic ctl done`` lines), so this tool needs no extra instrumentation.  It\nparses the log, records the requested and resulting S values, the arena size,\nfree VRAM, and the routing mass covered, and pairs them with the benchmark rows\nfrom ``bench_agentic.py``.\n\n  python3 tools/elastic_state.py docs/logs/raw/.../server.log --bench docs/logs/raw/.../bench-*.txt\n\"\"\"\nfrom __future__ import annotations\n\nimport argparse\nimport glob\nimport json\nimport os\nimport re\nimport sys\n\nfrom bench_agentic import parse_row\n\n\nCTL_RE\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n``elastic ctl done`` lines), so this tool needs no extra instrumentation.  It\nparses the log, records the requested and resulting S values, the arena size,\nfree VRAM, and the routing mass covered, and pairs them with the benchmark rows\nfrom ``bench_agentic.py``.\n\n  python3 tools/elastic_state.py docs/logs/raw/.../server.log --bench docs/logs/raw/.../bench-*.txt\n\"\"\"\nfrom __future__ import annotations\n\nimport argparse\nimport glob\nimport json\nimport os\nimport re\nimport sys\n\nfrom bench_agentic import parse_row\n\n\nCTL_RE =\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n``elastic ctl done`` lines), so this tool needs no extra instrumentation.  It\nparses the log, records the requested and resulting S values, the arena size,\nfree VRAM, and the routing mass covered, and pairs them with the benchmark rows\nfrom ``bench_agentic.py``.\n\n  python3 tools/elastic_state.py docs/logs/raw/.../server.log --bench docs/logs/raw/.../bench-*.txt\n\"\"\"\nfrom __future__ import annotations\n\nimport argparse\nimport glob\nimport json\nimport os\nimport re\nimport sys\n\nfrom bench_agentic import parse_row\n\n\nCTL_RE = re\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n``elastic ctl done`` lines), so this tool needs no extra instrumentation.  It\nparses the log, records the requested and resulting S values, the arena size,\nfree VRAM, and the routing mass covered, and pairs them with the benchmark rows\nfrom ``bench_agentic.py``.\n\n  python3 tools/elastic_state.py docs/logs/raw/.../server.log --bench docs/logs/raw/.../bench-*.txt\n\"\"\"\nfrom __future__ import annotations\n\nimport argparse\nimport glob\nimport json\nimport os\nimport re\nimport sys\n\nfrom bench_agentic import parse_row\n\n\nCTL_RE = re.compile\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n``elastic ctl done`` lines), so this tool needs no extra instrumentation.  It\nparses the log, records the requested and resulting S values, the arena size,\nfree VRAM, and the routing mass covered, and pairs them with the benchmark rows\nfrom ``bench_agentic.py``.\n\n  python3 tools/elastic_state.py docs/logs/raw/.../server.log --bench docs/logs/raw/.../bench-*.txt\n\"\"\"\nfrom __future__ import annotations\n\nimport argparse\nimport glob\nimport json\nimport os\nimport re\nimport sys\n\nfrom bench_agentic import parse_row\n\n\nCTL_RE = re.compile(r\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n``elastic ctl done`` lines), so this tool needs no extra instrumentation.  It\nparses the log, records the requested and resulting S values, the arena size,\nfree VRAM, and the routing mass covered, and pairs them with the benchmark rows\nfrom ``bench_agentic.py``.\n\n  python3 tools/elastic_state.py docs/logs/raw/.../server.log --bench docs/logs/raw/.../bench-*.txt\n\"\"\"\nfrom __future__ import annotations\n\nimport argparse\nimport glob\nimport json\nimport os\nimport re\nimport sys\n\nfrom bench_agentic import parse_row\n\n\nCTL_RE = re.compile(r\"\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n``elastic ctl done`` lines), so this tool needs no extra instrumentation.  It\nparses the log, records the requested and resulting S values, the arena size,\nfree VRAM, and the routing mass covered, and pairs them with the benchmark rows\nfrom ``bench_agentic.py``.\n\n  python3 tools/elastic_state.py docs/logs/raw/.../server.log --bench docs/logs/raw/.../bench-*.txt\n\"\"\"\nfrom __future__ import annotations\n\nimport argparse\nimport glob\nimport json\nimport os\nimport re\nimport sys\n\nfrom bench_agentic import parse_row\n\n\nCTL_RE = re.compile(r\"elastic\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n``elastic ctl done`` lines), so this tool needs no extra instrumentation.  It\nparses the log, records the requested and resulting S values, the arena size,\nfree VRAM, and the routing mass covered, and pairs them with the benchmark rows\nfrom ``bench_agentic.py``.\n\n  python3 tools/elastic_state.py docs/logs/raw/.../server.log --bench docs/logs/raw/.../bench-*.txt\n\"\"\"\nfrom __future__ import annotations\n\nimport argparse\nimport glob\nimport json\nimport os\nimport re\nimport sys\n\nfrom bench_agentic import parse_row\n\n\nCTL_RE = re.compile(r\"elastic ctl\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n``elastic ctl done`` lines), so this tool needs no extra instrumentation.  It\nparses the log, records the requested and resulting S values, the arena size,\nfree VRAM, and the routing mass covered, and pairs them with the benchmark rows\nfrom ``bench_agentic.py``.\n\n  python3 tools/elastic_state.py docs/logs/raw/.../server.log --bench docs/logs/raw/.../bench-*.txt\n\"\"\"\nfrom __future__ import annotations\n\nimport argparse\nimport glob\nimport json\nimport os\nimport re\nimport sys\n\nfrom bench_agentic import parse_row\n\n\nCTL_RE = re.compile(r\"elastic ctl '\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n``elastic ctl done`` lines), so this tool needs no extra instrumentation.  It\nparses the log, records the requested and resulting S values, the arena size,\nfree VRAM, and the routing mass covered, and pairs them with the benchmark rows\nfrom ``bench_agentic.py``.\n\n  python3 tools/elastic_state.py docs/logs/raw/.../server.log --bench docs/logs/raw/.../bench-*.txt\n\"\"\"\nfrom __future__ import annotations\n\nimport argparse\nimport glob\nimport json\nimport os\nimport re\nimport sys\n\nfrom bench_agentic import parse_row\n\n\nCTL_RE = re.compile(r\"elastic ctl '([^\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n``elastic ctl done`` lines), so this tool needs no extra instrumentation.  It\nparses the log, records the requested and resulting S values, the arena size,\nfree VRAM, and the routing mass covered, and pairs them with the benchmark rows\nfrom ``bench_agentic.py``.\n\n  python3 tools/elastic_state.py docs/logs/raw/.../server.log --bench docs/logs/raw/.../bench-*.txt\n\"\"\"\nfrom __future__ import annotations\n\nimport argparse\nimport glob\nimport json\nimport os\nimport re\nimport sys\n\nfrom bench_agentic import parse_row\n\n\nCTL_RE = re.compile(r\"elastic ctl '([^']+\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n``elastic ctl done`` lines), so this tool needs no extra instrumentation.  It\nparses the log, records the requested and resulting S values, the arena size,\nfree VRAM, and the routing mass covered, and pairs them with the benchmark rows\nfrom ``bench_agentic.py``.\n\n  python3 tools/elastic_state.py docs/logs/raw/.../server.log --bench docs/logs/raw/.../bench-*.txt\n\"\"\"\nfrom __future__ import annotations\n\nimport argparse\nimport glob\nimport json\nimport os\nimport re\nimport sys\n\nfrom bench_agentic import parse_row\n\n\nCTL_RE = re.compile(r\"elastic ctl '([^']+)'\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n``elastic ctl done`` lines), so this tool needs no extra instrumentation.  It\nparses the log, records the requested and resulting S values, the arena size,\nfree VRAM, and the routing mass covered, and pairs them with the benchmark rows\nfrom ``bench_agentic.py``.\n\n  python3 tools/elastic_state.py docs/logs/raw/.../server.log --bench docs/logs/raw/.../bench-*.txt\n\"\"\"\nfrom __future__ import annotations\n\nimport argparse\nimport glob\nimport json\nimport os\nimport re\nimport sys\n\nfrom bench_agentic import parse_row\n\n\nCTL_RE = re.compile(r\"elastic ctl '([^']+)' done\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n``elastic ctl done`` lines), so this tool needs no extra instrumentation.  It\nparses the log, records the requested and resulting S values, the arena size,\nfree VRAM, and the routing mass covered, and pairs them with the benchmark rows\nfrom ``bench_agentic.py``.\n\n  python3 tools/elastic_state.py docs/logs/raw/.../server.log --bench docs/logs/raw/.../bench-*.txt\n\"\"\"\nfrom __future__ import annotations\n\nimport argparse\nimport glob\nimport json\nimport os\nimport re\nimport sys\n\nfrom bench_agentic import parse_row\n\n\nCTL_RE = re.compile(r\"elastic ctl '([^']+)' done in\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n``elastic ctl done`` lines), so this tool needs no extra instrumentation.  It\nparses the log, records the requested and resulting S values, the arena size,\nfree VRAM, and the routing mass covered, and pairs them with the benchmark rows\nfrom ``bench_agentic.py``.\n\n  python3 tools/elastic_state.py docs/logs/raw/.../server.log --bench docs/logs/raw/.../bench-*.txt\n\"\"\"\nfrom __future__ import annotations\n\nimport argparse\nimport glob\nimport json\nimport os\nimport re\nimport sys\n\nfrom bench_agentic import parse_row\n\n\nCTL_RE = re.compile(r\"elastic ctl '([^']+)' done in ([\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n``elastic ctl done`` lines), so this tool needs no extra instrumentation.  It\nparses the log, records the requested and resulting S values, the arena size,\nfree VRAM, and the routing mass covered, and pairs them with the benchmark rows\nfrom ``bench_agentic.py``.\n\n  python3 tools/elastic_state.py docs/logs/raw/.../server.log --bench docs/logs/raw/.../bench-*.txt\n\"\"\"\nfrom __future__ import annotations\n\nimport argparse\nimport glob\nimport json\nimport os\nimport re\nimport sys\n\nfrom bench_agentic import parse_row\n\n\nCTL_RE = re.compile(r\"elastic ctl '([^']+)' done in ([\\\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n``elastic ctl done`` lines), so this tool needs no extra instrumentation.  It\nparses the log, records the requested and resulting S values, the arena size,\nfree VRAM, and the routing mass covered, and pairs them with the benchmark rows\nfrom ``bench_agentic.py``.\n\n  python3 tools/elastic_state.py docs/logs/raw/.../server.log --bench docs/logs/raw/.../bench-*.txt\n\"\"\"\nfrom __future__ import annotations\n\nimport argparse\nimport glob\nimport json\nimport os\nimport re\nimport sys\n\nfrom bench_agentic import parse_row\n\n\nCTL_RE = re.compile(r\"elastic ctl '([^']+)' done in ([\\d\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n``elastic ctl done`` lines), so this tool needs no extra instrumentation.  It\nparses the log, records the requested and resulting S values, the arena size,\nfree VRAM, and the routing mass covered, and pairs them with the benchmark rows\nfrom ``bench_agentic.py``.\n\n  python3 tools/elastic_state.py docs/logs/raw/.../server.log --bench docs/logs/raw/.../bench-*.txt\n\"\"\"\nfrom __future__ import annotations\n\nimport argparse\nimport glob\nimport json\nimport os\nimport re\nimport sys\n\nfrom bench_agentic import parse_row\n\n\nCTL_RE = re.compile(r\"elastic ctl '([^']+)' done in ([\\d.\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n``elastic ctl done`` lines), so this tool needs no extra instrumentation.  It\nparses the log, records the requested and resulting S values, the arena size,\nfree VRAM, and the routing mass covered, and pairs them with the benchmark rows\nfrom ``bench_agentic.py``.\n\n  python3 tools/elastic_state.py docs/logs/raw/.../server.log --bench docs/logs/raw/.../bench-*.txt\n\"\"\"\nfrom __future__ import annotations\n\nimport argparse\nimport glob\nimport json\nimport os\nimport re\nimport sys\n\nfrom bench_agentic import parse_row\n\n\nCTL_RE = re.compile(r\"elastic ctl '([^']+)' done in ([\\d.]+\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n``elastic ctl done`` lines), so this tool needs no extra instrumentation.  It\nparses the log, records the requested and resulting S values, the arena size,\nfree VRAM, and the routing mass covered, and pairs them with the benchmark rows\nfrom ``bench_agentic.py``.\n\n  python3 tools/elastic_state.py docs/logs/raw/.../server.log --bench docs/logs/raw/.../bench-*.txt\n\"\"\"\nfrom __future__ import annotations\n\nimport argparse\nimport glob\nimport json\nimport os\nimport re\nimport sys\n\nfrom bench_agentic import parse_row\n\n\nCTL_RE = re.compile(r\"elastic ctl '([^']+)' done in ([\\d.]+)\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n``elastic ctl done`` lines), so this tool needs no extra instrumentation.  It\nparses the log, records the requested and resulting S values, the arena size,\nfree VRAM, and the routing mass covered, and pairs them with the benchmark rows\nfrom ``bench_agentic.py``.\n\n  python3 tools/elastic_state.py docs/logs/raw/.../server.log --bench docs/logs/raw/.../bench-*.txt\n\"\"\"\nfrom __future__ import annotations\n\nimport argparse\nimport glob\nimport json\nimport os\nimport re\nimport sys\n\nfrom bench_agentic import parse_row\n\n\nCTL_RE = re.compile(r\"elastic ctl '([^']+)' done in ([\\d.]+)s\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n``elastic ctl done`` lines), so this tool needs no extra instrumentation.  It\nparses the log, records the requested and resulting S values, the arena size,\nfree VRAM, and the routing mass covered, and pairs them with the benchmark rows\nfrom ``bench_agentic.py``.\n\n  python3 tools/elastic_state.py docs/logs/raw/.../server.log --bench docs/logs/raw/.../bench-*.txt\n\"\"\"\nfrom __future__ import annotations\n\nimport argparse\nimport glob\nimport json\nimport os\nimport re\nimport sys\n\nfrom bench_agentic import parse_row\n\n\nCTL_RE = re.compile(r\"elastic ctl '([^']+)' done in ([\\d.]+)s\")\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n``elastic ctl done`` lines), so this tool needs no extra instrumentation.  It\nparses the log, records the requested and resulting S values, the arena size,\nfree VRAM, and the routing mass covered, and pairs them with the benchmark rows\nfrom ``bench_agentic.py``.\n\n  python3 tools/elastic_state.py docs/logs/raw/.../server.log --bench docs/logs/raw/.../bench-*.txt\n\"\"\"\nfrom __future__ import annotations\n\nimport argparse\nimport glob\nimport json\nimport os\nimport re\nimport sys\n\nfrom bench_agentic import parse_row\n\n\nCTL_RE = re.compile(r\"elastic ctl '([^']+)' done in ([\\d.]+)s\")\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n``elastic ctl done`` lines), so this tool needs no extra instrumentation.  It\nparses the log, records the requested and resulting S values, the arena size,\nfree VRAM, and the routing mass covered, and pairs them with the benchmark rows\nfrom ``bench_agentic.py``.\n\n  python3 tools/elastic_state.py docs/logs/raw/.../server.log --bench docs/logs/raw/.../bench-*.txt\n\"\"\"\nfrom __future__ import annotations\n\nimport argparse\nimport glob\nimport json\nimport os\nimport re\nimport sys\n\nfrom bench_agentic import parse_row\n\n\nCTL_RE = re.compile(r\"elastic ctl '([^']+)' done in ([\\d.]+)s\")\nSTATE\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n``elastic ctl done`` lines), so this tool needs no extra instrumentation.  It\nparses the log, records the requested and resulting S values, the arena size,\nfree VRAM, and the routing mass covered, and pairs them with the benchmark rows\nfrom ``bench_agentic.py``.\n\n  python3 tools/elastic_state.py docs/logs/raw/.../server.log --bench docs/logs/raw/.../bench-*.txt\n\"\"\"\nfrom __future__ import annotations\n\nimport argparse\nimport glob\nimport json\nimport os\nimport re\nimport sys\n\nfrom bench_agentic import parse_row\n\n\nCTL_RE = re.compile(r\"elastic ctl '([^']+)' done in ([\\d.]+)s\")\nSTATE_RE\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n    elif cmd == \"check\":\n        ref_name = sys.argv[2]\n        current = collect()\n        reference = json.loads((D / f\"{ref_name}.json\").read_text())\n        report = stats(reference, current)\n        print_report(f\"{ref_name} vs current\", report)\n    elif cmd == \"compare\":\n        a_name, b_name = sys.argv[2], sys.argv[3]\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"].json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n    else:\n        print(f\"  unknown command {cmd}\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the elastic expert cache's live state from the server log.\n\nThe elastic cache reports its own state in the server log (``elastic ctl`` /\n``elastic ctl done`` lines), so this tool needs no extra instrumentation.  It\nparses the log, records the requested and resulting S values, the arena size,\nfree VRAM, and the routing mass covered, and pairs them with the benchmark rows\nfrom ``bench_agentic.py``.\n\n  python3 tools/elastic_state.py docs/logs/raw/.../server.log --bench docs/logs/raw/.../bench-*.txt\n\"\"\"\nfrom __future__ import annotations\n\nimport argparse\nimport glob\nimport json\nimport os\nimport re\nimport sys\n\nfrom bench_agentic import parse_row\n\n\nCTL_RE = re.compile(r\"elastic ctl '([^']+)' done in ([\\d.]+)s\")\nSTATE_RE =",
 "ttft_seconds": 78.17264097300358,
 "status": "ok",
 "tg_tokens_per_s": 29.75832779170931,
 "decode_seconds_per_token": 0.033604038741672865,
 "individual_token_timing": true,
 "event_intervals_ms": {
  "n": 511,
  "median": 33.54986599879339,
  "p95": 42.21908299950883,
  "mean": 33.604038741672866
 },
 "class": "code",
 "requested_input_tokens": 131072
}
