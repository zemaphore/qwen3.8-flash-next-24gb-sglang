{
 "started_utc": "2026-09-16T23:45:37.712452+00:00",
 "wall_seconds": 96.66536592898774,
 "prompt_tokens": 131072,
 "completion_tokens": 512,
 "stream_events": 512,
 "ended_utc": "2026-09-16T23:47:14.377944+00:00",
 "warmup": false,
 "sample_index": 3,
 "requested_decode_tokens": 512,
 "gpu": {
  "n": 347,
  "temperature_c": {
   "min": 73.0,
   "max": 79.0,
   "mean": 76.48991354466858
  },
  "power_w": {
   "min": 153.24,
   "max": 283.81,
   "mean": 237.77345821325648
  },
  "util_pct": {
   "min": 0.0,
   "max": 100.0,
   "mean": 68.98847262247838
  },
  "clock_sm_mhz": {
   "min": 765.0,
   "max": 1905.0,
   "mean": 1687.9538904899136
  },
  "clock_mem_mhz": {
   "min": 9501.0,
   "max": 9501.0,
   "mean": 9501.0
  },
  "memory_free_mib": {
   "min": 736.0
  }
 },
 "output_text_sha256": "4c8fc43180b133caade0e1159eafa294819073425e96d58199a01fa3d5ab52b7",
 "output_text": "\"\"   \"    LOG\"    LOGPRO\"    LOGPROB\"    LOGPROB_MAX\"    LOGPROB_MAX={\"    LOGPROB_MAX={overall\"    LOGPROB_MAX={overall['\"    LOGPROB_MAX={overall['max\"    LOGPROB_MAX={overall['max']\"    LOGPROB_MAX={overall['max']:.\"    LOGPROB_MAX={overall['max']:.6\"    LOGPROB_MAX={overall['max']:.6f\"    LOGPROB_MAX={overall['max']:.6f}\"    LOGPROB_MAX={overall['max']:.6f} LOG\"    LOGPROB_MAX={overall['max']:.6f} LOGPRO\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_ME\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main()\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() ->\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n   \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser =\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n   \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\",\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\",\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\",\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n   \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\",\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n   \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\",\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n   \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args =\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n   \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd ==\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n       \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name =\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n       \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload =\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n       \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parent\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True,\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n       \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path =\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir)\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) /\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n       \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n       \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key,\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n           \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n               \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\" \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:1\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s}\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d}\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n               \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d}\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n               \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d}\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n           \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n       \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\" \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n       \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n   \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd ==\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n       \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name,\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name =\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0],\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n       \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference =\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir)\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) /\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n       \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current =\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n       \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report =\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference,\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n       \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name}\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\",\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n       \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n   \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd ==\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n       \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name,\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name =\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0],\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args.names\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args.names[\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args.names[1\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n       \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a =\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((Path\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((Path(args\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((Path(args.dir\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((Path(args.dir)\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((Path(args.dir) /\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((Path(args.dir) / f\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((Path(args.dir) / f\"{\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((Path(args.dir) / f\"{a\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((Path(args.dir) / f\"{a_name\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((Path(args.dir) / f\"{a_name}.\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((Path(args.dir) / f\"{a_name}.json\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((Path(args.dir) / f\"{a_name}.json\").\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((Path(args.dir) / f\"{a_name}.json\").read\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((Path(args.dir) / f\"{a_name}.json\").read_text\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((Path(args.dir) / f\"{a_name}.json\").read_text())\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((Path(args.dir) / f\"{a_name}.json\").read_text())\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((Path(args.dir) / f\"{a_name}.json\").read_text())\n       \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((Path(args.dir) / f\"{a_name}.json\").read_text())\n        b\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((Path(args.dir) / f\"{a_name}.json\").read_text())\n        b =\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((Path(args.dir) / f\"{a_name}.json\").read_text())\n        b = json\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((Path(args.dir) / f\"{a_name}.json\").read_text())\n        b = json.loads\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((Path(args.dir) / f\"{a_name}.json\").read_text())\n        b = json.loads((\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((Path(args.dir) / f\"{a_name}.json\").read_text())\n        b = json.loads((Path\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((Path(args.dir) / f\"{a_name}.json\").read_text())\n        b = json.loads((Path(args\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((Path(args.dir) / f\"{a_name}.json\").read_text())\n        b = json.loads((Path(args.dir\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((Path(args.dir) / f\"{a_name}.json\").read_text())\n        b = json.loads((Path(args.dir)\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((Path(args.dir) / f\"{a_name}.json\").read_text())\n        b = json.loads((Path(args.dir) /\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((Path(args.dir) / f\"{a_name}.json\").read_text())\n        b = json.loads((Path(args.dir) / f\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((Path(args.dir) / f\"{a_name}.json\").read_text())\n        b = json.loads((Path(args.dir) / f\"{\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((Path(args.dir) / f\"{a_name}.json\").read_text())\n        b = json.loads((Path(args.dir) / f\"{b\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((Path(args.dir) / f\"{a_name}.json\").read_text())\n        b = json.loads((Path(args.dir) / f\"{b_name\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((Path(args.dir) / f\"{a_name}.json\").read_text())\n        b = json.loads((Path(args.dir) / f\"{b_name}.\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((Path(args.dir) / f\"{a_name}.json\").read_text())\n        b = json.loads((Path(args.dir) / f\"{b_name}.json\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((Path(args.dir) / f\"{a_name}.json\").read_text())\n        b = json.loads((Path(args.dir) / f\"{b_name}.json\").\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((Path(args.dir) / f\"{a_name}.json\").read_text())\n        b = json.loads((Path(args.dir) / f\"{b_name}.json\").read\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((Path(args.dir) / f\"{a_name}.json\").read_text())\n        b = json.loads((Path(args.dir) / f\"{b_name}.json\").read_text\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((Path(args.dir) / f\"{a_name}.json\").read_text())\n        b = json.loads((Path(args.dir) / f\"{b_name}.json\").read_text())\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((Path(args.dir) / f\"{a_name}.json\").read_text())\n        b = json.loads((Path(args.dir) / f\"{b_name}.json\").read_text())\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((Path(args.dir) / f\"{a_name}.json\").read_text())\n        b = json.loads((Path(args.dir) / f\"{b_name}.json\").read_text())\n       \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((Path(args.dir) / f\"{a_name}.json\").read_text())\n        b = json.loads((Path(args.dir) / f\"{b_name}.json\").read_text())\n        report\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((Path(args.dir) / f\"{a_name}.json\").read_text())\n        b = json.loads((Path(args.dir) / f\"{b_name}.json\").read_text())\n        report =\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((Path(args.dir) / f\"{a_name}.json\").read_text())\n        b = json.loads((Path(args.dir) / f\"{b_name}.json\").read_text())\n        report = stats\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((Path(args.dir) / f\"{a_name}.json\").read_text())\n        b = json.loads((Path(args.dir) / f\"{b_name}.json\").read_text())\n        report = stats(a\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((Path(args.dir) / f\"{a_name}.json\").read_text())\n        b = json.loads((Path(args.dir) / f\"{b_name}.json\").read_text())\n        report = stats(a,\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((Path(args.dir) / f\"{a_name}.json\").read_text())\n        b = json.loads((Path(args.dir) / f\"{b_name}.json\").read_text())\n        report = stats(a, b\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((Path(args.dir) / f\"{a_name}.json\").read_text())\n        b = json.loads((Path(args.dir) / f\"{b_name}.json\").read_text())\n        report = stats(a, b)\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((Path(args.dir) / f\"{a_name}.json\").read_text())\n        b = json.loads((Path(args.dir) / f\"{b_name}.json\").read_text())\n        report = stats(a, b)\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((Path(args.dir) / f\"{a_name}.json\").read_text())\n        b = json.loads((Path(args.dir) / f\"{b_name}.json\").read_text())\n        report = stats(a, b)\n       \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((Path(args.dir) / f\"{a_name}.json\").read_text())\n        b = json.loads((Path(args.dir) / f\"{b_name}.json\").read_text())\n        report = stats(a, b)\n        print\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((Path(args.dir) / f\"{a_name}.json\").read_text())\n        b = json.loads((Path(args.dir) / f\"{b_name}.json\").read_text())\n        report = stats(a, b)\n        print_report\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((Path(args.dir) / f\"{a_name}.json\").read_text())\n        b = json.loads((Path(args.dir) / f\"{b_name}.json\").read_text())\n        report = stats(a, b)\n        print_report(f\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((Path(args.dir) / f\"{a_name}.json\").read_text())\n        b = json.loads((Path(args.dir) / f\"{b_name}.json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((Path(args.dir) / f\"{a_name}.json\").read_text())\n        b = json.loads((Path(args.dir) / f\"{b_name}.json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((Path(args.dir) / f\"{a_name}.json\").read_text())\n        b = json.loads((Path(args.dir) / f\"{b_name}.json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((Path(args.dir) / f\"{a_name}.json\").read_text())\n        b = json.loads((Path(args.dir) / f\"{b_name}.json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name}\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((Path(args.dir) / f\"{a_name}.json\").read_text())\n        b = json.loads((Path(args.dir) / f\"{b_name}.json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((Path(args.dir) / f\"{a_name}.json\").read_text())\n        b = json.loads((Path(args.dir) / f\"{b_name}.json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((Path(args.dir) / f\"{a_name}.json\").read_text())\n        b = json.loads((Path(args.dir) / f\"{b_name}.json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((Path(args.dir) / f\"{a_name}.json\").read_text())\n        b = json.loads((Path(args.dir) / f\"{b_name}.json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((Path(args.dir) / f\"{a_name}.json\").read_text())\n        b = json.loads((Path(args.dir) / f\"{b_name}.json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\",\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((Path(args.dir) / f\"{a_name}.json\").read_text())\n        b = json.loads((Path(args.dir) / f\"{b_name}.json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((Path(args.dir) / f\"{a_name}.json\").read_text())\n        b = json.loads((Path(args.dir) / f\"{b_name}.json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((Path(args.dir) / f\"{a_name}.json\").read_text())\n        b = json.loads((Path(args.dir) / f\"{b_name}.json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((Path(args.dir) / f\"{a_name}.json\").read_text())\n        b = json.loads((Path(args.dir) / f\"{b_name}.json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n       \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((Path(args.dir) / f\"{a_name}.json\").read_text())\n        b = json.loads((Path(args.dir) / f\"{b_name}.json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n        report\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((Path(args.dir) / f\"{a_name}.json\").read_text())\n        b = json.loads((Path(args.dir) / f\"{b_name}.json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n        report =\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((Path(args.dir) / f\"{a_name}.json\").read_text())\n        b = json.loads((Path(args.dir) / f\"{b_name}.json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n        report = stats\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((Path(args.dir) / f\"{a_name}.json\").read_text())\n        b = json.loads((Path(args.dir) / f\"{b_name}.json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n        report = stats(b\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((Path(args.dir) / f\"{a_name}.json\").read_text())\n        b = json.loads((Path(args.dir) / f\"{b_name}.json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n        report = stats(b,\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((Path(args.dir) / f\"{a_name}.json\").read_text())\n        b = json.loads((Path(args.dir) / f\"{b_name}.json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n        report = stats(b, a\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((Path(args.dir) / f\"{a_name}.json\").read_text())\n        b = json.loads((Path(args.dir) / f\"{b_name}.json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n        report = stats(b, a)\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((Path(args.dir) / f\"{a_name}.json\").read_text())\n        b = json.loads((Path(args.dir) / f\"{b_name}.json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n        report = stats(b, a)\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((Path(args.dir) / f\"{a_name}.json\").read_text())\n        b = json.loads((Path(args.dir) / f\"{b_name}.json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n        report = stats(b, a)\n       \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((Path(args.dir) / f\"{a_name}.json\").read_text())\n        b = json.loads((Path(args.dir) / f\"{b_name}.json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n        report = stats(b, a)\n        print\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((Path(args.dir) / f\"{a_name}.json\").read_text())\n        b = json.loads((Path(args.dir) / f\"{b_name}.json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n        report = stats(b, a)\n        print_report\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((Path(args.dir) / f\"{a_name}.json\").read_text())\n        b = json.loads((Path(args.dir) / f\"{b_name}.json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n        report = stats(b, a)\n        print_report(f\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((Path(args.dir) / f\"{a_name}.json\").read_text())\n        b = json.loads((Path(args.dir) / f\"{b_name}.json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n        report = stats(b, a)\n        print_report(f\"{\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((Path(args.dir) / f\"{a_name}.json\").read_text())\n        b = json.loads((Path(args.dir) / f\"{b_name}.json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n        report = stats(b, a)\n        print_report(f\"{a\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((Path(args.dir) / f\"{a_name}.json\").read_text())\n        b = json.loads((Path(args.dir) / f\"{b_name}.json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n        report = stats(b, a)\n        print_report(f\"{a_name\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((Path(args.dir) / f\"{a_name}.json\").read_text())\n        b = json.loads((Path(args.dir) / f\"{b_name}.json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n        report = stats(b, a)\n        print_report(f\"{a_name}\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((Path(args.dir) / f\"{a_name}.json\").read_text())\n        b = json.loads((Path(args.dir) / f\"{b_name}.json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n        report = stats(b, a)\n        print_report(f\"{a_name} vs\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((Path(args.dir) / f\"{a_name}.json\").read_text())\n        b = json.loads((Path(args.dir) / f\"{b_name}.json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n        report = stats(b, a)\n        print_report(f\"{a_name} vs {\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((Path(args.dir) / f\"{a_name}.json\").read_text())\n        b = json.loads((Path(args.dir) / f\"{b_name}.json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n        report = stats(b, a)\n        print_report(f\"{a_name} vs {b\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((Path(args.dir) / f\"{a_name}.json\").read_text())\n        b = json.loads((Path(args.dir) / f\"{b_name}.json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n        report = stats(b, a)\n        print_report(f\"{a_name} vs {b_name\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((Path(args.dir) / f\"{a_name}.json\").read_text())\n        b = json.loads((Path(args.dir) / f\"{b_name}.json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n        report = stats(b, a)\n        print_report(f\"{a_name} vs {b_name}\",\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((Path(args.dir) / f\"{a_name}.json\").read_text())\n        b = json.loads((Path(args.dir) / f\"{b_name}.json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n        report = stats(b, a)\n        print_report(f\"{a_name} vs {b_name}\", report\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((Path(args.dir) / f\"{a_name}.json\").read_text())\n        b = json.loads((Path(args.dir) / f\"{b_name}.json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n        report = stats(b, a)\n        print_report(f\"{a_name} vs {b_name}\", report)\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((Path(args.dir) / f\"{a_name}.json\").read_text())\n        b = json.loads((Path(args.dir) / f\"{b_name}.json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n        report = stats(b, a)\n        print_report(f\"{a_name} vs {b_name}\", report)\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((Path(args.dir) / f\"{a_name}.json\").read_text())\n        b = json.loads((Path(args.dir) / f\"{b_name}.json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n        report = stats(b, a)\n        print_report(f\"{a_name} vs {b_name}\", report)\n       \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((Path(args.dir) / f\"{a_name}.json\").read_text())\n        b = json.loads((Path(args.dir) / f\"{b_name}.json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n        report = stats(b, a)\n        print_report(f\"{a_name} vs {b_name}\", report)\n        return\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((Path(args.dir) / f\"{a_name}.json\").read_text())\n        b = json.loads((Path(args.dir) / f\"{b_name}.json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n        report = stats(b, a)\n        print_report(f\"{a_name} vs {b_name}\", report)\n        return\n\n\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((Path(args.dir) / f\"{a_name}.json\").read_text())\n        b = json.loads((Path(args.dir) / f\"{b_name}.json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n        report = stats(b, a)\n        print_report(f\"{a_name} vs {b_name}\", report)\n        return\n\n\nif\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((Path(args.dir) / f\"{a_name}.json\").read_text())\n        b = json.loads((Path(args.dir) / f\"{b_name}.json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n        report = stats(b, a)\n        print_report(f\"{a_name} vs {b_name}\", report)\n        return\n\n\nif __\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((Path(args.dir) / f\"{a_name}.json\").read_text())\n        b = json.loads((Path(args.dir) / f\"{b_name}.json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n        report = stats(b, a)\n        print_report(f\"{a_name} vs {b_name}\", report)\n        return\n\n\nif __name\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((Path(args.dir) / f\"{a_name}.json\").read_text())\n        b = json.loads((Path(args.dir) / f\"{b_name}.json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n        report = stats(b, a)\n        print_report(f\"{a_name} vs {b_name}\", report)\n        return\n\n\nif __name__\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((Path(args.dir) / f\"{a_name}.json\").read_text())\n        b = json.loads((Path(args.dir) / f\"{b_name}.json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n        report = stats(b, a)\n        print_report(f\"{a_name} vs {b_name}\", report)\n        return\n\n\nif __name__ ==\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((Path(args.dir) / f\"{a_name}.json\").read_text())\n        b = json.loads((Path(args.dir) / f\"{b_name}.json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n        report = stats(b, a)\n        print_report(f\"{a_name} vs {b_name}\", report)\n        return\n\n\nif __name__ == \"__\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((Path(args.dir) / f\"{a_name}.json\").read_text())\n        b = json.loads((Path(args.dir) / f\"{b_name}.json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n        report = stats(b, a)\n        print_report(f\"{a_name} vs {b_name}\", report)\n        return\n\n\nif __name__ == \"__main\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((Path(args.dir) / f\"{a_name}.json\").read_text())\n        b = json.loads((Path(args.dir) / f\"{b_name}.json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n        report = stats(b, a)\n        print_report(f\"{a_name} vs {b_name}\", report)\n        return\n\n\nif __name__ == \"__main__\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((Path(args.dir) / f\"{a_name}.json\").read_text())\n        b = json.loads((Path(args.dir) / f\"{b_name}.json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n        report = stats(b, a)\n        print_report(f\"{a_name} vs {b_name}\", report)\n        return\n\n\nif __name__ == \"__main__\":\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((Path(args.dir) / f\"{a_name}.json\").read_text())\n        b = json.loads((Path(args.dir) / f\"{b_name}.json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n        report = stats(b, a)\n        print_report(f\"{a_name} vs {b_name}\", report)\n        return\n\n\nif __name__ == \"__main__\":\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((Path(args.dir) / f\"{a_name}.json\").read_text())\n        b = json.loads((Path(args.dir) / f\"{b_name}.json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n        report = stats(b, a)\n        print_report(f\"{a_name} vs {b_name}\", report)\n        return\n\n\nif __name__ == \"__main__\":\n   \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((Path(args.dir) / f\"{a_name}.json\").read_text())\n        b = json.loads((Path(args.dir) / f\"{b_name}.json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n        report = stats(b, a)\n        print_report(f\"{a_name} vs {b_name}\", report)\n        return\n\n\nif __name__ == \"__main__\":\n    main\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((Path(args.dir) / f\"{a_name}.json\").read_text())\n        b = json.loads((Path(args.dir) / f\"{b_name}.json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n        report = stats(b, a)\n        print_report(f\"{a_name} vs {b_name}\", report)\n        return\n\n\nif __name__ == \"__main__\":\n    main()\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((Path(args.dir) / f\"{a_name}.json\").read_text())\n        b = json.loads((Path(args.dir) / f\"{b_name}.json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n        report = stats(b, a)\n        print_report(f\"{a_name} vs {b_name}\", report)\n        return\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((Path(args.dir) / f\"{a_name}.json\").read_text())\n        b = json.loads((Path(args.dir) / f\"{b_name}.json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n        report = stats(b, a)\n        print_report(f\"{a_name} vs {b_name}\", report)\n        return\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((Path(args.dir) / f\"{a_name}.json\").read_text())\n        b = json.loads((Path(args.dir) / f\"{b_name}.json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n        report = stats(b, a)\n        print_report(f\"{a_name} vs {b_name}\", report)\n        return\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((Path(args.dir) / f\"{a_name}.json\").read_text())\n        b = json.loads((Path(args.dir) / f\"{b_name}.json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n        report = stats(b, a)\n        print_report(f\"{a_name} vs {b_name}\", report)\n        return\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((Path(args.dir) / f\"{a_name}.json\").read_text())\n        b = json.loads((Path(args.dir) / f\"{b_name}.json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n        report = stats(b, a)\n        print_report(f\"{a_name} vs {b_name}\", report)\n        return\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((Path(args.dir) / f\"{a_name}.json\").read_text())\n        b = json.loads((Path(args.dir) / f\"{b_name}.json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n        report = stats(b, a)\n        print_report(f\"{a_name} vs {b_name}\", report)\n        return\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((Path(args.dir) / f\"{a_name}.json\").read_text())\n        b = json.loads((Path(args.dir) / f\"{b_name}.json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n        report = stats(b, a)\n        print_report(f\"{a_name} vs {b_name}\", report)\n        return\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((Path(args.dir) / f\"{a_name}.json\").read_text())\n        b = json.loads((Path(args.dir) / f\"{b_name}.json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n        report = stats(b, a)\n        print_report(f\"{a_name} vs {b_name}\", report)\n        return\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((Path(args.dir) / f\"{a_name}.json\").read_text())\n        b = json.loads((Path(args.dir) / f\"{b_name}.json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n        report = stats(b, a)\n        print_report(f\"{a_name} vs {b_name}\", report)\n        return\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((Path(args.dir) / f\"{a_name}.json\").read_text())\n        b = json.loads((Path(args.dir) / f\"{b_name}.json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n        report = stats(b, a)\n        print_report(f\"{a_name} vs {b_name}\", report)\n        return\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((Path(args.dir) / f\"{a_name}.json\").read_text())\n        b = json.loads((Path(args.dir) / f\"{b_name}.json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n        report = stats(b, a)\n        print_report(f\"{a_name} vs {b_name}\", report)\n        return\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-for\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((Path(args.dir) / f\"{a_name}.json\").read_text())\n        b = json.loads((Path(args.dir) / f\"{b_name}.json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n        report = stats(b, a)\n        print_report(f\"{a_name} vs {b_name}\", report)\n        return\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((Path(args.dir) / f\"{a_name}.json\").read_text())\n        b = json.loads((Path(args.dir) / f\"{b_name}.json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n        report = stats(b, a)\n        print_report(f\"{a_name} vs {b_name}\", report)\n        return\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced log\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((Path(args.dir) / f\"{a_name}.json\").read_text())\n        b = json.loads((Path(args.dir) / f\"{b_name}.json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n        report = stats(b, a)\n        print_report(f\"{a_name} vs {b_name}\", report)\n        return\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((Path(args.dir) / f\"{a_name}.json\").read_text())\n        b = json.loads((Path(args.dir) / f\"{b_name}.json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n        report = stats(b, a)\n        print_report(f\"{a_name} vs {b_name}\", report)\n        return\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((Path(args.dir) / f\"{a_name}.json\").read_text())\n        b = json.loads((Path(args.dir) / f\"{b_name}.json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n        report = stats(b, a)\n        print_report(f\"{a_name} vs {b_name}\", report)\n        return\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle:\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((Path(args.dir) / f\"{a_name}.json\").read_text())\n        b = json.loads((Path(args.dir) / f\"{b_name}.json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n        report = stats(b, a)\n        print_report(f\"{a_name} vs {b_name}\", report)\n        return\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle: capture\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((Path(args.dir) / f\"{a_name}.json\").read_text())\n        b = json.loads((Path(args.dir) / f\"{b_name}.json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n        report = stats(b, a)\n        print_report(f\"{a_name} vs {b_name}\", report)\n        return\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle: capture a\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((Path(args.dir) / f\"{a_name}.json\").read_text())\n        b = json.loads((Path(args.dir) / f\"{b_name}.json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n        report = stats(b, a)\n        print_report(f\"{a_name} vs {b_name}\", report)\n        return\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle: capture a reference\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((Path(args.dir) / f\"{a_name}.json\").read_text())\n        b = json.loads((Path(args.dir) / f\"{b_name}.json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n        report = stats(b, a)\n        print_report(f\"{a_name} vs {b_name}\", report)\n        return\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle: capture a reference,\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((Path(args.dir) / f\"{a_name}.json\").read_text())\n        b = json.loads((Path(args.dir) / f\"{b_name}.json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n        report = stats(b, a)\n        print_report(f\"{a_name} vs {b_name}\", report)\n        return\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle: capture a reference, or\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((Path(args.dir) / f\"{a_name}.json\").read_text())\n        b = json.loads((Path(args.dir) / f\"{b_name}.json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n        report = stats(b, a)\n        print_report(f\"{a_name} vs {b_name}\", report)\n        return\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle: capture a reference, or check\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((Path(args.dir) / f\"{a_name}.json\").read_text())\n        b = json.loads((Path(args.dir) / f\"{b_name}.json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n        report = stats(b, a)\n        print_report(f\"{a_name} vs {b_name}\", report)\n        return\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle: capture a reference, or check a\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((Path(args.dir) / f\"{a_name}.json\").read_text())\n        b = json.loads((Path(args.dir) / f\"{b_name}.json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n        report = stats(b, a)\n        print_report(f\"{a_name} vs {b_name}\", report)\n        return\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle: capture a reference, or check a run\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((Path(args.dir) / f\"{a_name}.json\").read_text())\n        b = json.loads((Path(args.dir) / f\"{b_name}.json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n        report = stats(b, a)\n        print_report(f\"{a_name} vs {b_name}\", report)\n        return\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle: capture a reference, or check a run against\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((Path(args.dir) / f\"{a_name}.json\").read_text())\n        b = json.loads((Path(args.dir) / f\"{b_name}.json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n        report = stats(b, a)\n        print_report(f\"{a_name} vs {b_name}\", report)\n        return\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle: capture a reference, or check a run against it\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((Path(args.dir) / f\"{a_name}.json\").read_text())\n        b = json.loads((Path(args.dir) / f\"{b_name}.json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n        report = stats(b, a)\n        print_report(f\"{a_name} vs {b_name}\", report)\n        return\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle: capture a reference, or check a run against it.\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((Path(args.dir) / f\"{a_name}.json\").read_text())\n        b = json.loads((Path(args.dir) / f\"{b_name}.json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n        report = stats(b, a)\n        print_report(f\"{a_name} vs {b_name}\", report)\n        return\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle: capture a reference, or check a run against it.\n\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((Path(args.dir) / f\"{a_name}.json\").read_text())\n        b = json.loads((Path(args.dir) / f\"{b_name}.json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n        report = stats(b, a)\n        print_report(f\"{a_name} vs {b_name}\", report)\n        return\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle: capture a reference, or check a run against it.\n\n \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((Path(args.dir) / f\"{a_name}.json\").read_text())\n        b = json.loads((Path(args.dir) / f\"{b_name}.json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n        report = stats(b, a)\n        print_report(f\"{a_name} vs {b_name}\", report)\n        return\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle: capture a reference, or check a run against it.\n\n  python\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((Path(args.dir) / f\"{a_name}.json\").read_text())\n        b = json.loads((Path(args.dir) / f\"{b_name}.json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n        report = stats(b, a)\n        print_report(f\"{a_name} vs {b_name}\", report)\n        return\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle: capture a reference, or check a run against it.\n\n  python3\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((Path(args.dir) / f\"{a_name}.json\").read_text())\n        b = json.loads((Path(args.dir) / f\"{b_name}.json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n        report = stats(b, a)\n        print_report(f\"{a_name} vs {b_name}\", report)\n        return\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle: capture a reference, or check a run against it.\n\n  python3 log\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((Path(args.dir) / f\"{a_name}.json\").read_text())\n        b = json.loads((Path(args.dir) / f\"{b_name}.json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n        report = stats(b, a)\n        print_report(f\"{a_name} vs {b_name}\", report)\n        return\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle: capture a reference, or check a run against it.\n\n  python3 logprob\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((Path(args.dir) / f\"{a_name}.json\").read_text())\n        b = json.loads((Path(args.dir) / f\"{b_name}.json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n        report = stats(b, a)\n        print_report(f\"{a_name} vs {b_name}\", report)\n        return\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle: capture a reference, or check a run against it.\n\n  python3 logprob_diff\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((Path(args.dir) / f\"{a_name}.json\").read_text())\n        b = json.loads((Path(args.dir) / f\"{b_name}.json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n        report = stats(b, a)\n        print_report(f\"{a_name} vs {b_name}\", report)\n        return\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle: capture a reference, or check a run against it.\n\n  python3 logprob_diff.py\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((Path(args.dir) / f\"{a_name}.json\").read_text())\n        b = json.loads((Path(args.dir) / f\"{b_name}.json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n        report = stats(b, a)\n        print_report(f\"{a_name} vs {b_name}\", report)\n        return\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle: capture a reference, or check a run against it.\n\n  python3 logprob_diff.py save\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((Path(args.dir) / f\"{a_name}.json\").read_text())\n        b = json.loads((Path(args.dir) / f\"{b_name}.json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n        report = stats(b, a)\n        print_report(f\"{a_name} vs {b_name}\", report)\n        return\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle: capture a reference, or check a run against it.\n\n  python3 logprob_diff.py save \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((Path(args.dir) / f\"{a_name}.json\").read_text())\n        b = json.loads((Path(args.dir) / f\"{b_name}.json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n        report = stats(b, a)\n        print_report(f\"{a_name} vs {b_name}\", report)\n        return\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle: capture a reference, or check a run against it.\n\n  python3 logprob_diff.py save  NAME\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", default=str(D))\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        Path(args.dir).mkdir(parents=True, exist_ok=True)\n        path = Path(args.dir) / f\"{name}.json\"\n        path.write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(\n                f\"  {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_tokens']:3d} chars={row['prompt_chars']:7d}\"\n            )\n        print(f\"  saved {path}\")\n        return\n\n    if args.cmd == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\n        reference = json.loads((Path(args.dir) / f\"{ref_name}.json\").read_text())\n        current = collect(reference)\n        report = stats(reference, current)\n        print_report(f\"{cur_name} vs {ref_name}\", report)\n        return\n\n    if args.cmd == \"compare\":\n        a_name, b_name = args.names[0], args.names[1]\n        a = json.loads((Path(args.dir) / f\"{a_name}.json\").read_text())\n        b = json.loads((Path(args.dir) / f\"{b_name}.json\").read_text())\n        report = stats(a, b)\n        print_report(f\"{b_name} vs {a_name}\", report)\n        report = stats(b, a)\n        print_report(f\"{a_name} vs {b_name}\", report)\n        return\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle: capture a reference, or check a run against it.\n\n  python3 logprob_diff.py save  NAME\n",
 "ttft_seconds": 79.27677321698866,
 "status": "ok",
 "tg_tokens_per_s": 29.38720967261426,
 "decode_seconds_per_token": 0.03402840933659289,
 "individual_token_timing": true,
 "event_intervals_ms": {
  "n": 511,
  "median": 34.164676995715126,
  "p95": 42.41820600873325,
  "mean": 34.02840933659289
 },
 "class": "code",
 "requested_input_tokens": 131072
}
