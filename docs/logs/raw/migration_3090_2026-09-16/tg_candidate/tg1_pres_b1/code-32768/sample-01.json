{
 "started_utc": "2026-09-16T23:22:08.607419+00:00",
 "wall_seconds": 29.283526605999214,
 "prompt_tokens": 32768,
 "completion_tokens": 512,
 "stream_events": 512,
 "ended_utc": "2026-09-16T23:22:37.891088+00:00",
 "warmup": false,
 "sample_index": 1,
 "requested_decode_tokens": 512,
 "gpu": {
  "n": 105,
  "temperature_c": {
   "min": 77.0,
   "max": 79.0,
   "mean": 78.0
  },
  "power_w": {
   "min": 246.88,
   "max": 281.97,
   "mean": 264.6631428571429
  },
  "util_pct": {
   "min": 65.0,
   "max": 99.0,
   "mean": 93.03809523809524
  },
  "clock_sm_mhz": {
   "min": 1125.0,
   "max": 1905.0,
   "mean": 1736.7142857142858
  },
  "clock_mem_mhz": {
   "min": 9501.0,
   "max": 9501.0,
   "mean": 9501.0
  },
  "memory_free_mib": {
   "min": 1526.0
  }
 },
 "output_text_sha256": "9ae68190fb89ef47ee16654b9119d80cb57b9c3db3b449bce8c929d6e7264587",
 "output_text": "++       +        \"\"\"+        \"\"\"Grow+        \"\"\"Grow every+        \"\"\"Grow every layer+        \"\"\"Grow every layer until+        \"\"\"Grow every layer until `+        \"\"\"Grow every layer until `reserve+        \"\"\"Grow every layer until `reserve_mb+        \"\"\"Grow every layer until `reserve_mb`+        \"\"\"Grow every layer until `reserve_mb` of+        \"\"\"Grow every layer until `reserve_mb` of VR+        \"\"\"Grow every layer until `reserve_mb` of VRAM+        \"\"\"Grow every layer until `reserve_mb` of VRAM is+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(res+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) <<+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <=+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free -+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) //+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E *+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).k+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur ++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VR+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                   +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time()+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() -+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0]+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] /+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**3+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+   +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Sh+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb`+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VR+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(res+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) <<+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0]+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] <+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            self.set+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            self.set_S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            self.set_S(cur+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            self.set_S(cur -+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            self.set_S(cur - step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            self.set_S(cur - step)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            self.set_S(cur - step)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            self.set_S(cur - step)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            self.set_S(cur - step)\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            self.set_S(cur - step)\n+            cur+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            self.set_S(cur - step)\n+            cur =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            self.set_S(cur - step)\n+            cur = max+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            self.set_S(cur - step)\n+            cur = max(st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            self.set_S(cur - step)\n+            cur = max(st.S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            self.set_S(cur - step)\n+            cur = max(st.S for+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            self.set_S(cur - step)\n+            cur = max(st.S for st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            self.set_S(cur - step)\n+            cur = max(st.S for st in+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            self.set_S(cur - step)\n+            cur = max(st.S for st in self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            self.set_S(cur - step)\n+            cur = max(st.S for st in self.layers+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            self.set_S(cur - step)\n+            cur = max(st.S for st in self.layers.values+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            self.set_S(cur - step)\n+            cur = max(st.S for st in self.layers.values())+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            self.set_S(cur - step)\n+            cur = max(st.S for st in self.layers.values())\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            self.set_S(cur - step)\n+            cur = max(st.S for st in self.layers.values())\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            self.set_S(cur - step)\n+            cur = max(st.S for st in self.layers.values())\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            self.set_S(cur - step)\n+            cur = max(st.S for st in self.layers.values())\n+        logger+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            self.set_S(cur - step)\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            self.set_S(cur - step)\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            self.set_S(cur - step)\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            self.set_S(cur - step)\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            self.set_S(cur - step)\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            self.set_S(cur - step)\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            self.set_S(cur - step)\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            self.set_S(cur - step)\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            self.set_S(cur - step)\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d (+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            self.set_S(cur - step)\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d (reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            self.set_S(cur - step)\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d (reserve %+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            self.set_S(cur - step)\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d (reserve %d+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            self.set_S(cur - step)\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d (reserve %d MB+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            self.set_S(cur - step)\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d (reserve %d MB)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            self.set_S(cur - step)\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d (reserve %d MB) in+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            self.set_S(cur - step)\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d (reserve %d MB) in %.+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            self.set_S(cur - step)\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d (reserve %d MB) in %.2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            self.set_S(cur - step)\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d (reserve %d MB) in %.2fs+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            self.set_S(cur - step)\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d (reserve %d MB) in %.2fs,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            self.set_S(cur - step)\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d (reserve %d MB) in %.2fs, VR+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            self.set_S(cur - step)\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d (reserve %d MB) in %.2fs, VRAM+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            self.set_S(cur - step)\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d (reserve %d MB) in %.2fs, VRAM free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            self.set_S(cur - step)\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d (reserve %d MB) in %.2fs, VRAM free %.+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            self.set_S(cur - step)\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d (reserve %d MB) in %.2fs, VRAM free %.2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            self.set_S(cur - step)\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            self.set_S(cur - step)\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            self.set_S(cur - step)\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            self.set_S(cur - step)\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            self.set_S(cur - step)\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            self.set_S(cur - step)\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                   +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            self.set_S(cur - step)\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            self.set_S(cur - step)\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            self.set_S(cur - step)\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            self.set_S(cur - step)\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            self.set_S(cur - step)\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            self.set_S(cur - step)\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            self.set_S(cur - step)\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            self.set_S(cur - step)\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time()+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            self.set_S(cur - step)\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() -+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            self.set_S(cur - step)\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            self.set_S(cur - step)\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            self.set_S(cur - step)\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            self.set_S(cur - step)\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            self.set_S(cur - step)\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            self.set_S(cur - step)\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            self.set_S(cur - step)\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            self.set_S(cur - step)\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            self.set_S(cur - step)\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            self.set_S(cur - step)\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            self.set_S(cur - step)\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0]+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            self.set_S(cur - step)\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] /+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            self.set_S(cur - step)\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            self.set_S(cur - step)\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            self.set_S(cur - step)\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            self.set_S(cur - step)\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**3+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            self.set_S(cur - step)\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            self.set_S(cur - step)\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            self.set_S(cur - step)\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            self.set_S(cur - step)\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            self.set_S(cur - step)\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            self.set_S(cur - step)\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            self.set_S(cur - step)\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            self.set_S(cur - step)\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_reg+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            self.set_S(cur - step)\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            self.set_S(cur - step)\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            self.set_S(cur - step)\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            self.set_S(cur - step)\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            self.set_S(cur - step)\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            self.set_S(cur - step)\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            self.set_S(cur - step)\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+        return+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            self.set_S(cur - step)\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+        return cur+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            self.set_S(cur - step)\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+        return cur\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            self.set_S(cur - step)\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+        return cur\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            self.set_S(cur - step)\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+        return cur\n+\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            self.set_S(cur - step)\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+        return cur\n+\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            self.set_S(cur - step)\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+        return cur\n+\n+   +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            self.set_S(cur - step)\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+        return cur\n+\n+    def+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            self.set_S(cur - step)\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+        return cur\n+\n+    def _+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            self.set_S(cur - step)\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+        return cur\n+\n+    def _reg+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            self.set_S(cur - step)\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+        return cur\n+\n+    def _regrow+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            self.set_S(cur - step)\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+        return cur\n+\n+    def _regrow(self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            self.set_S(cur - step)\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+        return cur\n+\n+    def _regrow(self):+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            self.set_S(cur - step)\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+        return cur\n+\n+    def _regrow(self):\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            self.set_S(cur - step)\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+        return cur\n+\n+    def _regrow(self):\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            self.set_S(cur - step)\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+        return cur\n+\n+    def _regrow(self):\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            self.set_S(cur - step)\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+        return cur\n+\n+    def _regrow(self):\n+        \"\"\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            self.set_S(cur - step)\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+        return cur\n+\n+    def _regrow(self):\n+        \"\"\"After+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            self.set_S(cur - step)\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+        return cur\n+\n+    def _regrow(self):\n+        \"\"\"After a+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            self.set_S(cur - step)\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+        return cur\n+\n+    def _regrow(self):\n+        \"\"\"After a forced+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            self.set_S(cur - step)\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+        return cur\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            self.set_S(cur - step)\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+        return cur\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            self.set_S(cur - step)\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+        return cur\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            self.set_S(cur - step)\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+        return cur\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            self.set_S(cur - step)\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+        return cur\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            self.set_S(cur - step)\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+        return cur\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            self.set_S(cur - step)\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+        return cur\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            self.set_S(cur - step)\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+        return cur\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            self.set_S(cur - step)\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+        return cur\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            self.set_S(cur - step)\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+        return cur\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            self.set_S(cur - step)\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+        return cur\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            self.set_S(cur - step)\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+        return cur\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve is+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            self.set_S(cur - step)\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+        return cur\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve is the+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            self.set_S(cur - step)\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+        return cur\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve is the target+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            self.set_S(cur - step)\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+        return cur\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve is the target).+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            self.set_S(cur - step)\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+        return cur\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve is the target).\"\"\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            self.set_S(cur - step)\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+        return cur\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve is the target).\"\"\"\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            self.set_S(cur - step)\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+        return cur\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve is the target).\"\"\"\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            self.set_S(cur - step)\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+        return cur\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve is the target).\"\"\"\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            self.set_S(cur - step)\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+        return cur\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve is the target).\"\"\"\n+        if+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            self.set_S(cur - step)\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+        return cur\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve is the target).\"\"\"\n+        if not+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            self.set_S(cur - step)\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+        return cur\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve is the target).\"\"\"\n+        if not self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            self.set_S(cur - step)\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+        return cur\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve is the target).\"\"\"\n+        if not self.pending+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            self.set_S(cur - step)\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+        return cur\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve is the target).\"\"\"\n+        if not self.pending_reg+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            self.set_S(cur - step)\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+        return cur\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve is the target).\"\"\"\n+        if not self.pending_regrow+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            self.set_S(cur - step)\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+        return cur\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve is the target).\"\"\"\n+        if not self.pending_regrow:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            self.set_S(cur - step)\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+        return cur\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve is the target).\"\"\"\n+        if not self.pending_regrow:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            self.set_S(cur - step)\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+        return cur\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve is the target).\"\"\"\n+        if not self.pending_regrow:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            self.set_S(cur - step)\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+        return cur\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve is the target).\"\"\"\n+        if not self.pending_regrow:\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            self.set_S(cur - step)\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+        return cur\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve is the target).\"\"\"\n+        if not self.pending_regrow:\n+            return+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            self.set_S(cur - step)\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+        return cur\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve is the target).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            self.set_S(cur - step)\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+        return cur\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve is the target).\"\"\"\n+        if not self.pending_regrow:\n+            return\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            self.set_S(cur - step)\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+        return cur\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve is the target).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            self.set_S(cur - step)\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+        return cur\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve is the target).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            self.set_S(cur - step)\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+        return cur\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve is the target).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            self.set_S(cur - step)\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+        return cur\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve is the target).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            self.set_S(cur - step)\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+        return cur\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve is the target).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            room = (free - reserve) // (self.E * sum(k.rb for k in next(iter(self.layers.values())).kinds.values()))\n+            step_n = min(step, max(1, room))\n+            try:\n+                self.set_S(cur + step_n)\n+            except Exception:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        return cur\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve:\n+            self.set_S(cur - step)\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d (reserve %d MB) in %.2fs, VRAM free %.2f GB\",\n+                    cur, reserve_mb, time.time() - t0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+        return cur\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the reserve is the target).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()",
 "ttft_seconds": 13.215595360990847,
 "status": "ok",
 "tg_tokens_per_s": 31.802626125984908,
 "decode_seconds_per_token": 0.03144394415852759,
 "individual_token_timing": true,
 "event_intervals_ms": {
  "n": 511,
  "median": 31.201197009067982,
  "p95": 40.24455200124066,
  "mean": 31.44394415852759
 },
 "class": "code",
 "requested_input_tokens": 32768
}
