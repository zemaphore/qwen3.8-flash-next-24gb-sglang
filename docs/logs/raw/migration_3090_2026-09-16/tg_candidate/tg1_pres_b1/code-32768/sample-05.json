{
 "started_utc": "2026-09-16T23:24:05.586908+00:00",
 "wall_seconds": 29.733010929005104,
 "prompt_tokens": 32768,
 "completion_tokens": 512,
 "stream_events": 512,
 "ended_utc": "2026-09-16T23:24:35.320063+00:00",
 "warmup": false,
 "sample_index": 5,
 "requested_decode_tokens": 512,
 "gpu": {
  "n": 107,
  "temperature_c": {
   "min": 77.0,
   "max": 80.0,
   "mean": 78.91588785046729
  },
  "power_w": {
   "min": 248.33,
   "max": 279.71,
   "mean": 263.82252336448596
  },
  "util_pct": {
   "min": 53.0,
   "max": 99.0,
   "mean": 93.34579439252336
  },
  "clock_sm_mhz": {
   "min": 1155.0,
   "max": 1905.0,
   "mean": 1736.3551401869158
  },
  "clock_mem_mhz": {
   "min": 9501.0,
   "max": 9501.0,
   "mean": 9501.0
  },
  "memory_free_mib": {
   "min": 1448.0
  }
 },
 "output_text_sha256": "ebf9d2113bec7cbb204a2ffec91d36e9d464bca861c7985b75c96947ab382928",
 "output_text": "++       +        \"\"\"+        \"\"\"Grow+        \"\"\"Grow every+        \"\"\"Grow every layer+        \"\"\"Grow every layer until+        \"\"\"Grow every layer until `+        \"\"\"Grow every layer until `reserve+        \"\"\"Grow every layer until `reserve_mb+        \"\"\"Grow every layer until `reserve_mb`+        \"\"\"Grow every layer until `reserve_mb` of+        \"\"\"Grow every layer until `reserve_mb` of VR+        \"\"\"Grow every layer until `reserve_mb` of VRAM+        \"\"\"Grow every layer until `reserve_mb` of VRAM is+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >>+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >=+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(res+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S ++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (Runtime+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, Arena+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(),+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S -+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+   +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Sh+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb`+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VR+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >>+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >=+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(res+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(),+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S -+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S ==+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+   +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _reg+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_reg+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.reg+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_reg+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except (+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except (Runtime+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except (RuntimeError+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except (RuntimeError,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except (RuntimeError, Arena+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except (RuntimeError, ArenaOOM+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except (RuntimeError, ArenaOOM):+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except (RuntimeError, ArenaOOM):\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except (RuntimeError, ArenaOOM):\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except (RuntimeError, ArenaOOM):\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except (RuntimeError, ArenaOOM):\n+            pass+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except (RuntimeError, ArenaOOM):\n+            pass\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except (RuntimeError, ArenaOOM):\n+            pass\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except (RuntimeError, ArenaOOM):\n+            pass\n+\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except (RuntimeError, ArenaOOM):\n+            pass\n+\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except (RuntimeError, ArenaOOM):\n+            pass\n+\n+   +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except (RuntimeError, ArenaOOM):\n+            pass\n+\n+    def+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except (RuntimeError, ArenaOOM):\n+            pass\n+\n+    def _+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except (RuntimeError, ArenaOOM):\n+            pass\n+\n+    def _apply+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except (RuntimeError, ArenaOOM):\n+            pass\n+\n+    def _apply(self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except (RuntimeError, ArenaOOM):\n+            pass\n+\n+    def _apply(self,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except (RuntimeError, ArenaOOM):\n+            pass\n+\n+    def _apply(self, cmd+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except (RuntimeError, ArenaOOM):\n+            pass\n+\n+    def _apply(self, cmd):+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except (RuntimeError, ArenaOOM):\n+            pass\n+\n+    def _apply(self, cmd):\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except (RuntimeError, ArenaOOM):\n+            pass\n+\n+    def _apply(self, cmd):\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except (RuntimeError, ArenaOOM):\n+            pass\n+\n+    def _apply(self, cmd):\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except (RuntimeError, ArenaOOM):\n+            pass\n+\n+    def _apply(self, cmd):\n+        parts+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except (RuntimeError, ArenaOOM):\n+            pass\n+\n+    def _apply(self, cmd):\n+        parts =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except (RuntimeError, ArenaOOM):\n+            pass\n+\n+    def _apply(self, cmd):\n+        parts = cmd+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except (RuntimeError, ArenaOOM):\n+            pass\n+\n+    def _apply(self, cmd):\n+        parts = cmd.split+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except (RuntimeError, ArenaOOM):\n+            pass\n+\n+    def _apply(self, cmd):\n+        parts = cmd.split()+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except (RuntimeError, ArenaOOM):\n+            pass\n+\n+    def _apply(self, cmd):\n+        parts = cmd.split()\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except (RuntimeError, ArenaOOM):\n+            pass\n+\n+    def _apply(self, cmd):\n+        parts = cmd.split()\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except (RuntimeError, ArenaOOM):\n+            pass\n+\n+    def _apply(self, cmd):\n+        parts = cmd.split()\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except (RuntimeError, ArenaOOM):\n+            pass\n+\n+    def _apply(self, cmd):\n+        parts = cmd.split()\n+        if+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except (RuntimeError, ArenaOOM):\n+            pass\n+\n+    def _apply(self, cmd):\n+        parts = cmd.split()\n+        if not+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except (RuntimeError, ArenaOOM):\n+            pass\n+\n+    def _apply(self, cmd):\n+        parts = cmd.split()\n+        if not parts+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except (RuntimeError, ArenaOOM):\n+            pass\n+\n+    def _apply(self, cmd):\n+        parts = cmd.split()\n+        if not parts:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except (RuntimeError, ArenaOOM):\n+            pass\n+\n+    def _apply(self, cmd):\n+        parts = cmd.split()\n+        if not parts:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except (RuntimeError, ArenaOOM):\n+            pass\n+\n+    def _apply(self, cmd):\n+        parts = cmd.split()\n+        if not parts:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except (RuntimeError, ArenaOOM):\n+            pass\n+\n+    def _apply(self, cmd):\n+        parts = cmd.split()\n+        if not parts:\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except (RuntimeError, ArenaOOM):\n+            pass\n+\n+    def _apply(self, cmd):\n+        parts = cmd.split()\n+        if not parts:\n+            return+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except (RuntimeError, ArenaOOM):\n+            pass\n+\n+    def _apply(self, cmd):\n+        parts = cmd.split()\n+        if not parts:\n+            return\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except (RuntimeError, ArenaOOM):\n+            pass\n+\n+    def _apply(self, cmd):\n+        parts = cmd.split()\n+        if not parts:\n+            return\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except (RuntimeError, ArenaOOM):\n+            pass\n+\n+    def _apply(self, cmd):\n+        parts = cmd.split()\n+        if not parts:\n+            return\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except (RuntimeError, ArenaOOM):\n+            pass\n+\n+    def _apply(self, cmd):\n+        parts = cmd.split()\n+        if not parts:\n+            return\n+        op+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except (RuntimeError, ArenaOOM):\n+            pass\n+\n+    def _apply(self, cmd):\n+        parts = cmd.split()\n+        if not parts:\n+            return\n+        op =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except (RuntimeError, ArenaOOM):\n+            pass\n+\n+    def _apply(self, cmd):\n+        parts = cmd.split()\n+        if not parts:\n+            return\n+        op = parts+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except (RuntimeError, ArenaOOM):\n+            pass\n+\n+    def _apply(self, cmd):\n+        parts = cmd.split()\n+        if not parts:\n+            return\n+        op = parts[+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except (RuntimeError, ArenaOOM):\n+            pass\n+\n+    def _apply(self, cmd):\n+        parts = cmd.split()\n+        if not parts:\n+            return\n+        op = parts[0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except (RuntimeError, ArenaOOM):\n+            pass\n+\n+    def _apply(self, cmd):\n+        parts = cmd.split()\n+        if not parts:\n+            return\n+        op = parts[0]+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except (RuntimeError, ArenaOOM):\n+            pass\n+\n+    def _apply(self, cmd):\n+        parts = cmd.split()\n+        if not parts:\n+            return\n+        op = parts[0]\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except (RuntimeError, ArenaOOM):\n+            pass\n+\n+    def _apply(self, cmd):\n+        parts = cmd.split()\n+        if not parts:\n+            return\n+        op = parts[0]\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except (RuntimeError, ArenaOOM):\n+            pass\n+\n+    def _apply(self, cmd):\n+        parts = cmd.split()\n+        if not parts:\n+            return\n+        op = parts[0]\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except (RuntimeError, ArenaOOM):\n+            pass\n+\n+    def _apply(self, cmd):\n+        parts = cmd.split()\n+        if not parts:\n+            return\n+        op = parts[0]\n+        if+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except (RuntimeError, ArenaOOM):\n+            pass\n+\n+    def _apply(self, cmd):\n+        parts = cmd.split()\n+        if not parts:\n+            return\n+        op = parts[0]\n+        if op+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except (RuntimeError, ArenaOOM):\n+            pass\n+\n+    def _apply(self, cmd):\n+        parts = cmd.split()\n+        if not parts:\n+            return\n+        op = parts[0]\n+        if op ==+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except (RuntimeError, ArenaOOM):\n+            pass\n+\n+    def _apply(self, cmd):\n+        parts = cmd.split()\n+        if not parts:\n+            return\n+        op = parts[0]\n+        if op == \"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except (RuntimeError, ArenaOOM):\n+            pass\n+\n+    def _apply(self, cmd):\n+        parts = cmd.split()\n+        if not parts:\n+            return\n+        op = parts[0]\n+        if op == \"S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except (RuntimeError, ArenaOOM):\n+            pass\n+\n+    def _apply(self, cmd):\n+        parts = cmd.split()\n+        if not parts:\n+            return\n+        op = parts[0]\n+        if op == \"S\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except (RuntimeError, ArenaOOM):\n+            pass\n+\n+    def _apply(self, cmd):\n+        parts = cmd.split()\n+        if not parts:\n+            return\n+        op = parts[0]\n+        if op == \"S\" and+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except (RuntimeError, ArenaOOM):\n+            pass\n+\n+    def _apply(self, cmd):\n+        parts = cmd.split()\n+        if not parts:\n+            return\n+        op = parts[0]\n+        if op == \"S\" and len+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except (RuntimeError, ArenaOOM):\n+            pass\n+\n+    def _apply(self, cmd):\n+        parts = cmd.split()\n+        if not parts:\n+            return\n+        op = parts[0]\n+        if op == \"S\" and len(parts+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except (RuntimeError, ArenaOOM):\n+            pass\n+\n+    def _apply(self, cmd):\n+        parts = cmd.split()\n+        if not parts:\n+            return\n+        op = parts[0]\n+        if op == \"S\" and len(parts)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except (RuntimeError, ArenaOOM):\n+            pass\n+\n+    def _apply(self, cmd):\n+        parts = cmd.split()\n+        if not parts:\n+            return\n+        op = parts[0]\n+        if op == \"S\" and len(parts) ==+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except (RuntimeError, ArenaOOM):\n+            pass\n+\n+    def _apply(self, cmd):\n+        parts = cmd.split()\n+        if not parts:\n+            return\n+        op = parts[0]\n+        if op == \"S\" and len(parts) == +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except (RuntimeError, ArenaOOM):\n+            pass\n+\n+    def _apply(self, cmd):\n+        parts = cmd.split()\n+        if not parts:\n+            return\n+        op = parts[0]\n+        if op == \"S\" and len(parts) == 2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except (RuntimeError, ArenaOOM):\n+            pass\n+\n+    def _apply(self, cmd):\n+        parts = cmd.split()\n+        if not parts:\n+            return\n+        op = parts[0]\n+        if op == \"S\" and len(parts) == 2:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except (RuntimeError, ArenaOOM):\n+            pass\n+\n+    def _apply(self, cmd):\n+        parts = cmd.split()\n+        if not parts:\n+            return\n+        op = parts[0]\n+        if op == \"S\" and len(parts) == 2:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except (RuntimeError, ArenaOOM):\n+            pass\n+\n+    def _apply(self, cmd):\n+        parts = cmd.split()\n+        if not parts:\n+            return\n+        op = parts[0]\n+        if op == \"S\" and len(parts) == 2:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except (RuntimeError, ArenaOOM):\n+            pass\n+\n+    def _apply(self, cmd):\n+        parts = cmd.split()\n+        if not parts:\n+            return\n+        op = parts[0]\n+        if op == \"S\" and len(parts) == 2:\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except (RuntimeError, ArenaOOM):\n+            pass\n+\n+    def _apply(self, cmd):\n+        parts = cmd.split()\n+        if not parts:\n+            return\n+        op = parts[0]\n+        if op == \"S\" and len(parts) == 2:\n+            self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except (RuntimeError, ArenaOOM):\n+            pass\n+\n+    def _apply(self, cmd):\n+        parts = cmd.split()\n+        if not parts:\n+            return\n+        op = parts[0]\n+        if op == \"S\" and len(parts) == 2:\n+            self.set+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except (RuntimeError, ArenaOOM):\n+            pass\n+\n+    def _apply(self, cmd):\n+        parts = cmd.split()\n+        if not parts:\n+            return\n+        op = parts[0]\n+        if op == \"S\" and len(parts) == 2:\n+            self.set_S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except (RuntimeError, ArenaOOM):\n+            pass\n+\n+    def _apply(self, cmd):\n+        parts = cmd.split()\n+        if not parts:\n+            return\n+        op = parts[0]\n+        if op == \"S\" and len(parts) == 2:\n+            self.set_S(int+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except (RuntimeError, ArenaOOM):\n+            pass\n+\n+    def _apply(self, cmd):\n+        parts = cmd.split()\n+        if not parts:\n+            return\n+        op = parts[0]\n+        if op == \"S\" and len(parts) == 2:\n+            self.set_S(int(parts+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except (RuntimeError, ArenaOOM):\n+            pass\n+\n+    def _apply(self, cmd):\n+        parts = cmd.split()\n+        if not parts:\n+            return\n+        op = parts[0]\n+        if op == \"S\" and len(parts) == 2:\n+            self.set_S(int(parts[+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except (RuntimeError, ArenaOOM):\n+            pass\n+\n+    def _apply(self, cmd):\n+        parts = cmd.split()\n+        if not parts:\n+            return\n+        op = parts[0]\n+        if op == \"S\" and len(parts) == 2:\n+            self.set_S(int(parts[1+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except (RuntimeError, ArenaOOM):\n+            pass\n+\n+    def _apply(self, cmd):\n+        parts = cmd.split()\n+        if not parts:\n+            return\n+        op = parts[0]\n+        if op == \"S\" and len(parts) == 2:\n+            self.set_S(int(parts[1]))+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except (RuntimeError, ArenaOOM):\n+            pass\n+\n+    def _apply(self, cmd):\n+        parts = cmd.split()\n+        if not parts:\n+            return\n+        op = parts[0]\n+        if op == \"S\" and len(parts) == 2:\n+            self.set_S(int(parts[1]))\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except (RuntimeError, ArenaOOM):\n+            pass\n+\n+    def _apply(self, cmd):\n+        parts = cmd.split()\n+        if not parts:\n+            return\n+        op = parts[0]\n+        if op == \"S\" and len(parts) == 2:\n+            self.set_S(int(parts[1]))\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except (RuntimeError, ArenaOOM):\n+            pass\n+\n+    def _apply(self, cmd):\n+        parts = cmd.split()\n+        if not parts:\n+            return\n+        op = parts[0]\n+        if op == \"S\" and len(parts) == 2:\n+            self.set_S(int(parts[1]))\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except (RuntimeError, ArenaOOM):\n+            pass\n+\n+    def _apply(self, cmd):\n+        parts = cmd.split()\n+        if not parts:\n+            return\n+        op = parts[0]\n+        if op == \"S\" and len(parts) == 2:\n+            self.set_S(int(parts[1]))\n+        elif+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except (RuntimeError, ArenaOOM):\n+            pass\n+\n+    def _apply(self, cmd):\n+        parts = cmd.split()\n+        if not parts:\n+            return\n+        op = parts[0]\n+        if op == \"S\" and len(parts) == 2:\n+            self.set_S(int(parts[1]))\n+        elif op+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except (RuntimeError, ArenaOOM):\n+            pass\n+\n+    def _apply(self, cmd):\n+        parts = cmd.split()\n+        if not parts:\n+            return\n+        op = parts[0]\n+        if op == \"S\" and len(parts) == 2:\n+            self.set_S(int(parts[1]))\n+        elif op ==+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except (RuntimeError, ArenaOOM):\n+            pass\n+\n+    def _apply(self, cmd):\n+        parts = cmd.split()\n+        if not parts:\n+            return\n+        op = parts[0]\n+        if op == \"S\" and len(parts) == 2:\n+            self.set_S(int(parts[1]))\n+        elif op == \"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except (RuntimeError, ArenaOOM):\n+            pass\n+\n+    def _apply(self, cmd):\n+        parts = cmd.split()\n+        if not parts:\n+            return\n+        op = parts[0]\n+        if op == \"S\" and len(parts) == 2:\n+            self.set_S(int(parts[1]))\n+        elif op == \"fill+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except (RuntimeError, ArenaOOM):\n+            pass\n+\n+    def _apply(self, cmd):\n+        parts = cmd.split()\n+        if not parts:\n+            return\n+        op = parts[0]\n+        if op == \"S\" and len(parts) == 2:\n+            self.set_S(int(parts[1]))\n+        elif op == \"fill\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except (RuntimeError, ArenaOOM):\n+            pass\n+\n+    def _apply(self, cmd):\n+        parts = cmd.split()\n+        if not parts:\n+            return\n+        op = parts[0]\n+        if op == \"S\" and len(parts) == 2:\n+            self.set_S(int(parts[1]))\n+        elif op == \"fill\" and+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except (RuntimeError, ArenaOOM):\n+            pass\n+\n+    def _apply(self, cmd):\n+        parts = cmd.split()\n+        if not parts:\n+            return\n+        op = parts[0]\n+        if op == \"S\" and len(parts) == 2:\n+            self.set_S(int(parts[1]))\n+        elif op == \"fill\" and len+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except (RuntimeError, ArenaOOM):\n+            pass\n+\n+    def _apply(self, cmd):\n+        parts = cmd.split()\n+        if not parts:\n+            return\n+        op = parts[0]\n+        if op == \"S\" and len(parts) == 2:\n+            self.set_S(int(parts[1]))\n+        elif op == \"fill\" and len(parts+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except (RuntimeError, ArenaOOM):\n+            pass\n+\n+    def _apply(self, cmd):\n+        parts = cmd.split()\n+        if not parts:\n+            return\n+        op = parts[0]\n+        if op == \"S\" and len(parts) == 2:\n+            self.set_S(int(parts[1]))\n+        elif op == \"fill\" and len(parts)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except (RuntimeError, ArenaOOM):\n+            pass\n+\n+    def _apply(self, cmd):\n+        parts = cmd.split()\n+        if not parts:\n+            return\n+        op = parts[0]\n+        if op == \"S\" and len(parts) == 2:\n+            self.set_S(int(parts[1]))\n+        elif op == \"fill\" and len(parts) ==+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except (RuntimeError, ArenaOOM):\n+            pass\n+\n+    def _apply(self, cmd):\n+        parts = cmd.split()\n+        if not parts:\n+            return\n+        op = parts[0]\n+        if op == \"S\" and len(parts) == 2:\n+            self.set_S(int(parts[1]))\n+        elif op == \"fill\" and len(parts) == +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except (RuntimeError, ArenaOOM):\n+            pass\n+\n+    def _apply(self, cmd):\n+        parts = cmd.split()\n+        if not parts:\n+            return\n+        op = parts[0]\n+        if op == \"S\" and len(parts) == 2:\n+            self.set_S(int(parts[1]))\n+        elif op == \"fill\" and len(parts) == 2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except (RuntimeError, ArenaOOM):\n+            pass\n+\n+    def _apply(self, cmd):\n+        parts = cmd.split()\n+        if not parts:\n+            return\n+        op = parts[0]\n+        if op == \"S\" and len(parts) == 2:\n+            self.set_S(int(parts[1]))\n+        elif op == \"fill\" and len(parts) == 2:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except (RuntimeError, ArenaOOM):\n+            pass\n+\n+    def _apply(self, cmd):\n+        parts = cmd.split()\n+        if not parts:\n+            return\n+        op = parts[0]\n+        if op == \"S\" and len(parts) == 2:\n+            self.set_S(int(parts[1]))\n+        elif op == \"fill\" and len(parts) == 2:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except (RuntimeError, ArenaOOM):\n+            pass\n+\n+    def _apply(self, cmd):\n+        parts = cmd.split()\n+        if not parts:\n+            return\n+        op = parts[0]\n+        if op == \"S\" and len(parts) == 2:\n+            self.set_S(int(parts[1]))\n+        elif op == \"fill\" and len(parts) == 2:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except (RuntimeError, ArenaOOM):\n+            pass\n+\n+    def _apply(self, cmd):\n+        parts = cmd.split()\n+        if not parts:\n+            return\n+        op = parts[0]\n+        if op == \"S\" and len(parts) == 2:\n+            self.set_S(int(parts[1]))\n+        elif op == \"fill\" and len(parts) == 2:\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except (RuntimeError, ArenaOOM):\n+            pass\n+\n+    def _apply(self, cmd):\n+        parts = cmd.split()\n+        if not parts:\n+            return\n+        op = parts[0]\n+        if op == \"S\" and len(parts) == 2:\n+            self.set_S(int(parts[1]))\n+        elif op == \"fill\" and len(parts) == 2:\n+            self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except (RuntimeError, ArenaOOM):\n+            pass\n+\n+    def _apply(self, cmd):\n+        parts = cmd.split()\n+        if not parts:\n+            return\n+        op = parts[0]\n+        if op == \"S\" and len(parts) == 2:\n+            self.set_S(int(parts[1]))\n+        elif op == \"fill\" and len(parts) == 2:\n+            self.fill+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except (RuntimeError, ArenaOOM):\n+            pass\n+\n+    def _apply(self, cmd):\n+        parts = cmd.split()\n+        if not parts:\n+            return\n+        op = parts[0]\n+        if op == \"S\" and len(parts) == 2:\n+            self.set_S(int(parts[1]))\n+        elif op == \"fill\" and len(parts) == 2:\n+            self.fill(int+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except (RuntimeError, ArenaOOM):\n+            pass\n+\n+    def _apply(self, cmd):\n+        parts = cmd.split()\n+        if not parts:\n+            return\n+        op = parts[0]\n+        if op == \"S\" and len(parts) == 2:\n+            self.set_S(int(parts[1]))\n+        elif op == \"fill\" and len(parts) == 2:\n+            self.fill(int(parts+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except (RuntimeError, ArenaOOM):\n+            pass\n+\n+    def _apply(self, cmd):\n+        parts = cmd.split()\n+        if not parts:\n+            return\n+        op = parts[0]\n+        if op == \"S\" and len(parts) == 2:\n+            self.set_S(int(parts[1]))\n+        elif op == \"fill\" and len(parts) == 2:\n+            self.fill(int(parts[+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except (RuntimeError, ArenaOOM):\n+            pass\n+\n+    def _apply(self, cmd):\n+        parts = cmd.split()\n+        if not parts:\n+            return\n+        op = parts[0]\n+        if op == \"S\" and len(parts) == 2:\n+            self.set_S(int(parts[1]))\n+        elif op == \"fill\" and len(parts) == 2:\n+            self.fill(int(parts[1+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except (RuntimeError, ArenaOOM):\n+            pass\n+\n+    def _apply(self, cmd):\n+        parts = cmd.split()\n+        if not parts:\n+            return\n+        op = parts[0]\n+        if op == \"S\" and len(parts) == 2:\n+            self.set_S(int(parts[1]))\n+        elif op == \"fill\" and len(parts) == 2:\n+            self.fill(int(parts[1]))+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except (RuntimeError, ArenaOOM):\n+            pass\n+\n+    def _apply(self, cmd):\n+        parts = cmd.split()\n+        if not parts:\n+            return\n+        op = parts[0]\n+        if op == \"S\" and len(parts) == 2:\n+            self.set_S(int(parts[1]))\n+        elif op == \"fill\" and len(parts) == 2:\n+            self.fill(int(parts[1]))\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except (RuntimeError, ArenaOOM):\n+            pass\n+\n+    def _apply(self, cmd):\n+        parts = cmd.split()\n+        if not parts:\n+            return\n+        op = parts[0]\n+        if op == \"S\" and len(parts) == 2:\n+            self.set_S(int(parts[1]))\n+        elif op == \"fill\" and len(parts) == 2:\n+            self.fill(int(parts[1]))\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except (RuntimeError, ArenaOOM):\n+            pass\n+\n+    def _apply(self, cmd):\n+        parts = cmd.split()\n+        if not parts:\n+            return\n+        op = parts[0]\n+        if op == \"S\" and len(parts) == 2:\n+            self.set_S(int(parts[1]))\n+        elif op == \"fill\" and len(parts) == 2:\n+            self.fill(int(parts[1]))\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except (RuntimeError, ArenaOOM):\n+            pass\n+\n+    def _apply(self, cmd):\n+        parts = cmd.split()\n+        if not parts:\n+            return\n+        op = parts[0]\n+        if op == \"S\" and len(parts) == 2:\n+            self.set_S(int(parts[1]))\n+        elif op == \"fill\" and len(parts) == 2:\n+            self.fill(int(parts[1]))\n+            self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except (RuntimeError, ArenaOOM):\n+            pass\n+\n+    def _apply(self, cmd):\n+        parts = cmd.split()\n+        if not parts:\n+            return\n+        op = parts[0]\n+        if op == \"S\" and len(parts) == 2:\n+            self.set_S(int(parts[1]))\n+        elif op == \"fill\" and len(parts) == 2:\n+            self.fill(int(parts[1]))\n+            self.pending+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except (RuntimeError, ArenaOOM):\n+            pass\n+\n+    def _apply(self, cmd):\n+        parts = cmd.split()\n+        if not parts:\n+            return\n+        op = parts[0]\n+        if op == \"S\" and len(parts) == 2:\n+            self.set_S(int(parts[1]))\n+        elif op == \"fill\" and len(parts) == 2:\n+            self.fill(int(parts[1]))\n+            self.pending_reg+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except (RuntimeError, ArenaOOM):\n+            pass\n+\n+    def _apply(self, cmd):\n+        parts = cmd.split()\n+        if not parts:\n+            return\n+        op = parts[0]\n+        if op == \"S\" and len(parts) == 2:\n+            self.set_S(int(parts[1]))\n+        elif op == \"fill\" and len(parts) == 2:\n+            self.fill(int(parts[1]))\n+            self.pending_regrow+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except (RuntimeError, ArenaOOM):\n+            pass\n+\n+    def _apply(self, cmd):\n+        parts = cmd.split()\n+        if not parts:\n+            return\n+        op = parts[0]\n+        if op == \"S\" and len(parts) == 2:\n+            self.set_S(int(parts[1]))\n+        elif op == \"fill\" and len(parts) == 2:\n+            self.fill(int(parts[1]))\n+            self.pending_regrow =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except (RuntimeError, ArenaOOM):\n+            pass\n+\n+    def _apply(self, cmd):\n+        parts = cmd.split()\n+        if not parts:\n+            return\n+        op = parts[0]\n+        if op == \"S\" and len(parts) == 2:\n+            self.set_S(int(parts[1]))\n+        elif op == \"fill\" and len(parts) == 2:\n+            self.fill(int(parts[1]))\n+            self.pending_regrow = True+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except (RuntimeError, ArenaOOM):\n+            pass\n+\n+    def _apply(self, cmd):\n+        parts = cmd.split()\n+        if not parts:\n+            return\n+        op = parts[0]\n+        if op == \"S\" and len(parts) == 2:\n+            self.set_S(int(parts[1]))\n+        elif op == \"fill\" and len(parts) == 2:\n+            self.fill(int(parts[1]))\n+            self.pending_regrow = True\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except (RuntimeError, ArenaOOM):\n+            pass\n+\n+    def _apply(self, cmd):\n+        parts = cmd.split()\n+        if not parts:\n+            return\n+        op = parts[0]\n+        if op == \"S\" and len(parts) == 2:\n+            self.set_S(int(parts[1]))\n+        elif op == \"fill\" and len(parts) == 2:\n+            self.fill(int(parts[1]))\n+            self.pending_regrow = True\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except (RuntimeError, ArenaOOM):\n+            pass\n+\n+    def _apply(self, cmd):\n+        parts = cmd.split()\n+        if not parts:\n+            return\n+        op = parts[0]\n+        if op == \"S\" and len(parts) == 2:\n+            self.set_S(int(parts[1]))\n+        elif op == \"fill\" and len(parts) == 2:\n+            self.fill(int(parts[1]))\n+            self.pending_regrow = True\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except (RuntimeError, ArenaOOM):\n+            pass\n+\n+    def _apply(self, cmd):\n+        parts = cmd.split()\n+        if not parts:\n+            return\n+        op = parts[0]\n+        if op == \"S\" and len(parts) == 2:\n+            self.set_S(int(parts[1]))\n+        elif op == \"fill\" and len(parts) == 2:\n+            self.fill(int(parts[1]))\n+            self.pending_regrow = True\n+        elif+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except (RuntimeError, ArenaOOM):\n+            pass\n+\n+    def _apply(self, cmd):\n+        parts = cmd.split()\n+        if not parts:\n+            return\n+        op = parts[0]\n+        if op == \"S\" and len(parts) == 2:\n+            self.set_S(int(parts[1]))\n+        elif op == \"fill\" and len(parts) == 2:\n+            self.fill(int(parts[1]))\n+            self.pending_regrow = True\n+        elif op+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except (RuntimeError, ArenaOOM):\n+            pass\n+\n+    def _apply(self, cmd):\n+        parts = cmd.split()\n+        if not parts:\n+            return\n+        op = parts[0]\n+        if op == \"S\" and len(parts) == 2:\n+            self.set_S(int(parts[1]))\n+        elif op == \"fill\" and len(parts) == 2:\n+            self.fill(int(parts[1]))\n+            self.pending_regrow = True\n+        elif op ==+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except (RuntimeError, ArenaOOM):\n+            pass\n+\n+    def _apply(self, cmd):\n+        parts = cmd.split()\n+        if not parts:\n+            return\n+        op = parts[0]\n+        if op == \"S\" and len(parts) == 2:\n+            self.set_S(int(parts[1]))\n+        elif op == \"fill\" and len(parts) == 2:\n+            self.fill(int(parts[1]))\n+            self.pending_regrow = True\n+        elif op == \"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except (RuntimeError, ArenaOOM):\n+            pass\n+\n+    def _apply(self, cmd):\n+        parts = cmd.split()\n+        if not parts:\n+            return\n+        op = parts[0]\n+        if op == \"S\" and len(parts) == 2:\n+            self.set_S(int(parts[1]))\n+        elif op == \"fill\" and len(parts) == 2:\n+            self.fill(int(parts[1]))\n+            self.pending_regrow = True\n+        elif op == \"free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except (RuntimeError, ArenaOOM):\n+            pass\n+\n+    def _apply(self, cmd):\n+        parts = cmd.split()\n+        if not parts:\n+            return\n+        op = parts[0]\n+        if op == \"S\" and len(parts) == 2:\n+            self.set_S(int(parts[1]))\n+        elif op == \"fill\" and len(parts) == 2:\n+            self.fill(int(parts[1]))\n+            self.pending_regrow = True\n+        elif op == \"free\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except (RuntimeError, ArenaOOM):\n+            pass\n+\n+    def _apply(self, cmd):\n+        parts = cmd.split()\n+        if not parts:\n+            return\n+        op = parts[0]\n+        if op == \"S\" and len(parts) == 2:\n+            self.set_S(int(parts[1]))\n+        elif op == \"fill\" and len(parts) == 2:\n+            self.fill(int(parts[1]))\n+            self.pending_regrow = True\n+        elif op == \"free\" and+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except (RuntimeError, ArenaOOM):\n+            pass\n+\n+    def _apply(self, cmd):\n+        parts = cmd.split()\n+        if not parts:\n+            return\n+        op = parts[0]\n+        if op == \"S\" and len(parts) == 2:\n+            self.set_S(int(parts[1]))\n+        elif op == \"fill\" and len(parts) == 2:\n+            self.fill(int(parts[1]))\n+            self.pending_regrow = True\n+        elif op == \"free\" and len+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except (RuntimeError, ArenaOOM):\n+            pass\n+\n+    def _apply(self, cmd):\n+        parts = cmd.split()\n+        if not parts:\n+            return\n+        op = parts[0]\n+        if op == \"S\" and len(parts) == 2:\n+            self.set_S(int(parts[1]))\n+        elif op == \"fill\" and len(parts) == 2:\n+            self.fill(int(parts[1]))\n+            self.pending_regrow = True\n+        elif op == \"free\" and len(parts+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except (RuntimeError, ArenaOOM):\n+            pass\n+\n+    def _apply(self, cmd):\n+        parts = cmd.split()\n+        if not parts:\n+            return\n+        op = parts[0]\n+        if op == \"S\" and len(parts) == 2:\n+            self.set_S(int(parts[1]))\n+        elif op == \"fill\" and len(parts) == 2:\n+            self.fill(int(parts[1]))\n+            self.pending_regrow = True\n+        elif op == \"free\" and len(parts)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except (RuntimeError, ArenaOOM):\n+            pass\n+\n+    def _apply(self, cmd):\n+        parts = cmd.split()\n+        if not parts:\n+            return\n+        op = parts[0]\n+        if op == \"S\" and len(parts) == 2:\n+            self.set_S(int(parts[1]))\n+        elif op == \"fill\" and len(parts) == 2:\n+            self.fill(int(parts[1]))\n+            self.pending_regrow = True\n+        elif op == \"free\" and len(parts) ==+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except (RuntimeError, ArenaOOM):\n+            pass\n+\n+    def _apply(self, cmd):\n+        parts = cmd.split()\n+        if not parts:\n+            return\n+        op = parts[0]\n+        if op == \"S\" and len(parts) == 2:\n+            self.set_S(int(parts[1]))\n+        elif op == \"fill\" and len(parts) == 2:\n+            self.fill(int(parts[1]))\n+            self.pending_regrow = True\n+        elif op == \"free\" and len(parts) == +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except (RuntimeError, ArenaOOM):\n+            pass\n+\n+    def _apply(self, cmd):\n+        parts = cmd.split()\n+        if not parts:\n+            return\n+        op = parts[0]\n+        if op == \"S\" and len(parts) == 2:\n+            self.set_S(int(parts[1]))\n+        elif op == \"fill\" and len(parts) == 2:\n+            self.fill(int(parts[1]))\n+            self.pending_regrow = True\n+        elif op == \"free\" and len(parts) == 2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except (RuntimeError, ArenaOOM):\n+            pass\n+\n+    def _apply(self, cmd):\n+        parts = cmd.split()\n+        if not parts:\n+            return\n+        op = parts[0]\n+        if op == \"S\" and len(parts) == 2:\n+            self.set_S(int(parts[1]))\n+        elif op == \"fill\" and len(parts) == 2:\n+            self.fill(int(parts[1]))\n+            self.pending_regrow = True\n+        elif op == \"free\" and len(parts) == 2:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except (RuntimeError, ArenaOOM):\n+            pass\n+\n+    def _apply(self, cmd):\n+        parts = cmd.split()\n+        if not parts:\n+            return\n+        op = parts[0]\n+        if op == \"S\" and len(parts) == 2:\n+            self.set_S(int(parts[1]))\n+        elif op == \"fill\" and len(parts) == 2:\n+            self.fill(int(parts[1]))\n+            self.pending_regrow = True\n+        elif op == \"free\" and len(parts) == 2:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except (RuntimeError, ArenaOOM):\n+            pass\n+\n+    def _apply(self, cmd):\n+        parts = cmd.split()\n+        if not parts:\n+            return\n+        op = parts[0]\n+        if op == \"S\" and len(parts) == 2:\n+            self.set_S(int(parts[1]))\n+        elif op == \"fill\" and len(parts) == 2:\n+            self.fill(int(parts[1]))\n+            self.pending_regrow = True\n+        elif op == \"free\" and len(parts) == 2:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except (RuntimeError, ArenaOOM):\n+            pass\n+\n+    def _apply(self, cmd):\n+        parts = cmd.split()\n+        if not parts:\n+            return\n+        op = parts[0]\n+        if op == \"S\" and len(parts) == 2:\n+            self.set_S(int(parts[1]))\n+        elif op == \"fill\" and len(parts) == 2:\n+            self.fill(int(parts[1]))\n+            self.pending_regrow = True\n+        elif op == \"free\" and len(parts) == 2:\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except (RuntimeError, ArenaOOM):\n+            pass\n+\n+    def _apply(self, cmd):\n+        parts = cmd.split()\n+        if not parts:\n+            return\n+        op = parts[0]\n+        if op == \"S\" and len(parts) == 2:\n+            self.set_S(int(parts[1]))\n+        elif op == \"fill\" and len(parts) == 2:\n+            self.fill(int(parts[1]))\n+            self.pending_regrow = True\n+        elif op == \"free\" and len(parts) == 2:\n+            self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except (RuntimeError, ArenaOOM):\n+            pass\n+\n+    def _apply(self, cmd):\n+        parts = cmd.split()\n+        if not parts:\n+            return\n+        op = parts[0]\n+        if op == \"S\" and len(parts) == 2:\n+            self.set_S(int(parts[1]))\n+        elif op == \"fill\" and len(parts) == 2:\n+            self.fill(int(parts[1]))\n+            self.pending_regrow = True\n+        elif op == \"free\" and len(parts) == 2:\n+            self.free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except (RuntimeError, ArenaOOM):\n+            pass\n+\n+    def _apply(self, cmd):\n+        parts = cmd.split()\n+        if not parts:\n+            return\n+        op = parts[0]\n+        if op == \"S\" and len(parts) == 2:\n+            self.set_S(int(parts[1]))\n+        elif op == \"fill\" and len(parts) == 2:\n+            self.fill(int(parts[1]))\n+            self.pending_regrow = True\n+        elif op == \"free\" and len(parts) == 2:\n+            self.free(int+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except (RuntimeError, ArenaOOM):\n+            pass\n+\n+    def _apply(self, cmd):\n+        parts = cmd.split()\n+        if not parts:\n+            return\n+        op = parts[0]\n+        if op == \"S\" and len(parts) == 2:\n+            self.set_S(int(parts[1]))\n+        elif op == \"fill\" and len(parts) == 2:\n+            self.fill(int(parts[1]))\n+            self.pending_regrow = True\n+        elif op == \"free\" and len(parts) == 2:\n+            self.free(int(parts+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except (RuntimeError, ArenaOOM):\n+            pass\n+\n+    def _apply(self, cmd):\n+        parts = cmd.split()\n+        if not parts:\n+            return\n+        op = parts[0]\n+        if op == \"S\" and len(parts) == 2:\n+            self.set_S(int(parts[1]))\n+        elif op == \"fill\" and len(parts) == 2:\n+            self.fill(int(parts[1]))\n+            self.pending_regrow = True\n+        elif op == \"free\" and len(parts) == 2:\n+            self.free(int(parts[+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except (RuntimeError, ArenaOOM):\n+            pass\n+\n+    def _apply(self, cmd):\n+        parts = cmd.split()\n+        if not parts:\n+            return\n+        op = parts[0]\n+        if op == \"S\" and len(parts) == 2:\n+            self.set_S(int(parts[1]))\n+        elif op == \"fill\" and len(parts) == 2:\n+            self.fill(int(parts[1]))\n+            self.pending_regrow = True\n+        elif op == \"free\" and len(parts) == 2:\n+            self.free(int(parts[1+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except (RuntimeError, ArenaOOM):\n+            pass\n+\n+    def _apply(self, cmd):\n+        parts = cmd.split()\n+        if not parts:\n+            return\n+        op = parts[0]\n+        if op == \"S\" and len(parts) == 2:\n+            self.set_S(int(parts[1]))\n+        elif op == \"fill\" and len(parts) == 2:\n+            self.fill(int(parts[1]))\n+            self.pending_regrow = True\n+        elif op == \"free\" and len(parts) == 2:\n+            self.free(int(parts[1]))+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except (RuntimeError, ArenaOOM):\n+            pass\n+\n+    def _apply(self, cmd):\n+        parts = cmd.split()\n+        if not parts:\n+            return\n+        op = parts[0]\n+        if op == \"S\" and len(parts) == 2:\n+            self.set_S(int(parts[1]))\n+        elif op == \"fill\" and len(parts) == 2:\n+            self.fill(int(parts[1]))\n+            self.pending_regrow = True\n+        elif op == \"free\" and len(parts) == 2:\n+            self.free(int(parts[1]))\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except (RuntimeError, ArenaOOM):\n+            pass\n+\n+    def _apply(self, cmd):\n+        parts = cmd.split()\n+        if not parts:\n+            return\n+        op = parts[0]\n+        if op == \"S\" and len(parts) == 2:\n+            self.set_S(int(parts[1]))\n+        elif op == \"fill\" and len(parts) == 2:\n+            self.fill(int(parts[1]))\n+            self.pending_regrow = True\n+        elif op == \"free\" and len(parts) == 2:\n+            self.free(int(parts[1]))\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except (RuntimeError, ArenaOOM):\n+            pass\n+\n+    def _apply(self, cmd):\n+        parts = cmd.split()\n+        if not parts:\n+            return\n+        op = parts[0]\n+        if op == \"S\" and len(parts) == 2:\n+            self.set_S(int(parts[1]))\n+        elif op == \"fill\" and len(parts) == 2:\n+            self.fill(int(parts[1]))\n+            self.pending_regrow = True\n+        elif op == \"free\" and len(parts) == 2:\n+            self.free(int(parts[1]))\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except (RuntimeError, ArenaOOM):\n+            pass\n+\n+    def _apply(self, cmd):\n+        parts = cmd.split()\n+        if not parts:\n+            return\n+        op = parts[0]\n+        if op == \"S\" and len(parts) == 2:\n+            self.set_S(int(parts[1]))\n+        elif op == \"fill\" and len(parts) == 2:\n+            self.fill(int(parts[1]))\n+            self.pending_regrow = True\n+        elif op == \"free\" and len(parts) == 2:\n+            self.free(int(parts[1]))\n+            self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except (RuntimeError, ArenaOOM):\n+            pass\n+\n+    def _apply(self, cmd):\n+        parts = cmd.split()\n+        if not parts:\n+            return\n+        op = parts[0]\n+        if op == \"S\" and len(parts) == 2:\n+            self.set_S(int(parts[1]))\n+        elif op == \"fill\" and len(parts) == 2:\n+            self.fill(int(parts[1]))\n+            self.pending_regrow = True\n+        elif op == \"free\" and len(parts) == 2:\n+            self.free(int(parts[1]))\n+            self.pending+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except (RuntimeError, ArenaOOM):\n+            pass\n+\n+    def _apply(self, cmd):\n+        parts = cmd.split()\n+        if not parts:\n+            return\n+        op = parts[0]\n+        if op == \"S\" and len(parts) == 2:\n+            self.set_S(int(parts[1]))\n+        elif op == \"fill\" and len(parts) == 2:\n+            self.fill(int(parts[1]))\n+            self.pending_regrow = True\n+        elif op == \"free\" and len(parts) == 2:\n+            self.free(int(parts[1]))\n+            self.pending_reg+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except (RuntimeError, ArenaOOM):\n+            pass\n+\n+    def _apply(self, cmd):\n+        parts = cmd.split()\n+        if not parts:\n+            return\n+        op = parts[0]\n+        if op == \"S\" and len(parts) == 2:\n+            self.set_S(int(parts[1]))\n+        elif op == \"fill\" and len(parts) == 2:\n+            self.fill(int(parts[1]))\n+            self.pending_regrow = True\n+        elif op == \"free\" and len(parts) == 2:\n+            self.free(int(parts[1]))\n+            self.pending_regrow+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except (RuntimeError, ArenaOOM):\n+            pass\n+\n+    def _apply(self, cmd):\n+        parts = cmd.split()\n+        if not parts:\n+            return\n+        op = parts[0]\n+        if op == \"S\" and len(parts) == 2:\n+            self.set_S(int(parts[1]))\n+        elif op == \"fill\" and len(parts) == 2:\n+            self.fill(int(parts[1]))\n+            self.pending_regrow = True\n+        elif op == \"free\" and len(parts) == 2:\n+            self.free(int(parts[1]))\n+            self.pending_regrow =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except (RuntimeError, ArenaOOM):\n+            pass\n+\n+    def _apply(self, cmd):\n+        parts = cmd.split()\n+        if not parts:\n+            return\n+        op = parts[0]\n+        if op == \"S\" and len(parts) == 2:\n+            self.set_S(int(parts[1]))\n+        elif op == \"fill\" and len(parts) == 2:\n+            self.fill(int(parts[1]))\n+            self.pending_regrow = True\n+        elif op == \"free\" and len(parts) == 2:\n+            self.free(int(parts[1]))\n+            self.pending_regrow = True+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except (RuntimeError, ArenaOOM):\n+            pass\n+\n+    def _apply(self, cmd):\n+        parts = cmd.split()\n+        if not parts:\n+            return\n+        op = parts[0]\n+        if op == \"S\" and len(parts) == 2:\n+            self.set_S(int(parts[1]))\n+        elif op == \"fill\" and len(parts) == 2:\n+            self.fill(int(parts[1]))\n+            self.pending_regrow = True\n+        elif op == \"free\" and len(parts) == 2:\n+            self.free(int(parts[1]))\n+            self.pending_regrow = True\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except (RuntimeError, ArenaOOM):\n+            pass\n+\n+    def _apply(self, cmd):\n+        parts = cmd.split()\n+        if not parts:\n+            return\n+        op = parts[0]\n+        if op == \"S\" and len(parts) == 2:\n+            self.set_S(int(parts[1]))\n+        elif op == \"fill\" and len(parts) == 2:\n+            self.fill(int(parts[1]))\n+            self.pending_regrow = True\n+        elif op == \"free\" and len(parts) == 2:\n+            self.free(int(parts[1]))\n+            self.pending_regrow = True\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except (RuntimeError, ArenaOOM):\n+                S = max(self.s_floor(), S - step)\n+                self.set_S(S)\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                break\n+            S = max(self.s_floor(), S - step)\n+            self.set_S(S)\n+            if S == self.s_floor():\n+                break\n+        return S\n+\n+    def _regrow(self):\n+        \"\"\"Grow back to the reserve after a forced shrink (fill/free/status commands).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        try:\n+            self.set_S(self.regrow_reserve)\n+            self.pending_regrow = False\n+        except (RuntimeError, ArenaOOM):\n+            pass\n+\n+    def _apply(self, cmd):\n+        parts = cmd.split()\n+        if not parts:\n+            return\n+        op = parts[0]\n+        if op == \"S\" and len(parts) == 2:\n+            self.set_S(int(parts[1]))\n+        elif op == \"fill\" and len(parts) == 2:\n+            self.fill(int(parts[1]))\n+            self.pending_regrow = True\n+        elif op == \"free\" and len(parts) == 2:\n+            self.free(int(parts[1]))\n+            self.pending_regrow = True\n+       ",
 "ttft_seconds": 13.280987171005108,
 "status": "ok",
 "tg_tokens_per_s": 31.060153735909502,
 "decode_seconds_per_token": 0.03219559080430025,
 "individual_token_timing": true,
 "event_intervals_ms": {
  "n": 511,
  "median": 32.24489999411162,
  "p95": 39.53216900117695,
  "mean": 32.195590804300245
 },
 "class": "code",
 "requested_input_tokens": 32768
}
